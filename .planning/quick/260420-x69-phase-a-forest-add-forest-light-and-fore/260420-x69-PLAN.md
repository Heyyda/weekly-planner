---
phase: 260420-x69
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/ui/themes.py
  - client/ui/settings.py
  - client/utils/tray.py
  - client/tests/ui/test_themes.py
  - client/tests/ui/test_settings.py
  - client/tests/conftest.py
  - client/tests/test_infrastructure.py
autonomous: false
requirements:
  - FOREST-A-01  # Add forest_light and forest_dark palettes to PALETTES
  - FOREST-A-02  # Make forest_light the new default theme (fresh installs)
  - FOREST-A-03  # Migration: invalid/legacy theme in SettingsStore → forest_light on next launch
  - FOREST-A-04  # Add tray menu radio items for Forest variants (keep existing 3)
  - FOREST-A-05  # Update existing tests that assume {"light","dark","beige"} or default="light"
must_haves:
  truths:
    - "PALETTES содержит 5 записей: light, dark, beige, forest_light, forest_dark"
    - "forest_light и forest_dark содержат все 11 ключей с точными hex-значениями из спецификации"
    - "ThemeManager() без аргументов даёт current == 'forest_light'"
    - "UISettings() без аргументов даёт theme == 'forest_light'"
    - "SettingsStore.load() для settings.json со значением theme='unknownvalue' возвращает UISettings с theme='forest_light' (миграция)"
    - "Tray-меню содержит 5 radio-опций тем (light, dark, beige, forest_light, forest_dark) + Системная, все работают без исключений"
    - "Приложение запускается `python main.py` без traceback — окно видимо с cream+forest look"
  artifacts:
    - path: "client/ui/themes.py"
      provides: "PALETTES[forest_light] и PALETTES[forest_dark] — 11 ключей каждая"
      contains: 'forest_light'
    - path: "client/ui/themes.py"
      provides: "ThemeManager default initial='forest_light'"
      contains: 'initial: str = "forest_light"'
    - path: "client/ui/settings.py"
      provides: "VALID_THEMES включает forest_light и forest_dark; default 'forest_light'; validate() migrates to forest_light"
      contains: 'forest_light'
    - path: "client/utils/tray.py"
      provides: "Radio-пункты меню для Forest (светлая) и Forest (тёмная)"
      contains: 'forest_light'
  key_links:
    - from: "client/utils/tray.py"
      to: "client/ui/themes.py::ThemeManager.set_theme"
      via: "self._cb_theme('forest_light') / self._cb_theme('forest_dark')"
      pattern: 'forest_light|forest_dark'
    - from: "client/ui/settings.py::UISettings.validate"
      to: "VALID_THEMES"
      via: "reset invalid theme to forest_light (migration path)"
      pattern: 'self.theme = "forest_light"'
    - from: "client/ui/themes.py::ThemeManager.__init__"
      to: "PALETTES"
      via: "initial defaults to forest_light, fallback also forest_light"
      pattern: 'initial: str = "forest_light"'
---

<objective>
Phase A рефакторинга Forest — добавить две новые палитры `forest_light` (льняной cream + бутылочный зелёный) и `forest_dark` (тёмный лес с зелёным undertone) в `client/ui/themes.py`, сделать `forest_light` дефолтной темой, и добавить пункты в tray-меню чтобы пользователь мог переключаться между всеми 5 палитрами (старые 3 — light/dark/beige — остаются для сравнения).

Purpose: Установить визуальную базу Forest-рефакторинга. Widget-level изменения цветов (Phase B-F) будут опираться на уже существующие токены палитры — чем меньше изменений в widget-коде в Phase A, тем меньше risk surface для визуального A/B сравнения. Keys в палитре НЕ меняются (bg_primary, accent_brand, и т.д.) — виджеты читают по ключу, поэтому добавление палитр не требует правок виджетов.

Output:
- PALETTES расширена с 3 до 5 тем
- Default theme: forest_light (свежая установка → Forest; migrant с legacy theme → Forest)
- Tray-меню имеет 5 radio-тем
- Тесты обновлены под новые ожидания (default, VALID_THEMES, palette set)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Existing files to be modified
@client/ui/themes.py
@client/ui/settings.py
@client/utils/tray.py

# Existing tests that WILL BREAK when palette set or defaults change — must be updated in same plan
@client/tests/ui/test_themes.py
@client/tests/ui/test_settings.py
@client/tests/conftest.py
@client/tests/test_infrastructure.py

<interfaces>
<!-- Извлечено из исходного кода — executor использует напрямую, без exploration. -->

From client/ui/themes.py:
```python
PALETTES: dict[str, dict[str, str]] = {
    "light": {...},   # существует
    "dark": {...},    # существует
    "beige": {...},   # существует
    # → ADD: "forest_light", "forest_dark"
}

# Все 11 ключей в каждой палитре (НЕ менять имена):
# bg_primary, bg_secondary, bg_tertiary,
# text_primary, text_secondary, text_tertiary,
# accent_brand, accent_brand_light,
# accent_done, accent_overdue,
# shadow_card

class ThemeManager:
    def __init__(self, initial: str = "light") -> None:  # → change default to "forest_light"
        self._theme: str = initial if initial in PALETTES else "light"  # → fallback "forest_light"

    def set_theme(self, theme: str) -> None:
        # line ~110: theme = "light" (fallback при unknown) → "forest_light"
        # line ~114: ctk.set_appearance_mode("dark" if theme == "dark" else "light")
        #           → keep using "dark" branch for BOTH dark and forest_dark (CTk built-in widgets)
        ...

    @staticmethod
    def detect_system_theme() -> str:
        # Returns "light" or "dark" — НЕ ТРОГАТЬ, это маппинг Windows AppsUseLightTheme,
        # не "default fallback". Оставить возвращающим "light"/"dark" как есть.
        ...
```

From client/ui/settings.py:
```python
VALID_THEMES = {"light", "dark", "beige", "system"}  # → add "forest_light", "forest_dark"

@dataclass
class UISettings:
    theme: str = "light"   # → change default to "forest_light"
    # ... (остальные поля не трогать)

    def validate(self) -> None:
        if self.theme not in VALID_THEMES:
            logger.warning("Неизвестная theme %r — reset на light", self.theme)
            self.theme = "light"   # → change to "forest_light" (это и есть migration path:
                                   # старые сейвы с theme='light'/'dark'/'beige' остаются валидны —
                                   # VALID_THEMES их содержит; только unknown значения мигрируют.)
```

NOTE по миграции: согласно constraints, ПРОСТОЙ подход — fallback на forest_light если theme не в PALETTES/VALID_THEMES. Это значит пользователи с ранее сохранённым theme="light"/"dark"/"beige" СОХРАНЯТ свою тему (т.к. эти значения остаются в VALID_THEMES). Это соответствует требованию constraints: "если saved theme not in PALETTES → use forest_light; no mapping old themes to forest".

From client/utils/tray.py (_build_menu, lines ~120-145):
```python
pystray.MenuItem("Тема", pystray.Menu(
    pystray.MenuItem("Светлая", lambda i, it: self._cb_theme("light"),
                     checked=lambda it: self._settings.theme == "light", radio=True),
    pystray.MenuItem("Тёмная", lambda i, it: self._cb_theme("dark"),
                     checked=lambda it: self._settings.theme == "dark", radio=True),
    pystray.MenuItem("Бежевая", lambda i, it: self._cb_theme("beige"),
                     checked=lambda it: self._settings.theme == "beige", radio=True),
    # → ADD: "Forest (светлая)" → forest_light
    # → ADD: "Forest (тёмная)" → forest_dark
    pystray.MenuItem("Системная", lambda i, it: self._cb_theme("system"),
                     checked=lambda it: self._settings.theme == "system", radio=True),
)),
```

From client/tests/ui/test_themes.py (tests that WILL FAIL after change, need update):
- test_palettes_has_three_themes — assert `set(PALETTES.keys()) == {"light","dark","beige"}` → update to 5-item set
- test_default_theme_is_light — assert `tm.current == "light"` → update to "forest_light"
- test_unknown_theme_falls_back_to_light — `tm.set_theme("rainbow"); assert tm.current == "light"` → update to "forest_light"

From client/tests/ui/test_settings.py (tests that WILL FAIL, need update):
- line 15: `assert s.theme == "light"` (UISettings() default) → "forest_light"
- line 56: similar (load default) → "forest_light"
- line 97: `assert loaded.theme == "light"  # validate() sanitized` (invalid-value migration) → "forest_light"

From client/tests/conftest.py line 258:
- `return ThemeManager(initial="light")` — можно оставить (явный initial), но лучше просто `ThemeManager()` чтобы тест отражал реальный default. Executor решит по месту — главное чтобы fixture возвращал рабочий ThemeManager.

From client/tests/test_infrastructure.py line 84:
- `assert mock_theme_manager.current == "light"` — если fixture сменится на default (forest_light) → обновить assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Forest palettes + default theme switch + settings migration</name>
  <files>client/ui/themes.py, client/ui/settings.py</files>
  <action>
**Файл 1: `client/ui/themes.py`**

В PALETTES dict (после существующей записи "beige", ДО закрывающей `}`) добавить две новые записи VERBATIM с точными hex-значениями:

```python
    "forest_light": {
        "bg_primary": "#EEE9DC",
        "bg_secondary": "#F5F0E3",
        "bg_tertiary": "#E2E0D2",
        "text_primary": "#2E2B24",
        "text_secondary": "#6A6558",
        "text_tertiary": "#9A958A",
        "accent_brand": "#1E5239",
        "accent_brand_light": "#234E3A",
        "accent_done": "#1E5239",
        "accent_overdue": "#9E6A5A",
        "shadow_card": "rgba(30, 40, 32, 0.10)",
    },
    "forest_dark": {
        "bg_primary": "#161E1A",
        "bg_secondary": "#202A24",
        "bg_tertiary": "#1B2620",
        "text_primary": "#E6E3D5",
        "text_secondary": "#A5A89A",
        "text_tertiary": "#6E7168",
        "accent_brand": "#5E9E7A",
        "accent_brand_light": "#6BAF8A",
        "accent_done": "#5E9E7A",
        "accent_overdue": "#B87D6F",
        "shadow_card": "rgba(0, 0, 0, 0.35)",
    },
```

ВАЖНО: accent_brand_light в forest_light — `#234E3A` (на шаг ТЕМНЕЕ accent_brand `#1E5239`), это осознанная Forest-эстетика. НЕ "исправлять" на более светлый hex.

В `ThemeManager.__init__` (строка ~88):
```python
def __init__(self, initial: str = "light") -> None:
    self._theme: str = initial if initial in PALETTES else "light"
```
Заменить обе `"light"` на `"forest_light"`:
```python
def __init__(self, initial: str = "forest_light") -> None:
    self._theme: str = initial if initial in PALETTES else "forest_light"
```

В `ThemeManager.set_theme` (строка ~108-110):
```python
if theme not in PALETTES:
    logger.warning("Неизвестная тема %r — fallback на light", theme)
    theme = "light"
```
Заменить на:
```python
if theme not in PALETTES:
    logger.warning("Неизвестная тема %r — fallback на forest_light", theme)
    theme = "forest_light"
```

**Строка 114 НЕ трогать**: `ctk.set_appearance_mode("dark" if theme == "dark" else "light")` — это управление CTk-built-in виджетами. Для forest_dark CTk будет в "light" mode, что OK (widget-level стилизация для dark variants — забота Phase B+). Если заметим визуальный glitch — зафиксируем в out-of-scope notes, исправим в Phase B.

**Строки 128-140 (detect_system_theme) НЕ трогать** — возвращает `"light"`/`"dark"` как маппинг реестра Windows AppsUseLightTheme; это НЕ default-fallback, а семантический перевод системного предпочтения. Оставить как есть.

**Файл 2: `client/ui/settings.py`**

Строка 22: `VALID_THEMES = {"light", "dark", "beige", "system"}` →
```python
VALID_THEMES = {"light", "dark", "beige", "forest_light", "forest_dark", "system"}
```

Строка 30: `theme: str = "light"` в UISettings dataclass →
```python
theme: str = "forest_light"
```

Строки 52-55 (в `validate()`):
```python
if self.theme not in VALID_THEMES:
    logger.warning("Неизвестная theme %r — reset на light", self.theme)
    self.theme = "light"
```
Заменить на:
```python
if self.theme not in VALID_THEMES:
    logger.warning("Неизвестная theme %r — reset на forest_light", self.theme)
    self.theme = "forest_light"
```

Строка 8 (docstring): `theme: "light" | "dark" | "beige" | "system"` →
```
theme: "light" | "dark" | "beige" | "forest_light" | "forest_dark" | "system"
```

**Migration semantics** (для proof):
- User с сохранённым theme="light"/"dark"/"beige" → эти значения остаются в VALID_THEMES → `validate()` не срабатывает → тема сохраняется как выбрана пользователем (но свежая установка даёт forest_light). Это соответствует constraints: "на load, if saved theme not in PALETTES → use forest_light; no mapping old themes to forest".
- User без settings.json (первый запуск) → UISettings() даёт `theme="forest_light"` → сохраняется в settings.json при первом save.
- User с повреждённым settings.json (theme="corrupted_value") → validate() → forest_light.

НЕ добавлять NO explicit remapping logic ("if theme == 'light': theme = 'forest_light'"). Простая семантика validate() + новый default — достаточно.
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник/.claude/worktrees/dazzling-allen-bd61dd" && python -c "from client.ui.themes import PALETTES, ThemeManager; assert set(PALETTES.keys()) == {'light','dark','beige','forest_light','forest_dark'}, PALETTES.keys(); assert PALETTES['forest_light']['bg_primary'] == '#EEE9DC'; assert PALETTES['forest_light']['accent_brand'] == '#1E5239'; assert PALETTES['forest_light']['accent_brand_light'] == '#234E3A'; assert PALETTES['forest_dark']['bg_primary'] == '#161E1A'; assert PALETTES['forest_dark']['accent_brand'] == '#5E9E7A'; assert len(PALETTES['forest_light']) == 11 and len(PALETTES['forest_dark']) == 11; tm = ThemeManager(); assert tm.current == 'forest_light'; tm.set_theme('nonexistent'); assert tm.current == 'forest_light'; from client.ui.settings import UISettings, VALID_THEMES; assert UISettings().theme == 'forest_light'; assert 'forest_light' in VALID_THEMES and 'forest_dark' in VALID_THEMES; s = UISettings(theme='garbage'); s.validate(); assert s.theme == 'forest_light'; s2 = UISettings(theme='light'); s2.validate(); assert s2.theme == 'light'; print('OK')"</automated>
  </verify>
  <done>
- PALETTES имеет 5 ключей: light, dark, beige, forest_light, forest_dark
- forest_light и forest_dark содержат все 11 ключей с hex-значениями из спецификации (verbatim)
- ThemeManager() без аргументов → current == "forest_light"
- ThemeManager при неизвестной теме → fallback "forest_light"
- UISettings() без аргументов → theme == "forest_light"
- VALID_THEMES включает forest_light и forest_dark (и сохраняет light/dark/beige)
- UISettings(theme="invalid").validate() → theme = "forest_light"
- UISettings(theme="light").validate() → theme остаётся "light" (backward-compat для существующих сейвов)
- detect_system_theme() не изменён
- ctk.set_appearance_mode строка не изменена
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Forest entries to tray theme menu + update breaking tests</name>
  <files>client/utils/tray.py, client/tests/ui/test_themes.py, client/tests/ui/test_settings.py, client/tests/conftest.py, client/tests/test_infrastructure.py</files>
  <action>
**Файл 1: `client/utils/tray.py`**

В методе `_build_menu`, в подменю "Тема" (строки ~120-145), добавить два новых radio-пункта ПОСЛЕ "Бежевая" и ДО "Системная":

Существующая структура меню "Тема":
```python
pystray.MenuItem("Тема", pystray.Menu(
    pystray.MenuItem("Светлая", lambda i, it: self._cb_theme("light"), checked=..., radio=True),
    pystray.MenuItem("Тёмная", lambda i, it: self._cb_theme("dark"), checked=..., radio=True),
    pystray.MenuItem("Бежевая", lambda i, it: self._cb_theme("beige"), checked=..., radio=True),
    pystray.MenuItem("Системная", lambda i, it: self._cb_theme("system"), checked=..., radio=True),
)),
```

Стать:
```python
pystray.MenuItem("Тема", pystray.Menu(
    pystray.MenuItem("Светлая", lambda i, it: self._cb_theme("light"),
                     checked=lambda it: self._settings.theme == "light", radio=True),
    pystray.MenuItem("Тёмная", lambda i, it: self._cb_theme("dark"),
                     checked=lambda it: self._settings.theme == "dark", radio=True),
    pystray.MenuItem("Бежевая", lambda i, it: self._cb_theme("beige"),
                     checked=lambda it: self._settings.theme == "beige", radio=True),
    pystray.MenuItem(
        "Forest (светлая)",
        lambda i, it: self._cb_theme("forest_light"),
        checked=lambda it: self._settings.theme == "forest_light",
        radio=True,
    ),
    pystray.MenuItem(
        "Forest (тёмная)",
        lambda i, it: self._cb_theme("forest_dark"),
        checked=lambda it: self._settings.theme == "forest_dark",
        radio=True,
    ),
    pystray.MenuItem("Системная", lambda i, it: self._cb_theme("system"),
                     checked=lambda it: self._settings.theme == "system", radio=True),
)),
```

НЕ менять логику `_cb_theme` — она уже принимает любую строку темы и делегирует `self._theme.set_theme(theme)` + `self._settings_store.save(self._settings)`. forest_light/forest_dark работают через тот же путь без изменений.

**Файл 2: `client/tests/ui/test_themes.py`**

Прочитать файл перед правками (он уже прочитан в контексте выше — 108 строк).

Обновить три теста:

Строка 6-7 (`test_palettes_has_three_themes`):
```python
def test_palettes_has_three_themes():
    assert set(PALETTES.keys()) == {"light", "dark", "beige"}
```
→
```python
def test_palettes_has_all_five_themes():
    assert set(PALETTES.keys()) == {"light", "dark", "beige", "forest_light", "forest_dark"}
```
(Переименовать функцию, чтобы pytest не застал stale cache; обновить ассерт.)

Строка 51-53 (`test_default_theme_is_light`):
```python
def test_default_theme_is_light():
    tm = ThemeManager()
    assert tm.current == "light"
```
→
```python
def test_default_theme_is_forest_light():
    tm = ThemeManager()
    assert tm.current == "forest_light"
```

Строка 93-96 (`test_unknown_theme_falls_back_to_light`):
```python
def test_unknown_theme_falls_back_to_light(mock_ctypes_dpi):
    tm = ThemeManager()
    tm.set_theme("rainbow")
    assert tm.current == "light"
```
→
```python
def test_unknown_theme_falls_back_to_forest_light(mock_ctypes_dpi):
    tm = ThemeManager()
    tm.set_theme("rainbow")
    assert tm.current == "forest_light"
```

ДОБАВИТЬ один новый тест после `test_beige_palette_exact_hex_per_ui_spec` (строка ~43):
```python
def test_forest_light_palette_exact_hex_per_spec():
    p = PALETTES["forest_light"]
    assert p["bg_primary"] == "#EEE9DC"
    assert p["bg_secondary"] == "#F5F0E3"
    assert p["bg_tertiary"] == "#E2E0D2"
    assert p["text_primary"] == "#2E2B24"
    assert p["accent_brand"] == "#1E5239"
    assert p["accent_brand_light"] == "#234E3A"  # intentionally darker than brand
    assert p["accent_done"] == "#1E5239"          # same as accent_brand by design
    assert p["accent_overdue"] == "#9E6A5A"


def test_forest_dark_palette_exact_hex_per_spec():
    p = PALETTES["forest_dark"]
    assert p["bg_primary"] == "#161E1A"
    assert p["bg_secondary"] == "#202A24"
    assert p["bg_tertiary"] == "#1B2620"
    assert p["text_primary"] == "#E6E3D5"
    assert p["accent_brand"] == "#5E9E7A"
    assert p["accent_brand_light"] == "#6BAF8A"
    assert p["accent_done"] == "#5E9E7A"
    assert p["accent_overdue"] == "#B87D6F"
```

Остальные тесты (`test_each_palette_has_11_tokens`, `test_subscribe_*`, `test_system_theme_*`, `test_get_*`) не трогать — они используют существующие методы API.

**Файл 3: `client/tests/ui/test_settings.py`**

Прочитать файл целиком перед изменениями (не в контексте). Найти и обновить следующие assertions (на основе grep-результата):

- Строка 15: `assert s.theme == "light"` (UISettings() дефолт) → `assert s.theme == "forest_light"`
- Строка 56: аналогично → `assert s.theme == "forest_light"`
- Строка 97: `assert loaded.theme == "light"  # validate() sanitized` → `assert loaded.theme == "forest_light"  # validate() sanitized`

ВАЖНО: если файл содержит другие assertions с `"light"` которые теряют смысл (например, тесты передающие невалидный theme и ожидающие `"light"` после validate) — обновить все на `"forest_light"`. Если есть тесты с `theme="light"` передачей валидной темы (т.е. "light" как нормальная тема, не как fallback) — НЕ трогать, light остаётся в VALID_THEMES.

Используй grep для локализации: все вхождения строк `"light"` в test_settings.py, и для каждой решить: это "default fallback" (→ forest_light) или "user-chosen valid light theme" (оставить).

**Файл 4: `client/tests/conftest.py`**

Прочитать файл. Строка 258: `return ThemeManager(initial="light")` — оставить КАК ЕСТЬ. Явный initial делает fixture детерминированным и не зависит от default. Тесты, использующие этот fixture, ожидают именно "light".

Проверить: если в conftest есть OTHER fixture, которые полагаются на `UISettings()` default == "light" → обновить на "forest_light". Используй grep `"light"` в conftest.py.

**Файл 5: `client/tests/test_infrastructure.py`**

Прочитать файл. Строка 84: `assert mock_theme_manager.current == "light"`. 
- Если `mock_theme_manager` создан в conftest строка 258 с `initial="light"` → оставить как есть.
- Если `mock_theme_manager` — это просто вызов `ThemeManager()` без initial → обновить на `"forest_light"`.

Executor должен открыть conftest.py и проверить fixture `mock_theme_manager`. Если `initial="light"` передан явно → тест `"light"` остаётся корректен (ничего не менять). Если нет — обновить тест.
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник/.claude/worktrees/dazzling-allen-bd61dd" && python -m pytest client/tests/ui/test_themes.py client/tests/ui/test_settings.py client/tests/test_infrastructure.py -x --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
- tray.py содержит 5 radio-пунктов тем в подменю "Тема" (light, dark, beige, forest_light, forest_dark) + "Системная"
- "Forest (светлая)" вызывает self._cb_theme("forest_light"), checked когда settings.theme == "forest_light"
- "Forest (тёмная)" вызывает self._cb_theme("forest_dark"), checked когда settings.theme == "forest_dark"
- test_themes.py: обновлены тесты default/fallback, добавлены 2 новых теста для Forest палитр — все проходят
- test_settings.py: обновлены assertions с "light" → "forest_light" где имеется в виду default (не user choice); все тесты проходят
- conftest.py: если там был неявный ThemeManager() — обновлён; явный initial="light" оставлен без изменений
- test_infrastructure.py: assertion согласован с fixture
- `python -m pytest client/tests/ui/test_themes.py client/tests/ui/test_settings.py client/tests/test_infrastructure.py` → PASS без errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human smoke-test — launch app, switch between 5 themes</name>
  <what-built>
- Две новые Forest-палитры в PALETTES
- forest_light — новый default theme (новая установка)
- Tray-меню с 5 темами (+ Системная)
- Обратная совместимость: старые settings.json с theme=light/dark/beige остаются валидны
- Обновлённые тесты под новые дефолты и состав палитр
  </what-built>
  <how-to-verify>
1. **Backup existing settings** (если есть):
   ```
   mv %APPDATA%/ЛичныйЕженедельник/settings.json %APPDATA%/ЛичныйЕженедельник/settings.json.bak 2>nul
   ```
   (Команда не обязательна — `validate()` справится с неизвестными значениями. Но чистый запуск проще для первичного теста.)

2. **Запустить приложение**:
   ```
   cd "S:/Проекты/ежедневник/.claude/worktrees/dazzling-allen-bd61dd"
   python main.py
   ```
   Ожидание: окно открывается без traceback в консоли. Визуально — приложение в forest_light palette.
   
   ПРИМЕЧАНИЕ: на Phase A widget-код ещё не обновлён, так что визуально виджеты могут выглядеть ровно как раньше (с `light`-цветами) — это OK. Главное: нет исключений, окно видимо.

3. **Открыть tray-меню** (правый клик на иконке в трее) → Настройки → Тема:
   - Убедиться что присутствуют ВСЕ 6 пунктов: Светлая, Тёмная, Бежевая, Forest (светлая), Forest (тёмная), Системная.
   - Checkmark должен стоять на "Forest (светлая)" (новый default).

4. **Переключить каждую тему** по очереди, убедиться:
   - Клик "Светлая" → no traceback, checkmark перемещается.
   - Клик "Тёмная" → аналогично.
   - Клик "Бежевая" → аналогично.
   - Клик "Forest (светлая)" → аналогично.
   - Клик "Forest (тёмная)" → аналогично.
   - Клик "Системная" → аналогично.
   Никаких visual glitches не ожидается в Phase A — только проверяем отсутствие crash.

5. **Проверить migration**: 
   - Закрыть приложение.
   - Открыть `%APPDATA%/ЛичныйЕженедельник/settings.json` в текстовом редакторе.
   - Изменить значение `theme` на `"nonexistent"`, сохранить.
   - Запустить `python main.py` снова.
   - Закрыть приложение.
   - Открыть settings.json → поле theme должно стать `"forest_light"` (validate() мигрировал).

6. **Проверить backward-compat**:
   - В settings.json вручную установить `theme: "light"`, сохранить.
   - Запустить приложение.
   - В tray-меню Тема → checkmark на "Светлая" (не мигрировало).

Все 6 шагов должны пройти без traceback в консоли Python.
  </how-to-verify>
  <resume-signal>
Напиши "approved" если всё работает, или опиши проблему (traceback, wrong checkmark, crash при смене темы). 
После approval — Никита вручную проверит и решит нужен ли commit. Согласно CLAUDE.md проекта: коммиты только с явного разрешения пользователя.
  </resume-signal>
</task>

</tasks>

<verification>

**Автоматизированная:**
```bash
cd "S:/Проекты/ежедневник/.claude/worktrees/dazzling-allen-bd61dd"
python -m pytest client/tests/ui/test_themes.py client/tests/ui/test_settings.py client/tests/test_infrastructure.py -v
```
Ожидание: все тесты PASS (включая новые для Forest палитр).

**Smoke-test импортов:**
```bash
python -c "from client.ui.themes import PALETTES, ThemeManager; from client.ui.settings import UISettings, VALID_THEMES; from client.utils.tray import TrayManager; assert len(PALETTES) == 5; assert UISettings().theme == 'forest_light'; print('imports OK')"
```

**Human smoke-test:**
- `python main.py` → окно видимо, нет traceback
- Tray menu → Тема → 6 radio-пунктов, переключение работает

</verification>

<success_criteria>

Phase A завершена когда:
- [ ] PALETTES содержит 5 тем (light, dark, beige, forest_light, forest_dark)
- [ ] Каждая Forest-палитра имеет 11 ключей с точными hex из спецификации (verbatim)
- [ ] ThemeManager() default → forest_light; fallback на unknown → forest_light
- [ ] UISettings() default theme → forest_light; VALID_THEMES включает Forest варианты
- [ ] validate() мигрирует unknown theme → forest_light; light/dark/beige остаются валидны (backward-compat)
- [ ] Tray-меню показывает 5 радио-тем + Системная; checkmarks работают для всех
- [ ] Старые тесты обновлены под новые дефолты; 2 новых теста для Forest палитр проходят
- [ ] Приложение запускается без traceback, можно переключаться между всеми темами
- [ ] `git diff --stat` показывает правки ТОЛЬКО в: themes.py, settings.py, tray.py, test_themes.py, test_settings.py, conftest.py (если потребовалось), test_infrastructure.py (если потребовалось)
- [ ] НЕТ правок в widget-файлах (sidebar.py, week_view.py, day_panel.py, task_widget.py, notes_panel.py, stats_panel.py, settings_panel.py, icon_compose.py, overlay.py)
- [ ] Human checkpoint approved
- [ ] Коммит сделан ТОЛЬКО если Никита дал разрешение (по правилу CLAUDE.md)

</success_criteria>

<non_goals>

**Explicit out-of-scope для Phase A** (будет в Phase B-F):

- Widget-level изменения цветов: sidebar, week-view, day-panel, task-widget, notes-panel, stats-panel, settings-panel — НЕ трогать
- `client/ui/icon_compose.py` (overlay icon rendering) — НЕ трогать (Phase E)
- Шрифтовые семейства и размеры (FONTS dict в themes.py) — НЕ менять
- Corner radii, borders, spacing — НЕ менять
- Drag-controller, undo-toast логика — НЕ трогать
- Удаление старых палитр (light/dark/beige) — остаются для A/B сравнения
- CTk built-in widgets appearance mode для forest_dark — НЕ адаптировать (зафиксируем visual glitch в Phase B если найдётся)
- Remapping logic "old light → forest_light" на migration — НЕ делать; validate() с forest_light fallback — достаточно
- Удаление или переименование legacy названий тем в коде/тестах/документации
- Обновление UI-SPEC.md или других design-docs — это отдельная задача

</non_goals>

<output>
После завершения (и approval от Никиты) создать `.planning/quick/260420-x69-phase-a-forest-add-forest-light-and-fore/260420-x69-SUMMARY.md` со списком:
- Какие файлы изменены (git diff --stat)
- Точное состояние PALETTES (список 5 тем)
- Поведение миграции (подтверждённое smoke-test)
- Ссылка на следующую Phase (B — widget cutover)
</output>
