---
phase: 260421-06u-frameless-main-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/ui/main_window.py
autonomous: false
requirements:
  - FRAMELESS-01
  - FRAMELESS-02
  - FRAMELESS-03
  - FRAMELESS-04
  - FRAMELESS-05

must_haves:
  truths:
    - "Главное окно открывается без нативного Windows title bar"
    - "Видна кастомная шапка 28px: 3px forest-полоса слева, заголовок 'Еженедельник', ✕ справа"
    - "Перетаскивание окна за шапку работает; позиция сохраняется в settings.json"
    - "Клик по ✕ прячет окно в трей (то же поведение что WM_DELETE_WINDOW)"
    - "Keyboard shortcuts (Ctrl+Left/Right/T, Ctrl+Space) продолжают работать после overrideredirect"
    - "На Win11 углы скруглены через DWM; на Win10 — приемлемо квадратные (без крашей)"
  artifacts:
    - path: "client/ui/main_window.py"
      provides: "MainWindow с overrideredirect, кастомной шапкой, drag-логикой, DWM rounded corners"
      contains: "_init_frameless_style, _build_title_bar, _on_title_drag_start, _on_title_drag_motion, _on_title_drag_end"
  key_links:
    - from: "MainWindow.__init__"
      to: "self._window.overrideredirect(True)"
      via: "self._window.after(100, self._init_frameless_style) — Pitfall 1 Win11 DWM timing"
      pattern: "after\\(.*_init_frameless_style"
    - from: "Custom ✕ button click"
      to: "self._on_close()"
      via: "CTkLabel.bind('<Button-1>', ...)"
      pattern: "bind\\(.*Button-1.*_on_close"
    - from: "Title bar drag-release"
      to: "SettingsStore.save"
      via: "_save_window_state() в _on_title_drag_end"
      pattern: "_save_window_state"
    - from: "DWM rounded corners"
      to: "ctypes.windll.dwmapi.DwmSetWindowAttribute"
      via: "GetParent(self._window.winfo_id()) + DWMWCP_ROUND=2, try/except"
      pattern: "DwmSetWindowAttribute"
---

<objective>
Убрать нативный Windows title bar у главного окна планировщика и заменить его минимальной 28px шапкой в стиле Forest (accent-полоса + заголовок + ✕). Добавить DWM скруглённые углы для Windows 11 (silent fail на Win10). Сохранить drag-логику, persist позиции, keyboard-шорткаты и поведение "закрыть = скрыть в трей".

Purpose: Устранить визуальный диссонанс между OS chrome (белый title bar + прямые углы) и Forest-эстетикой панели. Это Phase A2 — точечный follow-up после Phase A (палитра) перед Phase B (structure/today-bar).

Output: Обновлённый `client/ui/main_window.py` с frameless-окном и кастомной шапкой.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@client/ui/main_window.py
@client/ui/overlay.py
@client/ui/themes.py
@client/ui/settings.py
@client/ui/week_navigation.py

<interfaces>
<!-- Ключевые типы/сигнатуры уже в коде — чтобы executor не рыл кодовую базу -->

From client/ui/themes.py (палитра forest_light/forest_dark уже содержит нужные ключи):
- palette["bg_primary"]       — фон окна и шапки
- palette["bg_secondary"]     — hover fg_color для ✕
- palette["bg_tertiary"]      — 1px separator border-bottom
- palette["text_secondary"]   — цвет заголовка "Еженедельник"
- palette["text_tertiary"]    — цвет ✕ в idle
- palette["accent_brand"]     — 3px вертикальная forest-полоса слева
- palette["accent_overdue"]   — цвет ✕ на hover (destructive-clay)

From client/ui/overlay.py (reference — НЕ импортировать, паттерн копируется):
```python
# DWM constants
DWMWA_WINDOW_CORNER_PREFERENCE = 33
DWMWCP_ROUND = 2

# Паттерн применения после overrideredirect:
hwnd = ctypes.windll.user32.GetParent(self._overlay.winfo_id())
value = ctypes.c_int(DWMWCP_ROUND)
ctypes.windll.dwmapi.DwmSetWindowAttribute(
    hwnd, DWMWA_WINDOW_CORNER_PREFERENCE,
    ctypes.byref(value), ctypes.sizeof(value),
)

# PITFALL 1: overrideredirect через after(100, ...):
self._overlay.after(self.INIT_DELAY_MS, self._init_overlay_style)
```

From client/ui/main_window.py (точки вмешательства):
- `__init__` — после `self._window.protocol("WM_DELETE_WINDOW", self._on_close)` вставить вызов `self._window.after(100, self._init_frameless_style)`
- `_build_ui` — добавить `self._build_title_bar(self._root_frame)` ПЕРЕД `self._week_nav = WeekNavigation(...)`
- `_on_close` — уже существует, трогать не надо; ✕ должен вызывать его же

From client/ui/settings.py:
- `UISettings.window_position: Optional[list]` — [x, y]
- `SettingsStore.save(settings)` — atomic write
- MainWindow уже имеет `self._settings_store` и `self._settings`
- `_save_window_state()` — уже существует, читает winfo_x/y/width/height и сохраняет
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frameless window + custom title bar + drag + DWM rounded corners</name>
  <files>client/ui/main_window.py</files>
  <action>
Модифицировать `client/ui/main_window.py` (класс `MainWindow`). Только этот файл — никаких изменений в overlay.py, update_banner.py, edit_dialog.py, week_navigation.py.

**1. Импорты (верх файла, после существующих):**
Добавить `import ctypes` рядом с остальными импортами. `logging`, `tkinter as tk`, `customtkinter as ctk` уже есть.

**2. Константы класса (после `DEFAULT_SIZE`):**
```python
TITLE_BAR_HEIGHT = 28
ACCENT_STRIP_WIDTH = 3
FRAMELESS_INIT_DELAY_MS = 100   # Pitfall 1: Win11 DWM timing — копия паттерна из overlay.py
DWM_CORNER_DELAY_MS = 150       # Дополнительная задержка перед DWM-вызовом
DWMWA_WINDOW_CORNER_PREFERENCE = 33
DWMWCP_ROUND = 2
```

**3. Инициализация frameless в `__init__`:**
После `self._window.protocol("WM_DELETE_WINDOW", self._on_close)` и ПЕРЕД `self._day_sections = {}` добавить:
```python
# Drag-state (для кастомной шапки — см. _on_title_drag_*)
self._title_drag_offset_x = 0
self._title_drag_offset_y = 0

# Ссылки на виджеты шапки (заполняются в _build_title_bar, используются в _apply_theme)
self._title_bar: Optional[ctk.CTkFrame] = None
self._title_accent_strip: Optional[ctk.CTkFrame] = None
self._title_label: Optional[ctk.CTkLabel] = None
self._title_close_btn: Optional[ctk.CTkLabel] = None
self._title_separator: Optional[ctk.CTkFrame] = None
```

**4. Добавить инстанс-метод `_init_frameless_style`:**
Вызывается после задержки — снимает native chrome и применяет DWM.
```python
def _init_frameless_style(self) -> None:
    """Pitfall 1: Win11 DWM требует overrideredirect через after(100, ...).

    Копия паттерна из OverlayManager._init_overlay_style (без импорта — cross-phase
    coupling избегаем умышленно).
    """
    try:
        self._window.overrideredirect(True)
    except tk.TclError as exc:
        logger.debug("overrideredirect failed: %s", exc)
        return
    # Переприменить topmost (overrideredirect может сбросить)
    try:
        self._window.attributes("-topmost", self._settings.on_top)
    except tk.TclError:
        pass
    # DWM rounded corners (Win11) — после доп. задержки чтоб HWND был готов
    self._window.after(self.DWM_CORNER_DELAY_MS, self._apply_dwm_rounded_corners)

def _apply_dwm_rounded_corners(self) -> None:
    """Win11: DWMWCP_ROUND. Win10: silent fail (E_INVALIDARG)."""
    try:
        hwnd = ctypes.windll.user32.GetParent(self._window.winfo_id())
        value = ctypes.c_int(self.DWMWCP_ROUND)
        ctypes.windll.dwmapi.DwmSetWindowAttribute(
            hwnd, self.DWMWA_WINDOW_CORNER_PREFERENCE,
            ctypes.byref(value), ctypes.sizeof(value),
        )
        logger.debug("MainWindow DWM rounded corners applied (hwnd=%s)", hwnd)
    except Exception as exc:
        logger.debug("DWM rounded corners skipped (Win10?): %s", exc)
```
Затем в конце `__init__` (после существующей биндинга `<Control-space>`) добавить:
```python
self._window.after(self.FRAMELESS_INIT_DELAY_MS, self._init_frameless_style)
```

**5. Добавить `_build_title_bar(self, parent)` и вызвать из `_build_ui`:**
В `_build_ui` ВСТАВИТЬ СРАЗУ ПОСЛЕ `self._root_frame.pack(fill="both", expand=True)` и ПЕРЕД `self._week_nav = WeekNavigation(...)`:
```python
self._build_title_bar(self._root_frame)
```

Метод:
```python
def _build_title_bar(self, parent: ctk.CTkFrame) -> None:
    """Кастомная шапка 28px: 3px forest-полоса, заголовок 'Еженедельник', ✕.

    Drag-логика привязывается к self._title_bar и self._title_label (но НЕ к ✕).
    ✕ → self._on_close (тот же путь что WM_DELETE_WINDOW).
    """
    palette_bg = self._theme.get("bg_primary")
    palette_sep = self._theme.get("bg_tertiary")
    palette_accent = self._theme.get("accent_brand")
    palette_text = self._theme.get("text_secondary")
    palette_close = self._theme.get("text_tertiary")

    # Контейнер шапки
    self._title_bar = ctk.CTkFrame(
        parent, height=self.TITLE_BAR_HEIGHT,
        fg_color=palette_bg, corner_radius=0,
    )
    self._title_bar.pack(fill="x", side="top")
    self._title_bar.pack_propagate(False)

    # 3px accent strip слева
    self._title_accent_strip = ctk.CTkFrame(
        self._title_bar, width=self.ACCENT_STRIP_WIDTH,
        fg_color=palette_accent, corner_radius=0,
    )
    self._title_accent_strip.pack(side="left", fill="y")
    self._title_accent_strip.pack_propagate(False)

    # ✕ кнопка (пакается ПЕРВОЙ справа — до title_label — чтобы lable занял оставшееся)
    self._title_close_btn = ctk.CTkLabel(
        self._title_bar, text="✕", width=self.TITLE_BAR_HEIGHT,
        height=self.TITLE_BAR_HEIGHT,
        fg_color="transparent", text_color=palette_close,
        cursor="hand2", font=FONTS["caption"],
    )
    self._title_close_btn.pack(side="right")
    self._title_close_btn.bind("<Button-1>", lambda e: self._on_close())
    # Hover: destructive-clay + bg_secondary background
    self._title_close_btn.bind(
        "<Enter>",
        lambda e: self._title_close_btn.configure(
            text_color=self._theme.get("accent_overdue"),
            fg_color=self._theme.get("bg_secondary"),
        ),
    )
    self._title_close_btn.bind(
        "<Leave>",
        lambda e: self._title_close_btn.configure(
            text_color=self._theme.get("text_tertiary"),
            fg_color="transparent",
        ),
    )

    # Заголовок (анкор left с отступом 10px от accent strip)
    self._title_label = ctk.CTkLabel(
        self._title_bar, text="Еженедельник",
        fg_color="transparent", text_color=palette_text,
        font=FONTS["caption"], anchor="w",
    )
    self._title_label.pack(side="left", fill="both", expand=True, padx=(10, 0))

    # 1px separator border-bottom
    self._title_separator = ctk.CTkFrame(
        parent, height=1, fg_color=palette_sep, corner_radius=0,
    )
    self._title_separator.pack(fill="x", side="top")

    # Drag bindings: на контейнер шапки И заголовок (НЕ на ✕ — он свой обработчик имеет)
    for w in (self._title_bar, self._title_label, self._title_accent_strip):
        w.bind("<ButtonPress-1>", self._on_title_drag_start)
        w.bind("<B1-Motion>", self._on_title_drag_motion)
        w.bind("<ButtonRelease-1>", self._on_title_drag_end)
```

**6. Drag handlers:**
```python
def _on_title_drag_start(self, event) -> None:
    """Запомнить offset курсор→левый-верхний-угол окна."""
    try:
        self._title_drag_offset_x = event.x_root - self._window.winfo_x()
        self._title_drag_offset_y = event.y_root - self._window.winfo_y()
    except tk.TclError:
        pass

def _on_title_drag_motion(self, event) -> None:
    """Переместить окно за курсором. Не зажимаем в bounds — окно большое,
    virtual desktop clamp тут излишен (overlay это оправданно 56px)."""
    try:
        new_x = event.x_root - self._title_drag_offset_x
        new_y = event.y_root - self._title_drag_offset_y
        self._window.geometry(f"+{new_x}+{new_y}")
    except tk.TclError:
        pass

def _on_title_drag_end(self, event) -> None:
    """Persist позицию через SettingsStore — тот же путь что _save_window_state."""
    self._save_window_state()
```

**7. Обновить `_apply_theme` чтобы перекрашивать шапку:**
Расширить существующий метод (ничего не удалять!):
```python
def _apply_theme(self, palette: dict) -> None:
    bg = palette.get("bg_primary", "#F5EFE6")
    try:
        self._window.configure(fg_color=bg)
        if hasattr(self, "_root_frame"):
            self._root_frame.configure(fg_color=bg)
    except tk.TclError:
        pass
    # Title bar перекрашивание
    try:
        if self._title_bar is not None and self._title_bar.winfo_exists():
            self._title_bar.configure(fg_color=palette.get("bg_primary"))
        if self._title_accent_strip is not None and self._title_accent_strip.winfo_exists():
            self._title_accent_strip.configure(fg_color=palette.get("accent_brand"))
        if self._title_label is not None and self._title_label.winfo_exists():
            self._title_label.configure(text_color=palette.get("text_secondary"))
        if self._title_close_btn is not None and self._title_close_btn.winfo_exists():
            self._title_close_btn.configure(text_color=palette.get("text_tertiary"))
        if self._title_separator is not None and self._title_separator.winfo_exists():
            self._title_separator.configure(fg_color=palette.get("bg_tertiary"))
    except tk.TclError:
        pass
```

**8. ЧТО НЕ ТРОГАТЬ (проверить что не сломалось):**
- `WM_DELETE_WINDOW` протокол — оставить как есть; Alt+F4 всё ещё генерит WM_CLOSE.
- `self._window.bind("<Configure>", ...)` — работает после overrideredirect.
- `self._window.bind("<Control-space>", ...)` — работает (биндинги на окне, не на chrome).
- WeekNavigation внутри неё биндится на `self._window` для Ctrl+Left/Right/T — работает.
- `self._window.attributes("-topmost", ...)` — переприменяется в `_init_frameless_style` после overrideredirect.
- `_root_frame.corner_radius=0` (как было) + window bg = palette bg → визуально без швов на Win10.

**9. Логирование:**
Уже настроен module-level `logger = logging.getLogger(__name__)`. Использовать `logger.debug` для DWM и overrideredirect ошибок (не crash, не warning — silent на Win10).
  </action>
  <verify>
    <automated>python -c "import ast, sys; tree = ast.parse(open('client/ui/main_window.py', encoding='utf-8').read()); names = {n.name for c in ast.walk(tree) if isinstance(c, ast.ClassDef) and c.name=='MainWindow' for n in c.body if isinstance(n, ast.FunctionDef)}; required = {'_init_frameless_style','_apply_dwm_rounded_corners','_build_title_bar','_on_title_drag_start','_on_title_drag_motion','_on_title_drag_end'}; missing = required - names; sys.exit(0 if not missing else print(f'MISSING: {missing}') or 1)"</automated>
  </verify>
  <done>
- `client/ui/main_window.py` парсится (no SyntaxError)
- `MainWindow` содержит методы: `_init_frameless_style`, `_apply_dwm_rounded_corners`, `_build_title_bar`, `_on_title_drag_start`, `_on_title_drag_motion`, `_on_title_drag_end`
- `import ctypes` присутствует
- Константы `TITLE_BAR_HEIGHT=28`, `ACCENT_STRIP_WIDTH=3`, `DWMWCP_ROUND=2`, `DWMWA_WINDOW_CORNER_PREFERENCE=33` добавлены
- В `__init__` вызов `self._window.after(self.FRAMELESS_INIT_DELAY_MS, self._init_frameless_style)` присутствует
- `_build_ui` вызывает `self._build_title_bar(self._root_frame)` ПЕРЕД `self._week_nav = WeekNavigation(...)`
- DWM-вызов обёрнут в `try/except` с `logger.debug`
- ✕ bind вызывает `self._on_close()` — то же что `WM_DELETE_WINDOW`
- `_apply_theme` расширен (не заменён!) — все виджеты шапки перекрашиваются
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Визуальная проверка frameless окна</name>
  <what-built>
Главное окно без нативного Windows title bar. Кастомная шапка 28px:
- 3px вертикальная forest-полоса слева (accent_brand)
- Заголовок "Еженедельник" мелкий (font caption, text_secondary)
- ✕ справа 28×28, меняет цвет на клай-красный + bg_secondary фон при hover
- 1px разделитель снизу (bg_tertiary)
На Win11 углы скруглены через DWM. На текущем Win10 (19045) — приемлемо квадратные.
  </what-built>
  <how-to-verify>
Из корня проекта (`S:\Проекты\ежедневник\.claude\worktrees\dazzling-allen-bd61dd`):

1. Запуск: `python main.py`

2. Окно появляется БЕЗ нативного Windows title bar (нет строки "Личный Еженедельник" сверху, нет ─ ☐ ✕ кнопок ОС).

3. Кастомная шапка видна:
   - Слева 3px forest-зелёная полоса (в forest_light палитре — #1E5239)
   - Заголовок "Еженедельник" мелким шрифтом, приглушённый серо-коричневый
   - ✕ справа, приглушённый (text_tertiary)

4. Hover на ✕: символ краснеет (accent_overdue), появляется фон bg_secondary. Отведение мыши — возврат к idle.

5. Клик на ✕: окно скрывается (уходит в трей). Тап по иконке трея или Win+Q (хоткей) — окно возвращается.

6. Drag: зажать ЛКМ на шапке (но не на ✕), потянуть — окно движется за курсором. Отпустить — окно остаётся на новой позиции. Перезапустить `python main.py` — окно открывается на ТОЙ ЖЕ позиции (persistence работает).

7. Keyboard shortcuts:
   - Ctrl+Left / Ctrl+Right — переключение недель (стрелки в week_nav реагируют)
   - Ctrl+T — возврат на текущую неделю
   - Ctrl+Space — открывает QuickCapture (если wired в приложении)

8. Always-on-top: через трей-меню → "Всегда сверху" (если есть toggle) — окно остаётся поверх других. Проверить что после overrideredirect топмост сохранён.

9. Week navigation, day sections, tasks, edit dialog — всё рендерится ниже шапки без перекрытий.

10. Консоль: никаких tracebacks. Допустимы debug-сообщения про DWM (на Win10 будет `logger.debug("DWM rounded corners skipped (Win10?): ...")`).

11. (Опционально, если доступен Win11 PC) — углы окна скруглены.
  </how-to-verify>
  <resume-signal>Напиши "approved" если всё работает, либо опиши проблемы (скриншот приветствуется).</resume-signal>
</task>

</tasks>

<verification>
Плановые проверки:
- AST-проверка в Task 1 подтверждает наличие всех требуемых методов
- Визуальная проверка в Task 2 покрывает все acceptance criteria
- Нет изменений в overlay.py, update_banner.py, edit_dialog.py, week_navigation.py, settings.py — cross-phase coupling избежан
</verification>

<success_criteria>
- `python main.py` запускается без ошибок
- Нативный Windows title bar отсутствует
- Кастомная шапка 28px видна и корректна визуально
- Drag через шапку перемещает окно; позиция сохраняется между запусками
- ✕ прячет в трей (тот же callback что WM_DELETE_WINDOW)
- Tray и хоткеи продолжают работать
- Keyboard shortcuts (Ctrl+Left/Right/T, Ctrl+Space) работают
- Always-on-top сохранён
- Темы forest_light/forest_dark корректно перекрашивают шапку при live-switching
- На Win11 — DWM rounded corners; на Win10 — silent fail без crash
</success_criteria>

<output>
После завершения создать `.planning/quick/260421-06u-frameless-main-window-remove-windows-tit/260421-06u-SUMMARY.md` с:
- Что было сделано (frameless + custom header + drag + DWM)
- Какие файлы изменены (только `client/ui/main_window.py`)
- Подтверждение acceptance criteria после визуальной проверки
- Замечания о Win10 fallback и готовности для Phase B
</output>
