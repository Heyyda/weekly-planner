---
phase: 03-overlay-system
plan: 06
type: execute
wave: 3
depends_on: [02]
files_modified:
  - client/ui/main_window.py
  - client/tests/ui/test_main_window.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "MainWindow — ресайзабельное CTkToplevel, min 320x320, default 460x600 (UI-SPEC §Resizable)"
    - "Позиция и размер сохраняются в settings через SettingsStore при close/resize"
    - "toggle() переключает visibility; show()/hide() — явные методы"
    - "set_always_on_top применяется к main window (для OVR-06 propagation из overlay)"
    - "Window содержит placeholder-аккордеон секций дней (Пн-Вс), заглушки — Phase 4 наполнит"
    - "Today-секция имеет ДВА индикатора: (a) синяя вертикальная полоска 3px слева (accent_brand) + (b) bold заголовок (D-07, UI-SPEC §Day Section — Expanded State)"
    - "theme subscribe: при смене темы MainWindow обновляет fg_color своих виджетов ВКЛЮЧАЯ today-strip (accent_brand)"
  artifacts:
    - path: "client/ui/main_window.py"
      provides: "MainWindow — shell с аккордеоном дней + navigation header + resizable + today-strip"
      contains: "class MainWindow"
      min_lines: 130
  key_links:
    - from: "client/ui/main_window.py"
      to: "client/ui/settings.py (SettingsStore)"
      via: "save window_position + window_size on resize/close"
      pattern: "window_size|window_position"
    - from: "client/ui/main_window.py"
      to: "client/ui/themes.py (ThemeManager.subscribe)"
      via: "theme switch re-configures fg_color"
      pattern: "theme_manager\\.subscribe"
    - from: "client/ui/main_window.py today-section"
      to: "palette['accent_brand']"
      via: "blue 3px vertical strip (CTkFrame width=3) — D-07 today-indicator"
      pattern: "accent_brand"
---

<objective>
MainWindow — shell главного окна планировщика: resizable CTkToplevel, вертикальный аккордеон
заглушек-дней (Пн-Вс), navigation header (стрелки ← → "Сегодня"), тема-aware.

Phase 3 scope: СТРУКТУРА window, управление lifecycle (show/hide/toggle/destroy), persistence
размера+позиции, navigation placeholder, theme propagation, **today-indicator (D-07: синяя
полоска 3px + bold заголовок)**. БЕЗ actual task rendering (Phase 4).

Purpose: OVR-04 — click по overlay открывает ЭТО окно. Если окно не готово к показу, весь
overlay-flow не имеет смысла. Также OVR-06 требует что main window уважает always-on-top
toggle (см. overlay.on_top_changed hook из Plan 03-04).

Placeholder-дни — пустые CTkFrame в аккордеоне с заголовком `"Пн 14 (0)"`, без задач.
Today-секция получает ОБА индикатора согласно D-07 и UI-SPEC §Day Section — Expanded State:
  (a) Синяя вертикальная полоска 3px слева (accent_brand) — визуальный "strip"
  (b) Bold заголовок с суффиксом "• сегодня"
Plan 03-10 wire'нет main window как target для overlay.on_click. Phase 4 заменит placeholder
на настоящий task list.

Output: `client/ui/main_window.py` MainWindow + headless-тесты.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-overlay-system/03-UI-SPEC.md
@client/ui/themes.py
@client/ui/settings.py

<interfaces>
<!-- Dependencies -->
From client/ui/settings.py:
```python
@dataclass
class UISettings:
    window_size: list = [460, 600]
    window_position: Optional[list] = None  # None = auto-center
    on_top: bool = True

class SettingsStore:
    def load(self) -> UISettings: ...
    def save(self, settings: UISettings) -> None: ...
```

From client/ui/themes.py:
```python
class ThemeManager:
    def subscribe(self, callback: Callable[[dict], None]) -> None: ...
    def get(self, key: str) -> str: ...
```

<!-- New interface -->
Target API for client/ui/main_window.py:
```python
class MainWindow:
    MIN_SIZE = (320, 320)
    DEFAULT_SIZE = (460, 600)
    TODAY_STRIP_WIDTH = 3  # D-07: синяя полоска 3px

    def __init__(
        self,
        root,
        settings_store,
        settings,
        theme_manager,
    ): ...

    def show(self) -> None: ...
    def hide(self) -> None: ...
    def toggle(self) -> None: ...     # wired to overlay.on_click
    def is_visible(self) -> bool: ...
    def set_always_on_top(self, enabled: bool) -> None: ...
    def destroy(self) -> None: ...

    def _apply_theme(self, palette: dict[str, str]) -> None: ...  # theme subscriber
    def _save_window_state(self) -> None: ...                     # on close/resize
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать MainWindow — shell + аккордеон-placeholder + today-strip (D-07) + persistence + theme subscribe</name>
  <files>client/ui/main_window.py, client/tests/ui/test_main_window.py</files>

  <read_first>
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Main Window (layout, Resizable, Day Section, Header)
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Day Section — Expanded State (CRITICAL: two indicators — blue strip 3px + bold)
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Color Palette (bg_primary, text_primary, accent_brand)
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Typography (h1 14 bold, body 13, caption 11)
    - .planning/phases/03-overlay-system/03-CONTEXT.md §D-07 (Today-indicator: синяя полоска + bold заголовок оба одновременно)
    - client/ui/themes.py (subscribe API, palette keys including accent_brand)
    - client/ui/settings.py (UISettings.window_size + window_position)
  </read_first>

  <behavior>
    - Test 1: MainWindow(root, ...) создаётся как Toplevel, withdrawn изначально (невидим)
    - Test 2: show() → is_visible() True; hide() → False; toggle переключает
    - Test 3: Минимальный размер 320x320 применяется через minsize
    - Test 4: Default size 460x600 если window_size=default
    - Test 5: При resize _save_window_state() вызывается (тест через direct call, не events)
    - Test 6: set_always_on_top(True) устанавливает -topmost=True на главное окно
    - Test 7: Header содержит 7 day-section frames (Пн-Вс)
    - Test 8: Каждая day section имеет заголовок в формате "{День} {число}"
    - Test 9: _apply_theme({"bg_primary": "#123456"}) меняет fg_color главного фрейма
    - Test 10: theme_manager.subscribe вызван в __init__ (проверяем через spy)
    - Test 11: destroy() убирает окно
    - Test 12: При window_position=None окно не позиционируется (авто-центр)
    - **Test 13 (D-07 strip):** Today-секция имеет инстанс-атрибут `_today_strip` — CTkFrame шириной 3px, fg_color == palette['accent_brand'] (синий акцент)
    - **Test 14 (D-07 bold):** Today-секция имеет bold font в заголовке (сохраняется существующая логика)
    - **Test 15 (D-07 theme):** При _apply_theme с новым accent_brand ("#ff0000") цвет today-strip обновляется (проверка через configure() call или post-update fg_color)
    - **Test 16 (D-07 non-today):** Секции НЕ сегодняшних дней не имеют today-strip (strip отсутствует либо width=0)
  </behavior>

  <action>
    ШАГ 1 — Создать `client/ui/main_window.py`:

    ```python
    """
    MainWindow — главное окно планировщика (shell, Phase 3).

    Scope Phase 3:
      - Resizable CTkToplevel, min 320x320, default 460x600 (UI-SPEC)
      - Navigation header: ← стрелка, "Неделя N", → стрелка, "Сегодня" кнопка
      - Аккордеон placeholder-дней Пн-Вс (без содержимого — Phase 4 добавит)
      - Today-indicator (D-07): синяя полоска 3px слева (accent_brand) + bold заголовок
      - Persistence window size+position через SettingsStore
      - Theme-aware через ThemeManager.subscribe (today-strip перекрашивается при смене темы)

    Scope Phase 4:
      - Реальные task rendering
      - Навигация между неделями (работающая)
      - Add/edit/delete задач
      - Drag-and-drop
    """
    from __future__ import annotations

    import logging
    import tkinter as tk
    from datetime import date, timedelta
    from typing import Optional

    import customtkinter as ctk

    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import FONTS, ThemeManager

    logger = logging.getLogger(__name__)

    DAY_NAMES_RU = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]


    class MainWindow:
        """Главное окно — аккордеон дней + navigation. См. 03-UI-SPEC §Main Window."""

        MIN_SIZE = (320, 320)
        DEFAULT_SIZE = (460, 600)
        TODAY_STRIP_WIDTH = 3  # D-07: синяя полоска 3px слева today-секции

        def __init__(
            self,
            root: ctk.CTk,
            settings_store: SettingsStore,
            settings: UISettings,
            theme_manager: ThemeManager,
        ) -> None:
            self._root = root
            self._settings_store = settings_store
            self._settings = settings
            self._theme = theme_manager

            self._window = ctk.CTkToplevel(root)
            self._window.withdraw()
            self._window.title("Личный Еженедельник")
            self._window.minsize(*self.MIN_SIZE)

            # Восстановить размер
            w, h = self._resolve_initial_size()
            self._window.geometry(f"{w}x{h}")

            # Восстановить позицию (если None — CTk автоцентрирует)
            pos = self._settings.window_position
            if pos is not None and len(pos) == 2:
                try:
                    x, y = int(pos[0]), int(pos[1])
                    self._window.geometry(f"{w}x{h}+{x}+{y}")
                except (TypeError, ValueError):
                    pass

            # Always-on-top из settings
            try:
                self._window.attributes("-topmost", self._settings.on_top)
            except tk.TclError:
                pass

            # Protocol — перехват close через tray, не реальный exit
            self._window.protocol("WM_DELETE_WINDOW", self._on_close)

            # Content
            self._day_sections: list[ctk.CTkFrame] = []
            # D-07: reference к today-strip (CTkFrame width=3 accent_brand). None если секция не today.
            self._today_strip: Optional[ctk.CTkFrame] = None
            self._build_ui()

            # Theme subscribe
            self._theme.subscribe(self._apply_theme)
            self._apply_theme({
                "bg_primary": self._theme.get("bg_primary"),
                "text_primary": self._theme.get("text_primary"),
                "bg_secondary": self._theme.get("bg_secondary"),
                "accent_brand": self._theme.get("accent_brand"),
            })

            # Сохранять размер при resize (через Configure event)
            self._window.bind("<Configure>", self._on_configure)

        # ---- Public API ----

        def show(self) -> None:
            self._window.deiconify()
            self._window.lift()

        def hide(self) -> None:
            self._window.withdraw()

        def toggle(self) -> None:
            """Wired to overlay.on_click — переключает visibility."""
            if self.is_visible():
                self.hide()
            else:
                self.show()

        def is_visible(self) -> bool:
            try:
                return self._window.winfo_viewable() != 0
            except tk.TclError:
                return False

        def set_always_on_top(self, enabled: bool) -> None:
            """OVR-06 propagation из OverlayManager.on_top_changed."""
            try:
                self._window.attributes("-topmost", enabled)
            except tk.TclError:
                pass

        def destroy(self) -> None:
            try:
                self._window.destroy()
            except Exception as exc:
                logger.debug("MainWindow destroy: %s", exc)

        # ---- Theme ----

        def _apply_theme(self, palette: dict) -> None:
            """ThemeManager.subscribe callback — перекрасить контейнеры.

            D-07: today-strip (если есть) перекрашивается в palette['accent_brand'].
            """
            bg = palette.get("bg_primary", "#F5EFE6")
            accent = palette.get("accent_brand", "#1E73E8")
            try:
                self._window.configure(fg_color=bg)
                if hasattr(self, "_root_frame"):
                    self._root_frame.configure(fg_color=bg)
                for section in self._day_sections:
                    section.configure(fg_color=palette.get("bg_secondary", bg))
                # D-07: today-strip должен реагировать на смену темы
                if self._today_strip is not None:
                    try:
                        self._today_strip.configure(fg_color=accent)
                    except tk.TclError:
                        pass
            except tk.TclError:
                pass

        # ---- Layout ----

        def _build_ui(self) -> None:
            self._root_frame = ctk.CTkFrame(self._window, corner_radius=0)
            self._root_frame.pack(fill="both", expand=True)

            # Header — navigation placeholder
            header = ctk.CTkFrame(self._root_frame, height=40, corner_radius=0)
            header.pack(fill="x", side="top")
            ctk.CTkButton(header, text="←", width=32, command=lambda: None).pack(side="left", padx=4)
            ctk.CTkLabel(header, text="Неделя 16  14-20 апр",
                         font=FONTS["h1"]).pack(side="left", expand=True)
            ctk.CTkButton(header, text="Сегодня", width=72, command=lambda: None).pack(side="left", padx=4)
            ctk.CTkButton(header, text="→", width=32, command=lambda: None).pack(side="left", padx=4)

            # Scrollable content для аккордеона
            scroll = ctk.CTkScrollableFrame(self._root_frame)
            scroll.pack(fill="both", expand=True, padx=8, pady=4)

            # 7 placeholder day sections
            today = date.today()
            # weekday(): Mon=0..Sun=6
            monday = today - timedelta(days=today.weekday())
            for i in range(7):
                d = monday + timedelta(days=i)
                section = self._build_day_section(scroll, DAY_NAMES_RU[i], d, is_today=(d == today))
                self._day_sections.append(section)

        def _build_day_section(self, parent, day_name: str, d: date, is_today: bool) -> ctk.CTkFrame:
            """Placeholder-секция дня: заголовок + пустое тело. Phase 4 заменит.

            D-07 (today-specific): если is_today=True — добавить ДВА индикатора:
              (a) Синяя вертикальная полоска 3px слева (accent_brand color).
              (b) Bold font для заголовка.
            Не-today секции — обычный заголовок без strip.
            """
            section = ctk.CTkFrame(parent, corner_radius=6)
            section.pack(fill="x", pady=4)

            # Горизонтальный контейнер: [strip | content]
            row = ctk.CTkFrame(section, fg_color="transparent")
            row.pack(fill="x")

            # D-07 (a): blue strip 3px слева — ТОЛЬКО для today
            if is_today:
                accent = self._theme.get("accent_brand")
                strip = ctk.CTkFrame(
                    row,
                    width=self.TODAY_STRIP_WIDTH,
                    fg_color=accent,
                    corner_radius=0,
                )
                strip.pack(side="left", fill="y", padx=(0, 6))
                # Форсируем фиксированную ширину (CTkFrame иначе может растягиваться)
                strip.pack_propagate(False)
                self._today_strip = strip  # reference для theme switch

            # Content (header + счётчик)
            header_frame = ctk.CTkFrame(row, fg_color="transparent")
            header_frame.pack(side="left", fill="x", expand=True)

            # D-07 (b): bold font для today + суффикс "• сегодня"
            label_text = f"{day_name} {d.day}"
            if is_today:
                label_text += "  • сегодня"
            font = FONTS["body"]
            if is_today:
                font = (font[0], font[1], "bold")
            ctk.CTkLabel(header_frame, text=label_text, font=font).pack(side="left", padx=8)
            # Placeholder счётчика "(0)"
            ctk.CTkLabel(header_frame, text="(0)", font=FONTS["caption"]).pack(side="right", padx=8)
            return section

        # ---- Persistence ----

        def _resolve_initial_size(self) -> tuple:
            ws = self._settings.window_size
            try:
                w, h = int(ws[0]), int(ws[1])
                if w >= self.MIN_SIZE[0] and h >= self.MIN_SIZE[1]:
                    return (w, h)
            except (TypeError, ValueError, IndexError):
                pass
            return self.DEFAULT_SIZE

        def _on_configure(self, event) -> None:
            """Resize event — сохранить если размер изменился."""
            # Фильтр: только события именно окна, не child widgets
            if event.widget is self._window:
                try:
                    new_size = [self._window.winfo_width(), self._window.winfo_height()]
                    new_pos = [self._window.winfo_x(), self._window.winfo_y()]
                    # Обновим settings только если действительно отличается
                    if new_size != self._settings.window_size:
                        self._settings.window_size = new_size
                        self._settings.window_position = new_pos
                        # Не сохраняем на каждый пиксель — defer до _on_close
                except tk.TclError:
                    pass

        def _on_close(self) -> None:
            """WM_DELETE → сохранить состояние и hide (не destroy). App control exit через tray."""
            self._save_window_state()
            self.hide()

        def _save_window_state(self) -> None:
            try:
                self._settings.window_size = [self._window.winfo_width(),
                                              self._window.winfo_height()]
                self._settings.window_position = [self._window.winfo_x(),
                                                  self._window.winfo_y()]
                self._settings_store.save(self._settings)
                logger.debug("MainWindow state saved: %s", self._settings.window_size)
            except tk.TclError as exc:
                logger.debug("_save_window_state skip: %s", exc)
    ```

    ШАГ 2 — Создать `client/tests/ui/test_main_window.py`:

    ```python
    """Unit-тесты MainWindow (Plan 03-06). Shell + persistence + theme subscribe + D-07 today-strip."""
    from unittest.mock import MagicMock

    import pytest

    from client.core.paths import AppPaths
    from client.core.storage import LocalStorage
    from client.ui.main_window import MainWindow, DAY_NAMES_RU
    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import ThemeManager


    @pytest.fixture
    def mw_deps(tmp_appdata, headless_tk, mock_ctypes_dpi):
        storage = LocalStorage(AppPaths())
        storage.init()
        return {
            "root": headless_tk,
            "settings_store": SettingsStore(storage),
            "settings": UISettings(),
            "theme": ThemeManager(),
        }


    def test_creates_window(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        assert mw._window.winfo_exists()
        mw.destroy()


    def test_initially_hidden(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        assert not mw.is_visible()
        mw.destroy()


    def test_show_makes_visible(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        mw.show()
        mw_deps["root"].update()
        assert mw.is_visible()
        mw.destroy()


    def test_toggle_alternates(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        mw.toggle()
        mw_deps["root"].update()
        v1 = mw.is_visible()
        mw.toggle()
        mw_deps["root"].update()
        v2 = mw.is_visible()
        assert v1 != v2
        mw.destroy()


    def test_min_size_is_320(mw_deps):
        assert MainWindow.MIN_SIZE == (320, 320)


    def test_default_size_is_460x600(mw_deps):
        assert MainWindow.DEFAULT_SIZE == (460, 600)


    def test_today_strip_width_is_3(mw_deps):
        """D-07: полоска точно 3px (per UI-SPEC §Day Section — Expanded State)."""
        assert MainWindow.TODAY_STRIP_WIDTH == 3


    def test_seven_day_sections(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        assert len(mw._day_sections) == 7
        mw.destroy()


    def test_day_names_russian(mw_deps):
        assert DAY_NAMES_RU == ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]


    def test_today_section_has_blue_strip(mw_deps):
        """D-07: today-секция должна иметь инстанс _today_strip (CTkFrame, accent_brand, 3px)."""
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        # _today_strip существует (сегодня всегда в текущей неделе)
        assert mw._today_strip is not None, "Today-секция должна иметь blue strip (D-07)"
        # Цвет == accent_brand текущей темы
        strip_color = mw._today_strip.cget("fg_color")
        expected = mw_deps["theme"].get("accent_brand")
        assert strip_color == expected or (
            isinstance(strip_color, (list, tuple)) and expected in strip_color
        ), f"Strip fg_color {strip_color!r} не совпадает с accent_brand {expected!r}"
        mw.destroy()


    def test_today_strip_updates_on_theme_change(mw_deps):
        """D-07: при смене темы strip перекрашивается в новый accent_brand."""
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        assert mw._today_strip is not None
        # Применяем новую палитру с другим accent
        mw._apply_theme({
            "bg_primary": "#000000",
            "bg_secondary": "#111111",
            "text_primary": "#ffffff",
            "accent_brand": "#ff0000",
        })
        mw_deps["root"].update()
        strip_color = mw._today_strip.cget("fg_color")
        assert strip_color == "#ff0000" or (
            isinstance(strip_color, (list, tuple)) and "#ff0000" in strip_color
        ), f"После _apply_theme strip должен быть #ff0000, got {strip_color!r}"
        mw.destroy()


    def test_apply_theme_changes_bg(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        mw._apply_theme({
            "bg_primary": "#123456",
            "bg_secondary": "#abcdef",
            "text_primary": "#000000",
            "accent_brand": "#ff0000",
        })
        # Без crash
        mw.destroy()


    def test_theme_subscribe_called_in_init(mw_deps):
        spy_theme = MagicMock(wraps=mw_deps["theme"])
        spy_theme.subscribe = MagicMock()
        spy_theme.get = mw_deps["theme"].get
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], spy_theme)
        mw_deps["root"].update()
        spy_theme.subscribe.assert_called_once()
        mw.destroy()


    def test_save_window_state_persists(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        spy = MagicMock(wraps=mw._settings_store.save)
        mw._settings_store.save = spy
        mw._save_window_state()
        spy.assert_called_once()
        mw.destroy()


    def test_set_always_on_top(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        mw.set_always_on_top(False)
        # Attribute не крэшит
        mw.set_always_on_top(True)
        mw.destroy()


    def test_destroy_cleanup(mw_deps):
        mw = MainWindow(mw_deps["root"], mw_deps["settings_store"],
                        mw_deps["settings"], mw_deps["theme"])
        mw_deps["root"].update()
        mw.destroy()
        # Не крэшит
    ```

    ШАГ 3 — Запустить тесты.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_main_window.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/main_window.py exists
    - grep "class MainWindow" client/ui/main_window.py >= 1
    - grep "MIN_SIZE = (320, 320)" client/ui/main_window.py >= 1
    - grep "DEFAULT_SIZE = (460, 600)" client/ui/main_window.py >= 1
    - grep "theme.*subscribe" client/ui/main_window.py >= 1
    - grep "window_size\|window_position" client/ui/main_window.py >= 2 (persistence)
    - grep "set_always_on_top" client/ui/main_window.py >= 1 (OVR-06 propagation)
    - grep "Пн\|Вт\|Ср" client/ui/main_window.py >= 3 (russian day names)
    - **grep "TODAY_STRIP_WIDTH\s*=\s*3" client/ui/main_window.py >= 1 (D-07: strip 3px)**
    - **grep "accent_brand" client/ui/main_window.py >= 2 (D-07: strip colored + theme propagation)**
    - **grep "_today_strip" client/ui/main_window.py >= 3 (reference создаётся + используется в _apply_theme)**
    - **grep "fg_color=accent" client/ui/main_window.py >= 1 (D-07: strip окрашивается)**
    - pytest client/tests/ui/test_main_window.py — проходит >= 16 тестов (включая test_today_section_has_blue_strip и test_today_strip_updates_on_theme_change)
  </acceptance_criteria>

  <done>
    MainWindow shell готов. D-07 today-indicator реализован полностью:
      (a) Синяя полоска 3px слева today-секции (accent_brand).
      (b) Bold заголовок с суффиксом "• сегодня".
      Оба реагируют на смену темы.
    Plan 03-10 wire'нет:
    ```python
    overlay.on_click = main_window.toggle
    overlay.on_top_changed = main_window.set_always_on_top
    ```
    Phase 4 заменит day_section placeholders на реальные task lists (но strip+bold останутся).
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_main_window.py — 16+ тестов зелёные (включая D-07 strip tests)
- grep UI-SPEC sizes (320, 460, 600) верифицирован
- grep "TODAY_STRIP_WIDTH = 3" и "accent_brand" в main_window.py — D-07 verified
- Theme subscribe wiring проверен (через spy)
- Today-strip перекрашивается при смене темы (test_today_strip_updates_on_theme_change)
</verification>

<success_criteria>
- MainWindow shell готов для wire-up в Plan 03-10
- Persistence работает через SettingsStore
- Aккордеон 7 заглушек Пн-Вс с сегодня-маркером (ДВА индикатора: strip + bold, D-07)
- Theme switching triggers _apply_theme и обновляет today-strip
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-system/03-06-SUMMARY.md`
</output>
