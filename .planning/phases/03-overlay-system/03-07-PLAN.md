---
phase: 03-overlay-system
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - client/utils/tray.py
  - client/tests/ui/test_tray.py
autonomous: true
requirements:
  - TRAY-01
  - TRAY-02
  - TRAY-03
  - TRAY-04
must_haves:
  truths:
    - "TrayManager создаёт pystray.Icon с меню per UI-SPEC §Tray Menu (TRAY-01)"
    - "Меню содержит: Открыть/Скрыть, Добавить, Настройки(Тема/Стиль/Уведомления/Поверх/Автозапуск), Sync, Logout, Выход (TRAY-02)"
    - "icon.run_detached() используется ВМЕСТО run() — D-26, PITFALL 2 (TRAY-04)"
    - "ВСЕ callback'и делают root.after(0, fn) — D-27, PITFALL 2 (TRAY-04)"
    - "Toggle в меню (тема/стиль/on_top/автозапуск/notifications_mode) сохраняет settings instantly через SettingsStore (TRAY-03)"
    - "Иконка отрисовывается через render_overlay_image(16, ...) или (32, ...)"
  artifacts:
    - path: "client/utils/tray.py"
      provides: "TrayManager — pystray wrapper с thread-safe callbacks"
      contains: "class TrayManager"
      min_lines: 200
  key_links:
    - from: "client/utils/tray.py"
      to: "pystray.Icon.run_detached"
      via: "D-26 anti-crash pattern"
      pattern: "run_detached\\(\\)"
    - from: "client/utils/tray.py"
      to: "root.after(0, ...)"
      via: "D-27 thread safety for ALL callbacks"
      pattern: "self\\._root\\.after\\(0"
    - from: "client/utils/tray.py"
      to: "client/ui/settings.py SettingsStore"
      via: "TRAY-03 instant save on toggle"
      pattern: "settings_store\\.save"
---

<objective>
TrayManager — pystray wrapper с thread-safe callbacks. Covers TRAY-01, 02, 03, 04.

Structure меню per UI-SPEC §Tray Menu:
```
Открыть окно    ← bold default
Скрыть
───
Добавить задачу
───
Настройки ▶
  Тема ▶ (Светлая/Тёмная/Бежевая/Системная)
  Вид задач ▶ (Карточки/Строки/Минимализм)
  Уведомления ▶ (Звук+pulse/Только pulse/Тихо)
  Поверх всех окон [✓]
  Автозапуск [✓]
───
Обновить синхронизацию
───
Разлогиниться
Выход
```

Критические pitfall'ы (ВСЕ обязательно обработаны):
 - D-26 / PITFALL 2 / TRAY-04: `icon.run_detached()` — НЕ `run()`. Иначе Tk apartment crash при 20+ кликах.
 - D-27 / PITFALL 2: Каждый callback делает `self._root.after(0, lambda: _do_work())`. НЕ трогаем Tk виджеты напрямую из pystray-потока.
 - Callable menu checkmarks: `checked=lambda item: self._on_top` для dynamic state.

Output: `client/utils/tray.py` TrayManager с полным меню + тесты с mock_pystray_icon.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-overlay-system/03-UI-SPEC.md
@.planning/phases/03-overlay-system/03-RESEARCH.md
@client/ui/settings.py
@client/ui/themes.py
@client/ui/icon_compose.py

<interfaces>
<!-- Dependencies -->
From client/ui/icon_compose.py:
```python
def render_overlay_image(size: int, state: str, task_count: int, overdue_count: int, pulse_t: float = 0.0) -> PIL.Image.Image: ...
```

From client/ui/settings.py:
```python
class SettingsStore: ...  # save/load
@dataclass
class UISettings:
    theme: str
    task_style: str
    notifications_mode: str
    on_top: bool
    autostart: bool
```

From client/ui/themes.py:
```python
class ThemeManager:
    def set_theme(self, theme: str) -> None: ...
```

<!-- NEW: will be created by Plan 03-09 autostart -->
From client/utils/autostart.py (shared, skeleton exists):
```python
def is_autostart_enabled() -> bool: ...
def enable_autostart() -> None: ...
def disable_autostart() -> None: ...
```

<!-- New interface -->
Target API for client/utils/tray.py:
```python
class TrayManager:
    def __init__(
        self,
        root,               # ctk.CTk (для root.after(0, ...))
        settings_store,     # SettingsStore
        settings,           # UISettings (live)
        theme_manager,      # ThemeManager
        callbacks: dict,    # {"on_show", "on_hide", "on_add", "on_sync", "on_logout", "on_quit", "on_top_changed", "on_task_style_changed", "on_notifications_mode_changed"}
    ): ...

    def start(self) -> None: ...      # run_detached
    def stop(self) -> None: ...
    def update_icon(self, state: str, task_count: int, overdue_count: int) -> None: ...
    def update_tooltip(self, text: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать TrayManager — pystray wrapper с thread-safe callbacks</name>
  <files>client/utils/tray.py, client/tests/ui/test_tray.py</files>

  <read_first>
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Tray Menu (точная структура меню + русские labels)
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Tray Icon (default blue + overdue dot, 16/32px)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pattern 2 pystray + Tkinter Threading (канонический код)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pitfall 2 (детали crash + mitigation)
    - client/ui/settings.py (UISettings fields)
    - client/ui/icon_compose.py (render_overlay_image)
  </read_first>

  <behavior>
    - Test 1: TrayManager instantiation создаёт instance без pystray side-effects (start не вызван)
    - Test 2: start() создаёт pystray.Icon и вызывает run_detached() (mock verifies)
    - Test 3: Меню содержит "Открыть окно", "Скрыть", "Добавить задачу", "Настройки", "Обновить синхронизацию", "Разлогиниться", "Выход" — grep строк
    - Test 4: Callback on_show проходит через root.after(0, ...) — mock spy на root.after
    - Test 5: Callback on_sync проходит через root.after(0, ...)
    - Test 6: Callback on_quit проходит через root.after(0, ...)
    - Test 7: Toggle theme через меню вызывает theme_manager.set_theme + settings_store.save
    - Test 8: Toggle on_top → settings.on_top меняется + callbacks["on_top_changed"] вызывается + save
    - Test 9: Toggle autostart → callbacks["on_autostart_changed"] вызывается + save
    - Test 10: update_icon(state="overdue", ...) вызывает icon.icon = new_image
    - Test 11: grep "run_detached()" в файле (TRAY-04)
    - Test 12: grep "root.after(0" >= 6 (каждый callback threadsafe)
    - Test 13: Menu items "Звук+pulse", "Только pulse", "Тихо" присутствуют (3 режима NOTIF)
    - Test 14: Menu items "Карточки", "Строки", "Минимализм" присутствуют (3 task styles D-11)
  </behavior>

  <action>
    ШАГ 1 — Создать `client/utils/tray.py` (перезаписать skeleton):

    ```python
    """
    TrayManager — pystray system tray icon + menu (TRAY-01..04).

    Критические pitfall'ы (строго):
      PITFALL 2 / D-26 / TRAY-04: pystray.Icon.run_detached() ТОЛЬКО. НЕ run() — apartment crash.
      PITFALL 2 / D-27: каждый callback делает self._root.after(0, lambda: work()).
                        НЕ трогаем Tk виджеты из pystray thread напрямую.

    Меню — per 03-UI-SPEC §Tray Menu verbatim.
    """
    from __future__ import annotations

    import logging
    from typing import Callable, Optional

    import pystray
    from PIL import Image

    from client.ui.icon_compose import render_overlay_image
    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import ThemeManager

    logger = logging.getLogger(__name__)

    TRAY_ICON_SIZE = 32  # Windows DPI-friendly (16 слишком мелкий на HiDPI)
    APP_TITLE = "Личный Еженедельник"

    # ---- Callback keys (контракт с WeeklyPlannerApp Plan 03-10) ----
    CALLBACK_KEYS = {
        "on_show", "on_hide", "on_add", "on_sync", "on_logout", "on_quit",
        "on_top_changed", "on_task_style_changed",
        "on_notifications_mode_changed", "on_autostart_changed",
        "is_autostart_enabled",  # предикат для галочки в меню
    }


    class TrayManager:
        """System tray icon wrapper. См. 03-UI-SPEC §Tray Menu + §Tray Icon."""

        def __init__(
            self,
            root,
            settings_store: SettingsStore,
            settings: UISettings,
            theme_manager: ThemeManager,
            callbacks: dict,
        ) -> None:
            self._root = root
            self._settings_store = settings_store
            self._settings = settings
            self._theme = theme_manager
            self._callbacks = dict(callbacks)
            self._icon: Optional[pystray.Icon] = None
            # Validate callbacks
            missing = CALLBACK_KEYS - set(self._callbacks.keys())
            if missing:
                logger.warning("TrayManager missing callbacks: %s", missing)

        # ---- Lifecycle ----

        def start(self) -> None:
            """Создать pystray.Icon и запустить в detached thread (D-26)."""
            image = render_overlay_image(
                size=TRAY_ICON_SIZE,
                state="default",
                task_count=0,
                overdue_count=0,
            )
            self._icon = pystray.Icon(
                "weekly-planner",
                icon=image,
                title=APP_TITLE,
                menu=self._build_menu(),
            )
            # D-26 / TRAY-04: run_detached — не run()
            self._icon.run_detached()
            logger.info("TrayManager started (run_detached)")

        def stop(self) -> None:
            if self._icon is not None:
                try:
                    self._icon.stop()
                except Exception as exc:
                    logger.debug("tray.stop failed: %s", exc)
                self._icon = None

        def update_icon(self, state: str, task_count: int, overdue_count: int) -> None:
            """Обновить tray иконку (вызывать из main thread через root.after(0, ...))."""
            if self._icon is None:
                return
            try:
                new_image = render_overlay_image(
                    size=TRAY_ICON_SIZE,
                    state=state,
                    task_count=task_count,
                    overdue_count=overdue_count,
                )
                self._icon.icon = new_image
            except Exception as exc:
                logger.error("update_icon failed: %s", exc)

        def update_tooltip(self, text: str) -> None:
            if self._icon is not None:
                self._icon.title = text

        # ---- Menu building ----

        def _build_menu(self) -> pystray.Menu:
            return pystray.Menu(
                pystray.MenuItem("Открыть окно", self._cb_show, default=True),
                pystray.MenuItem("Скрыть", self._cb_hide),
                pystray.Menu.SEPARATOR,
                pystray.MenuItem("Добавить задачу", self._cb_add),
                pystray.Menu.SEPARATOR,
                pystray.MenuItem("Настройки", pystray.Menu(
                    pystray.MenuItem("Тема", pystray.Menu(
                        pystray.MenuItem("Светлая",
                                         lambda i, it: self._cb_theme("light"),
                                         checked=lambda it: self._settings.theme == "light",
                                         radio=True),
                        pystray.MenuItem("Тёмная",
                                         lambda i, it: self._cb_theme("dark"),
                                         checked=lambda it: self._settings.theme == "dark",
                                         radio=True),
                        pystray.MenuItem("Бежевая",
                                         lambda i, it: self._cb_theme("beige"),
                                         checked=lambda it: self._settings.theme == "beige",
                                         radio=True),
                        pystray.MenuItem("Системная",
                                         lambda i, it: self._cb_theme("system"),
                                         checked=lambda it: self._settings.theme == "system",
                                         radio=True),
                    )),
                    pystray.MenuItem("Вид задач", pystray.Menu(
                        pystray.MenuItem("Карточки",
                                         lambda i, it: self._cb_task_style("card"),
                                         checked=lambda it: self._settings.task_style == "card",
                                         radio=True),
                        pystray.MenuItem("Строки",
                                         lambda i, it: self._cb_task_style("line"),
                                         checked=lambda it: self._settings.task_style == "line",
                                         radio=True),
                        pystray.MenuItem("Минимализм",
                                         lambda i, it: self._cb_task_style("minimal"),
                                         checked=lambda it: self._settings.task_style == "minimal",
                                         radio=True),
                    )),
                    pystray.MenuItem("Уведомления", pystray.Menu(
                        pystray.MenuItem("Звук+pulse",
                                         lambda i, it: self._cb_notifications_mode("sound_pulse"),
                                         checked=lambda it: self._settings.notifications_mode == "sound_pulse",
                                         radio=True),
                        pystray.MenuItem("Только pulse",
                                         lambda i, it: self._cb_notifications_mode("pulse_only"),
                                         checked=lambda it: self._settings.notifications_mode == "pulse_only",
                                         radio=True),
                        pystray.MenuItem("Тихо",
                                         lambda i, it: self._cb_notifications_mode("silent"),
                                         checked=lambda it: self._settings.notifications_mode == "silent",
                                         radio=True),
                    )),
                    pystray.MenuItem("Поверх всех окон",
                                     self._cb_toggle_on_top,
                                     checked=lambda it: self._settings.on_top),
                    pystray.MenuItem("Автозапуск",
                                     self._cb_toggle_autostart,
                                     checked=lambda it: self._is_autostart_enabled()),
                )),
                pystray.Menu.SEPARATOR,
                pystray.MenuItem("Обновить синхронизацию", self._cb_sync),
                pystray.Menu.SEPARATOR,
                pystray.MenuItem("Разлогиниться", self._cb_logout),
                pystray.MenuItem("Выход", self._cb_quit),
            )

        # ---- Callbacks (ALL go through root.after(0, ...) per D-27 / PITFALL 2) ----

        def _cb_show(self, icon=None, item=None) -> None:
            self._root.after(0, lambda: self._invoke("on_show"))

        def _cb_hide(self, icon=None, item=None) -> None:
            self._root.after(0, lambda: self._invoke("on_hide"))

        def _cb_add(self, icon=None, item=None) -> None:
            self._root.after(0, lambda: self._invoke("on_add"))

        def _cb_sync(self, icon=None, item=None) -> None:
            self._root.after(0, lambda: self._invoke("on_sync"))

        def _cb_logout(self, icon=None, item=None) -> None:
            self._root.after(0, lambda: self._invoke("on_logout"))

        def _cb_quit(self, icon=None, item=None) -> None:
            self._root.after(0, lambda: self._invoke("on_quit"))

        def _cb_theme(self, theme: str) -> None:
            def apply():
                self._settings.theme = theme
                self._theme.set_theme(theme)
                self._settings_store.save(self._settings)
                self._refresh_menu()
            self._root.after(0, apply)

        def _cb_task_style(self, style: str) -> None:
            def apply():
                self._settings.task_style = style
                self._settings_store.save(self._settings)
                self._invoke("on_task_style_changed", style)
                self._refresh_menu()
            self._root.after(0, apply)

        def _cb_notifications_mode(self, mode: str) -> None:
            def apply():
                self._settings.notifications_mode = mode
                self._settings_store.save(self._settings)
                self._invoke("on_notifications_mode_changed", mode)
                self._refresh_menu()
            self._root.after(0, apply)

        def _cb_toggle_on_top(self, icon=None, item=None) -> None:
            def apply():
                self._settings.on_top = not self._settings.on_top
                self._settings_store.save(self._settings)
                self._invoke("on_top_changed", self._settings.on_top)
                self._refresh_menu()
            self._root.after(0, apply)

        def _cb_toggle_autostart(self, icon=None, item=None) -> None:
            def apply():
                enabled = self._is_autostart_enabled()
                new_state = not enabled
                self._settings.autostart = new_state
                self._invoke("on_autostart_changed", new_state)
                self._settings_store.save(self._settings)
                self._refresh_menu()
            self._root.after(0, apply)

        # ---- Helpers ----

        def _is_autostart_enabled(self) -> bool:
            predicate = self._callbacks.get("is_autostart_enabled")
            if predicate is None:
                return self._settings.autostart
            try:
                return bool(predicate())
            except Exception as exc:
                logger.debug("is_autostart_enabled failed: %s", exc)
                return self._settings.autostart

        def _invoke(self, key: str, *args) -> None:
            cb = self._callbacks.get(key)
            if cb is None:
                logger.warning("TrayManager: callback %r not wired", key)
                return
            try:
                cb(*args)
            except Exception as exc:
                logger.error("TrayManager callback %s failed: %s", key, exc)

        def _refresh_menu(self) -> None:
            """Обновить checkmarks меню (pystray update_menu — thread-safe внутренне)."""
            if self._icon is not None:
                try:
                    self._icon.update_menu()
                except Exception as exc:
                    logger.debug("update_menu failed: %s", exc)
    ```

    ШАГ 2 — Создать `client/tests/ui/test_tray.py`:

    ```python
    """Unit-тесты TrayManager (Plan 03-07). Covers TRAY-01, 02, 03, 04."""
    from unittest.mock import MagicMock

    import pytest

    from client.core.paths import AppPaths
    from client.core.storage import LocalStorage
    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import ThemeManager
    from client.utils.tray import TrayManager, CALLBACK_KEYS


    @pytest.fixture
    def tray_deps(tmp_appdata, headless_tk, mock_ctypes_dpi, mock_pystray_icon):
        storage = LocalStorage(AppPaths())
        storage.init()
        callbacks = {k: MagicMock() for k in CALLBACK_KEYS}
        callbacks["is_autostart_enabled"] = MagicMock(return_value=False)
        return {
            "root": headless_tk,
            "settings_store": SettingsStore(storage),
            "settings": UISettings(),
            "theme": ThemeManager(),
            "callbacks": callbacks,
            "fake_icon_cls": mock_pystray_icon,
        }


    def test_instantiation_no_side_effects(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        # Никакой icon не создан до start()
        assert tm._icon is None
        assert len(tray_deps["fake_icon_cls"].instances) == 0


    def test_start_creates_icon_and_runs_detached(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        assert len(tray_deps["fake_icon_cls"].instances) == 1
        icon = tray_deps["fake_icon_cls"].instances[0]
        # TRAY-04: run_detached использован
        assert icon.run_detached_called is True


    def test_stop_calls_icon_stop(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        tm.stop()
        assert tray_deps["fake_icon_cls"].instances[0].stopped is True


    def test_cb_show_uses_root_after_zero(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        spy = MagicMock(wraps=tray_deps["root"].after)
        tray_deps["root"].after = spy
        tm._cb_show()
        # root.after вызван с (0, callable)
        args_list = [call.args for call in spy.call_args_list]
        assert any(a[0] == 0 for a in args_list), "_cb_show должен использовать root.after(0, ...)"


    def test_cb_sync_triggers_callback_via_after(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm._cb_sync()
        tray_deps["root"].update_idletasks()  # процессируем after(0)
        tray_deps["callbacks"]["on_sync"].assert_called_once()


    def test_cb_quit_triggers_callback_via_after(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm._cb_quit()
        tray_deps["root"].update_idletasks()
        tray_deps["callbacks"]["on_quit"].assert_called_once()


    def test_cb_theme_updates_settings_and_persists(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        spy_save = MagicMock(wraps=tray_deps["settings_store"].save)
        tray_deps["settings_store"].save = spy_save
        tm._cb_theme("dark")
        tray_deps["root"].update_idletasks()
        assert tray_deps["settings"].theme == "dark"
        spy_save.assert_called_once()


    def test_cb_toggle_on_top_flips_and_notifies(tray_deps):
        tray_deps["settings"].on_top = True
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        tm._cb_toggle_on_top()
        tray_deps["root"].update_idletasks()
        assert tray_deps["settings"].on_top is False
        tray_deps["callbacks"]["on_top_changed"].assert_called_with(False)


    def test_cb_toggle_autostart_calls_callback(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        tm._cb_toggle_autostart()
        tray_deps["root"].update_idletasks()
        tray_deps["callbacks"]["on_autostart_changed"].assert_called_once()


    def test_cb_task_style_persists(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        tm._cb_task_style("line")
        tray_deps["root"].update_idletasks()
        assert tray_deps["settings"].task_style == "line"
        tray_deps["callbacks"]["on_task_style_changed"].assert_called_with("line")


    def test_cb_notifications_mode_persists(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        tm._cb_notifications_mode("silent")
        tray_deps["root"].update_idletasks()
        assert tray_deps["settings"].notifications_mode == "silent"


    def test_update_icon_changes_icon(tray_deps):
        tm = TrayManager(
            tray_deps["root"], tray_deps["settings_store"],
            tray_deps["settings"], tray_deps["theme"], tray_deps["callbacks"],
        )
        tm.start()
        original = tm._icon.icon
        tm.update_icon("overdue", task_count=5, overdue_count=2)
        assert tm._icon.icon is not original


    def test_menu_structure_has_all_required_labels():
        """TRAY-01/02: структурная проверка — меню содержит все обязательные пункты."""
        import inspect
        from client.utils import tray as tray_mod
        source = inspect.getsource(tray_mod)
        required = [
            "Открыть окно", "Скрыть", "Добавить задачу", "Настройки",
            "Тема", "Светлая", "Тёмная", "Бежевая", "Системная",
            "Вид задач", "Карточки", "Строки", "Минимализм",
            "Уведомления", "Звук+pulse", "Только pulse", "Тихо",
            "Поверх всех окон", "Автозапуск",
            "Обновить синхронизацию", "Разлогиниться", "Выход",
        ]
        for label in required:
            assert label in source, f"Menu missing label: {label}"


    def test_run_detached_not_run_grep():
        """TRAY-04 source grep guard."""
        import inspect
        from client.utils import tray as tray_mod
        source = inspect.getsource(tray_mod)
        assert "run_detached()" in source
        # Убедимся что нет вызова `icon.run()` или `self._icon.run()`
        assert "self._icon.run()" not in source


    def test_all_callbacks_use_after_zero():
        """TRAY-04/D-27 source grep — каждый _cb_ использует root.after(0, ...)."""
        import inspect
        from client.utils import tray as tray_mod
        source = inspect.getsource(tray_mod)
        # Считаем случаи "self._root.after(0" — минимум по числу callbacks (9+)
        count = source.count("self._root.after(0")
        assert count >= 9, f"Ожидалось >= 9 вызовов self._root.after(0, ...), got {count}"
    ```

    ШАГ 3 — Запустить тесты.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_tray.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/utils/tray.py exists
    - grep "class TrayManager" client/utils/tray.py >= 1
    - grep "run_detached()" client/utils/tray.py >= 1 (TRAY-04)
    - grep "self._icon.run()" client/utils/tray.py == 0 (anti-pattern absent)
    - grep "self._root.after(0" client/utils/tray.py >= 9 (each callback thread-safe)
    - grep "Открыть окно" client/utils/tray.py >= 1
    - grep "Настройки" client/utils/tray.py >= 1
    - grep "Светлая" client/utils/tray.py >= 1
    - grep "Звук+pulse" client/utils/tray.py >= 1 (notifications mode)
    - grep "Карточки" client/utils/tray.py >= 1 (task style)
    - grep "Поверх всех окон" client/utils/tray.py >= 1
    - grep "Автозапуск" client/utils/tray.py >= 1
    - grep "Разлогиниться" client/utils/tray.py >= 1
    - grep "settings_store.save" client/utils/tray.py >= 3 (TRAY-03 persist on toggle)
    - pytest client/tests/ui/test_tray.py проходит с >= 14 тестами зелёными
  </acceptance_criteria>

  <done>
    TrayManager готов. TRAY-01, 02, 03, 04 покрыты. Plan 03-10 wire'нет callbacks:
    on_show → main_window.show, on_sync → sync_manager.force_sync, etc.
    ВСЕ критические pitfall'ы обработаны с grep-verified markers.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_tray.py — 14+ тестов зелёные
- grep подтверждает run_detached + root.after(0 присутствие
- grep подтверждает все русские menu labels из UI-SPEC
</verification>

<success_criteria>
- 4 TRAY требования покрыты (TRAY-01..04)
- Меню соответствует UI-SPEC §Tray Menu до последнего label
- Thread safety проверена через grep + unit tests
- Toggle settings instantly сохраняются через SettingsStore
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-system/03-07-SUMMARY.md`
</output>
