---
phase: 03-overlay-system
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/tests/conftest.py
  - client/tests/ui/__init__.py
  - client/requirements-dev.txt
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test suite может запускаться с headless Tk без открытия реальных окон"
    - "pystray.Icon, winotify.Notification, winreg, ctypes Windows-зависимости замокированы"
    - "Существующие Phase 2 тесты продолжают зелёно проходить"
  artifacts:
    - path: "client/tests/conftest.py"
      provides: "headless_tk, mock_pystray_icon, mock_winotify, mock_winreg, mock_ctypes_dpi fixtures"
    - path: "client/tests/ui/__init__.py"
      provides: "Маркер пакета для UI-тестов"
  key_links:
    - from: "client/tests/ui/*.py"
      to: "client/tests/conftest.py"
      via: "pytest fixture resolution (parent conftest)"
      pattern: "def test_.*\\(.*headless_tk.*\\)"
---

<objective>
Расширить pytest-инфраструктуру Phase 2 новыми fixtures для headless-тестов Tkinter UI и
мок-обёртками Windows-специфичных API (pystray, winotify, winreg, ctypes DPI). Ни один
Phase 3 план не сможет писать тесты без этих фикстур — они Wave 0.

Purpose: Без моков pystray/winotify/winreg тесты ронят CI (требуют реальной Windows GUI-session
или реального subprocess PowerShell). Без headless_tk — CTk открывает визуальные окна во время
unit-тестов. Обе проблемы должны быть решены ДО того, как Plan 03-04+ начнут писать UI-тесты.

Output: Расширенный `conftest.py` с 5 новыми фикстурами + пустой `client/tests/ui/__init__.py`
для маркировки UI-тестового подпакета. Phase 2 фикстуры (`tmp_appdata`, `mock_api`, `api_base`)
сохраняются без изменений.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-overlay-system/03-CONTEXT.md
@.planning/phases/03-overlay-system/03-RESEARCH.md
@.planning/phases/03-overlay-system/03-VALIDATION.md
@client/tests/conftest.py
@client/requirements-dev.txt

<interfaces>
<!-- Existing Phase 2 fixtures (must remain working) -->
From client/tests/conftest.py:
```python
API_BASE_URL = "https://planner.heyda.ru/api"

@pytest.fixture
def api_base() -> str: ...

@pytest.fixture
def tmp_appdata(tmp_path, monkeypatch) -> Path: ...

@pytest.fixture
def mock_api(): ...  # requests_mock.Mocker
```

<!-- New fixtures this plan creates -->
Target additions to client/tests/conftest.py:
```python
@pytest.fixture
def headless_tk(): ...          # CTk().withdraw() + destroy teardown

@pytest.fixture
def mock_pystray_icon(monkeypatch): ...  # fake Icon class, tracks calls

@pytest.fixture
def mock_winotify(monkeypatch): ...      # fake Notification class, calls list

@pytest.fixture
def mock_winreg(monkeypatch): ...        # fake winreg module, in-memory dict

@pytest.fixture
def mock_ctypes_dpi(monkeypatch): ...    # stub SetProcessDpiAwareness, no-op
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить conftest.py новыми фикстурами + создать ui/ подпакет</name>
  <files>client/tests/conftest.py, client/tests/ui/__init__.py</files>

  <read_first>
    - client/tests/conftest.py (полностью — существующие фикстуры сохранить)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Test Strategy для Tkinter Code (паттерны mock_pystray, mock_winotify)
    - .planning/phases/03-overlay-system/03-VALIDATION.md §Wave 0 Requirements
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Tray Icon (структура меню — важно для mock дизайна)
  </read_first>

  <behavior>
    - headless_tk fixture создаёт ctk.CTk() с withdraw(), делает yield, в teardown вызывает destroy(); scope="function"
    - mock_pystray_icon patch'ит `pystray.Icon` → FakeIcon класс. FakeIcon запоминает: icon_image, title, menu, run_detached_called (bool). Методы run_detached(), stop(), update_menu() no-op.
    - mock_winotify patch'ит `winotify.Notification` → FakeNotification. Параметры __init__ (app_id, title, msg, icon) запоминаются в list calls (возвращается фикстурой). show() append'ит self.kwargs в calls.
    - mock_winreg patch'ит `winreg.OpenKey`, `winreg.SetValueEx`, `winreg.DeleteValue`, `winreg.QueryValueEx` — всё через in-memory dict `{ (hkey, path, name): value }`. Возвращает этот dict.
    - mock_ctypes_dpi patch'ит `ctypes.windll.shcore.SetProcessDpiAwareness` и `ctypes.windll.user32.SetProcessDPIAware` → no-op (return 0).
    - Все mock fixtures yield (не return) — чтобы teardown был чистый.
    - Phase 2 фикстуры НЕ трогать (api_base, tmp_appdata, mock_api остаются).
    - client/tests/ui/__init__.py создаётся пустым (маркер пакета).
  </behavior>

  <action>
    ШАГ 1 — Создать `client/tests/ui/__init__.py` пустым (одна строка docstring: `"""UI-тесты Phase 3 (overlay, tray, notifications, main window)."""`).

    ШАГ 2 — Расширить `client/tests/conftest.py`. Добавить в конец файла (после `mock_api`):

    ```python
    # ---------- Phase 3 UI fixtures ----------

    @pytest.fixture
    def headless_tk():
        """
        CTk root в headless-режиме для UI-тестов.
        withdraw() — окно невидимо, update() процессирует events без mainloop().
        """
        import customtkinter as ctk
        root = ctk.CTk()
        root.withdraw()
        try:
            yield root
        finally:
            try:
                root.update_idletasks()
                root.destroy()
            except Exception:
                pass


    @pytest.fixture
    def mock_pystray_icon(monkeypatch):
        """
        Патчит pystray.Icon → FakeIcon. Yield'ит класс для инспекции.
        Каждый инстанс запоминает: icon, title, menu, run_detached_called.
        """
        class FakeIcon:
            instances = []

            def __init__(self, name, icon=None, title="", menu=None):
                self.name = name
                self.icon = icon
                self.title = title
                self.menu = menu
                self.run_detached_called = False
                self.stopped = False
                FakeIcon.instances.append(self)

            def run_detached(self):
                self.run_detached_called = True

            def stop(self):
                self.stopped = True

            def update_menu(self):
                pass

        FakeIcon.instances = []
        monkeypatch.setattr("pystray.Icon", FakeIcon)
        yield FakeIcon


    @pytest.fixture
    def mock_winotify(monkeypatch):
        """
        Патчит winotify.Notification → FakeNotification.
        Yield'ит list — каждый toast.show() append'ит dict параметров.
        """
        calls = []

        class FakeNotification:
            def __init__(self, app_id=None, title=None, msg=None, icon=None, **kwargs):
                self.kwargs = {
                    "app_id": app_id,
                    "title": title,
                    "msg": msg,
                    "icon": icon,
                }
                self.kwargs.update(kwargs)

            def show(self):
                calls.append(dict(self.kwargs))

            def set_audio(self, *a, **kw):
                pass

            def add_actions(self, *a, **kw):
                pass

        monkeypatch.setattr("winotify.Notification", FakeNotification)
        yield calls


    @pytest.fixture
    def mock_winreg(monkeypatch):
        """
        In-memory stub для winreg. Yield'ит dict store: {(hkey, path, name): value}.
        """
        import winreg as _real_winreg
        store = {}

        class _FakeKeyCtx:
            def __init__(self, hkey, path):
                self.hkey = hkey
                self.path = path

            def __enter__(self):
                return self

            def __exit__(self, *a):
                pass

        def fake_open(hkey, path, reserved=0, access=0):
            return _FakeKeyCtx(hkey, path)

        def fake_set(key, name, reserved, typ, value):
            store[(key.hkey, key.path, name)] = value

        def fake_query(key, name):
            v = store.get((key.hkey, key.path, name))
            if v is None:
                raise FileNotFoundError
            return v, _real_winreg.REG_SZ

        def fake_delete(key, name):
            k = (key.hkey, key.path, name)
            if k not in store:
                raise FileNotFoundError
            del store[k]

        monkeypatch.setattr("winreg.OpenKey", fake_open)
        monkeypatch.setattr("winreg.SetValueEx", fake_set)
        monkeypatch.setattr("winreg.QueryValueEx", fake_query)
        monkeypatch.setattr("winreg.DeleteValue", fake_delete)
        yield store


    @pytest.fixture
    def mock_ctypes_dpi(monkeypatch):
        """
        No-op замены SetProcessDpiAwareness / SetProcessDPIAware.
        Нужно чтобы CTk на CI без DPI-контекста не падал.
        """
        try:
            import ctypes
            monkeypatch.setattr(
                ctypes.windll.shcore, "SetProcessDpiAwareness",
                lambda *a, **kw: 0, raising=False,
            )
            monkeypatch.setattr(
                ctypes.windll.user32, "SetProcessDPIAware",
                lambda *a, **kw: 0, raising=False,
            )
        except (AttributeError, OSError):
            pass  # не-Windows — пропускаем
        yield
    ```

    ШАГ 3 — Проверить/добавить в `client/requirements-dev.txt`:
    ```
    pytest>=8.0
    pytest-mock>=3.12
    requests-mock>=1.11
    ```
    (Phase 2 уже могла добавить — добавлять только отсутствующие.)

    ШАГ 4 — Убедиться что Phase 2 тесты всё ещё проходят:
    ```
    python -m pytest client/tests -x -q
    ```

    ПРИМЕЧАНИЕ: НЕ переопределять существующие фикстуры `tmp_appdata`, `mock_api`, `api_base`.
  </action>

  <verify>
    <automated>python -m pytest client/tests/test_storage.py client/tests/test_auth.py client/tests/test_sync.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/tests/ui/__init__.py exists
    - grep "def headless_tk" client/tests/conftest.py returns >= 1 line
    - grep "def mock_pystray_icon" client/tests/conftest.py returns >= 1 line
    - grep "def mock_winotify" client/tests/conftest.py returns >= 1 line
    - grep "def mock_winreg" client/tests/conftest.py returns >= 1 line
    - grep "def mock_ctypes_dpi" client/tests/conftest.py returns >= 1 line
    - grep "def tmp_appdata" client/tests/conftest.py returns >= 1 line (Phase 2 preserved)
    - grep "def mock_api" client/tests/conftest.py returns >= 1 line (Phase 2 preserved)
    - pytest client/tests/test_storage.py проходит (Phase 2 регрессия)
  </acceptance_criteria>

  <done>
    Wave 0 готова: все последующие Plans 03-02..03-11 могут импортировать фикстуры
    headless_tk, mock_pystray_icon, mock_winotify, mock_winreg, mock_ctypes_dpi
    без дальнейших доработок conftest.py.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/test_storage.py -x passes (Phase 2 regression guard)
- pytest collects client/tests/ui/ without errors (import check)
- grep confirms all 5 new fixtures present
</verification>

<success_criteria>
- 5 новых фикстур определены и экспортированы из conftest.py
- client/tests/ui/ пакет создан
- Phase 2 тесты не сломаны (регрессия = 0)
- Автоматическая команда verify проходит < 30 сек
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-system/03-01-SUMMARY.md`
</output>
