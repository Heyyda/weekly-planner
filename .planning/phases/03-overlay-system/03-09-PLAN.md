---
phase: 03-overlay-system
plan: 09
type: execute
wave: 4
depends_on: [01]
files_modified:
  - client/utils/autostart.py
  - client/tests/ui/test_autostart.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "is_autostart_enabled() возвращает True/False на основе winreg HKCU\\...\\Run"
    - "enable_autostart() записывает путь к exe/python в реестр под value name 'LichnyEzhednevnik' (ASCII)"
    - "disable_autostart() удаляет value из реестра, обрабатывая FileNotFoundError"
    - "ASCII value name вместо Cyrillic для frozen-exe safety (parallel с D-25 keyring pattern)"
  artifacts:
    - path: "client/utils/autostart.py"
      provides: "is_autostart_enabled / enable_autostart / disable_autostart"
      contains: "HKEY_CURRENT_USER"
      min_lines: 50
  key_links:
    - from: "client/utils/autostart.py"
      to: "winreg HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run"
      via: "OpenKey + SetValueEx + DeleteValue + QueryValueEx"
      pattern: "HKEY_CURRENT_USER"
---

<objective>
Переписать `client/utils/autostart.py` (skeleton → production) — 3 функции для управления
записью в `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`. Value name `LichnyEzhednevnik`
(ASCII) per D-30 + frozen-exe safety parallel с D-25.

Purpose: Plan 03-07 TrayManager уже импортирует `autostart.is_autostart_enabled` и вызывает
enable/disable через callback hooks. Этот plan — единственный источник autostart API.

Phase 6 будет использовать эти же функции для PyInstaller --onefile — единственное изменение
будет `sys.frozen = True` → `exe_path = sys.executable`.

Output: Переписанный autostart.py + unit tests с mock_winreg fixture.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-overlay-system/03-RESEARCH.md
@client/utils/autostart.py

<interfaces>
<!-- Existing skeleton (will be rewritten) -->
From client/utils/autostart.py:
```python
APP_NAME = "ЛичныйЕженедельник"  # BAD: Cyrillic — change to ASCII
REG_PATH = r"Software\Microsoft\Windows\CurrentVersion\Run"

def is_autostart_enabled() -> bool: ...
def enable_autostart() -> None: ...
def disable_autostart() -> None: ...
```

<!-- New interface -->
Target API for client/utils/autostart.py:
```python
APP_REG_VALUE = "LichnyEzhednevnik"  # ASCII per frozen-exe parallel to D-25
REG_PATH = r"Software\Microsoft\Windows\CurrentVersion\Run"

def is_autostart_enabled() -> bool: ...
def enable_autostart() -> None: ...
def disable_autostart() -> None: ...
def get_autostart_command() -> str: ...  # computes sys.executable + sys.argv[0] for dev or sys.executable for frozen
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Переписать client/utils/autostart.py + тесты</name>
  <files>client/utils/autostart.py, client/tests/ui/test_autostart.py</files>

  <read_first>
    - client/utils/autostart.py (существующий skeleton — переписать под ASCII)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pattern 9 Autostart
    - .planning/phases/03-overlay-system/03-CONTEXT.md D-30 (HKCU path)
    - client/core/config.py (KEYRING_SERVICE = "WeeklyPlanner" — тот же ASCII-паттерн parallels D-25)
  </read_first>

  <behavior>
    - Test 1: is_autostart_enabled() на пустом mock_winreg → False
    - Test 2: enable_autostart() → запись в mock_winreg под key (HKCU, REG_PATH, "LichnyEzhednevnik")
    - Test 3: После enable_autostart() is_autostart_enabled() → True
    - Test 4: disable_autostart() удаляет value
    - Test 5: disable_autostart() на отсутствующем value НЕ крэшит (FileNotFoundError caught)
    - Test 6: APP_REG_VALUE == "LichnyEzhednevnik" (ASCII — не Cyrillic)
    - Test 7: REG_PATH содержит "Software\\Microsoft\\Windows\\CurrentVersion\\Run"
    - Test 8: get_autostart_command() для не-frozen возвращает строку с кавычками (обработка пробелов в пути)
  </behavior>

  <action>
    ШАГ 1 — Переписать `client/utils/autostart.py`:

    ```python
    """
    Autostart — управление записью в HKCU\\...\\Run для автозапуска Windows.

    Value name (ASCII): "LichnyEzhednevnik" — избегаем frozen-exe проблему с Cyrillic
    (параллельный паттерн с config.KEYRING_SERVICE = "WeeklyPlanner" per D-25 / PITFALL 4).

    Phase 3: dev-режим → python.exe main.py
    Phase 6: frozen exe → sys.executable (PyInstaller --onefile).
    Переключение автоматическое через getattr(sys, 'frozen', False).
    """
    from __future__ import annotations

    import logging
    import sys
    import winreg
    from pathlib import Path

    logger = logging.getLogger(__name__)

    APP_REG_VALUE = "LichnyEzhednevnik"  # ASCII — frozen-exe safety
    REG_PATH = r"Software\Microsoft\Windows\CurrentVersion\Run"


    def is_autostart_enabled() -> bool:
        """Проверить наличие value в HKCU\\...\\Run."""
        try:
            with winreg.OpenKey(winreg.HKEY_CURRENT_USER, REG_PATH, 0, winreg.KEY_READ) as key:
                winreg.QueryValueEx(key, APP_REG_VALUE)
                return True
        except FileNotFoundError:
            return False
        except OSError as exc:
            logger.warning("is_autostart_enabled OS error: %s", exc)
            return False


    def enable_autostart() -> None:
        """Записать путь запуска в реестр HKCU\\...\\Run."""
        command = get_autostart_command()
        try:
            with winreg.OpenKey(winreg.HKEY_CURRENT_USER, REG_PATH, 0, winreg.KEY_SET_VALUE) as key:
                winreg.SetValueEx(key, APP_REG_VALUE, 0, winreg.REG_SZ, command)
            logger.info("Autostart enabled: %s", command)
        except OSError as exc:
            logger.error("enable_autostart failed: %s", exc)
            raise


    def disable_autostart() -> None:
        """Удалить value из реестра. Если не было — no-op (FileNotFoundError handled)."""
        try:
            with winreg.OpenKey(winreg.HKEY_CURRENT_USER, REG_PATH, 0, winreg.KEY_SET_VALUE) as key:
                winreg.DeleteValue(key, APP_REG_VALUE)
            logger.info("Autostart disabled")
        except FileNotFoundError:
            logger.debug("Autostart value not present — skip delete")
        except OSError as exc:
            logger.error("disable_autostart failed: %s", exc)


    def get_autostart_command() -> str:
        """
        Вернуть строку команды для реестра.

        Frozen exe (PyInstaller): sys.executable — путь к .exe, в кавычках.
        Dev: "python.exe" "path/to/main.py" — обе части в кавычках (могут содержать кириллицу/пробелы).
        """
        if getattr(sys, "frozen", False):
            # Phase 6 - собранный exe
            return f'"{sys.executable}"'
        # Dev-режим — python + main.py
        script = Path(sys.argv[0]).resolve() if sys.argv and sys.argv[0] else Path("main.py").resolve()
        return f'"{sys.executable}" "{script}"'
    ```

    ШАГ 2 — Создать `client/tests/ui/test_autostart.py`:

    ```python
    """Unit-тесты autostart (Plan 03-09)."""
    import winreg

    import pytest

    from client.utils.autostart import (
        APP_REG_VALUE, REG_PATH,
        is_autostart_enabled, enable_autostart, disable_autostart,
        get_autostart_command,
    )


    def test_empty_registry_returns_false(mock_winreg):
        assert is_autostart_enabled() is False


    def test_enable_writes_to_registry(mock_winreg):
        enable_autostart()
        key = (winreg.HKEY_CURRENT_USER, REG_PATH, APP_REG_VALUE)
        assert key in mock_winreg


    def test_enable_then_is_enabled_true(mock_winreg):
        enable_autostart()
        assert is_autostart_enabled() is True


    def test_disable_removes_value(mock_winreg):
        enable_autostart()
        disable_autostart()
        key = (winreg.HKEY_CURRENT_USER, REG_PATH, APP_REG_VALUE)
        assert key not in mock_winreg


    def test_disable_without_enable_no_crash(mock_winreg):
        # Don't set anything
        disable_autostart()  # не должно крэшить
        assert is_autostart_enabled() is False


    def test_app_reg_value_is_ascii():
        assert APP_REG_VALUE == "LichnyEzhednevnik"
        # ASCII-only check
        assert APP_REG_VALUE.isascii()


    def test_reg_path_matches_expected():
        assert "Software" in REG_PATH
        assert "Microsoft" in REG_PATH
        assert "Windows" in REG_PATH
        assert "CurrentVersion" in REG_PATH
        assert "Run" in REG_PATH


    def test_get_autostart_command_has_quotes():
        cmd = get_autostart_command()
        # Путь должен быть в кавычках для обработки пробелов/кириллицы
        assert cmd.startswith('"')
        assert cmd.count('"') >= 2


    def test_roundtrip_enable_disable(mock_winreg):
        assert is_autostart_enabled() is False
        enable_autostart()
        assert is_autostart_enabled() is True
        disable_autostart()
        assert is_autostart_enabled() is False
    ```

    ШАГ 3 — Запустить тесты.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_autostart.py -x -q --timeout=15</automated>
  </verify>

  <acceptance_criteria>
    - File client/utils/autostart.py exists
    - grep "APP_REG_VALUE = \"LichnyEzhednevnik\"" client/utils/autostart.py >= 1 (ASCII)
    - grep "APP_REG_VALUE = \"ЛичныйЕженедельник\"" client/utils/autostart.py == 0 (no Cyrillic)
    - grep "HKEY_CURRENT_USER" client/utils/autostart.py >= 3 (three operations)
    - grep "FileNotFoundError" client/utils/autostart.py >= 1 (robust disable)
    - grep "def is_autostart_enabled" client/utils/autostart.py >= 1
    - grep "def enable_autostart" client/utils/autostart.py >= 1
    - grep "def disable_autostart" client/utils/autostart.py >= 1
    - grep "def get_autostart_command" client/utils/autostart.py >= 1
    - grep "sys.frozen" client/utils/autostart.py >= 1 (Phase 6 compat)
    - pytest client/tests/ui/test_autostart.py проходит >= 9 тестов зелёными
  </acceptance_criteria>

  <done>
    autostart.py переписан с ASCII value name. Plan 03-07 TrayManager уже готов вызывать
    `autostart.enable_autostart()` через callback. Phase 6 сможет использовать те же функции
    с frozen exe без изменений.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_autostart.py — 9+ тестов зелёные
- grep подтверждает ASCII-only value name
- Robust error handling (FileNotFoundError) на disable без предварительного enable
</verification>

<success_criteria>
- autostart.py готов к wire-up в Plan 03-10 (через tray callback)
- ASCII value name (no Cyrillic encoding risk в frozen exe)
- Dev и frozen-exe оба режима поддерживаются
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-system/03-09-SUMMARY.md`
</output>
