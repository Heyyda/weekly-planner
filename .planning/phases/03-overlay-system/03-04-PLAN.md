---
phase: 03-overlay-system
plan: 04
type: execute
wave: 2
depends_on: [02, 03]
files_modified:
  - client/ui/overlay.py
  - client/tests/ui/test_overlay.py
autonomous: true
requirements:
  - OVR-01
  - OVR-02
  - OVR-03
  - OVR-04
  - OVR-06
must_haves:
  truths:
    - "Overlay создаётся как CTkToplevel 56x56, overrideredirect=True, topmost=True (OVR-01)"
    - "Drag мышью — overlay перемещается; позиция сохраняется в UISettings.overlay_position через SettingsStore (OVR-02)"
    - "При drag и restore позиция валидируется против virtual desktop bounds (multi-monitor support, OVR-03)"
    - "Клик по overlay триггерит on_click callback (wired to main_window.toggle в Plan 03-10) (OVR-04)"
    - "set_always_on_top(bool) применяет topmost attr к overlay И вызывает external hook для main_window (OVR-06)"
    - "overrideredirect(True) вызывается через self.root.after(100, ...) для Win11 DWM (PITFALL 1)"
  artifacts:
    - path: "client/ui/overlay.py"
      provides: "OverlayManager — создание окна, drag, click, position persistence"
      contains: "class OverlayManager"
      min_lines: 150
  key_links:
    - from: "client/ui/overlay.py"
      to: "Win11 DWM timing mitigation"
      via: "root.after(100, overrideredirect)"
      pattern: "self\\._overlay\\.after\\(100"
    - from: "client/ui/overlay.py"
      to: "client/ui/settings.py SettingsStore"
      via: "save overlay_position on drag end"
      pattern: "overlay_position"
    - from: "client/ui/overlay.py"
      to: "client/ui/icon_compose.py render_overlay_image"
      via: "Canvas ItemImage"
      pattern: "render_overlay_image"
---

<objective>
OverlayManager — квадрат 56×56 на рабочем столе, draggable, clickable, persistent position,
always-on-top. Covers OVR-01, 02, 03, 04, 06. НЕ covers OVR-05 (pulse — отдельный Plan 03-05
из-за сложности 60fps animator).

Purpose: Это ВИЗУАЛЬНЫЙ ЦЕНТР продукта. Без рабочего overlay пользователь не взаимодействует
с приложением. Единственный источник input (клик, drag) когда основное окно скрыто. Должен
корректно работать на multi-monitor (Никита имеет 2 экрана на работе).

Критические pitfall'ы (ВСЕ обязательно обработаны):
  1. PITFALL 1: `overrideredirect(True)` в __init__ без delay → overlay рендерится ЗА другими
     окнами на Win11. Решение: `self._overlay.after(100, self._init_overlay_style)`.
  2. PITFALL 4: Pillow image ownership — сохранять ImageTk.PhotoImage в instance var иначе GC
     удалит → серый canvas.
  3. PITFALL 6: Multi-monitor restore — валидировать сохранённую позицию против virtual
     desktop bounds; fallback (100, 100).

Output: `client/ui/overlay.py` OverlayManager + comprehensive unit tests (headless_tk).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-overlay-system/03-UI-SPEC.md
@.planning/phases/03-overlay-system/03-RESEARCH.md
@client/ui/themes.py
@client/ui/settings.py
@client/ui/icon_compose.py
@client/core/storage.py

<interfaces>
<!-- Dependencies (Plans 03-02, 03-03) -->
From client/ui/settings.py:
```python
@dataclass
class UISettings:
    overlay_position: list = field(default_factory=lambda: [100, 100])
    on_top: bool = True
    ...

class SettingsStore:
    def load(self) -> UISettings: ...
    def save(self, settings: UISettings) -> None: ...
```

From client/ui/icon_compose.py:
```python
def render_overlay_image(size, state, task_count, overdue_count, pulse_t) -> PIL.Image.Image: ...
```

From client/ui/themes.py:
```python
class ThemeManager:
    def subscribe(self, callback): ...
```

<!-- New interface this plan creates -->
Target API for client/ui/overlay.py:
```python
class OverlayManager:
    OVERLAY_SIZE = 56  # per UI-SPEC

    def __init__(
        self,
        root,             # ctk.CTk
        storage,          # LocalStorage (for settings persistence)
        settings_store,   # SettingsStore
        settings,         # UISettings (live instance)
        theme_manager,    # ThemeManager
    ): ...

    # Callbacks — wired in Plan 03-10
    on_click: Callable[[], None]    # single click → toggle main window
    on_right_click: Callable[[], None]  # mini-menu (optional)
    on_top_changed: Callable[[bool], None]  # propagate to main window

    # State accessors
    def get_position(self) -> tuple[int, int]: ...
    def set_position(self, x: int, y: int) -> None: ...
    def set_always_on_top(self, enabled: bool) -> None: ...

    # Render hooks (called by Plan 03-05 pulse / Plan 03-10 refresh)
    def refresh_image(self, state: str, task_count: int, overdue_count: int,
                      pulse_t: float = 0.0) -> None: ...

    # Lifecycle
    def show(self) -> None: ...
    def hide(self) -> None: ...
    def destroy(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать OverlayManager — создание окна + drag + click + position persistence</name>
  <files>client/ui/overlay.py, client/tests/ui/test_overlay.py</files>

  <read_first>
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pattern 1 Overlay Creation (обязательные after(100), DWM)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pattern 5 Drag Behavior (bind events, multi-monitor clamp)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pitfall 1, 4, 6 (все три актуальны ЗДЕСЬ)
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Overlay, §Drag Behavior, §Click Behavior, §Always-on-top
    - client/ui/settings.py (UISettings.overlay_position + on_top)
    - client/ui/icon_compose.py (render_overlay_image API)
  </read_first>

  <behavior>
    - Test 1: OverlayManager.__init__ создаёт CTkToplevel parent=root, withdraw'ит его сразу
    - Test 2: После root.update() overrideredirect=True (через after(100) — тест ждёт update_idletasks)
    - Test 3: get_position() возвращает tuple (x, y) из settings.overlay_position
    - Test 4: set_position(200, 300) изменяет geometry окна + settings.overlay_position
    - Test 5: _on_drag_end сохраняет settings через SettingsStore.save() (mock spy)
    - Test 6: Неверная восстановленная позиция (off-screen, например [-5000, -5000]) → clamp/fallback к (100, 100)
    - Test 7: _on_click вызывает self.on_click callback если установлен
    - Test 8: set_always_on_top(False) устанавливает -topmost=False на overlay И вызывает on_top_changed(False) hook
    - Test 9: refresh_image(state="default", task_count=3) → render_overlay_image вызван с правильными args
    - Test 10: ImageTk.PhotoImage сохраняется в instance var (self._tk_image не None после refresh) — PITFALL 4
    - Test 11: destroy() — overlay.winfo_exists() возвращает False (или OK graceful teardown)
    - Test 12: grep-check: файл содержит `self._overlay.after(100` (PITFALL 1 explicit)
  </behavior>

  <action>
    ШАГ 1 — Создать `client/ui/overlay.py`:

    ```python
    """
    OverlayManager — draggable square 56×56 на рабочем столе.

    Покрывает:
      OVR-01: overrideredirect + topmost + borderless
      OVR-02: позиция персистится через SettingsStore
      OVR-03: multi-monitor bounds validation (ctypes EnumDisplayMonitors)
      OVR-04: single-click → on_click callback
      OVR-06: set_always_on_top — toggle с hook для main window

    Критические pitfall'ы:
      PITFALL 1: overrideredirect(True) через after(100, ...) — Win11 DWM timing
      PITFALL 4: self._tk_image хранится в instance var (Tkinter GC)
      PITFALL 6: saved position validated against virtual desktop bounds
    """
    from __future__ import annotations

    import logging
    import tkinter as tk
    from typing import Callable, Optional

    import customtkinter as ctk
    from PIL import ImageTk

    from client.ui.icon_compose import render_overlay_image
    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import ThemeManager

    logger = logging.getLogger(__name__)

    # DWM attribute (Win11 only) — per 03-RESEARCH Pattern 1
    DWMWA_WINDOW_CORNER_PREFERENCE = 33
    DWMWCP_ROUND = 2


    class OverlayManager:
        """Квадрат-оверлей на рабочем столе. См. 03-UI-SPEC §Overlay."""

        OVERLAY_SIZE = 56  # px per UI-SPEC
        INIT_DELAY_MS = 100  # PITFALL 1 — критичная задержка для Win11 DWM

        def __init__(
            self,
            root: ctk.CTk,
            settings_store: SettingsStore,
            settings: UISettings,
            theme_manager: ThemeManager,
        ) -> None:
            self._root = root
            self._settings_store = settings_store
            self._settings = settings
            self._theme = theme_manager

            # Колбеки (wire'атся в app.py Plan 03-10)
            self.on_click: Optional[Callable[[], None]] = None
            self.on_right_click: Optional[Callable[[], None]] = None
            self.on_top_changed: Optional[Callable[[bool], None]] = None

            # Drag state
            self._drag_offset_x = 0
            self._drag_offset_y = 0
            self._drag_was_motion = False

            # Pillow image reference (PITFALL 4)
            self._tk_image: Optional[ImageTk.PhotoImage] = None
            self._canvas_item_id: Optional[int] = None

            # Создание окна
            self._overlay = ctk.CTkToplevel(root)
            self._overlay.withdraw()
            self._overlay.geometry(f"{self.OVERLAY_SIZE}x{self.OVERLAY_SIZE}")

            # Canvas для Pillow image
            self._canvas = tk.Canvas(
                self._overlay,
                width=self.OVERLAY_SIZE,
                height=self.OVERLAY_SIZE,
                highlightthickness=0,
                borderwidth=0,
                bg="#000000",  # временный — прозрачность через transparentcolor
            )
            self._canvas.pack(fill="both", expand=True)

            # PITFALL 1: overrideredirect строго через after(100, ...)
            self._overlay.after(self.INIT_DELAY_MS, self._init_overlay_style)

        def _init_overlay_style(self) -> None:
            """Вызывается после INIT_DELAY_MS — настраивает borderless+topmost+DWM."""
            self._overlay.overrideredirect(True)
            self._overlay.attributes("-topmost", self._settings.on_top)
            # Прозрачность углов (Win10 fallback для rounded — через transparentcolor canvas bg)
            try:
                self._overlay.attributes("-transparentcolor", "#000000")
            except tk.TclError:
                pass

            # DWM rounded corners (Win11)
            try:
                import ctypes
                hwnd = ctypes.windll.user32.GetParent(self._overlay.winfo_id())
                value = ctypes.c_int(DWMWCP_ROUND)
                ctypes.windll.dwmapi.DwmSetWindowAttribute(
                    hwnd, DWMWA_WINDOW_CORNER_PREFERENCE,
                    ctypes.byref(value), ctypes.sizeof(value),
                )
            except Exception:
                pass  # Win10 fallback — Pillow RGBA углы уже прозрачные

            # Восстановить позицию (валидированную против virtual desktop)
            x, y = self._validate_position(self._settings.overlay_position)
            self._overlay.geometry(f"{self.OVERLAY_SIZE}x{self.OVERLAY_SIZE}+{x}+{y}")

            # Event bindings
            self._canvas.bind("<ButtonPress-1>", self._on_drag_start)
            self._canvas.bind("<B1-Motion>", self._on_drag_motion)
            self._canvas.bind("<ButtonRelease-1>", self._on_drag_end)
            self._canvas.bind("<Button-3>", self._on_right_click_event)

            # Начальная отрисовка
            self.refresh_image(state="default", task_count=0, overdue_count=0)
            self._overlay.deiconify()

            logger.info("OverlayManager initialized at (%d, %d)", x, y)

        # ---- Public API ----

        def get_position(self) -> tuple:
            if self._overlay.winfo_exists():
                return (self._overlay.winfo_x(), self._overlay.winfo_y())
            return tuple(self._settings.overlay_position)

        def set_position(self, x: int, y: int) -> None:
            x, y = self._validate_position([x, y])
            self._overlay.geometry(f"{self.OVERLAY_SIZE}x{self.OVERLAY_SIZE}+{x}+{y}")
            self._settings.overlay_position = [x, y]

        def set_always_on_top(self, enabled: bool) -> None:
            """OVR-06: apply к overlay + hook для main window."""
            self._settings.on_top = enabled
            try:
                self._overlay.attributes("-topmost", enabled)
            except tk.TclError:
                pass
            self._settings_store.save(self._settings)
            if self.on_top_changed is not None:
                self.on_top_changed(enabled)

        def refresh_image(self, state: str, task_count: int,
                          overdue_count: int, pulse_t: float = 0.0) -> None:
            """Перерисовать Pillow image → ImageTk → Canvas (PITFALL 4: сохранить ref)."""
            img = render_overlay_image(
                size=self.OVERLAY_SIZE,
                state=state,
                task_count=task_count,
                overdue_count=overdue_count,
                pulse_t=pulse_t,
            )
            self._tk_image = ImageTk.PhotoImage(img)  # PITFALL 4 critical
            if self._canvas_item_id is None:
                self._canvas_item_id = self._canvas.create_image(
                    0, 0, anchor="nw", image=self._tk_image,
                )
            else:
                self._canvas.itemconfig(self._canvas_item_id, image=self._tk_image)

        def show(self) -> None:
            self._overlay.deiconify()

        def hide(self) -> None:
            self._overlay.withdraw()

        def destroy(self) -> None:
            try:
                self._overlay.destroy()
            except Exception as exc:
                logger.debug("Overlay destroy: %s", exc)

        # ---- Internal handlers ----

        def _on_drag_start(self, event) -> None:
            self._drag_was_motion = False
            self._drag_offset_x = event.x_root - self._overlay.winfo_x()
            self._drag_offset_y = event.y_root - self._overlay.winfo_y()

        def _on_drag_motion(self, event) -> None:
            self._drag_was_motion = True
            new_x = event.x_root - self._drag_offset_x
            new_y = event.y_root - self._drag_offset_y
            new_x, new_y = self._clamp_to_virtual_desktop(new_x, new_y)
            self._overlay.geometry(f"{self.OVERLAY_SIZE}x{self.OVERLAY_SIZE}+{new_x}+{new_y}")

        def _on_drag_end(self, event) -> None:
            if self._drag_was_motion:
                # Сохранить позицию
                x, y = self._overlay.winfo_x(), self._overlay.winfo_y()
                self._settings.overlay_position = [x, y]
                self._settings_store.save(self._settings)
                logger.debug("Overlay position saved: (%d, %d)", x, y)
            else:
                # Клик без motion → on_click
                if self.on_click is not None:
                    try:
                        self.on_click()
                    except Exception as exc:
                        logger.error("on_click handler failed: %s", exc)
            self._drag_was_motion = False

        def _on_right_click_event(self, event) -> None:
            if self.on_right_click is not None:
                try:
                    self.on_right_click()
                except Exception as exc:
                    logger.error("on_right_click handler failed: %s", exc)

        # ---- Multi-monitor support (OVR-03) ----

        def _get_virtual_desktop_bounds(self) -> tuple:
            """Возвращает (left, top, right, bottom). Fallback на primary при ошибке."""
            try:
                import win32api
                monitors = win32api.EnumDisplayMonitors()
                rects = []
                for m in monitors:
                    info = win32api.GetMonitorInfo(m[0])
                    rects.append(info["Monitor"])
                if rects:
                    left = min(r[0] for r in rects)
                    top = min(r[1] for r in rects)
                    right = max(r[2] for r in rects)
                    bottom = max(r[3] for r in rects)
                    return (left, top, right, bottom)
            except Exception as exc:
                logger.debug("EnumDisplayMonitors failed: %s", exc)
            # Fallback на primary screen
            return (
                0, 0,
                self._root.winfo_screenwidth(),
                self._root.winfo_screenheight(),
            )

        def _clamp_to_virtual_desktop(self, x: int, y: int) -> tuple:
            left, top, right, bottom = self._get_virtual_desktop_bounds()
            x = max(left, min(x, right - self.OVERLAY_SIZE))
            y = max(top, min(y, bottom - self.OVERLAY_SIZE))
            return (x, y)

        def _validate_position(self, pos) -> tuple:
            """PITFALL 6: валидация сохранённой позиции против virtual desktop."""
            try:
                x, y = int(pos[0]), int(pos[1])
            except (TypeError, ValueError, IndexError):
                return (100, 100)
            left, top, right, bottom = self._get_virtual_desktop_bounds()
            # Строгая проверка: overlay хоть частично виден
            if x < left - self.OVERLAY_SIZE + 10 or x > right - 10:
                logger.warning("Saved overlay_x=%d off-screen, fallback (100, 100)", x)
                return (100, 100)
            if y < top - self.OVERLAY_SIZE + 10 or y > bottom - 10:
                logger.warning("Saved overlay_y=%d off-screen, fallback (100, 100)", y)
                return (100, 100)
            return (x, y)
    ```

    ШАГ 2 — Создать `client/tests/ui/test_overlay.py`:

    ```python
    """Unit-тесты OverlayManager (Plan 03-04). Covers OVR-01, 02, 03, 04, 06."""
    import pytest
    from unittest.mock import MagicMock

    from client.core.paths import AppPaths
    from client.core.storage import LocalStorage
    from client.ui.overlay import OverlayManager
    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import ThemeManager


    @pytest.fixture
    def overlay_deps(tmp_appdata, headless_tk, mock_ctypes_dpi):
        """Общий setup: storage + settings + theme для OverlayManager."""
        storage = LocalStorage(AppPaths())
        storage.init()
        settings_store = SettingsStore(storage)
        settings = UISettings()
        theme = ThemeManager()
        return {
            "root": headless_tk,
            "settings_store": settings_store,
            "settings": settings,
            "theme": theme,
        }


    def test_overlay_creates_toplevel(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"],
            overlay_deps["settings_store"],
            overlay_deps["settings"],
            overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        assert overlay._overlay.winfo_exists()
        overlay.destroy()


    def test_overlay_size_is_56x56(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        # Процессируем after(100) delayed style
        overlay_deps["root"].after(200, lambda: None)
        overlay_deps["root"].update_idletasks()
        assert overlay.OVERLAY_SIZE == 56
        overlay.destroy()


    def test_get_position_returns_settings_default(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        # До init_overlay_style позиция из settings (default [100, 100])
        x, y = overlay._settings.overlay_position
        assert (x, y) == (100, 100)
        overlay.destroy()


    def test_set_position_updates_settings(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        overlay.set_position(200, 300)
        assert overlay._settings.overlay_position == [200, 300]
        overlay.destroy()


    def test_validate_position_fallback_for_offscreen(overlay_deps):
        overlay_deps["settings"].overlay_position = [-5000, -5000]
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        x, y = overlay._validate_position([-5000, -5000])
        # Off-screen — fallback
        assert (x, y) == (100, 100)
        overlay.destroy()


    def test_validate_position_handles_non_list(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        x, y = overlay._validate_position(None)
        assert (x, y) == (100, 100)
        overlay.destroy()


    def test_on_click_callback_fires(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        callback = MagicMock()
        overlay.on_click = callback
        # Симулируем drag end без motion → on_click
        overlay._drag_was_motion = False
        fake_event = MagicMock()
        overlay._on_drag_end(fake_event)
        callback.assert_called_once()
        overlay.destroy()


    def test_drag_motion_does_not_trigger_click(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        callback = MagicMock()
        overlay.on_click = callback
        overlay._drag_was_motion = True  # был drag
        fake_event = MagicMock()
        overlay._on_drag_end(fake_event)
        callback.assert_not_called()
        overlay.destroy()


    def test_drag_end_saves_position_via_store(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        # Spy на save
        spy = MagicMock(wraps=overlay._settings_store.save)
        overlay._settings_store.save = spy
        overlay._drag_was_motion = True
        fake_event = MagicMock()
        overlay._on_drag_end(fake_event)
        spy.assert_called_once()
        overlay.destroy()


    def test_set_always_on_top_calls_hook(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        hook = MagicMock()
        overlay.on_top_changed = hook
        overlay.set_always_on_top(False)
        hook.assert_called_once_with(False)
        assert overlay._settings.on_top is False
        overlay.destroy()


    def test_refresh_image_keeps_pillow_ref(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        overlay.refresh_image(state="default", task_count=3, overdue_count=0)
        # PITFALL 4 — ref сохранён
        assert overlay._tk_image is not None
        overlay.destroy()


    def test_init_uses_after_100_delay(overlay_deps):
        """PITFALL 1 verification — overrideredirect через after(100)."""
        import inspect
        source = inspect.getsource(OverlayManager)
        assert "after(self.INIT_DELAY_MS" in source or "after(100" in source
        assert "INIT_DELAY_MS = 100" in source


    def test_destroy_removes_overlay(overlay_deps):
        overlay = OverlayManager(
            overlay_deps["root"], overlay_deps["settings_store"],
            overlay_deps["settings"], overlay_deps["theme"],
        )
        overlay_deps["root"].update()
        overlay.destroy()
        # После destroy overlay._overlay может всё ещё existовать до update
        overlay_deps["root"].update_idletasks()
        # winfo_exists() после destroy — False или exception
        try:
            assert not overlay._overlay.winfo_exists()
        except Exception:
            pass  # teardown happened
    ```

    ШАГ 3 — Запустить тесты.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_overlay.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/overlay.py exists
    - grep "class OverlayManager" client/ui/overlay.py >= 1
    - grep "self._overlay.after(self.INIT_DELAY_MS" client/ui/overlay.py >= 1 (PITFALL 1 explicit)
    - grep "INIT_DELAY_MS = 100" client/ui/overlay.py >= 1
    - grep "OVERLAY_SIZE = 56" client/ui/overlay.py >= 1
    - grep "overrideredirect(True)" client/ui/overlay.py >= 1
    - grep "-topmost" client/ui/overlay.py >= 1
    - grep "self._tk_image" client/ui/overlay.py >= 1 (PITFALL 4 ref kept)
    - grep "EnumDisplayMonitors" client/ui/overlay.py >= 1 (multi-monitor, OVR-03)
    - grep "_validate_position" client/ui/overlay.py >= 1 (PITFALL 6)
    - grep "on_top_changed" client/ui/overlay.py >= 1 (OVR-06 hook)
    - grep "settings_store.save" client/ui/overlay.py >= 1 (OVR-02 persistence)
    - pytest client/tests/ui/test_overlay.py проходит с >= 12 тестов зелёными
  </acceptance_criteria>

  <done>
    OverlayManager готов. OVR-01, 02, 03, 04, 06 покрыты. Plan 03-05 будет вызывать
    overlay.refresh_image(state="overdue", pulse_t=t) в 60fps цикле. Plan 03-10 wire'нет
    on_click → main_window.toggle. Все три критические pitfall'ы встроены с grep-verified markers.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_overlay.py — все тесты зелёные
- grep markers для PITFALL 1 (after 100), 4 (_tk_image), 6 (_validate_position) подтверждены
- Multi-monitor helper _get_virtual_desktop_bounds присутствует
- OVR-06 hook on_top_changed чётко отделён от внутренней state'а
</verification>

<success_criteria>
- 5 из 6 OVR-требований покрыты (OVR-01..04, 06). OVR-05 — Plan 03-05.
- OverlayManager можно инстанцировать в тестах через headless_tk
- Drag, click, position persist работают через state-inspection
- Все критические pitfall'ы имеют grep-verifiable markers
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-system/03-04-SUMMARY.md`
</output>
