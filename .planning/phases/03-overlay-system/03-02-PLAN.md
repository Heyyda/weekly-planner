---
phase: 03-overlay-system
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - client/ui/themes.py
  - client/ui/settings.py
  - client/tests/ui/test_themes.py
  - client/tests/ui/test_settings.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ThemeManager хранит 3 палитры (light warm cream / dark warm / beige sepia) + опцию 'system'"
    - "ThemeManager поддерживает subscribe/notify — виджеты получают callback при смене темы"
    - "UISettings — dataclass со всеми Phase 3 полями и round-trip serialization"
    - "SettingsStore обёртка вокруг LocalStorage.load_settings/save_settings — никакой новой I/O логики"
  artifacts:
    - path: "client/ui/themes.py"
      provides: "ThemeManager + PALETTES dict + FONTS + subscribe/set_theme/get"
      contains: "class ThemeManager"
      min_lines: 80
    - path: "client/ui/settings.py"
      provides: "UISettings dataclass + SettingsStore wrapper"
      contains: "class UISettings"
  key_links:
    - from: "client/ui/themes.py"
      to: "callback subscribers"
      via: "subscribe() + set_theme() iteration"
      pattern: "for cb in self\\._callbacks"
    - from: "client/ui/settings.py"
      to: "client/core/storage.py"
      via: "LocalStorage.load_settings()/save_settings()"
      pattern: "storage\\.save_settings|storage\\.load_settings"
---

<objective>
Создать темизационный слой (3 палитры warm/dark/beige + системная) и единый schema для
Phase 3 settings (theme, task_style, notifications_mode, on_top, autostart, overlay_position,
window_size/position). Переписать skeleton `client/ui/themes.py` под UI-SPEC палитру. Новый
файл `client/ui/settings.py` — dataclass wrapper поверх существующего `LocalStorage.save_settings()`.

Purpose: Темы и настройки — fundamental layer, от которого зависят ВСЕ остальные Phase 3
компоненты (overlay colors, tray toggles, notifications mode). Без этого слоя нельзя начинать
Plans 03-03..03-10. Подписочный механизм (subscribe callback) решает PITFALL 5 — виджеты
получают новую палитру через callback, не хардкодят цвета.

Output: `client/ui/themes.py` с PALETTES из UI-SPEC verbatim + ThemeManager; `client/ui/settings.py`
с UISettings dataclass. Оба покрыты unit-тестами.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-overlay-system/03-UI-SPEC.md
@.planning/phases/03-overlay-system/03-RESEARCH.md
@client/ui/themes.py
@client/core/storage.py

<interfaces>
<!-- Existing Phase 2 API — USE THIS -->
From client/core/storage.py (LocalStorage methods used by settings):
```python
class LocalStorage:
    def load_settings(self) -> dict: ...   # returns {} if file missing
    def save_settings(self, settings: dict) -> None: ...  # atomic tmp+replace
```

<!-- New interfaces this plan creates -->
Target API for client/ui/themes.py:
```python
PALETTES: dict[str, dict[str, str]]  # keys: "light", "dark", "beige"
FONTS: dict[str, tuple]

class ThemeManager:
    def __init__(self) -> None: ...
    @property
    def current(self) -> str: ...             # "light" | "dark" | "beige"
    def subscribe(self, callback) -> None: ... # callback(palette: dict[str,str])
    def set_theme(self, theme: str) -> None: ... # "light"|"dark"|"beige"|"system"
    def get(self, key: str) -> str: ...        # hex color from current palette
    @staticmethod
    def detect_system_theme() -> str: ...      # reads winreg AppsUseLightTheme
```

Target API for client/ui/settings.py:
```python
@dataclass
class UISettings:
    theme: str = "light"
    task_style: str = "card"
    notifications_mode: str = "sound_pulse"
    on_top: bool = True
    autostart: bool = False
    overlay_position: list[int] = field(default_factory=lambda: [100, 100])  # [x, y]
    window_size: list[int] = field(default_factory=lambda: [460, 600])        # [w, h]
    window_position: list[int] | None = None                                   # [x, y] or None
    version: int = 1

class SettingsStore:
    def __init__(self, storage: LocalStorage) -> None: ...
    def load(self) -> UISettings: ...       # from LocalStorage.load_settings()
    def save(self, settings: UISettings) -> None: ... # LocalStorage.save_settings(asdict(...))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Переписать client/ui/themes.py — 3 палитры UI-SPEC + subscriber pattern</name>
  <files>client/ui/themes.py, client/tests/ui/test_themes.py</files>

  <read_first>
    - client/ui/themes.py (текущий skeleton — полностью переписать)
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Color Palette (Light/Dark/Beige) — ВСЕ hex верно до символа
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Typography
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pattern 7 Theme Manager + §Pitfall 5
  </read_first>

  <behavior>
    - Test 1: PALETTES имеет ключи "light", "dark", "beige"; каждая палитра содержит 11 токенов (bg_primary, bg_secondary, bg_tertiary, text_primary, text_secondary, text_tertiary, accent_brand, accent_brand_light, accent_done, accent_overdue, shadow_card)
    - Test 2: Light палитра содержит ровно эти hex'ы (per UI-SPEC): bg_primary=#F5EFE6, text_primary=#2B2420, accent_brand=#1E73E8, accent_done=#38A169, accent_overdue=#E85A5A
    - Test 3: Dark палитра содержит: bg_primary=#1F1B16, accent_brand=#4EA1FF, accent_done=#48B97D
    - Test 4: Beige палитра содержит: bg_primary=#E8DDC4, text_primary=#3D2F1F, accent_brand=#2966C4
    - Test 5: ThemeManager().current по умолчанию — "light"
    - Test 6: set_theme("dark") изменяет current и вызывает все зарегистрированные callbacks с новой палитрой
    - Test 7: set_theme("system") резолвит через detect_system_theme() (замоканный winreg → "light")
    - Test 8: subscribe(cb) добавляет cb в список; set_theme триггерит cb(palette_dict)
    - Test 9: get("accent_brand") возвращает hex из текущей темы
    - Test 10: Неизвестный ключ get("xxx") возвращает fallback "#ffffff" (не crash)
  </behavior>

  <action>
    ШАГ 1 — Переписать `client/ui/themes.py` с нуля (удалить старые DARK/LIGHT — не подходят под UI-SPEC):

    ```python
    """
    ThemeManager — 3 темы per UI-SPEC (light cream, dark warm, beige sepia) + subscriber pattern.

    Паттерн subscribe/notify избегает PITFALLS.md Pitfall 5: виджеты НЕ хардкодят цвета,
    а получают палитру через callback при смене темы. Live-switching без рестарта.

    Все hex-токены — verbatim из .planning/phases/03-overlay-system/03-UI-SPEC.md §Color Palette.
    """
    from __future__ import annotations

    import logging
    from typing import Callable, Optional

    import customtkinter as ctk

    logger = logging.getLogger(__name__)

    # ---- Palettes (verbatim from UI-SPEC §Color Palette) ----

    PALETTES: dict[str, dict[str, str]] = {
        "light": {
            "bg_primary": "#F5EFE6",
            "bg_secondary": "#EDE6D9",
            "bg_tertiary": "#E6DDC9",
            "text_primary": "#2B2420",
            "text_secondary": "#6B5E4E",
            "text_tertiary": "#9A8F7D",
            "accent_brand": "#1E73E8",
            "accent_brand_light": "#4EA1FF",
            "accent_done": "#38A169",
            "accent_overdue": "#E85A5A",
            "shadow_card": "rgba(70, 55, 40, 0.08)",
        },
        "dark": {
            "bg_primary": "#1F1B16",
            "bg_secondary": "#2B2620",
            "bg_tertiary": "#3A332C",
            "text_primary": "#F0E9DC",
            "text_secondary": "#B8AE9C",
            "text_tertiary": "#7A715F",
            "accent_brand": "#4EA1FF",
            "accent_brand_light": "#85BFFF",
            "accent_done": "#48B97D",
            "accent_overdue": "#F07272",
            "shadow_card": "rgba(0, 0, 0, 0.3)",
        },
        "beige": {
            "bg_primary": "#E8DDC4",
            "bg_secondary": "#D9CFB8",
            "bg_tertiary": "#C9BEA2",
            "text_primary": "#3D2F1F",
            "text_secondary": "#6E5F48",
            "text_tertiary": "#968769",
            "accent_brand": "#2966C4",
            "accent_brand_light": "#4E86DA",
            "accent_done": "#4A7A3D",
            "accent_overdue": "#C04B3C",
            "shadow_card": "rgba(80, 55, 30, 0.12)",
        },
    }

    # ---- Fonts (verbatim from UI-SPEC §Typography) ----
    FONTS: dict[str, tuple] = {
        "h1":       ("Segoe UI Variable", 14, "bold"),
        "body":     ("Segoe UI Variable", 13, "normal"),
        "caption":  ("Segoe UI Variable", 11, "normal"),
        "icon":     ("Segoe UI Variable", 28, "bold"),
        "mono":     ("Cascadia Code", 11, "normal"),
    }


    class ThemeManager:
        """
        Держит текущую тему, оповещает подписчиков при set_theme.

        Canonical usage:
            tm = ThemeManager()
            tm.subscribe(lambda palette: widget.configure(fg_color=palette["bg_primary"]))
            tm.set_theme("dark")  # все подписчики вызываются
        """

        def __init__(self, initial: str = "light") -> None:
            self._theme: str = initial if initial in PALETTES else "light"
            self._callbacks: list[Callable[[dict[str, str]], None]] = []

        @property
        def current(self) -> str:
            return self._theme

        def subscribe(self, callback: Callable[[dict[str, str]], None]) -> None:
            """Регистрация callback — каждый виджет/менеджер вызывает при старте."""
            self._callbacks.append(callback)

        def set_theme(self, theme: str) -> None:
            """
            Установить тему. Поддерживает "system" — резолвит через detect_system_theme().
            Вызывает все callbacks с новой палитрой.
            """
            if theme == "system":
                theme = self.detect_system_theme()
            if theme not in PALETTES:
                logger.warning("Неизвестная тема %r — fallback на light", theme)
                theme = "light"
            self._theme = theme
            # CustomTkinter built-in mode (для стандартных CTk виджетов)
            try:
                ctk.set_appearance_mode("dark" if theme == "dark" else "light")
            except Exception as exc:
                logger.debug("CTk set_appearance_mode failed: %s", exc)
            palette = PALETTES[theme]
            for cb in self._callbacks:
                try:
                    cb(palette)
                except Exception as exc:
                    logger.error("Theme callback failed: %s", exc)

        def get(self, key: str) -> str:
            """Получить hex-цвет из текущей палитры. Fallback на #ffffff если key неизвестен."""
            return PALETTES[self._theme].get(key, "#ffffff")

        @staticmethod
        def detect_system_theme() -> str:
            """Reads HKCU\\...\\Themes\\Personalize\\AppsUseLightTheme. Returns 'light' or 'dark'."""
            try:
                import winreg
                with winreg.OpenKey(
                    winreg.HKEY_CURRENT_USER,
                    r"Software\\Microsoft\\Windows\\CurrentVersion\\Themes\\Personalize",
                ) as key:
                    value, _ = winreg.QueryValueEx(key, "AppsUseLightTheme")
                    return "light" if value else "dark"
            except Exception:
                return "light"
    ```

    ШАГ 2 — Создать `client/tests/ui/test_themes.py`:

    ```python
    """Unit-тесты ThemeManager (Plan 03-02)."""
    import pytest
    from client.ui.themes import PALETTES, FONTS, ThemeManager


    def test_palettes_has_three_themes():
        assert set(PALETTES.keys()) == {"light", "dark", "beige"}


    def test_light_palette_exact_hex_per_ui_spec():
        p = PALETTES["light"]
        assert p["bg_primary"] == "#F5EFE6"
        assert p["text_primary"] == "#2B2420"
        assert p["accent_brand"] == "#1E73E8"
        assert p["accent_brand_light"] == "#4EA1FF"
        assert p["accent_done"] == "#38A169"
        assert p["accent_overdue"] == "#E85A5A"


    def test_dark_palette_exact_hex_per_ui_spec():
        p = PALETTES["dark"]
        assert p["bg_primary"] == "#1F1B16"
        assert p["accent_brand"] == "#4EA1FF"
        assert p["accent_done"] == "#48B97D"


    def test_beige_palette_exact_hex_per_ui_spec():
        p = PALETTES["beige"]
        assert p["bg_primary"] == "#E8DDC4"
        assert p["text_primary"] == "#3D2F1F"
        assert p["accent_brand"] == "#2966C4"


    def test_fonts_has_segoe_and_cascadia():
        assert FONTS["body"][0] == "Segoe UI Variable"
        assert FONTS["mono"][0] == "Cascadia Code"


    def test_default_theme_is_light():
        tm = ThemeManager()
        assert tm.current == "light"


    def test_set_theme_dark_changes_current(mock_ctypes_dpi):
        tm = ThemeManager()
        tm.set_theme("dark")
        assert tm.current == "dark"


    def test_subscribe_callback_fires_on_set_theme(mock_ctypes_dpi):
        tm = ThemeManager()
        received = []
        tm.subscribe(lambda p: received.append(p["bg_primary"]))
        tm.set_theme("beige")
        assert received == ["#E8DDC4"]


    def test_multiple_subscribers_all_fire(mock_ctypes_dpi):
        tm = ThemeManager()
        calls = []
        tm.subscribe(lambda p: calls.append("a"))
        tm.subscribe(lambda p: calls.append("b"))
        tm.set_theme("dark")
        assert calls == ["a", "b"]


    def test_system_theme_resolves_via_winreg(mock_winreg, mock_ctypes_dpi):
        import winreg
        mock_winreg[(winreg.HKEY_CURRENT_USER,
                     r"Software\\Microsoft\\Windows\\CurrentVersion\\Themes\\Personalize",
                     "AppsUseLightTheme")] = 0  # dark
        tm = ThemeManager()
        tm.set_theme("system")
        assert tm.current == "dark"


    def test_unknown_theme_falls_back_to_light(mock_ctypes_dpi):
        tm = ThemeManager()
        tm.set_theme("rainbow")
        assert tm.current == "light"


    def test_get_returns_hex_from_current_palette(mock_ctypes_dpi):
        tm = ThemeManager()
        tm.set_theme("dark")
        assert tm.get("accent_brand") == "#4EA1FF"


    def test_get_unknown_key_returns_white_fallback(mock_ctypes_dpi):
        tm = ThemeManager()
        assert tm.get("nonexistent") == "#ffffff"
    ```

    ШАГ 3 — Запустить тесты, убедиться что все 13 проходят.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_themes.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/themes.py exists, grep "class ThemeManager" >= 1
    - grep "#F5EFE6" client/ui/themes.py >= 1 (light bg_primary verbatim)
    - grep "#1F1B16" client/ui/themes.py >= 1 (dark bg_primary)
    - grep "#E8DDC4" client/ui/themes.py >= 1 (beige bg_primary)
    - grep "#1E73E8" client/ui/themes.py >= 1 (accent_brand light per UI-SPEC + D-01)
    - grep "def subscribe" client/ui/themes.py >= 1
    - grep "for cb in self._callbacks" client/ui/themes.py >= 1 (notify loop present)
    - grep "AppsUseLightTheme" client/ui/themes.py >= 1 (system theme detect)
    - pytest client/tests/ui/test_themes.py проходит с 13 зелёными тестами
  </acceptance_criteria>

  <done>
    ThemeManager готов с 3 палитрами из UI-SPEC verbatim + subscribe/notify паттерн.
    Следующие планы (overlay, tray, main_window) могут импортировать `from client.ui.themes import ThemeManager, PALETTES, FONTS`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать client/ui/settings.py — UISettings dataclass + SettingsStore обёртка</name>
  <files>client/ui/settings.py, client/tests/ui/test_settings.py</files>

  <read_first>
    - client/core/storage.py (LocalStorage.load_settings / save_settings — существующий API, НЕ трогать)
    - .planning/phases/03-overlay-system/03-RESEARCH.md §Pattern 6 Settings Schema
    - .planning/phases/03-overlay-system/03-UI-SPEC.md §Color Palette, §Task Block, §Notifications — для определения допустимых values
  </read_first>

  <behavior>
    - Test 1: UISettings() (defaults) создаётся без параметров; все 9 полей имеют нужные defaults
    - Test 2: asdict(UISettings()) → валидный dict с 9 ключами
    - Test 3: UISettings(**dict_from_json) — round-trip сериализация
    - Test 4: SettingsStore.load() на свежем AppPaths возвращает UISettings() с defaults
    - Test 5: SettingsStore.save(settings) → LocalStorage.save_settings(asdict) вызывается
    - Test 6: save() затем load() — round-trip: значения сохраняются
    - Test 7: Загрузка dict с неизвестными ключами игнорирует их (forward compat)
    - Test 8: Загрузка dict без полей использует defaults (backward compat)
  </behavior>

  <action>
    ШАГ 1 — Создать `client/ui/settings.py`:

    ```python
    """
    UISettings — dataclass-схема для settings.json (Phase 3).

    Источник: 03-CONTEXT.md D-25 (LocalStorage.save_settings/load_settings как транспорт).
    SettingsStore — тонкая обёртка: никакой новой I/O, только маршалинг dataclass ↔ dict.

    Пороги настроек:
        theme: "light" | "dark" | "beige" | "system"
        task_style: "card" | "line" | "minimal"
        notifications_mode: "sound_pulse" | "pulse_only" | "silent"
    """
    from __future__ import annotations

    import logging
    from dataclasses import asdict, dataclass, field, fields
    from typing import Optional

    from client.core.storage import LocalStorage

    logger = logging.getLogger(__name__)

    VALID_THEMES = {"light", "dark", "beige", "system"}
    VALID_TASK_STYLES = {"card", "line", "minimal"}
    VALID_NOTIFICATIONS = {"sound_pulse", "pulse_only", "silent"}


    @dataclass
    class UISettings:
        """Пользовательские настройки UI. Сохраняется в settings.json как dict."""
        theme: str = "light"
        task_style: str = "card"
        notifications_mode: str = "sound_pulse"
        on_top: bool = True
        autostart: bool = False
        overlay_position: list = field(default_factory=lambda: [100, 100])   # [x, y]
        window_size: list = field(default_factory=lambda: [460, 600])         # [w, h]
        window_position: Optional[list] = None                                # [x, y] или None (центр)
        version: int = 1

        def to_dict(self) -> dict:
            return asdict(self)

        @classmethod
        def from_dict(cls, data: dict) -> "UISettings":
            """Backward/forward compat: игнорируем неизвестные ключи, подставляем defaults для отсутствующих."""
            known = {f.name for f in fields(cls)}
            filtered = {k: v for k, v in (data or {}).items() if k in known}
            return cls(**filtered)

        def validate(self) -> None:
            """Чистит невалидные значения на defaults. Вызывается после load()."""
            if self.theme not in VALID_THEMES:
                logger.warning("Неизвестная theme %r — reset на light", self.theme)
                self.theme = "light"
            if self.task_style not in VALID_TASK_STYLES:
                self.task_style = "card"
            if self.notifications_mode not in VALID_NOTIFICATIONS:
                self.notifications_mode = "sound_pulse"


    class SettingsStore:
        """Обёртка поверх LocalStorage.load_settings/save_settings."""

        def __init__(self, storage: LocalStorage) -> None:
            self._storage = storage

        def load(self) -> UISettings:
            """Load from settings.json или defaults если пусто/коррупция."""
            raw = self._storage.load_settings()
            settings = UISettings.from_dict(raw)
            settings.validate()
            return settings

        def save(self, settings: UISettings) -> None:
            """Atomic save через LocalStorage (tmp+os.replace)."""
            self._storage.save_settings(settings.to_dict())
    ```

    ШАГ 2 — Создать `client/tests/ui/test_settings.py`:

    ```python
    """Unit-тесты UISettings + SettingsStore (Plan 03-02)."""
    from dataclasses import asdict

    import pytest
    from client.core.storage import LocalStorage
    from client.core.paths import AppPaths
    from client.ui.settings import (
        UISettings, SettingsStore,
        VALID_THEMES, VALID_TASK_STYLES, VALID_NOTIFICATIONS,
    )


    def test_defaults():
        s = UISettings()
        assert s.theme == "light"
        assert s.task_style == "card"
        assert s.notifications_mode == "sound_pulse"
        assert s.on_top is True
        assert s.autostart is False
        assert s.overlay_position == [100, 100]
        assert s.window_size == [460, 600]
        assert s.window_position is None
        assert s.version == 1


    def test_to_dict_has_all_keys():
        d = UISettings().to_dict()
        expected = {
            "theme", "task_style", "notifications_mode", "on_top",
            "autostart", "overlay_position", "window_size", "window_position", "version",
        }
        assert set(d.keys()) == expected


    def test_from_dict_round_trip():
        original = UISettings(theme="beige", autostart=True, overlay_position=[50, 75])
        restored = UISettings.from_dict(original.to_dict())
        assert restored == original


    def test_from_dict_ignores_unknown_keys():
        s = UISettings.from_dict({"theme": "dark", "unknown_future_key": 42})
        assert s.theme == "dark"
        assert not hasattr(s, "unknown_future_key")


    def test_from_dict_uses_defaults_for_missing():
        s = UISettings.from_dict({"theme": "dark"})
        assert s.theme == "dark"
        assert s.task_style == "card"  # default


    def test_validate_resets_invalid_theme():
        s = UISettings(theme="purple")
        s.validate()
        assert s.theme == "light"


    def test_validate_resets_invalid_task_style():
        s = UISettings(task_style="bubble")
        s.validate()
        assert s.task_style == "card"


    def test_validate_resets_invalid_notifications():
        s = UISettings(notifications_mode="loud")
        s.validate()
        assert s.notifications_mode == "sound_pulse"


    def test_store_load_fresh_returns_defaults(tmp_appdata):
        storage = LocalStorage(AppPaths())
        storage.init()
        store = SettingsStore(storage)
        loaded = store.load()
        assert loaded == UISettings()


    def test_store_save_then_load_round_trip(tmp_appdata):
        storage = LocalStorage(AppPaths())
        storage.init()
        store = SettingsStore(storage)
        original = UISettings(theme="dark", task_style="minimal", on_top=False,
                              overlay_position=[200, 300])
        store.save(original)
        restored = store.load()
        assert restored == original


    def test_store_load_with_corrupted_theme_sanitizes(tmp_appdata):
        storage = LocalStorage(AppPaths())
        storage.init()
        # Подсовываем невалидный theme напрямую
        storage.save_settings({"theme": "corrupted", "task_style": "card"})
        store = SettingsStore(storage)
        loaded = store.load()
        assert loaded.theme == "light"  # validate() sanitized
    ```

    ШАГ 3 — Запустить тесты.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_settings.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/settings.py exists, grep "class UISettings" >= 1
    - grep "class SettingsStore" client/ui/settings.py >= 1
    - grep "def from_dict" client/ui/settings.py >= 1
    - grep "def validate" client/ui/settings.py >= 1
    - grep "notifications_mode" client/ui/settings.py >= 1
    - grep "overlay_position" client/ui/settings.py >= 1
    - grep "sound_pulse" client/ui/settings.py >= 1 (per D-13)
    - pytest client/tests/ui/test_settings.py проходит с 11 зелёными тестами
  </acceptance_criteria>

  <done>
    UISettings dataclass + SettingsStore готовы. Следующие планы могут:
    `from client.ui.settings import UISettings, SettingsStore` и читать/писать settings.json
    через единую schema.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_themes.py + test_settings.py — 100% pass
- grep проверки всех UI-SPEC hex'ов confirm verbatim
- UI-SPEC colors присутствуют дословно (ни одного случайно искажённого hex)
</verification>

<success_criteria>
- client/ui/themes.py и client/ui/settings.py созданы
- 24 unit-теста (13 themes + 11 settings) зелёные
- Phase 2 регрессия: pytest client/tests/test_storage.py проходит
- Готов слой, от которого зависят все Phase 3 Wave 2+ планы
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-system/03-02-SUMMARY.md`
</output>
