---
phase: 01-server-auth
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/pyproject.toml
  - server/requirements.txt
  - server/requirements-dev.txt
  - server/tests/__init__.py
  - server/tests/conftest.py
  - server/__init__.py
  - server/api/__init__.py
  - server/auth/__init__.py
  - server/db/__init__.py
  - server/bot/__init__.py
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "Команда `cd server && pytest -x --timeout=10` запускается и показывает pytest config loaded, 0 tests collected"
    - "Структура `server/{api,auth,db,bot}/__init__.py` и `server/tests/conftest.py` существуют"
    - "`server/requirements.txt` и `server/requirements-dev.txt` содержат все нужные зависимости с версиями из RESEARCH.md"
    - "Python venv можно создать из server/requirements-dev.txt без ошибок зависимостей"
  artifacts:
    - path: "server/pyproject.toml"
      provides: "pytest config (asyncio_mode=auto, testpaths=tests)"
      contains: "[tool.pytest.ini_options]"
    - path: "server/requirements.txt"
      provides: "Production deps для VPS"
      contains: "fastapi"
    - path: "server/requirements-dev.txt"
      provides: "Test deps (pytest, pytest-asyncio, httpx, aiosqlite)"
      contains: "pytest-asyncio"
    - path: "server/tests/conftest.py"
      provides: "Async SQLite in-memory engine fixture + httpx AsyncClient fixture"
      min_lines: 30
    - path: "server/api/__init__.py"
      provides: "Package marker для server.api подмодуля"
    - path: "server/auth/__init__.py"
      provides: "Package marker для server.auth подмодуля"
    - path: "server/db/__init__.py"
      provides: "Package marker для server.db подмодуля"
    - path: "server/bot/__init__.py"
      provides: "Package marker для server.bot подмодуля"
  key_links:
    - from: "server/tests/conftest.py"
      to: "server.api.app"
      via: "import (пока закомментирован — будет раскомментирован в plan 06)"
      pattern: "from server"
---

<objective>
Заложить тестовую инфраструктуру и структуру директорий для всей Фазы 1. Wave 0 — это фундамент: после этого plan каждый последующий plan сможет писать тесты в `server/tests/` и запускать их через `pytest`.

Цель: pytest работает, зависимости декларированы, структура `server/{api,auth,db,bot}/` готова принять код последующих plans.

Purpose: Без Wave 0 ни один task в Waves 1-5 не сможет добавлять automated verification. Wave 0 создаёт общий контракт: test framework + dir layout.

Output: `server/pyproject.toml` (pytest config), `server/requirements.txt` + `requirements-dev.txt` (зависимости), `server/tests/conftest.py` (fixtures skeleton), пустые `__init__.py` для subpackages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-auth/01-CONTEXT.md
@.planning/phases/01-server-auth/01-RESEARCH.md
@.planning/phases/01-server-auth/01-VALIDATION.md
@server/__init__.py
@server/api.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать pyproject.toml, requirements.txt, requirements-dev.txt для server</name>
  <files>server/pyproject.toml, server/requirements.txt, server/requirements-dev.txt</files>
  <read_first>
    - server/api.py (текущий скелет FastAPI)
    - .planning/phases/01-server-auth/01-RESEARCH.md (разделы Standard Stack и Validation Architecture — точные версии библиотек)
    - .planning/phases/01-server-auth/01-VALIDATION.md (требования к тест-инфре)
    - CLAUDE.md (project instructions — русские комментарии)
  </read_first>
  <action>
Создать три файла в директории `server/`:

**1. `server/pyproject.toml`** — конфиг pytest (не используется для сборки пакета, только для test runner):

```toml
# Конфигурация pytest для серверной части Личного Еженедельника.
# Для сборки пакета не используется — сервер деплоится как git checkout + venv.

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-ra",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "integration: integration tests (могут быть медленнее)",
    "unit: быстрые unit-тесты",
]
```

**2. `server/requirements.txt`** — production-зависимости для VPS (точные версии из RESEARCH.md Standard Stack):

```
# Серверные зависимости для VPS 109.94.211.29.
# Устанавливаются в /opt/planner/venv на сервере.
# Клиент (CustomTkinter и т.д.) живёт в корневом requirements.txt — не здесь.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
PyJWT>=2.9.0
passlib[bcrypt]>=1.7.4
aiogram>=3.15.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
alembic>=1.13.0
slowapi>=0.1.9
httpx>=0.27.0
```

**3. `server/requirements-dev.txt`** — dev/test-зависимости (pytest + coverage + httpx для AsyncClient):

```
# Dev-зависимости (тесты, линтеры) — не деплоятся на VPS.
# Устанавливаются локально: pip install -r server/requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
pytest-timeout>=2.2.0
```

Все русские комментарии — согласно CLAUDE.md project instructions.
  </action>
  <verify>
    <automated>python -c "import tomllib; tomllib.load(open('server/pyproject.toml','rb'))" && test -f server/requirements.txt && test -f server/requirements-dev.txt && grep -q "fastapi>=0.115" server/requirements.txt && grep -q "pytest-asyncio" server/requirements-dev.txt && grep -q "asyncio_mode = \"auto\"" server/pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `server/pyproject.toml` существует и парсится как валидный TOML (python `tomllib.load()` не падает)
    - `server/pyproject.toml` содержит `asyncio_mode = "auto"` в `[tool.pytest.ini_options]`
    - `server/pyproject.toml` содержит `testpaths = ["tests"]`
    - `server/requirements.txt` содержит `fastapi>=0.115.0`, `uvicorn[standard]>=0.30.0`, `sqlalchemy>=2.0.30`, `aiosqlite>=0.20.0`, `PyJWT>=2.9.0`, `passlib[bcrypt]>=1.7.4`, `aiogram>=3.15.0`, `alembic>=1.13.0`, `slowapi>=0.1.9`, `httpx>=0.27.0`, `pydantic-settings>=2.0.0`
    - `server/requirements-dev.txt` начинается с `-r requirements.txt` и содержит `pytest>=8.0.0`, `pytest-asyncio>=0.23.0`, `pytest-timeout>=2.2.0`
  </acceptance_criteria>
  <done>Три файла созданы, parseable, содержат все декларированные пакеты из RESEARCH.md Standard Stack. Команда `pip install -r server/requirements-dev.txt --dry-run` успешно резолвит граф зависимостей.</done>
</task>

<task type="auto">
  <name>Task 2: Создать структуру subpackages server/{api,auth,db,bot} и server/tests</name>
  <files>server/__init__.py, server/api/__init__.py, server/auth/__init__.py, server/db/__init__.py, server/bot/__init__.py, server/tests/__init__.py, server/tests/conftest.py, .gitignore</files>
  <read_first>
    - server/__init__.py (может быть пустым — проверить)
    - server/api.py (текущий плоский файл — станет server/api/__init__.py после plan 06; в этом plan пока не трогаем)
    - .planning/phases/01-server-auth/01-RESEARCH.md §Recommended Project Structure и §Validation Architecture (conftest.py паттерн)
    - .planning/phases/01-server-auth/01-VALIDATION.md (Wave 0 Requirements list)
    - .planning/research/PITFALLS.md Pitfall 11 (Cyrillic paths — упоминается в тестах)
  </read_first>
  <action>
**ВАЖНО:** В этом plan мы НЕ удаляем существующие плоские файлы `server/api.py`, `server/auth.py`, `server/db.py`, `server/config.py`. Они останутся на месте параллельно с новыми subpackages. Plan 06 (endpoints) переедет в `server/api/` как полноценная замена, и тогда же плоский `api.py` будет удалён. Это сознательный выбор: не ломаем импорт `from server.api import app` прямо сейчас — пусть скелет работает, пока plans 02-06 строят настоящую структуру в subpackages.

Создать пустые `__init__.py` файлы и `conftest.py`:

**1. `server/__init__.py`** — оставить как есть если уже пустой; иначе — пустой файл с комментарием:
```python
# Серверная часть Личного Еженедельника. Деплоится на VPS 109.94.211.29.
```

**2. `server/api/__init__.py`** — пустой пакет-маркер:
```python
# Модуль FastAPI приложения. Наполняется в Plan 06 (auth endpoints) и далее.
```

**3. `server/auth/__init__.py`** — пустой пакет-маркер:
```python
# JWT + Telegram auth-код логика. Наполняется в Plan 04 (JWT) и Plan 05 (auth-code).
```

**4. `server/db/__init__.py`** — пустой пакет-маркер:
```python
# Модели SQLAlchemy и async engine. Наполняется в Plan 02 (models) и Plan 03 (engine).
```

**5. `server/bot/__init__.py`** — пустой пакет-маркер:
```python
# Telegram-бот (aiogram 3.x). В Фазе 1 — только /start handler (Plan 09). В Фазе 5 — команды задач.
```

**6. `server/tests/__init__.py`** — пустой файл-маркер (pytest нужен, чтобы считать tests как package для imports):
```python
# Tests package marker. Оставить пустым.
```

**7. `server/tests/conftest.py`** — skeleton с базовыми fixtures (async engine, httpx client). Импорты из `server.api` закомментированы пока код не существует — будут раскомментированы в Plan 06:

```python
"""
Общие pytest fixtures для серверных тестов.

Паттерн:
- `test_db` — async SQLite in-memory engine, создаёт все таблицы через Base.metadata.create_all
- `client` — httpx.AsyncClient, привязан к FastAPI приложению через ASGITransport
- `mock_telegram_send` — патчит httpx POST к api.telegram.org (чтобы тесты не ходили в реальный Telegram API)
"""
import pytest
import pytest_asyncio
from httpx import AsyncClient, ASGITransport
from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker, AsyncSession


# Импорты ниже будут раскомментированы по мере появления кода в plans 02-06:
# from server.db.engine import Base, get_db
# from server.api.app import app


@pytest_asyncio.fixture
async def test_engine():
    """
    Async SQLite in-memory engine для тестов.
    
    PRAGMA WAL и busy_timeout не применяются в in-memory режиме (бессмысленно) —
    только foreign_keys=ON для консистентности с production.
    """
    engine = create_async_engine(
        "sqlite+aiosqlite:///:memory:",
        echo=False,
    )
    # Импорт Base и create_all будут добавлены в Plan 02, когда модели существуют:
    # async with engine.begin() as conn:
    #     await conn.run_sync(Base.metadata.create_all)
    yield engine
    await engine.dispose()


@pytest_asyncio.fixture
async def db_session(test_engine) -> AsyncSession:
    """Async-сессия SQLAlchemy для одного теста."""
    async_session_factory = async_sessionmaker(test_engine, expire_on_commit=False)
    async with async_session_factory() as session:
        yield session


@pytest_asyncio.fixture
async def client():
    """
    httpx.AsyncClient привязанный к FastAPI приложению через in-memory ASGI транспорт.
    
    В Plan 06 будет дополнен dependency_overrides для подмены get_db на test_engine.
    """
    # app будет импортирован в Plan 06 когда api.app существует:
    # from server.api.app import app
    # async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
    #     yield ac
    pytest.skip("Fixture будет активирован в Plan 06 когда FastAPI app.py существует")


@pytest.fixture
def mock_telegram_send(monkeypatch):
    """
    Патчит httpx.AsyncClient.post чтобы перехватить вызовы к api.telegram.org.
    
    В Plan 05 будет наполнен конкретной логикой записи sent-сообщений в list.
    """
    sent = []
    # Заглушка — конкретная логика добавится в Plan 05
    return sent
```

**8. `.gitignore`** — добавить секции для Python/pytest/venv, если их ещё нет:

Прочитать текущий `.gitignore`, затем ДОПОЛНИТЬ (не перезаписывать) следующими секциями если их нет:

```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/

# Секреты — никогда не коммитить
.env
.env.local
.env.production
*.env
!.env.example

# SQLite базы (local dev)
*.db
*.db-journal
*.db-wal
*.db-shm

# Build artifacts
dist/
build/
*.spec
*.exe

# IDE
.idea/
.vscode/
*.swp
```

Если какие-то строки уже есть — не дублировать.
  </action>
  <verify>
    <automated>test -f server/__init__.py && test -f server/api/__init__.py && test -f server/auth/__init__.py && test -f server/db/__init__.py && test -f server/bot/__init__.py && test -f server/tests/__init__.py && test -f server/tests/conftest.py && python -c "import ast; ast.parse(open('server/tests/conftest.py').read())" && grep -q "test_engine" server/tests/conftest.py && grep -q "venv/" .gitignore && grep -q "\.env" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Существуют пустые файлы: `server/__init__.py`, `server/api/__init__.py`, `server/auth/__init__.py`, `server/db/__init__.py`, `server/bot/__init__.py`, `server/tests/__init__.py`
    - Файл `server/tests/conftest.py` существует и парсится как валидный Python (ast.parse не падает)
    - `server/tests/conftest.py` содержит fixture `test_engine` и `db_session` (`grep -q "test_engine"` и `grep -q "db_session"` успешны)
    - `server/tests/conftest.py` использует `sqlite+aiosqlite:///:memory:` (grep успешен)
    - `.gitignore` содержит `venv/`, `__pycache__/`, `.env`, `*.db`, `.pytest_cache/` (каждая — отдельный grep)
    - Старые файлы `server/api.py`, `server/auth.py`, `server/db.py`, `server/config.py` остаются на месте — не удалены (test -f)
  </acceptance_criteria>
  <done>Subpackages созданы как namespace placeholder, conftest.py валидный Python с fixtures, которые позже активируются. Существующий плоский скелет не сломан. .gitignore защищает от коммита секретов, БД и venv.</done>
</task>

<task type="auto">
  <name>Task 3: Проверить pytest collection и зафиксировать Wave 0</name>
  <files>server/tests/test_infrastructure.py</files>
  <read_first>
    - server/pyproject.toml (pytest config из Task 1)
    - server/tests/conftest.py (fixtures из Task 2)
  </read_first>
  <action>
Создать маркер-тест `server/tests/test_infrastructure.py`, который проверяет, что pytest настроен и fixtures импортируются. Этот тест — единственный "живой" тест Wave 0, он даёт проверяемое доказательство, что Wave 0 работает и последующие plans могут добавлять тесты рядом с этим файлом.

```python
"""
Маркер-тест Wave 0: pytest собирается, fixtures импортируются, asyncio-режим работает.

Последующие plans (02-11) добавляют тесты в этот же каталог.
Этот файл остаётся как self-check инфраструктуры.
"""
import pytest
import sys


def test_python_version():
    """Python >= 3.10 нужен для SQLAlchemy 2.x async и aiogram 3.x."""
    assert sys.version_info >= (3, 10), f"Нужен Python 3.10+, установлен {sys.version_info}"


@pytest.mark.asyncio
async def test_asyncio_mode_auto_works():
    """Проверяет что pytest-asyncio в auto-режиме подхватывает async test без декоратора-маркера на каждом."""
    import asyncio
    await asyncio.sleep(0)
    assert True


def test_conftest_fixtures_importable():
    """Проверяет что conftest.py парсится и модуль tests — корректный пакет."""
    from server.tests import conftest
    assert hasattr(conftest, "test_engine")
    assert hasattr(conftest, "db_session")
```

После создания файла запустить `cd server && python -m pytest tests/test_infrastructure.py -x -v` и убедиться что 3 теста проходят. Если pytest не установлен — установить через `pip install -r requirements-dev.txt` первым.
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/test_infrastructure.py -x -v 2>&1 | tee /tmp/pytest.out && grep -q "3 passed" /tmp/pytest.out</automated>
  </verify>
  <acceptance_criteria>
    - Файл `server/tests/test_infrastructure.py` существует
    - Команда `cd server && python -m pytest tests/test_infrastructure.py -x -v` завершается с exit code 0
    - Output содержит "3 passed" (три теста прошли)
    - `test_conftest_fixtures_importable` не падает — значит `server/tests/conftest.py` импортируется и содержит объявленные fixtures
  </acceptance_criteria>
  <done>Тестовая инфраструктура работает end-to-end. Последующие plans могут писать `server/tests/test_xxx.py` и запускать через `cd server && pytest -x --timeout=10`.</done>
</task>

</tasks>

<verification>
После всех трёх tasks:
1. `cd server && python -m pytest -x --timeout=10` — проходит с 3/3 passed
2. `ls server/api server/auth server/db server/bot server/tests` — все директории существуют с `__init__.py`
3. `grep -c "^[a-zA-Z]" server/requirements.txt` — >= 12 строк (все пакеты из Standard Stack)
4. `cat .gitignore | grep -E "venv|\\.env|__pycache__|\\*\\.db" | wc -l` — >= 4 совпадения
</verification>

<success_criteria>
Wave 0 завершена когда:
- pytest инфраструктура работает (test_infrastructure.py passes)
- Все subpackages существуют как пустые placeholders
- Зависимости декларированы в requirements.txt и requirements-dev.txt
- .gitignore защищает от коммита секретов
- Существующий скелет (server/api.py, server/auth.py, server/db.py, server/config.py) НЕ СЛОМАН — остаётся работать параллельно до Plan 06
- Планы 02-11 могут добавлять тесты в server/tests/ и импорты в subpackages без инфраструктурных проблем
</success_criteria>

<output>
После завершения создать `.planning/phases/01-server-auth/01-01-SUMMARY.md` с перечнем созданных файлов, версий зависимостей и подтверждением что pytest работает.
</output>
