---
phase: 01-server-auth
plan: 07
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - server/api/sync_routes.py
  - server/api/sync_schemas.py
  - server/api/app.py
  - server/tests/test_sync_routes.py
autonomous: true
requirements: ["SRV-02", "SRV-06"]
must_haves:
  truths:
    - "POST /api/sync требует Bearer access и возвращает 401 без него"
    - "POST /api/sync принимает {since: ISO datetime | null, changes: [op, ...]} и возвращает {server_timestamp, changes}"
    - "Операция op=create/update вставляет или обновляет task для текущего user через upsert; updated_at устанавливается сервером (SRV-06)"
    - "Операция op=delete ставит tasks.deleted_at = now (tombstone)"
    - "Delta по since: возвращаются tasks у user где updated_at > since (включая tombstones если deleted_at стал > since)"
    - "Клиент не может вставить task с чужим user_id — сервер подставляет current_user.id"
  artifacts:
    - path: "server/api/sync_schemas.py"
      provides: "Pydantic схемы: SyncIn, SyncOut, TaskChange, SyncOperation enum"
      contains: "class SyncIn"
      min_lines: 40
    - path: "server/api/sync_routes.py"
      provides: "POST /api/sync — delta upsert + tombstone"
      contains: "server_timestamp"
      min_lines: 60
  key_links:
    - from: "server/api/sync_routes.py"
      to: "server.auth.dependencies.get_current_user"
      via: "Depends"
      pattern: "get_current_user"
    - from: "server/api/sync_routes.py"
      to: "server.db.models.Task"
      via: "ORM upsert + select by updated_at"
      pattern: "Task"
    - from: "server/api/app.py"
      to: "server.api.sync_routes.router"
      via: "app.include_router"
      pattern: "sync_router"
---

<objective>
Реализовать `POST /api/sync` — единственный endpoint для синхронизации задач между устройствами (Фаза 2 клиент будет его использовать).

Покрывает:
- SRV-02: delta-синхронизация по since timestamp с tombstones (deleted_at)
- SRV-06: server-side updated_at — клиент присылает данные задачи, но updated_at игнорируется — выставляется сервером через onupdate=func.now()

Протокол (RESEARCH.md Pattern 10, ARCHITECTURE.md §Sync protocol):
- Вход: `{since: ISO datetime | null, changes: [{op, task_id, text?, day?, time_deadline?, done?, position?}]}`
- Выход: `{server_timestamp: ISO, changes: [{task_id, text, day, ..., updated_at, deleted_at}]}`

Output: sync endpoint с Pydantic schemas, integration тесты (auth-protected, upsert, delete с tombstone, delta по since).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-server-auth/01-CONTEXT.md
@.planning/phases/01-server-auth/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@server/api/app.py
@server/auth/dependencies.py
@server/db/engine.py
@server/db/models.py

<interfaces>
```
POST /api/sync
  headers: Authorization: Bearer <access>
  body: {
    "since": "2026-04-14T10:00:00Z" | null,
    "changes": [
      {"op": "create", "task_id": "uuid", "text": "...", "day": "2026-04-14", "time_deadline": null, "done": false, "position": 0},
      {"op": "update", "task_id": "uuid", "text": "new", "done": true},
      {"op": "delete", "task_id": "uuid"}
    ]
  }
  200: {
    "server_timestamp": "2026-04-14T10:01:23.456Z",
    "changes": [
      {"task_id": "uuid", "text": "...", "day": "2026-04-14", "done": false, "position": 0,
       "time_deadline": null, "updated_at": "2026-04-14T10:01:23.456Z", "deleted_at": null}
    ]
  }
  401: стандартная ошибка через get_current_user
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic schemas для sync endpoint</name>
  <files>server/api/sync_schemas.py</files>
  <read_first>
    - server/db/models.py (Task fields)
    - .planning/phases/01-server-auth/01-CONTEXT.md D-17 (/api/sync spec)
    - .planning/research/ARCHITECTURE.md §API Surface Sketch (sync контракт)
  </read_first>
  <action>
Создать `server/api/sync_schemas.py`:

```python
"""
Pydantic v2 схемы для /api/sync endpoint.

Протокол:
  Request:  SyncIn {since: datetime | None, changes: list[TaskChange]}
  Response: SyncOut {server_timestamp: datetime, changes: list[TaskState]}

TaskChange — операция от клиента (create/update/delete).
TaskState — полное состояние task с серверными полями (updated_at, deleted_at).
"""
from __future__ import annotations

import enum
from datetime import datetime
from typing import List, Optional

from pydantic import BaseModel, Field, field_validator


class SyncOp(str, enum.Enum):
    CREATE = "create"
    UPDATE = "update"
    DELETE = "delete"


class TaskChange(BaseModel):
    """
    Операция от клиента.
    
    CREATE: требуется task_id (UUID) + все поля task (text, day обязательные).
    UPDATE: task_id + partial fields (те что меняются).
    DELETE: только task_id.
    """
    op: SyncOp
    task_id: str = Field(..., description="UUID от клиента (SYNC-06: client-generated id)")
    text: Optional[str] = None
    day: Optional[str] = Field(default=None, description="ISO date: 2026-04-14")
    time_deadline: Optional[datetime] = None
    done: Optional[bool] = None
    position: Optional[int] = None
    
    @field_validator("day")
    @classmethod
    def validate_day_format(cls, v):
        if v is None:
            return v
        # Простая ISO date проверка — не strict parse, пусть datetime.fromisoformat разбирается
        if len(v) != 10 or v[4] != "-" or v[7] != "-":
            raise ValueError("day должен быть ISO date 'YYYY-MM-DD'")
        return v


class SyncIn(BaseModel):
    since: Optional[datetime] = Field(
        default=None,
        description="Клиент присылает updated_at последней успешной синхронизации; null = полный sync"
    )
    changes: List[TaskChange] = Field(default_factory=list)


class TaskState(BaseModel):
    """Полное серверное состояние task — возвращается в delta."""
    task_id: str
    text: str
    day: str
    time_deadline: Optional[datetime] = None
    done: bool
    position: int
    created_at: datetime
    updated_at: datetime
    deleted_at: Optional[datetime] = None


class SyncOut(BaseModel):
    server_timestamp: datetime = Field(..., description="Время ответа; клиент сохраняет как новый since для следующего sync")
    changes: List[TaskState] = Field(default_factory=list)
```
  </action>
  <verify>
    <automated>cd server && python -c "from server.api.sync_schemas import SyncIn, SyncOut, TaskChange, SyncOp, TaskState; s = SyncIn(since=None, changes=[TaskChange(op='create', task_id='t1', text='hi', day='2026-04-14')]); assert s.changes[0].op == SyncOp.CREATE; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/sync_schemas.py` существует, содержит классы: SyncIn, SyncOut, TaskChange, TaskState, SyncOp
    - `grep -q "enum.Enum" server/api/sync_schemas.py` успешен (SyncOp)
    - `grep -q '"create"' server/api/sync_schemas.py` успешен (SyncOp.CREATE)
    - `grep -q "field_validator" server/api/sync_schemas.py` успешен (валидация day)
    - Python проверка выше: SyncIn с TaskChange ("create") парсится корректно
  </acceptance_criteria>
  <done>Pydantic схемы готовы. Client может построить SyncIn, server может вернуть SyncOut. Валидация ISO-date для day.</done>
</task>

<task type="auto">
  <name>Task 2: sync endpoint с upsert, tombstone и delta-query; подключить в app.py</name>
  <files>server/api/sync_routes.py, server/api/app.py</files>
  <read_first>
    - server/api/sync_schemas.py (из Task 1)
    - server/auth/dependencies.py (get_current_user)
    - server/db/engine.py (get_db)
    - server/db/models.py (Task, User)
    - server/api/app.py (add include_router)
    - .planning/research/ARCHITECTURE.md §Pattern 1, §Pattern 3 (Operation Queue + Tombstones)
  </read_first>
  <action>
**Создать `server/api/sync_routes.py`:**

```python
"""
POST /api/sync — delta-синхронизация задач (SRV-02, SRV-06).

Алгоритм:
1. Авторизация через Bearer (get_current_user)
2. Для каждой TaskChange в request.changes:
   - CREATE: INSERT OR IGNORE (идемпотентно — SYNC-06 UUID client-generated)
   - UPDATE: SET only переданные поля + updated_at = server_now (SRV-06)
   - DELETE: SET deleted_at = now (tombstone — не hard delete)
3. Выполнить delta-query: SELECT tasks WHERE user_id=? AND updated_at > since
   (если since None — все tasks user'а)
4. Вернуть server_timestamp + changes (включая tombstones с deleted_at != None)

Server-wins конфликт-резолюция: клиент присылает изменения — сервер применяет их
без версий/vector clocks; при конфликте последний writer wins (простой подход для
single-user проекта). См. ARCHITECTURE.md §Key Architectural Tradeoffs.
"""
from __future__ import annotations

from datetime import datetime, timezone

from fastapi import APIRouter, Depends
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from server.api.sync_schemas import SyncIn, SyncOp, SyncOut, TaskChange, TaskState
from server.auth.dependencies import get_current_user
from server.db.engine import get_db
from server.db.models import Task, User

router = APIRouter(prefix="/api/sync", tags=["sync"])


def _utcnow() -> datetime:
    return datetime.now(timezone.utc)


async def _apply_create(db: AsyncSession, user_id: str, change: TaskChange) -> None:
    """
    INSERT OR IGNORE idempotent — при повторной попытке create с тем же task_id
    просто скипаем (SYNC-06: CREATE идемпотентен).
    """
    existing_stmt = select(Task).where(Task.id == change.task_id, Task.user_id == user_id)
    existing = (await db.execute(existing_stmt)).scalar_one_or_none()
    if existing is not None:
        # Идемпотентность: уже есть — просто трактуем как update (клиент возможно retry'ит)
        await _apply_update(db, user_id, change, existing)
        return
    
    task = Task(
        id=change.task_id,
        user_id=user_id,
        text=change.text or "",
        day=change.day or "",
        time_deadline=change.time_deadline,
        done=bool(change.done) if change.done is not None else False,
        position=change.position if change.position is not None else 0,
    )
    db.add(task)


async def _apply_update(
    db: AsyncSession, user_id: str, change: TaskChange, existing: Task | None = None
) -> None:
    """Apply partial update to task (updated_at выставится сервером через onupdate)."""
    if existing is None:
        stmt = select(Task).where(Task.id == change.task_id, Task.user_id == user_id)
        existing = (await db.execute(stmt)).scalar_one_or_none()
    if existing is None:
        # Апдейт для несуществующей task — игнорируем (клиент пропустил CREATE; resync подтянет)
        return
    
    if change.text is not None:
        existing.text = change.text
    if change.day is not None:
        existing.day = change.day
    if change.time_deadline is not None:
        existing.time_deadline = change.time_deadline
    if change.done is not None:
        existing.done = change.done
    if change.position is not None:
        existing.position = change.position
    # updated_at обновляется автоматически через onupdate=func.now() в модели


async def _apply_delete(db: AsyncSession, user_id: str, change: TaskChange) -> None:
    """Soft-delete — ставим deleted_at (SYNC-08 tombstone)."""
    stmt = select(Task).where(Task.id == change.task_id, Task.user_id == user_id)
    existing = (await db.execute(stmt)).scalar_one_or_none()
    if existing is None:
        return
    if existing.deleted_at is None:
        existing.deleted_at = _utcnow()


def _to_state(t: Task) -> TaskState:
    return TaskState(
        task_id=t.id,
        text=t.text,
        day=t.day,
        time_deadline=t.time_deadline,
        done=t.done,
        position=t.position,
        created_at=t.created_at,
        updated_at=t.updated_at,
        deleted_at=t.deleted_at,
    )


@router.post("", response_model=SyncOut)
async def sync(
    body: SyncIn,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    """
    Delta-sync endpoint (SRV-02).
    
    SRV-06: updated_at всегда из БД (onupdate=func.now() у Task), не из клиента.
    """
    # Применить changes
    for change in body.changes:
        if change.op == SyncOp.CREATE:
            await _apply_create(db, current_user.id, change)
        elif change.op == SyncOp.UPDATE:
            await _apply_update(db, current_user.id, change)
        elif change.op == SyncOp.DELETE:
            await _apply_delete(db, current_user.id, change)
    
    # Commit — заставит onupdate применить к обновлённым rows
    await db.commit()
    
    # Delta: все tasks user с updated_at > since (или все если since=None)
    server_now = _utcnow()
    stmt = select(Task).where(Task.user_id == current_user.id)
    if body.since is not None:
        stmt = stmt.where(Task.updated_at > body.since)
    stmt = stmt.order_by(Task.updated_at.asc())
    
    result = await db.execute(stmt)
    tasks = result.scalars().all()
    
    return SyncOut(
        server_timestamp=server_now,
        changes=[_to_state(t) for t in tasks],
    )
```

**Обновить `server/api/app.py`:** добавить import sync_router и include_router. Найти строку `from server.api.auth_routes import router as auth_router` и рядом добавить:

```python
from server.api.sync_routes import router as sync_router
```

Найти `app.include_router(auth_router)` и после неё добавить:
```python
app.include_router(sync_router)
```

Остальное не трогать.
  </action>
  <verify>
    <automated>cd server && python -c "from server.api.app import app; paths = [r.path for r in app.routes]; assert '/api/sync' in paths, f'routes: {paths}'; print('ok', paths)"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/sync_routes.py` существует, содержит `router = APIRouter(prefix="/api/sync"` и `@router.post("")`
    - `grep -q "get_current_user" server/api/sync_routes.py` успешен (auth required)
    - `grep -q "SyncOp.CREATE\|SyncOp.UPDATE\|SyncOp.DELETE" server/api/sync_routes.py` — все 3 op покрыты
    - `grep -q "deleted_at" server/api/sync_routes.py` успешен (tombstone)
    - `grep -q "updated_at > since\|updated_at > body.since" server/api/sync_routes.py` успешен (delta query)
    - `server/api/app.py` обновлён: `grep -q "sync_router" server/api/app.py` успешен
    - Python проверка выше: app содержит /api/sync route
  </acceptance_criteria>
  <done>/api/sync endpoint работает через Bearer auth, upsert tasks, soft-delete через deleted_at, delta-query по since. Подключён в app.py.</done>
</task>

<task type="auto">
  <name>Task 3: Integration тесты для sync endpoint</name>
  <files>server/tests/test_sync_routes.py</files>
  <read_first>
    - server/api/sync_routes.py (из Task 2)
    - server/tests/test_auth_routes.py (паттерн override_get_db + get login через /verify)
  </read_first>
  <action>
Создать `server/tests/test_sync_routes.py`:

```python
"""
Integration тесты /api/sync — auth required, CRUD через op, tombstone, delta.

Использует паттерн из test_auth_routes.py: логинимся через /verify, получаем
access, дальше все sync вызовы с Bearer.
"""
from __future__ import annotations

import asyncio
from datetime import datetime, timezone, timedelta

import pytest
import pytest_asyncio
from httpx import ASGITransport, AsyncClient
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

from server.db.base import Base


@pytest.fixture(autouse=True)
def _setup_env(monkeypatch, tmp_path):
    monkeypatch.setenv("DATABASE_URL", f"sqlite+aiosqlite:///{tmp_path / 'sync.db'}")
    monkeypatch.setenv("JWT_SECRET", "a" * 32)
    monkeypatch.setenv("JWT_REFRESH_SECRET", "b" * 32)
    monkeypatch.setenv("BOT_TOKEN", "token")
    monkeypatch.setenv("ALLOWED_USERNAMES", "nikita")
    from server.config import get_settings
    get_settings.cache_clear()


@pytest_asyncio.fixture
async def authed_client(monkeypatch):
    """App + client + логин → возвращает (client, access_token, user_id)."""
    from server.config import get_settings
    settings = get_settings()
    engine = create_async_engine(settings.database_url, echo=False)
    from server.db.engine import _attach_pragma_listener, get_db
    _attach_pragma_listener(engine)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    factory = async_sessionmaker(engine, expire_on_commit=False)
    
    async def override_get_db():
        async with factory() as session:
            yield session
    
    # Мок Telegram
    sent_codes = []
    async def fake_send(chat_id, code, hostname, msk_time_str, **kwargs):
        from server.auth.telegram import TelegramSendError
        sent_codes.append({"chat_id": chat_id, "code": code})
        return TelegramSendError.OK
    monkeypatch.setattr("server.api.auth_routes.send_auth_code", fake_send)
    
    # Предсоздать user с chat_id
    from server.db.models import User
    async with factory() as session:
        session.add(User(telegram_username="nikita", telegram_chat_id=12345))
        await session.commit()
    
    from server.api.app import app
    app.dependency_overrides[get_db] = override_get_db
    
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        # Login flow
        r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
        assert r1.status_code == 200
        r2 = await ac.post("/api/auth/verify", json={"request_id": r1.json()["request_id"], "code": sent_codes[-1]["code"]})
        assert r2.status_code == 200
        access = r2.json()["access_token"]
        user_id = r2.json()["user_id"]
        yield ac, access, user_id, factory
    
    app.dependency_overrides.clear()
    await engine.dispose()


@pytest.mark.asyncio
async def test_sync_requires_auth(authed_client):
    ac, access, user_id, _ = authed_client
    # Без Bearer
    r = await ac.post("/api/sync", json={"since": None, "changes": []})
    assert r.status_code == 401


@pytest.mark.asyncio
async def test_sync_empty_returns_empty_delta(authed_client):
    ac, access, user_id, _ = authed_client
    r = await ac.post(
        "/api/sync",
        headers={"Authorization": f"Bearer {access}"},
        json={"since": None, "changes": []},
    )
    assert r.status_code == 200
    data = r.json()
    assert "server_timestamp" in data
    assert data["changes"] == []


@pytest.mark.asyncio
async def test_sync_create_task(authed_client):
    ac, access, user_id, _ = authed_client
    body = {
        "since": None,
        "changes": [
            {"op": "create", "task_id": "task-1", "text": "buy milk", "day": "2026-04-14",
             "done": False, "position": 0},
        ],
    }
    r = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"}, json=body)
    assert r.status_code == 200
    data = r.json()
    assert len(data["changes"]) == 1
    t = data["changes"][0]
    assert t["task_id"] == "task-1"
    assert t["text"] == "buy milk"
    assert t["day"] == "2026-04-14"
    assert t["done"] is False
    assert t["deleted_at"] is None
    assert t["updated_at"]  # SRV-06: server-provided


@pytest.mark.asyncio
async def test_sync_create_idempotent(authed_client):
    """CREATE с тем же task_id дважды — не создаёт дубликат, обновляет."""
    ac, access, user_id, _ = authed_client
    op = {"op": "create", "task_id": "t1", "text": "v1", "day": "2026-04-14", "done": False, "position": 0}
    r1 = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"}, json={"since": None, "changes": [op]})
    assert r1.status_code == 200
    
    # Ещё один create с другим text
    op2 = {**op, "text": "v2"}
    r2 = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"}, json={"since": None, "changes": [op2]})
    data = r2.json()
    # Должна быть одна task с text = v2 (идемпотентный create → update семантика)
    tasks_for_t1 = [t for t in data["changes"] if t["task_id"] == "t1"]
    assert len(tasks_for_t1) == 1
    assert tasks_for_t1[0]["text"] == "v2"


@pytest.mark.asyncio
async def test_sync_update_partial(authed_client):
    ac, access, user_id, _ = authed_client
    # Сначала create
    await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                   json={"since": None, "changes": [
                       {"op": "create", "task_id": "t1", "text": "original", "day": "2026-04-14",
                        "done": False, "position": 0}]})
    
    # Update только done
    r = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                       json={"since": None, "changes": [{"op": "update", "task_id": "t1", "done": True}]})
    data = r.json()
    t1 = [t for t in data["changes"] if t["task_id"] == "t1"][0]
    assert t1["done"] is True
    assert t1["text"] == "original"  # не затёрто


@pytest.mark.asyncio
async def test_sync_delete_sets_tombstone(authed_client):
    ac, access, user_id, _ = authed_client
    await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                   json={"since": None, "changes": [
                       {"op": "create", "task_id": "t1", "text": "tbd", "day": "2026-04-14",
                        "done": False, "position": 0}]})
    
    r = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                       json={"since": None, "changes": [{"op": "delete", "task_id": "t1"}]})
    data = r.json()
    t1 = [t for t in data["changes"] if t["task_id"] == "t1"][0]
    assert t1["deleted_at"] is not None


@pytest.mark.asyncio
async def test_sync_delta_by_since(authed_client):
    ac, access, user_id, _ = authed_client
    # t0 — базовый timestamp; создадим 2 tasks, затем возьмём since=сейчас, создадим 3-ю — должна вернуться только 3-я
    await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                   json={"since": None, "changes": [
                       {"op": "create", "task_id": "a", "text": "1", "day": "2026-04-14", "done": False, "position": 0},
                       {"op": "create", "task_id": "b", "text": "2", "day": "2026-04-14", "done": False, "position": 0}]})
    
    r_mid = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                           json={"since": None, "changes": []})
    mid_ts = r_mid.json()["server_timestamp"]
    
    # Небольшая пауза чтобы updated_at был строго больше mid_ts
    await asyncio.sleep(1.1)
    
    await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                   json={"since": None, "changes": [
                       {"op": "create", "task_id": "c", "text": "3", "day": "2026-04-14", "done": False, "position": 0}]})
    
    r = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                       json={"since": mid_ts, "changes": []})
    data = r.json()
    ids_returned = {t["task_id"] for t in data["changes"]}
    assert "c" in ids_returned  # новая task возвращается
    # a и b — возможно тоже если их updated_at оказался позже mid_ts из-за round-trip; проверим что c точно есть


@pytest.mark.asyncio
async def test_sync_cannot_affect_other_users(authed_client, monkeypatch):
    """User A не может создать/обновить task user B. user_id всегда == current_user."""
    ac, access, user_id, factory = authed_client
    # Создадим "чужого" user в БД
    from server.db.models import User, Task
    async with factory() as session:
        other = User(telegram_username="other")
        session.add(other)
        await session.commit()
        await session.refresh(other)
        other_id = other.id
        # И задачу у other
        other_task = Task(id="other-task", user_id=other_id, text="secret", day="2026-04-14", done=False, position=0)
        session.add(other_task)
        await session.commit()
    
    # Nikita пытается обновить чужую task
    r = await ac.post("/api/sync", headers={"Authorization": f"Bearer {access}"},
                       json={"since": None, "changes": [{"op": "update", "task_id": "other-task", "text": "hijacked"}]})
    assert r.status_code == 200
    
    # Проверить что other-task не изменилась
    async with factory() as session:
        from sqlalchemy import select
        result = await session.execute(select(Task).where(Task.id == "other-task"))
        t = result.scalar_one()
        assert t.text == "secret"
        assert t.user_id == other_id
```
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/test_sync_routes.py -x -v 2>&1 | tee /tmp/sync.out && grep -qE "[78] passed" /tmp/sync.out</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/test_sync_routes.py` существует
    - Покрыты сценарии: requires_auth, empty_delta, create, create_idempotent, update_partial, delete_tombstone, delta_by_since, cannot_affect_other_users
    - `cd server && pytest tests/test_sync_routes.py -v` — минимум 7 passed
    - Тест `test_sync_cannot_affect_other_users` доказывает row-level isolation (user_id подменяется на current_user)
    - Тест `test_sync_delete_sets_tombstone` доказывает SYNC-08 tombstone
    - Тест `test_sync_create_task` доказывает SRV-06 (updated_at server-side)
  </acceptance_criteria>
  <done>/api/sync endpoint работает end-to-end. CRUD через op, tombstone, delta-query. Cross-user isolation. 7+ integration тестов зелёные.</done>
</task>

</tasks>

<verification>
- `cd server && pytest tests/test_sync_routes.py -v` — 7+ passed
- `cd server && pytest -x` — all green (running всех тестов Wave 0-3)
- `python -c "from server.api.app import app; print('/api/sync' in [r.path for r in app.routes])"` — True
</verification>

<success_criteria>
Plan завершён когда:
- /api/sync endpoint подключён к FastAPI app (/api/sync, метод POST)
- Bearer auth через get_current_user работает (401 без токена)
- CREATE/UPDATE/DELETE операции выполняют upsert/partial update/tombstone
- Delta-query по `since` возвращает только updated_at > since
- Row-level isolation: user A не может изменять tasks user B
- SRV-06 гарантирован: updated_at всегда server-side через onupdate
- 7+ integration тестов зелёные
- Plans 08 (rate-limit) и 10 (deploy) могут использовать тот же app
</success_criteria>

<output>
Создать `.planning/phases/01-server-auth/01-07-SUMMARY.md` с примером request/response sync, списком поддержанных операций и доказательством что SRV-02 + SRV-06 покрыты.
</output>
