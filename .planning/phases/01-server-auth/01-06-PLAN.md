---
phase: 01-server-auth
plan: 06
type: execute
wave: 3
depends_on: ["01-04", "01-05"]
files_modified:
  - server/api/app.py
  - server/api/schemas.py
  - server/api/errors.py
  - server/api/auth_routes.py
  - server/tests/test_auth_routes.py
autonomous: true
requirements: ["AUTH-01", "AUTH-02", "AUTH-04", "AUTH-05", "SRV-01"]
must_haves:
  truths:
    - "POST /api/auth/request-code возвращает {request_id, expires_in} и отправляет код в Telegram при валидном username"
    - "POST /api/auth/verify с правильным кодом возвращает {access_token, refresh_token, expires_in, user_id} (AUTH-02)"
    - "POST /api/auth/refresh с валидным refresh → новый access и новый refresh (rolling, AUTH-04)"
    - "POST /api/auth/logout с Bearer → session revoked; последующий refresh → 401 (AUTH-05)"
    - "GET /api/auth/me с Bearer → {user_id, username, created_at}"
    - "Все ошибки в формате {\"error\": {\"code\": \"...\", \"message\": \"...\"}} (D-18)"
    - "Usernames вне ALLOWED_USERNAMES получают USER_NOT_ALLOWED без отправки кода"
  artifacts:
    - path: "server/api/app.py"
      provides: "FastAPI app с lifespan, роутеры подключены"
      contains: "FastAPI("
    - path: "server/api/schemas.py"
      provides: "Pydantic v2 request/response модели"
      contains: "RequestCodeIn"
    - path: "server/api/errors.py"
      provides: "api_error helper + предопределённые err_xxx"
      contains: "def api_error"
    - path: "server/api/auth_routes.py"
      provides: "APIRouter с 5 auth endpoints"
      min_lines: 100
  key_links:
    - from: "server/api/auth_routes.py"
      to: "server.auth.codes.AuthCodeService"
      via: "AuthCodeService(db)"
      pattern: "AuthCodeService"
    - from: "server/api/auth_routes.py"
      to: "server.auth.sessions.SessionService"
      via: "SessionService(db)"
      pattern: "SessionService"
    - from: "server/api/app.py"
      to: "server.api.auth_routes.router"
      via: "app.include_router"
      pattern: "include_router"
---

<objective>
Собрать 5 auth endpoints из компонентов Plans 04-05: POST /api/auth/request-code, POST /api/auth/verify, POST /api/auth/refresh, POST /api/auth/logout, GET /api/auth/me. Это тонкий слой — вся логика в сервисах.

Output: FastAPI app с auth router, Pydantic schemas, error helpers, интеграционные тесты через httpx AsyncClient.

**Важно:** `server/api.py` (плоский скелет) остаётся на месте. Новый `server/api/app.py` — новый app instance в подпакете.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-server-auth/01-CONTEXT.md
@.planning/phases/01-server-auth/01-RESEARCH.md
@server/api.py
@server/auth/jwt.py
@server/auth/sessions.py
@server/auth/codes.py
@server/auth/telegram.py
@server/auth/dependencies.py
@server/db/engine.py
@server/db/models.py

<interfaces>
Endpoints (для Plans 07-11):
```
POST /api/auth/request-code  {username, hostname} → {request_id, expires_in} | 403 USER_NOT_ALLOWED | 400 BOT_NOT_STARTED
POST /api/auth/verify        {request_id, code, device_name?} → {access_token, refresh_token, expires_in, user_id, token_type}
POST /api/auth/refresh       {refresh_token} → {access_token, refresh_token, expires_in, token_type} | 401 INVALID_REFRESH
POST /api/auth/logout        Bearer + optional {refresh_token} → 204
GET  /api/auth/me            Bearer → {user_id, username, created_at}
```
`server.api.app.app` — uvicorn entry point.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать Pydantic schemas и error helpers</name>
  <files>server/api/schemas.py, server/api/errors.py</files>
  <read_first>
    - .planning/phases/01-server-auth/01-CONTEXT.md D-17, D-18
    - .planning/phases/01-server-auth/01-RESEARCH.md §Code Examples (Pydantic v2)
  </read_first>
  <action>
Создать `server/api/schemas.py` со всеми Pydantic моделями для auth endpoints:

- Request: `RequestCodeIn(username, hostname="неизвестно")`, `VerifyCodeIn(request_id, code, device_name=None)`, `RefreshTokenIn(refresh_token)`, `LogoutIn(refresh_token=None)`
- Response: `RequestCodeOut(request_id, expires_in)`, `TokenPairOut(access_token, refresh_token, expires_in, token_type="bearer", user_id)`, `AccessTokenOut(access_token, refresh_token=None, expires_in, token_type="bearer")`, `UserMeOut(user_id, username, created_at)`, `ErrorDetail(code, message)`, `ErrorOut(error: ErrorDetail)`

Валидация:
- `RequestCodeIn.username`: field_validator mode="before" strip, lstrip("@"), lower, длина 1-100
- `VerifyCodeIn.code`: field_validator требует 6 цифр (isdigit + len==6), сообщение "Код должен быть 6 цифр"
- `RequestCodeIn.hostname`: default "неизвестно", max_length 100

Создать `server/api/errors.py`:
```python
from fastapi import HTTPException, status

def api_error(code: str, message: str, status_code: int = 400, headers: dict | None = None) -> HTTPException:
    return HTTPException(
        status_code=status_code,
        detail={"error": {"code": code, "message": message}},
        headers=headers,
    )

def err_user_not_allowed() -> HTTPException: return api_error("USER_NOT_ALLOWED", "Пользователь не в allow-list", 403)
def err_bot_not_started() -> HTTPException: return api_error("BOT_NOT_STARTED", "Напишите /start боту @Jazzways_bot в Telegram, затем попробуйте снова", 400)
def err_telegram_send() -> HTTPException: return api_error("TELEGRAM_ERROR", "Не удалось отправить код — попробуйте через минуту", 502)
def err_invalid_code() -> HTTPException: return api_error("INVALID_CODE", "Код неверный", 400)
def err_code_expired() -> HTTPException: return api_error("CODE_EXPIRED", "Срок действия кода истёк — запросите новый", 400)
def err_already_used() -> HTTPException: return api_error("ALREADY_USED", "Код уже был использован — запросите новый", 400)
def err_invalid_refresh() -> HTTPException: return api_error("INVALID_REFRESH", "Refresh-токен недействителен или истёк", 401, {"WWW-Authenticate": "Bearer"})
def err_request_not_found() -> HTTPException: return api_error("INVALID_CODE", "request_id не найден", 400)
```

Все сообщения — на русском (D-05 user-facing + CLAUDE.md project instructions).
  </action>
  <verify>
    <automated>cd server && python -c "from server.api.schemas import RequestCodeIn, VerifyCodeIn, TokenPairOut, UserMeOut, ErrorOut, AccessTokenOut, LogoutIn, RefreshTokenIn; from server.api.errors import api_error, err_invalid_code, err_user_not_allowed; e = err_invalid_code(); assert e.detail['error']['code'] == 'INVALID_CODE'; r = RequestCodeIn(username='@Nikita'); assert r.username == 'nikita'; import pytest; v = None;\ntry: VerifyCodeIn(request_id='x', code='12345')\nexcept Exception: v='ok'\nassert v == 'ok'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/schemas.py` содержит классы: RequestCodeIn, VerifyCodeIn, RefreshTokenIn, LogoutIn, RequestCodeOut, TokenPairOut, AccessTokenOut, UserMeOut, ErrorDetail, ErrorOut
    - `grep -q "field_validator" server/api/schemas.py` успешен (валидация кода и username)
    - `grep -q "lstrip.*@" server/api/schemas.py` успешен (нормализация username)
    - `server/api/errors.py` содержит `api_error`, `err_user_not_allowed`, `err_bot_not_started`, `err_telegram_send`, `err_invalid_code`, `err_code_expired`, `err_already_used`, `err_invalid_refresh` — всё найдено через grep
    - RequestCodeIn нормализует "@Nikita" → "nikita" (проверено командой выше)
    - VerifyCodeIn с кодом из 5 цифр — raises ValidationError
    - Все error_* хелперы возвращают HTTPException с detail формата `{"error": {"code":..., "message":...}}` (D-18)
  </acceptance_criteria>
  <done>Pydantic schemas и error helpers готовы к использованию в auth_routes.py. Формат ошибок соответствует D-18.</done>
</task>

<task type="auto">
  <name>Task 2: Создать auth_routes.py с 5 endpoints и FastAPI app.py</name>
  <files>server/api/auth_routes.py, server/api/app.py</files>
  <read_first>
    - server/api/schemas.py (из Task 1)
    - server/api/errors.py (из Task 1)
    - server/auth/codes.py (AuthCodeService, VerifyResult)
    - server/auth/telegram.py (send_auth_code, TelegramSendError)
    - server/auth/sessions.py (SessionService)
    - server/auth/jwt.py (create_access_token, decode_refresh_token)
    - server/auth/dependencies.py (get_current_user)
    - server/db/engine.py (get_db)
    - server/db/models.py (User)
    - .planning/phases/01-server-auth/01-CONTEXT.md D-17
    - .planning/phases/01-server-auth/01-RESEARCH.md Pattern 4 (lifespan), Pattern 5 (chat_id flow)
  </read_first>
  <action>
**Создать `server/api/auth_routes.py`** — APIRouter со всеми 5 endpoints:

```python
"""
Auth endpoints Фазы 1.

Тонкий слой над server.auth.{codes, sessions, jwt, telegram} — парсит request,
вызывает сервис, формирует response. Никакой бизнес-логики здесь — только routing.
"""
from __future__ import annotations

import socket
from datetime import datetime, timezone
from zoneinfo import ZoneInfo

from fastapi import APIRouter, Depends, Request, Response, status
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from server.api import errors as err
from server.api.schemas import (
    AccessTokenOut, LogoutIn, RefreshTokenIn, RequestCodeIn, RequestCodeOut,
    TokenPairOut, UserMeOut, VerifyCodeIn,
)
from server.auth.codes import AuthCodeService, VerifyResult
from server.auth.dependencies import get_current_user
from server.auth.jwt import create_access_token, decode_refresh_token, hash_refresh_token
from server.auth.sessions import SessionService
from server.auth.telegram import TelegramSendError, send_auth_code
from server.config import get_settings
from server.db.engine import get_db
from server.db.models import AuthCode, User

router = APIRouter(prefix="/api/auth", tags=["auth"])

MSK = ZoneInfo("Europe/Moscow")


def _now_msk_str() -> str:
    return datetime.now(MSK).strftime("%Y-%m-%d %H:%M")


@router.post("/request-code", response_model=RequestCodeOut)
async def request_code(body: RequestCodeIn, db: AsyncSession = Depends(get_db)):
    """
    Шаг 1: пользователь ввёл username → мы шлём 6-значный код в Telegram.
    
    - Проверить allow-list (CONTEXT.md D-27 ALLOWED_USERNAMES)
    - Найти или создать User (chat_id может быть NULL — заполнит /start handler Plan 09)
    - Сгенерировать код через AuthCodeService
    - Отправить через send_auth_code
    - Вернуть request_id для UI
    """
    settings = get_settings()
    username = body.username
    
    if username not in settings.allowed_usernames:
        raise err.err_user_not_allowed()
    
    # Найти или создать user (без chat_id если первый запрос)
    stmt = select(User).where(User.telegram_username == username)
    result = await db.execute(stmt)
    user = result.scalar_one_or_none()
    if user is None:
        user = User(telegram_username=username)
        db.add(user)
        await db.commit()
        await db.refresh(user)
    
    # Сгенерировать код
    svc = AuthCodeService(db)
    code_result = await svc.request_code(username)
    
    # Отправить через Telegram
    send_result = await send_auth_code(
        chat_id=user.telegram_chat_id,
        code=code_result.code,
        hostname=body.hostname,
        msk_time_str=_now_msk_str(),
    )
    
    if send_result == TelegramSendError.BOT_NOT_STARTED:
        raise err.err_bot_not_started()
    if send_result != TelegramSendError.OK:
        raise err.err_telegram_send()
    
    return RequestCodeOut(
        request_id=code_result.request_id,
        expires_in=settings.auth_code_ttl_seconds,
    )


@router.post("/verify", response_model=TokenPairOut)
async def verify_code(body: VerifyCodeIn, db: AsyncSession = Depends(get_db)):
    """
    Шаг 2: пользователь ввёл код → выдаём access+refresh пару.
    
    - Найти AuthCode по request_id, взять username
    - Verify через AuthCodeService (D-08 single-use, D-07 TTL)
    - Найти User по username (должен уже существовать — его создал request-code)
    - SessionService.create → (session, refresh_plaintext)
    - Сгенерировать access через create_access_token
    - Вернуть TokenPairOut
    """
    settings = get_settings()
    
    # Найти request по id — узнать username
    stmt = select(AuthCode).where(AuthCode.id == body.request_id)
    res = await db.execute(stmt)
    auth_code_record = res.scalar_one_or_none()
    if auth_code_record is None:
        raise err.err_request_not_found()
    
    username = auth_code_record.username
    
    # Verify
    svc = AuthCodeService(db)
    verdict = await svc.verify_code(username, body.code)
    if verdict == VerifyResult.INVALID:
        raise err.err_invalid_code()
    if verdict == VerifyResult.EXPIRED:
        raise err.err_code_expired()
    if verdict == VerifyResult.ALREADY_USED:
        raise err.err_already_used()
    
    # Найти user
    stmt_user = select(User).where(User.telegram_username == username)
    user_res = await db.execute(stmt_user)
    user = user_res.scalar_one_or_none()
    if user is None:
        raise err.api_error("USER_NOT_FOUND", "Пользователь не найден", 500)
    
    # Создать session + refresh
    sess_svc = SessionService(db)
    session_obj, refresh_plaintext = await sess_svc.create(
        user_id=user.id, device_name=body.device_name
    )
    
    # Access token
    access = create_access_token(user.id)
    
    return TokenPairOut(
        access_token=access,
        refresh_token=refresh_plaintext,
        expires_in=settings.access_token_ttl_seconds,
        user_id=user.id,
    )


@router.post("/refresh", response_model=AccessTokenOut)
async def refresh_access(body: RefreshTokenIn, db: AsyncSession = Depends(get_db)):
    """
    Обновить access через refresh (AUTH-04, rolling D-13).
    
    SessionService.rotate_refresh revokes старый и выдаёт новый → возвращаем новый access + новый refresh.
    """
    settings = get_settings()
    sess_svc = SessionService(db)
    rot_result = await sess_svc.rotate_refresh(body.refresh_token)
    if rot_result is None:
        raise err.err_invalid_refresh()
    
    new_session, new_refresh_plaintext = rot_result
    access = create_access_token(new_session.user_id)
    
    return AccessTokenOut(
        access_token=access,
        refresh_token=new_refresh_plaintext,
        expires_in=settings.access_token_ttl_seconds,
    )


@router.post("/logout", status_code=status.HTTP_204_NO_CONTENT)
async def logout(
    body: LogoutIn | None = None,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
) -> Response:
    """
    Разлогинить текущий device (AUTH-05 + D-15).
    
    Если передан refresh_token в body — revoke именно эту session (через hash lookup).
    Если не передан — revoke все sessions этого user (full logout).
    """
    sess_svc = SessionService(db)
    
    if body is not None and body.refresh_token:
        # Revoke конкретную session по hash lookup
        target_hash = hash_refresh_token(body.refresh_token)
        session = await sess_svc.get_by_refresh_hash(target_hash)
        if session is not None and session.user_id == current_user.id:
            await sess_svc.revoke(session.id)
    else:
        # Revoke все session'ы этого user
        from server.db.models import Session as SessionRecord
        from datetime import datetime, timezone
        stmt = select(SessionRecord).where(
            SessionRecord.user_id == current_user.id,
            SessionRecord.revoked_at.is_(None),
        )
        result = await db.execute(stmt)
        for session in result.scalars().all():
            session.revoked_at = datetime.now(timezone.utc)
        await db.commit()
    
    return Response(status_code=status.HTTP_204_NO_CONTENT)


@router.get("/me", response_model=UserMeOut)
async def me(current_user: User = Depends(get_current_user)):
    """Информация о текущем пользователе."""
    return UserMeOut(
        user_id=current_user.id,
        username=current_user.telegram_username,
        created_at=current_user.created_at,
    )
```

**Создать `server/api/app.py`** — FastAPI app с lifespan и подключёнными роутерами:

```python
"""
FastAPI приложение — entry point для uvicorn.

Запуск локально:
    cd server && uvicorn server.api.app:app --reload --port 8100
На VPS (через systemd, Plan 10):
    ExecStart=/opt/planner/venv/bin/uvicorn server.api.app:app --host 127.0.0.1 --port 8100 --workers 1
"""
from __future__ import annotations

from contextlib import asynccontextmanager
import logging

from fastapi import FastAPI

from server.api.auth_routes import router as auth_router
from server.config import get_settings

logger = logging.getLogger(__name__)


@asynccontextmanager
async def lifespan(app: FastAPI):
    """
    Startup/shutdown hooks.
    
    В Фазе 1 здесь минимум. Alembic миграции — через отдельную команду (Plan 10 deploy).
    Engine.dispose() при shutdown — чтобы корректно закрыть aiosqlite connections.
    """
    settings = get_settings()
    logger.info(
        "Starting Личный Еженедельник API v%s (host=%s, port=%d)",
        settings.app_version, settings.host, settings.port,
    )
    yield
    # Shutdown
    from server.db.engine import engine
    await engine.dispose()
    logger.info("API stopped")


app = FastAPI(
    title="Личный Еженедельник API",
    version=get_settings().app_version,
    docs_url="/api/docs",
    redoc_url=None,
    openapi_url="/api/openapi.json",
    lifespan=lifespan,
)

app.include_router(auth_router)
```
  </action>
  <verify>
    <automated>cd server && python -c "from server.api.app import app; from server.api.auth_routes import router; routes = [r.path for r in app.routes]; assert '/api/auth/request-code' in routes; assert '/api/auth/verify' in routes; assert '/api/auth/refresh' in routes; assert '/api/auth/logout' in routes; assert '/api/auth/me' in routes; print('ok', len(routes), 'routes')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/auth_routes.py` существует, содержит APIRouter с prefix="/api/auth"
    - Все 5 endpoints определены (grep на `@router.post("/request-code"`, `/verify`, `/refresh`, `/logout`, `@router.get("/me"`)
    - `grep -q "AuthCodeService" server/api/auth_routes.py` успешен
    - `grep -q "SessionService" server/api/auth_routes.py` успешен
    - `grep -q "send_auth_code" server/api/auth_routes.py` успешен
    - `grep -q "get_current_user" server/api/auth_routes.py` успешен (защита logout+me)
    - `grep -q "allowed_usernames" server/api/auth_routes.py` успешен (allow-list check)
    - `server/api/app.py` существует, содержит `app = FastAPI(`, `app.include_router(auth_router)`, `lifespan=lifespan`
    - `grep -q "/api/docs" server/api/app.py` успешен (OpenAPI доступна)
    - Python проверка выше показывает 5 auth routes подключены
    - Старый `server/api.py` плоский не удалён (test -f)
  </acceptance_criteria>
  <done>FastAPI app стартует без ошибок. 5 auth endpoints маршрутизированы. Сервисы из Plans 04-05 используются через Depends(get_db) и ручной инстанс.</done>
</task>

<task type="auto">
  <name>Task 3: Integration тесты auth endpoints через httpx AsyncClient</name>
  <files>server/tests/test_auth_routes.py</files>
  <read_first>
    - server/api/auth_routes.py (из Task 2)
    - server/api/app.py (из Task 2)
    - server/tests/test_dependencies.py (паттерн override_get_db — из Plan 04)
    - server/tests/test_telegram.py (паттерн мок httpx — из Plan 05)
  </read_first>
  <action>
Создать `server/tests/test_auth_routes.py` — end-to-end flow через ASGI AsyncClient с мокнутым Telegram:

```python
"""
Integration тесты auth endpoints.

Паттерн:
- fixture `app` — FastAPI app с override_get_db (тестовая БД)
- fixture `mock_telegram` — monkeypatch send_auth_code → возвращает TelegramSendError.OK
  и сохраняет (chat_id, code) в list для проверки
- client — httpx.AsyncClient привязан к app через ASGITransport
"""
from __future__ import annotations

import pytest
import pytest_asyncio
from httpx import ASGITransport, AsyncClient
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

from server.db.base import Base


@pytest.fixture(autouse=True)
def _setup_env(monkeypatch, tmp_path):
    monkeypatch.setenv("DATABASE_URL", f"sqlite+aiosqlite:///{tmp_path / 'e2e.db'}")
    monkeypatch.setenv("JWT_SECRET", "a" * 32)
    monkeypatch.setenv("JWT_REFRESH_SECRET", "b" * 32)
    monkeypatch.setenv("BOT_TOKEN", "token")
    monkeypatch.setenv("ALLOWED_USERNAMES", "nikita")
    from server.config import get_settings
    get_settings.cache_clear()


@pytest_asyncio.fixture
async def app_client(monkeypatch):
    """App + client + перехваченный send_auth_code."""
    from server.config import get_settings
    settings = get_settings()
    engine = create_async_engine(settings.database_url, echo=False)
    from server.db.engine import _attach_pragma_listener, get_db
    _attach_pragma_listener(engine)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    
    factory = async_sessionmaker(engine, expire_on_commit=False)
    
    async def override_get_db():
        async with factory() as session:
            yield session
    
    # Мокаем send_auth_code — сохраняем вызовы в список
    sent_codes = []
    
    async def fake_send_auth_code(chat_id, code, hostname, msk_time_str, **kwargs):
        from server.auth.telegram import TelegramSendError
        sent_codes.append({"chat_id": chat_id, "code": code, "hostname": hostname})
        if chat_id is None:
            return TelegramSendError.BOT_NOT_STARTED
        return TelegramSendError.OK
    
    monkeypatch.setattr("server.api.auth_routes.send_auth_code", fake_send_auth_code)
    
    from server.api.app import app
    app.dependency_overrides[get_db] = override_get_db
    
    # Предзаполнить chat_id для nikita (иначе будет BOT_NOT_STARTED)
    from server.db.models import User
    async with factory() as session:
        user = User(telegram_username="nikita", telegram_chat_id=12345)
        session.add(user)
        await session.commit()
    
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        yield ac, sent_codes, factory
    
    app.dependency_overrides.clear()
    await engine.dispose()


@pytest.mark.asyncio
async def test_request_code_user_not_allowed(app_client):
    ac, _, _ = app_client
    resp = await ac.post("/api/auth/request-code", json={"username": "stranger", "hostname": "pc"})
    assert resp.status_code == 403
    assert resp.json()["detail"]["error"]["code"] == "USER_NOT_ALLOWED"


@pytest.mark.asyncio
async def test_request_code_success_sends_telegram(app_client):
    ac, sent, _ = app_client
    resp = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "work-pc"})
    assert resp.status_code == 200
    data = resp.json()
    assert "request_id" in data
    assert data["expires_in"] == 300
    # Telegram got called
    assert len(sent) == 1
    assert sent[0]["chat_id"] == 12345
    assert sent[0]["hostname"] == "work-pc"
    assert len(sent[0]["code"]) == 6


@pytest.mark.asyncio
async def test_request_code_bot_not_started(app_client, monkeypatch):
    """Если user.telegram_chat_id is None → BOT_NOT_STARTED."""
    ac, sent, factory = app_client
    # Удалить chat_id у nikita
    from server.db.models import User
    from sqlalchemy import update
    async with factory() as session:
        await session.execute(update(User).where(User.telegram_username == "nikita").values(telegram_chat_id=None))
        await session.commit()
    
    resp = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    assert resp.status_code == 400
    assert resp.json()["detail"]["error"]["code"] == "BOT_NOT_STARTED"


@pytest.mark.asyncio
async def test_full_auth_flow_request_verify_me(app_client):
    ac, sent, _ = app_client
    
    # Step 1: request code
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    assert r1.status_code == 200
    request_id = r1.json()["request_id"]
    code = sent[-1]["code"]
    
    # Step 2: verify
    r2 = await ac.post("/api/auth/verify", json={"request_id": request_id, "code": code, "device_name": "test-pc"})
    assert r2.status_code == 200
    tokens = r2.json()
    assert "access_token" in tokens
    assert "refresh_token" in tokens
    assert tokens["expires_in"] == 900
    assert tokens["token_type"] == "bearer"
    assert tokens["user_id"]
    
    access = tokens["access_token"]
    refresh = tokens["refresh_token"]
    
    # Step 3: GET /me с access
    r3 = await ac.get("/api/auth/me", headers={"Authorization": f"Bearer {access}"})
    assert r3.status_code == 200
    me_data = r3.json()
    assert me_data["username"] == "nikita"
    assert me_data["user_id"] == tokens["user_id"]
    
    return access, refresh, tokens["user_id"]


@pytest.mark.asyncio
async def test_verify_invalid_code(app_client):
    ac, _, _ = app_client
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    request_id = r1.json()["request_id"]
    r2 = await ac.post("/api/auth/verify", json={"request_id": request_id, "code": "000000"})
    # Может быть INVALID если случайно сгенерировался код ≠ 000000 (шанс 999999/1000000)
    assert r2.status_code == 400
    assert r2.json()["detail"]["error"]["code"] in ("INVALID_CODE",)


@pytest.mark.asyncio
async def test_verify_malformed_code_rejected_by_pydantic(app_client):
    ac, _, _ = app_client
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    request_id = r1.json()["request_id"]
    # Код из 5 цифр — должен упасть на уровне Pydantic
    r2 = await ac.post("/api/auth/verify", json={"request_id": request_id, "code": "12345"})
    assert r2.status_code == 422  # Pydantic validation


@pytest.mark.asyncio
async def test_refresh_rotates_tokens(app_client):
    ac, sent, _ = app_client
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    request_id = r1.json()["request_id"]
    code = sent[-1]["code"]
    r2 = await ac.post("/api/auth/verify", json={"request_id": request_id, "code": code})
    old_refresh = r2.json()["refresh_token"]
    
    r3 = await ac.post("/api/auth/refresh", json={"refresh_token": old_refresh})
    assert r3.status_code == 200
    new_tokens = r3.json()
    assert new_tokens["access_token"]
    assert new_tokens["refresh_token"] != old_refresh  # rolling rotation
    
    # Старый refresh больше не работает
    r4 = await ac.post("/api/auth/refresh", json={"refresh_token": old_refresh})
    assert r4.status_code == 401
    assert r4.json()["detail"]["error"]["code"] == "INVALID_REFRESH"


@pytest.mark.asyncio
async def test_refresh_invalid_returns_401(app_client):
    ac, _, _ = app_client
    r = await ac.post("/api/auth/refresh", json={"refresh_token": "garbage.token.here"})
    assert r.status_code == 401
    assert r.json()["detail"]["error"]["code"] == "INVALID_REFRESH"


@pytest.mark.asyncio
async def test_logout_revokes_session(app_client):
    ac, sent, _ = app_client
    # Login
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    code = sent[-1]["code"]
    r2 = await ac.post("/api/auth/verify", json={"request_id": r1.json()["request_id"], "code": code})
    access = r2.json()["access_token"]
    refresh = r2.json()["refresh_token"]
    
    # Logout (без refresh_token — revoke все)
    r3 = await ac.post("/api/auth/logout", headers={"Authorization": f"Bearer {access}"})
    assert r3.status_code == 204
    
    # Refresh старым refresh больше не работает (session revoked)
    r4 = await ac.post("/api/auth/refresh", json={"refresh_token": refresh})
    assert r4.status_code == 401


@pytest.mark.asyncio
async def test_me_without_auth_returns_401(app_client):
    ac, _, _ = app_client
    r = await ac.get("/api/auth/me")
    assert r.status_code == 401
    assert r.json()["detail"]["error"]["code"] == "MISSING_TOKEN"


@pytest.mark.asyncio
async def test_logout_with_specific_refresh_token(app_client):
    """Logout с body {refresh_token} → только эту конкретную session revoke."""
    ac, sent, _ = app_client
    # Первая session
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    code1 = sent[-1]["code"]
    r2 = await ac.post("/api/auth/verify", json={"request_id": r1.json()["request_id"], "code": code1})
    access1 = r2.json()["access_token"]
    refresh1 = r2.json()["refresh_token"]
    
    # Вторая session (как с другого устройства)
    r3 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "phone"})
    code2 = sent[-1]["code"]
    r4 = await ac.post("/api/auth/verify", json={"request_id": r3.json()["request_id"], "code": code2})
    refresh2 = r4.json()["refresh_token"]
    
    # Logout только первой session (передаём её refresh)
    r5 = await ac.post(
        "/api/auth/logout",
        headers={"Authorization": f"Bearer {access1}"},
        json={"refresh_token": refresh1},
    )
    assert r5.status_code == 204
    
    # refresh1 не работает
    r6 = await ac.post("/api/auth/refresh", json={"refresh_token": refresh1})
    assert r6.status_code == 401
    
    # refresh2 всё ещё работает
    r7 = await ac.post("/api/auth/refresh", json={"refresh_token": refresh2})
    assert r7.status_code == 200
```
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/test_auth_routes.py -x -v 2>&1 | tee /tmp/routes.out && grep -q "10 passed" /tmp/routes.out</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/test_auth_routes.py` существует
    - `cd server && pytest tests/test_auth_routes.py -v` — 10 passed
    - Покрыты сценарии: user_not_allowed, request_ok, bot_not_started, full_flow, invalid_code, malformed_code, refresh_rotation, invalid_refresh, logout_all, me_no_auth, logout_specific
    - Тест `test_full_auth_flow_request_verify_me` доказывает end-to-end AUTH-01 + AUTH-02 + SRV-01
    - Тест `test_refresh_rotates_tokens` доказывает AUTH-04 rolling refresh
    - Тест `test_logout_revokes_session` доказывает AUTH-05
    - Ошибки во всех негативных сценариях в формате D-18
  </acceptance_criteria>
  <done>Все 5 auth endpoints проверены integration-тестами через настоящий FastAPI + реальную SQLite. End-to-end flow AUTH-01 → AUTH-02 проходит. Rolling refresh и logout revoke доказаны.</done>
</task>

</tasks>

<verification>
- `cd server && pytest tests/test_auth_routes.py -v` — 10 passed
- `cd server && pytest -x --timeout=10` — все тесты Wave 0-3 зелёные (суммарно ~60+ тестов)
- `python -c "from server.api.app import app; print(len(app.routes))"` — >= 5 (+ defaults docs, openapi)
- `python -c "from server.api.app import app; print([r.path for r in app.routes if '/api/auth' in getattr(r, 'path', '')])"` — все 5 auth paths
</verification>

<success_criteria>
Plan завершён когда:
- 5 auth endpoints работают end-to-end через httpx integration tests
- Pydantic v2 валидация request bodies (422 при malformed)
- Allow-list блокирует unknown usernames (USER_NOT_ALLOWED)
- BOT_NOT_STARTED когда chat_id NULL (Pattern 5)
- AUTH-04 rolling refresh: старый refresh revoked, новая пара выдаётся
- AUTH-05 logout: revoke session, последующий refresh возвращает 401
- FastAPI app стартует без ошибок (uvicorn может его импортировать)
- 10 integration тестов зелёные
- Plan 07 (sync) и Plan 08 (health+rate-limit) могут подключать свои routers к этому app
</success_criteria>

<output>
Создать `.planning/phases/01-server-auth/01-06-SUMMARY.md` с:
- Список 5 endpoints и их request/response форматы
- Список covered requirements (AUTH-01, AUTH-02, AUTH-04, AUTH-05, SRV-01)
- Пример curl для end-to-end flow (для Plan 11)
- Count тестов (10 integration)
</output>
