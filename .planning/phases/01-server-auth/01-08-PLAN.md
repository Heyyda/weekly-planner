---
phase: 01-server-auth
plan: 08
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - server/api/misc_routes.py
  - server/api/rate_limit.py
  - server/api/app.py
  - server/api/auth_routes.py
  - server/tests/test_health_version.py
  - server/tests/test_rate_limit.py
autonomous: true
requirements: ["SRV-01"]
must_haves:
  truths:
    - "GET /api/health возвращает 200 с {status: 'ok'}"
    - "GET /api/version возвращает {version: '0.1.0', ...}"
    - "POST /api/auth/request-code второй раз в течение 60 секунд → 429 TOO_MANY_REQUESTS"
    - "Rate-limit применяется через slowapi Limiter по IP address (get_remote_address)"
    - "Health и version — БЕЗ auth (для мониторинга/reverse-proxy healthcheck)"
  artifacts:
    - path: "server/api/misc_routes.py"
      provides: "GET /api/health, GET /api/version"
      contains: "health"
    - path: "server/api/rate_limit.py"
      provides: "Limiter instance + rate_limit_exceeded_handler"
      contains: "Limiter"
  key_links:
    - from: "server/api/auth_routes.py"
      to: "server.api.rate_limit.limiter"
      via: "декоратор @limiter.limit на request-code"
      pattern: "limiter.limit"
    - from: "server/api/app.py"
      to: "server.api.rate_limit.limiter"
      via: "app.state.limiter + exception_handler"
      pattern: "state.limiter"
---

<objective>
Добавить (1) Health/Version endpoints (обязательны для ROADMAP Success Criterion #2 и мониторинга systemd/reverse-proxy), (2) rate-limiting через slowapi для /api/auth/request-code (CONTEXT.md D-09: 1/мин, 5/час).

Покрывает финальную часть SRV-01 (rate limit) и обеспечивает health-check пути.

**Важное решение:** Rate-limit по IP (get_remote_address), НЕ по username из body. RESEARCH.md Pitfall 6 предупреждает: slowapi's key_func синхронный и не может читать async request.json() — попытка сделать key по username приведёт к `RuntimeError: body read twice`. Для personal-use VPS rate limit по IP достаточен.

Output: misc_routes.py (health + version), rate_limit.py (Limiter setup), декоратор на request-code, подключение в app.py, тесты.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-server-auth/01-CONTEXT.md
@.planning/phases/01-server-auth/01-RESEARCH.md
@server/api/app.py
@server/api/auth_routes.py
@server/config.py

<interfaces>
```
GET /api/health   → 200 {"status": "ok"}
GET /api/version  → 200 {"version": "0.1.0", "download_url": "https://heyda.ru/planner/download", "sha256": ""}
POST /api/auth/request-code (повторный вызов с того же IP в течение минуты) → 429 TOO_MANY_REQUESTS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать misc_routes.py (health + version) и подключить в app</name>
  <files>server/api/misc_routes.py, server/api/app.py</files>
  <read_first>
    - server/api.py (старый плоский скелет имеет health + version заглушки — используем их как референс)
    - server/api/app.py
    - server/config.py (settings.app_version)
    - .planning/phases/01-server-auth/01-CONTEXT.md (D-10 URL, implicit ROADMAP Success Criterion #2)
  </read_first>
  <action>
**Создать `server/api/misc_routes.py`:**

```python
"""
Health + Version endpoints.

Health — для systemd liveness check и reverse-proxy healthcheck.
Version — для авто-обновления клиента (Фаза 6) — возвращает текущую версию
и SHA256 файла для проверки интегрии после скачивания.

Оба endpoint — БЕЗ auth (public), но они находятся под /api/ префиксом.
"""
from __future__ import annotations

from fastapi import APIRouter

from server.config import get_settings

router = APIRouter(prefix="/api", tags=["misc"])


@router.get("/health")
async def health():
    """
    Liveness check. Используется systemd WatchdogSec/ExecStartPost и Caddy/nginx healthchecks.
    
    Return 200 всегда — если процесс живой, health OK. Для readiness (DB доступна)
    можно добавить ping БД, но для Фазы 1 избыточно.
    """
    return {"status": "ok"}


@router.get("/version")
async def version():
    """
    Версия сервера и download URL для авто-обновления клиента (Фаза 6 — DIST-05).
    
    sha256 пока пустая — заполнится при билд-процессе клиента, когда появится
    реальный .exe артефакт для публикации.
    """
    settings = get_settings()
    return {
        "version": settings.app_version,
        "download_url": "https://heyda.ru/planner/download",
        "sha256": "",
    }
```

**Обновить `server/api/app.py`** — добавить import и include_router для misc_router. Найти блок импортов `from server.api.auth_routes import router as auth_router` и добавить:

```python
from server.api.misc_routes import router as misc_router
```

Найти `app.include_router(sync_router)` и после неё добавить:

```python
app.include_router(misc_router)
```
  </action>
  <verify>
    <automated>cd server && python -c "from server.api.app import app; paths = [r.path for r in app.routes]; assert '/api/health' in paths; assert '/api/version' in paths; print('ok', sorted(p for p in paths if p.startswith('/api/')))"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/misc_routes.py` существует, содержит `@router.get("/health")` и `@router.get("/version")`
    - `grep -q "status.*ok" server/api/misc_routes.py` успешен
    - `grep -q "app_version" server/api/misc_routes.py` успешен (читает версию из settings)
    - `server/api/app.py` обновлён: `grep -q "misc_router" server/api/app.py` успешен
    - Python проверка: /api/health и /api/version присутствуют в app.routes
  </acceptance_criteria>
  <done>Health и version endpoints доступны без auth. app.py включает misc_router. Готово для systemd/proxy healthchecks.</done>
</task>

<task type="auto">
  <name>Task 2: Rate limiting через slowapi для /api/auth/request-code</name>
  <files>server/api/rate_limit.py, server/api/auth_routes.py, server/api/app.py</files>
  <read_first>
    - server/api/auth_routes.py (декоратор вешается на endpoint request_code)
    - server/api/app.py (app.state.limiter + exception handler)
    - .planning/phases/01-server-auth/01-CONTEXT.md D-09 (1/мин, 5/час)
    - .planning/phases/01-server-auth/01-RESEARCH.md Pattern 6 (slowapi setup)
    - .planning/phases/01-server-auth/01-RESEARCH.md Pitfall 6 (key_func не может читать body — используем IP)
  </read_first>
  <action>
**Создать `server/api/rate_limit.py`:**

```python
"""
Rate limiter через slowapi (CONTEXT.md D-09).

Ключ — IP address (get_remote_address), не username — RESEARCH.md Pitfall 6:
slowapi's key_func вызывается синхронно, не может читать async request.body().

Для personal VPS single-user кейса IP-based rate limit достаточен — защищает от
тривиальных ботов и случайных повторных кликов.

Лимиты (D-09): 1 запрос/минуту, 5 запросов/час на один IP.
"""
from __future__ import annotations

from fastapi import Request, status
from fastapi.responses import JSONResponse
from slowapi import Limiter
from slowapi.errors import RateLimitExceeded
from slowapi.util import get_remote_address


# Default limits применяются ТОЛЬКО к endpoints с @limiter.limit декоратором —
# не ко всем. Объявляем пустыми; per-endpoint лимиты в auth_routes.py.
limiter = Limiter(key_func=get_remote_address, default_limits=[])


async def rate_limit_exceeded_handler(request: Request, exc: RateLimitExceeded) -> JSONResponse:
    """
    Кастомный handler для rate-limit — формат D-18 вместо дефолтного текста slowapi.
    """
    return JSONResponse(
        status_code=status.HTTP_429_TOO_MANY_REQUESTS,
        content={
            "error": {
                "code": "RATE_LIMIT_EXCEEDED",
                "message": "Слишком много запросов. Попробуйте через минуту.",
            }
        },
        headers={"Retry-After": "60"},
    )
```

**Обновить `server/api/app.py`** — подключить limiter к app и зарегистрировать exception handler. Найти блок `from server.api.misc_routes import router as misc_router` и добавить:

```python
from server.api.rate_limit import limiter, rate_limit_exceeded_handler
from slowapi.errors import RateLimitExceeded
```

Найти строку `app = FastAPI(...)` (через lifespan) и СРАЗУ ПОСЛЕ создания app, ДО include_router-ов добавить:

```python
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, rate_limit_exceeded_handler)
```

**Обновить `server/api/auth_routes.py`** — повесить декоратор на request_code. Найти импорты и добавить:

```python
from server.api.rate_limit import limiter
```

Найти функцию:
```python
@router.post("/request-code", response_model=RequestCodeOut)
async def request_code(body: RequestCodeIn, db: AsyncSession = Depends(get_db)):
```

Заменить её на версию с декоратором и Request параметром (slowapi требует `request: Request` в signature):

```python
@router.post("/request-code", response_model=RequestCodeOut)
@limiter.limit("1/minute;5/hour")  # CONTEXT.md D-09
async def request_code(
    request: Request,  # нужен slowapi для получения IP
    body: RequestCodeIn,
    db: AsyncSession = Depends(get_db),
):
```

И в импортах добавить `Request`:
```python
from fastapi import APIRouter, Depends, Request, Response, status
```
(если Request ещё не импортирован)

Остальная логика request_code не меняется.
  </action>
  <verify>
    <automated>cd server && python -c "from server.api.app import app; from server.api.rate_limit import limiter; assert app.state.limiter is limiter; print('ok')" && python -c "import inspect; from server.api.auth_routes import request_code; sig = str(inspect.signature(request_code)); assert 'Request' in sig, sig; print('sig ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/rate_limit.py` существует с `limiter = Limiter(key_func=get_remote_address)` и `rate_limit_exceeded_handler`
    - `grep -q "RATE_LIMIT_EXCEEDED" server/api/rate_limit.py` успешен (формат D-18)
    - `grep -q "Retry-After" server/api/rate_limit.py` успешен
    - `server/api/app.py` имеет `app.state.limiter = limiter` и `add_exception_handler(RateLimitExceeded`
    - `server/api/auth_routes.py` функция request_code декорирована `@limiter.limit("1/minute;5/hour")`
    - Signature request_code содержит `request: Request`
    - Python проверки выше проходят
  </acceptance_criteria>
  <done>Rate-limiter подключён к app. /api/auth/request-code декорирован "1/minute;5/hour". Ошибка в формате D-18.</done>
</task>

<task type="auto">
  <name>Task 3: Integration тесты health, version, rate-limit</name>
  <files>server/tests/test_health_version.py, server/tests/test_rate_limit.py</files>
  <read_first>
    - server/tests/test_auth_routes.py (паттерн AsyncClient + override_get_db)
    - server/api/rate_limit.py (из Task 2)
  </read_first>
  <action>
**Создать `server/tests/test_health_version.py`:**

```python
"""Тесты health + version — без auth, public endpoints."""
import pytest
from httpx import ASGITransport, AsyncClient


@pytest.fixture(autouse=True)
def _setup_env(monkeypatch, tmp_path):
    monkeypatch.setenv("DATABASE_URL", f"sqlite+aiosqlite:///{tmp_path / 'hv.db'}")
    monkeypatch.setenv("JWT_SECRET", "a" * 32)
    monkeypatch.setenv("JWT_REFRESH_SECRET", "b" * 32)
    monkeypatch.setenv("BOT_TOKEN", "token")
    monkeypatch.setenv("ALLOWED_USERNAMES", "nikita")
    from server.config import get_settings
    get_settings.cache_clear()


@pytest.mark.asyncio
async def test_health_returns_ok():
    from server.api.app import app
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/health")
    assert r.status_code == 200
    assert r.json() == {"status": "ok"}


@pytest.mark.asyncio
async def test_version_returns_schema():
    from server.api.app import app
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/version")
    assert r.status_code == 200
    data = r.json()
    assert data["version"] == "0.1.0"
    assert data["download_url"].startswith("https://")
    assert "sha256" in data


@pytest.mark.asyncio
async def test_health_no_auth_required():
    """Health доступен без Bearer — для monitoring."""
    from server.api.app import app
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        r = await ac.get("/api/health")
    assert r.status_code == 200  # НЕ 401
```

**Создать `server/tests/test_rate_limit.py`:**

```python
"""
Тесты rate-limit на /api/auth/request-code.

slowapi key_func = IP address. В httpx AsyncClient все requests идут с одного
"IP" (127.0.0.1 / test-client), поэтому limit 1/minute срабатывает на втором запросе.
"""
import pytest
import pytest_asyncio
from httpx import ASGITransport, AsyncClient
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

from server.db.base import Base


@pytest.fixture(autouse=True)
def _setup_env(monkeypatch, tmp_path):
    monkeypatch.setenv("DATABASE_URL", f"sqlite+aiosqlite:///{tmp_path / 'rl.db'}")
    monkeypatch.setenv("JWT_SECRET", "a" * 32)
    monkeypatch.setenv("JWT_REFRESH_SECRET", "b" * 32)
    monkeypatch.setenv("BOT_TOKEN", "token")
    monkeypatch.setenv("ALLOWED_USERNAMES", "nikita")
    from server.config import get_settings
    get_settings.cache_clear()


@pytest_asyncio.fixture
async def client_with_user(monkeypatch):
    from server.config import get_settings
    settings = get_settings()
    engine = create_async_engine(settings.database_url, echo=False)
    from server.db.engine import _attach_pragma_listener, get_db
    _attach_pragma_listener(engine)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    factory = async_sessionmaker(engine, expire_on_commit=False)
    
    async def override_get_db():
        async with factory() as session:
            yield session
    
    async def fake_send(chat_id, code, hostname, msk_time_str, **kwargs):
        from server.auth.telegram import TelegramSendError
        return TelegramSendError.OK
    monkeypatch.setattr("server.api.auth_routes.send_auth_code", fake_send)
    
    from server.db.models import User
    async with factory() as session:
        session.add(User(telegram_username="nikita", telegram_chat_id=12345))
        await session.commit()
    
    from server.api.app import app
    app.dependency_overrides[get_db] = override_get_db
    
    # КРИТИЧНО: limiter состояние сохраняется между тестами — нужно сбросить
    from server.api.rate_limit import limiter
    limiter.reset()
    
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as ac:
        yield ac
    
    app.dependency_overrides.clear()
    limiter.reset()
    await engine.dispose()


@pytest.mark.asyncio
async def test_request_code_rate_limited_on_second_call(client_with_user):
    """CONTEXT.md D-09: 1 запрос/минуту."""
    ac = client_with_user
    # Первый запрос — 200
    r1 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    assert r1.status_code == 200
    
    # Второй запрос в той же минуте — 429
    r2 = await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    assert r2.status_code == 429
    data = r2.json()
    assert "error" in data or "detail" in data
    # Наш кастомный handler формата D-18
    err_body = data.get("error") or data.get("detail", {}).get("error")
    assert err_body is not None
    assert err_body["code"] == "RATE_LIMIT_EXCEEDED"
    # Retry-After header
    assert "retry-after" in {k.lower() for k in r2.headers.keys()}


@pytest.mark.asyncio
async def test_rate_limit_does_not_affect_other_endpoints(client_with_user):
    """Rate-limit только на /request-code — health и version не ограничены."""
    ac = client_with_user
    # Hit request-code один раз чтобы limiter засчитал
    await ac.post("/api/auth/request-code", json={"username": "nikita", "hostname": "pc"})
    
    # Health доступен
    r = await ac.get("/api/health")
    assert r.status_code == 200
    r2 = await ac.get("/api/health")
    assert r2.status_code == 200  # не ограничено
    r3 = await ac.get("/api/version")
    assert r3.status_code == 200
```
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/test_health_version.py tests/test_rate_limit.py -x -v 2>&1 | tee /tmp/rl.out && grep -qE "5 passed|6 passed" /tmp/rl.out</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/test_health_version.py` существует — 3 теста
    - `server/tests/test_rate_limit.py` существует — 2 теста
    - `cd server && pytest tests/test_health_version.py tests/test_rate_limit.py -v` — 5 passed
    - Тест `test_request_code_rate_limited_on_second_call` получает 429 на втором запросе
    - Response body содержит `error.code = RATE_LIMIT_EXCEEDED`
    - Retry-After header присутствует
    - `test_rate_limit_does_not_affect_other_endpoints` доказывает что health/version не затронуты
  </acceptance_criteria>
  <done>Rate-limit работает. Health и version доступны без ограничений. Все 5 тестов зелёные.</done>
</task>

</tasks>

<verification>
- `cd server && pytest -x` — все тесты Wave 0-3 зелёные
- `python -c "from server.api.app import app; print(sorted([r.path for r in app.routes if getattr(r, 'path', '').startswith('/api')]))"` — содержит все auth + sync + health + version
</verification>

<success_criteria>
Plan завершён когда:
- GET /api/health возвращает 200 без auth — готов для systemd/proxy healthcheck
- GET /api/version возвращает {version, download_url, sha256}
- /api/auth/request-code rate-limited: 1/min, 5/hour по IP
- Ошибка rate-limit в формате D-18 с Retry-After header
- Health и version НЕ затронуты rate-limitом (per-endpoint)
- 5 новых integration тестов зелёные
- SRV-01 теперь полностью покрыт (endpoints + rate-limit) — в связке с Plan 06
</success_criteria>

<output>
Создать `.planning/phases/01-server-auth/01-08-SUMMARY.md` с примером curl для health/version и описанием rate-limit лимитов + как их сбрасывать в тестах (limiter.reset()).
</output>
