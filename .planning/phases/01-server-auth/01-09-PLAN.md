---
phase: 01-server-auth
plan: 09
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - server/bot/main.py
  - server/bot/handlers.py
  - server/tests/test_bot_handlers.py
autonomous: true
requirements: ["AUTH-01"]
must_haves:
  truths:
    - "aiogram 3.x bot запускается как standalone process через python -m server.bot.main"
    - "/start handler записывает message.chat.id в users.telegram_chat_id для matched user (по username из allowed list)"
    - "Если username не в ALLOWED_USERNAMES — бот отвечает 'доступ запрещён' и НЕ создаёт chat_id привязку"
    - "Если user уже существует с telegram_chat_id, /start обновляет его (на случай смены Telegram-аккаунта)"
    - "Запуск бота не требует FastAPI (отдельный процесс, отдельный systemd unit — CONTEXT.md D-24)"
  artifacts:
    - path: "server/bot/main.py"
      provides: "aiogram Dispatcher setup + polling entry point"
      contains: "Dispatcher"
      min_lines: 30
    - path: "server/bot/handlers.py"
      provides: "/start handler — регистрирует chat_id для user"
      contains: "@dp.message(CommandStart"
      min_lines: 40
  key_links:
    - from: "server/bot/handlers.py"
      to: "server.db.models.User"
      via: "ORM update users.telegram_chat_id"
      pattern: "telegram_chat_id"
    - from: "server/bot/main.py"
      to: "server.bot.handlers"
      via: "include router"
      pattern: "include_router"
---

<objective>
Создать минимальный aiogram 3.x бот — только /start handler, который записывает message.chat.id в users.telegram_chat_id. Это закрывает критическую проблему RESEARCH.md Pattern 5: auth endpoint /request-code не может отправить код пока user не написал /start.

Покрывает:
- AUTH-01 (частично): предпосылка — без /start от пользователя auth flow недоступен

Output: `server/bot/main.py` (Dispatcher + Bot + polling entry), `server/bot/handlers.py` (/start), unit-тесты на handler логику (без реального Telegram API).

**Scope:** Только /start. Команды /add, /week, /today — Фаза 5 (тот же bot instance, просто добавятся handlers). В Фазе 1 бот нужен для auth-flow, не для работы с задачами.

**Запуск:** `python -m server.bot.main` — отдельный process, отдельный systemd unit `planner-bot.service` (CONTEXT.md D-24). В Plan 10 deploy создаст unit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-server-auth/01-CONTEXT.md
@.planning/phases/01-server-auth/01-RESEARCH.md
@server/config.py
@server/db/engine.py
@server/db/models.py

<interfaces>
```python
# server/bot/main.py
def create_dispatcher() -> Dispatcher  # для тестов
async def main() -> None  # entry point для python -m server.bot.main
```

```python
# server/bot/handlers.py
router: Router  # aiogram Router
@router.message(CommandStart())
async def handle_start(message: Message) -> None
```

**Процесс запуска (Plan 10 deploy):**
```ini
# /etc/systemd/system/planner-bot.service
ExecStart=/opt/planner/venv/bin/python -m server.bot.main
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: aiogram /start handler + Dispatcher setup</name>
  <files>server/bot/main.py, server/bot/handlers.py</files>
  <read_first>
    - server/config.py (settings.bot_token, settings.allowed_usernames)
    - server/db/models.py (User.telegram_username, User.telegram_chat_id)
    - server/db/engine.py (AsyncSessionLocal — для standalone бота, без FastAPI DI)
    - .planning/phases/01-server-auth/01-CONTEXT.md D-24 (отдельный process)
    - .planning/phases/01-server-auth/01-RESEARCH.md Pattern 4, Pattern 5 (почему отдельный процесс, почему /start нужен)
  </read_first>
  <action>
**Создать `server/bot/handlers.py`:**

```python
"""
/start handler — регистрирует chat_id для user.

Проверяем:
1. Telegram username пользователя есть в settings.allowed_usernames
2. Сохраняем/обновляем users.telegram_chat_id = message.chat.id
3. Отвечаем инструкцией "Бот готов; возвращайтесь в приложение"

Если username не в allow-list — бот отвечает "доступ ограничен" и ничего не пишет в БД.
"""
from __future__ import annotations

import logging

from aiogram import Router
from aiogram.filters import CommandStart
from aiogram.types import Message
from sqlalchemy import select

from server.config import get_settings
from server.db.engine import AsyncSessionLocal
from server.db.models import User

logger = logging.getLogger(__name__)

router = Router(name="planner-bot-start")


ACCESS_DENIED_TEXT = (
    "⛔ Доступ к Личному Еженедельнику ограничен.\n\n"
    "Этот бот обслуживает приватный проект. Если вы получили его ссылку по ошибке — "
    "просто игнорируйте сообщение."
)

START_SUCCESS_TEXT = (
    "✅ Бот готов к работе!\n\n"
    "Теперь вернитесь в приложение Личный Еженедельник и введите ваш Telegram username — "
    "я пришлю вам 6-значный код подтверждения.\n\n"
    "Коды действуют 5 минут и одноразовые."
)


@router.message(CommandStart())
async def handle_start(message: Message) -> None:
    """
    /start: привязать chat_id к user.
    
    Идемпотентно — повторный /start просто обновляет chat_id (на случай
    смены Telegram-клиента или restore).
    """
    tg_user = message.from_user
    if tg_user is None:
        logger.warning("Got /start without from_user — ignoring")
        return
    
    tg_username = (tg_user.username or "").lower()
    if not tg_username:
        await message.answer(
            "У вашего Telegram-аккаунта не установлен username. "
            "Зайдите в настройки Telegram → Username → задайте его, и запустите /start снова."
        )
        return
    
    settings = get_settings()
    if tg_username not in settings.allowed_usernames:
        logger.info("Unknown username /start attempt: %s", tg_username)
        await message.answer(ACCESS_DENIED_TEXT)
        return
    
    chat_id = message.chat.id
    
    async with AsyncSessionLocal() as session:
        # Найти или создать user
        stmt = select(User).where(User.telegram_username == tg_username)
        result = await session.execute(stmt)
        user = result.scalar_one_or_none()
        
        if user is None:
            user = User(telegram_username=tg_username, telegram_chat_id=chat_id)
            session.add(user)
            logger.info("Created new user: %s with chat_id=%d", tg_username, chat_id)
        else:
            user.telegram_chat_id = chat_id
            logger.info("Updated chat_id for %s: %d", tg_username, chat_id)
        
        await session.commit()
    
    await message.answer(START_SUCCESS_TEXT)
```

**Создать `server/bot/main.py`:**

```python
"""
Запуск Telegram-бота как отдельного процесса.

Запуск локально:     python -m server.bot.main
Запуск на VPS:       через systemd unit planner-bot.service (Plan 10)

Почему отдельный процесс (не часть FastAPI uvicorn):
- aiogram long-polling блокирует event loop
- Простая изоляция: если FastAPI падает, бот остаётся; и наоборот
- Отдельные systemd units → независимый Restart=always
См. RESEARCH.md Pattern 4.
"""
from __future__ import annotations

import asyncio
import logging
import sys

from aiogram import Bot, Dispatcher
from aiogram.client.default import DefaultBotProperties
from aiogram.enums import ParseMode

from server.config import get_settings


def create_dispatcher() -> Dispatcher:
    """Создать Dispatcher с подключёнными handlers — для тестов и main."""
    dp = Dispatcher()
    from server.bot.handlers import router as start_router
    dp.include_router(start_router)
    return dp


def create_bot() -> Bot:
    settings = get_settings()
    return Bot(
        token=settings.bot_token,
        default=DefaultBotProperties(parse_mode=ParseMode.HTML),
    )


async def main() -> None:
    """Long-polling entry point. Вызывается как `python -m server.bot.main`."""
    logging.basicConfig(
        level=get_settings().log_level,
        stream=sys.stdout,
        format="%(asctime)s %(levelname)s %(name)s: %(message)s",
    )
    logger = logging.getLogger("server.bot")
    logger.info("Starting Telegram bot (Фаза 1: /start handler only)")
    
    bot = create_bot()
    dp = create_dispatcher()
    
    try:
        # drop_pending_updates=True — игнорируем старые сообщения при рестарте
        # (иначе бот может обрабатывать /start'ы, которые пользователь отправил неделю назад)
        await dp.start_polling(bot, drop_pending_updates=True)
    finally:
        await bot.session.close()


if __name__ == "__main__":
    asyncio.run(main())
```
  </action>
  <verify>
    <automated>cd server && python -c "from server.bot.main import create_dispatcher, create_bot; from server.bot.handlers import router, handle_start, ACCESS_DENIED_TEXT, START_SUCCESS_TEXT; import os; os.environ.update({'DATABASE_URL':'sqlite+aiosqlite:///:memory:','JWT_SECRET':'a'*32,'JWT_REFRESH_SECRET':'b'*32,'BOT_TOKEN':'1234567890:abcdefghij','ALLOWED_USERNAMES':'test'}); from server.config import get_settings; get_settings.cache_clear(); dp = create_dispatcher(); assert dp is not None; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `server/bot/handlers.py` существует, содержит `router = Router(...)`, `@router.message(CommandStart())`, `async def handle_start`
    - `grep -q "ALLOWED_USERNAMES\|allowed_usernames" server/bot/handlers.py` успешен (allow-list check)
    - `grep -q "telegram_chat_id" server/bot/handlers.py` успешен (запись chat_id)
    - `server/bot/main.py` существует, содержит `create_dispatcher`, `async def main`, `__name__ == "__main__"`
    - `grep -q "from aiogram import Bot, Dispatcher" server/bot/main.py` успешен
    - `grep -q "start_polling" server/bot/main.py` успешен
    - `grep -q "drop_pending_updates" server/bot/main.py` успешен (игнор старых updates)
    - Python проверка выше: create_dispatcher не падает, handlers зарегистрированы
  </acceptance_criteria>
  <done>aiogram бот готов: handler регистрирует chat_id, main() запускает polling. Может быть запущен командой `python -m server.bot.main`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit тесты handle_start без реального Telegram</name>
  <files>server/tests/test_bot_handlers.py</files>
  <read_first>
    - server/bot/handlers.py (из Task 1)
    - aiogram docs — aiogram.types.Message, User, Chat (mockable dataclasses)
  </read_first>
  <behavior>
    - Test 1: Allowed user с username + /start → users.telegram_chat_id обновлён (или создан), ответ START_SUCCESS_TEXT
    - Test 2: Allowed user без username → ответ с инструкцией "задайте username"
    - Test 3: Not-allowed user → ответ ACCESS_DENIED_TEXT, НЕТ записи в users
    - Test 4: Повторный /start для existing user → chat_id обновлён (идемпотентно)
  </behavior>
  <action>
Создать `server/tests/test_bot_handlers.py` — тестируем handler без реального aiogram polling, создавая `Message` вручную:

```python
"""
Unit тесты handle_start — создаём aiogram Message вручную через pydantic-style,
не гоняем long-polling.

Алгоритм: aiogram.types.Message — это pydantic BaseModel, можно создать прямо:
    msg = Message.model_validate({...})
"""
from __future__ import annotations

from unittest.mock import AsyncMock

import pytest
import pytest_asyncio
from sqlalchemy import select
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

from server.db.base import Base
from server.db import models


@pytest.fixture(autouse=True)
def _setup_env(monkeypatch, tmp_path):
    monkeypatch.setenv("DATABASE_URL", f"sqlite+aiosqlite:///{tmp_path / 'bot.db'}")
    monkeypatch.setenv("JWT_SECRET", "a" * 32)
    monkeypatch.setenv("JWT_REFRESH_SECRET", "b" * 32)
    monkeypatch.setenv("BOT_TOKEN", "1234567890:abcdefghij")
    monkeypatch.setenv("ALLOWED_USERNAMES", "nikita_heyyda")
    from server.config import get_settings
    get_settings.cache_clear()


@pytest_asyncio.fixture
async def db_ready(monkeypatch):
    """Инициализировать тестовую БД и замонкипатчить AsyncSessionLocal в handlers."""
    from server.config import get_settings
    settings = get_settings()
    engine = create_async_engine(settings.database_url, echo=False)
    from server.db.engine import _attach_pragma_listener
    _attach_pragma_listener(engine)
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
    factory = async_sessionmaker(engine, expire_on_commit=False)
    
    # Подменить AsyncSessionLocal в server.bot.handlers
    monkeypatch.setattr("server.bot.handlers.AsyncSessionLocal", factory)
    
    yield factory
    await engine.dispose()


def _make_message(*, chat_id: int, tg_username: str | None, user_id: int = 111):
    """Создать aiogram Message через pydantic.model_validate (без реального API call)."""
    from aiogram.types import Message
    data = {
        "message_id": 1,
        "date": 1234567890,
        "chat": {"id": chat_id, "type": "private"},
        "from": {"id": user_id, "is_bot": False, "first_name": "Test",
                 "username": tg_username} if tg_username is not None else
                {"id": user_id, "is_bot": False, "first_name": "Test"},
        "text": "/start",
    }
    msg = Message.model_validate(data)
    # Подмена метода .answer на AsyncMock — handler вызывает message.answer(text)
    msg.answer = AsyncMock()
    return msg


@pytest.mark.asyncio
async def test_start_allowed_user_registers_chat_id(db_ready):
    from server.bot.handlers import handle_start, START_SUCCESS_TEXT
    factory = db_ready
    
    msg = _make_message(chat_id=12345, tg_username="nikita_heyyda")
    await handle_start(msg)
    
    msg.answer.assert_called_once_with(START_SUCCESS_TEXT)
    
    # Проверка БД: user создан с chat_id
    async with factory() as session:
        result = await session.execute(select(models.User).where(models.User.telegram_username == "nikita_heyyda"))
        user = result.scalar_one()
        assert user.telegram_chat_id == 12345


@pytest.mark.asyncio
async def test_start_allowed_user_case_insensitive(db_ready):
    """Telegram username может приходить в любом регистре — сравнение lowercase."""
    from server.bot.handlers import handle_start, START_SUCCESS_TEXT
    msg = _make_message(chat_id=12345, tg_username="Nikita_Heyyda")
    await handle_start(msg)
    msg.answer.assert_called_once_with(START_SUCCESS_TEXT)


@pytest.mark.asyncio
async def test_start_no_username(db_ready):
    from server.bot.handlers import handle_start
    msg = _make_message(chat_id=12345, tg_username=None)
    await handle_start(msg)
    
    # Ответ содержит инструкцию про username
    msg.answer.assert_called_once()
    args, _ = msg.answer.call_args
    assert "username" in args[0].lower()


@pytest.mark.asyncio
async def test_start_not_allowed_user(db_ready):
    from server.bot.handlers import handle_start, ACCESS_DENIED_TEXT
    factory = db_ready
    
    msg = _make_message(chat_id=99999, tg_username="stranger")
    await handle_start(msg)
    
    msg.answer.assert_called_once_with(ACCESS_DENIED_TEXT)
    
    # Проверка: в users ничего не записано
    async with factory() as session:
        result = await session.execute(select(models.User))
        users = result.scalars().all()
        assert all(u.telegram_username != "stranger" for u in users)


@pytest.mark.asyncio
async def test_start_idempotent_updates_chat_id(db_ready):
    """Повторный /start обновляет chat_id (user перешёл на другое устройство)."""
    from server.bot.handlers import handle_start
    factory = db_ready
    
    # Первый /start — chat_id=12345
    msg1 = _make_message(chat_id=12345, tg_username="nikita_heyyda")
    await handle_start(msg1)
    
    # Второй /start — chat_id=67890
    msg2 = _make_message(chat_id=67890, tg_username="nikita_heyyda")
    await handle_start(msg2)
    
    # В БД один user с последним chat_id
    async with factory() as session:
        result = await session.execute(select(models.User).where(models.User.telegram_username == "nikita_heyyda"))
        users = result.scalars().all()
        assert len(users) == 1
        assert users[0].telegram_chat_id == 67890
```
  </action>
  <verify>
    <automated>cd server && python -m pytest tests/test_bot_handlers.py -x -v 2>&1 | tee /tmp/bot.out && grep -q "5 passed" /tmp/bot.out</automated>
  </verify>
  <acceptance_criteria>
    - `server/tests/test_bot_handlers.py` существует — 5 тестов
    - `cd server && pytest tests/test_bot_handlers.py -v` — 5 passed
    - Тест `test_start_not_allowed_user` доказывает: stranger НЕ попадает в users table
    - Тест `test_start_idempotent_updates_chat_id` доказывает: повторный /start обновляет chat_id
    - Тест `test_start_allowed_user_case_insensitive` доказывает нормализацию username (lowercase)
    - Ни один тест не делает реальный HTTP вызов в Telegram API
  </acceptance_criteria>
  <done>Все 5 handler тестов зелёные. Handle_start работает с реальной SQLite. Access-control через ALLOWED_USERNAMES действует.</done>
</task>

</tasks>

<verification>
- `cd server && pytest tests/test_bot_handlers.py -v` — 5 passed
- `cd server && python -c "from server.bot.main import create_dispatcher, create_bot; import os; os.environ.update({'DATABASE_URL':'sqlite+aiosqlite:///:memory:','JWT_SECRET':'a'*32,'JWT_REFRESH_SECRET':'b'*32,'BOT_TOKEN':'1234567890:abcdefghij','ALLOWED_USERNAMES':'test'}); from server.config import get_settings; get_settings.cache_clear(); dp = create_dispatcher(); print(type(dp).__name__)"` — Dispatcher
</verification>

<success_criteria>
Plan завершён когда:
- aiogram 3.x /start handler записывает chat_id в БД для allowed users
- Allow-list enforced в боте (тот же ALLOWED_USERNAMES что и в API)
- Handler идемпотентен (повторный /start обновляет chat_id)
- Отдельный entry point `python -m server.bot.main`
- 5 unit тестов зелёные (без реального Telegram API)
- Plan 10 (deploy) создаст systemd unit planner-bot.service для запуска main()
- Фаза 5 расширит handlers.py (/add, /week, /today) без замены main.py
</success_criteria>

<output>
Создать `.planning/phases/01-server-auth/01-09-SUMMARY.md` с:
- Полный flow: user /start → chat_id в БД → auth-endpoint /request-code может отправить код
- Схема запуска (systemd command, log format)
- Объяснение что Фаза 5 просто добавит handlers в тот же router
</output>
