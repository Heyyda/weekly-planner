---
phase: 04-week-tasks
plan: 11
type: execute
wave: 5
depends_on: [10]
files_modified:
  - client/tests/ui/test_e2e_phase4.py
  - .planning/phases/04-week-tasks/04-VALIDATION.md
autonomous: false
requirements:
  - WEEK-01
  - WEEK-02
  - WEEK-03
  - WEEK-04
  - WEEK-05
  - WEEK-06
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - TASK-05
  - TASK-06
  - TASK-07
must_haves:
  truths:
    - "E2E тесты покрывают 10+ сценариев: add→toggle→edit→delete→undo→move→archive→keyboard"
    - "Full test suite (client/tests/) зелёный без регрессий Phase 1/2/3"
    - "04-VALIDATION.md финальный checklist: каждый test mapped to REQ-ID"
    - "Human-verify checkpoint: Никита запускает приложение на Windows и подтверждает 13 REQ-IDs работают"
  artifacts:
    - path: "client/tests/ui/test_e2e_phase4.py"
      provides: "E2E integration test covering full Phase 4 flow (quick_capture → add → edit → delete → undo → move)"
      contains: "def test_e2e_add_toggle_edit_delete_undo"
      min_lines: 200
    - path: ".planning/phases/04-week-tasks/04-VALIDATION.md"
      provides: "Final sign-off checklist, human-verify checkpoint completed"
      contains: "approved_by_owner"
  key_links:
    - from: "client/tests/ui/test_e2e_phase4.py"
      to: "All Phase 4 plans (02-10)"
      via: "integration scenarios exercising each component"
      pattern: "Phase 4"
---

<objective>
Финальная валидация Phase 4:
  1. E2E integration test suite — 10+ сценариев покрывающих все 13 REQ-IDs
  2. Full regression: Phase 1/2/3 suite остаётся зелёным
  3. Human-verify checkpoint: Никита запускает клиент на Windows и подтверждает каждое
     Success Criterion из 04-UI-SPEC §Success Criteria

Purpose: Последний gate Phase 4 — подтверждение что всё работает end-to-end. Unit тесты
(plans 04-01..09) проверяли изолированные компоненты; Plan 04-10 проверял integration через
моки. Это — реальное выполнение на Windows.

**Critical:** Human-verify — unavoidable manual step (не автоматизируется). Требует Никиту
запустить `python main.py`, взаимодействовать с UI, подтвердить что каждое поведение
соответствует 04-UI-SPEC.

После завершения:
  - Phase 4 ROADMAP.md → `[x]` Phase 4
  - STATE.md → current_phase = 5 (Telegram bot)
  - 04-VALIDATION.md → `approved_by_owner: YYYY-MM-DD`
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-week-tasks/04-UI-SPEC.md
@.planning/phases/04-week-tasks/04-VALIDATION.md
@.planning/phases/04-week-tasks/04-CONTEXT.md
@client/app.py
@client/ui/main_window.py
@client/ui/quick_capture.py
@client/ui/edit_dialog.py
@client/ui/undo_toast.py
@client/ui/drag_controller.py
@client/ui/week_navigation.py
@client/ui/day_section.py
@client/ui/task_widget.py
@client/ui/parse_input.py
@client/tests/ui/test_app_integration.py

<interfaces>
<!-- All Phase 4 integrated components (см. Plan 04-10) -->
MainWindow + handle_quick_capture_save + _delete_task_with_undo + _on_task_moved
WeeklyPlannerApp._on_overlay_right_click + _handle_quick_capture_save + _trigger_quick_capture_centered
QuickCapturePopup + EditDialog + UndoToastManager + DragController

<!-- Success Criteria (from 04-UI-SPEC §Success Criteria) -->
9 items для human-verify:
  1. Right-click overlay → quick-capture input; Enter "позвонить завтра 14:00" → task на завтра
  2. 3 task-style (card/line/minimal) переключатель в tray — мгновенно без рестарта
  3. Hover на задачу → ✏+🗑 icons; click ✏ → edit dialog с 4 полями + 3 кнопками
  4. Click 🗑 или "Удалить" → task fade-out + undo-toast 5 сек; "Отменить" → задача возвращается
  5. Drag задачи → ghost следует за курсором; day-sections highlighted; drop → day меняется
  6. Next-week drop zone при drag; drop в неё → task.day = следующая неделя
  7. Прошлая неделя: opacity 0.7, banner "Архив • Вернуться", DnD/edit заблокированы
  8. Ctrl+Left/Right/T навигация
  9. Пустой день: "+" по центру, click → inline input
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать client/tests/ui/test_e2e_phase4.py — 10+ integration сценариев покрывающих 13 REQ-IDs</name>
  <files>client/tests/ui/test_e2e_phase4.py</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-UI-SPEC.md §Success Criteria (9 items — базис для E2E)
    - .planning/phases/04-week-tasks/04-CONTEXT.md §Decisions (checklist coverage D-01..D-37)
    - client/ui/main_window.py (после Plan 04-10 — полный integration)
    - client/app.py (после Plan 04-10 — quick_capture wiring)
    - client/tests/ui/test_main_window.py (extension примеры Phase 4)
    - client/tests/conftest.py (mock_storage, timestamped_task_factory, mock_theme_manager)
  </read_first>

  <action>
    Создать `client/tests/ui/test_e2e_phase4.py`:

    ```python
    """
    E2E integration тесты Phase 4.

    Покрывает все 13 REQ-IDs интеграционными сценариями (не unit).
    Использует реальный MainWindow + все Phase 4 компоненты + mock_storage.

    Каждый сценарий — несколько шагов в правильном порядке, имитирует пользовательский flow.
    """
    from datetime import date, timedelta
    from unittest.mock import MagicMock

    import pytest

    from client.core.models import Task
    from client.ui.main_window import MainWindow
    from client.ui.settings import SettingsStore, UISettings


    @pytest.fixture
    def e2e_app(headless_tk, mock_theme_manager, mock_storage, tmp_appdata):
        settings = UISettings()
        settings_store = SettingsStore(mock_storage)
        mw = MainWindow(
            headless_tk, settings_store, settings, mock_theme_manager,
            storage=mock_storage, user_id="e2e-user",
        )
        headless_tk.update_idletasks()
        yield {
            "root": headless_tk,
            "mw": mw,
            "storage": mock_storage,
            "theme": mock_theme_manager,
        }
        try:
            mw.destroy()
        except Exception:
            pass


    # ---------- E2E: Add → Toggle → Edit → Delete → Undo ----------

    def test_e2e_add_via_quick_capture_save(e2e_app):
        """WEEK-01, TASK-01: Quick-capture "встреча завтра 14:00" → Task в завтрашней DaySection."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        # Симулируем QuickCapturePopup.on_save callback
        tomorrow = (date.today() + timedelta(days=1)).isoformat()
        mw.handle_quick_capture_save("встреча", tomorrow, "14:00")

        # Verify task создан
        tasks = storage.get_visible_tasks()
        assert len(tasks) == 1
        assert tasks[0].text == "встреча"
        assert tasks[0].day == tomorrow
        assert tasks[0].time_deadline == "14:00"

        # Verify task отрендерен в завтрашнем DaySection
        mw._refresh_tasks()
        from datetime import date as _d
        tomorrow_date = _d.fromisoformat(tomorrow)
        ds = mw._day_sections.get(tomorrow_date)
        assert ds is not None
        assert len(ds._task_widgets) == 1


    def test_e2e_toggle_done_updates_storage(e2e_app):
        """TASK-02: checkbox click → update_task(done=True) → storage persisted."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        today_iso = date.today().isoformat()
        mw.handle_quick_capture_save("task", today_iso, None)
        tasks = storage.get_visible_tasks()
        task_id = tasks[0].id

        mw._on_task_toggle(task_id, True)

        updated = storage.get_task(task_id)
        assert updated.done is True


    def test_e2e_edit_dialog_saves_changes(e2e_app):
        """TASK-03: _on_edit_save → update_task с новыми text/day/time."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        mw.handle_quick_capture_save("original", date.today().isoformat(), None)
        task_id = storage.get_visible_tasks()[0].id

        # Симулируем EditDialog.on_save (обычно вызывается из dialog UI)
        updated_task = storage.get_task(task_id)
        updated_task.text = "edited"
        updated_task.time_deadline = "10:30"
        mw._on_edit_save(updated_task)

        final = storage.get_task(task_id)
        assert final.text == "edited"
        assert final.time_deadline == "10:30"


    def test_e2e_delete_shows_undo_toast(e2e_app):
        """TASK-04: _delete_task_with_undo → soft_delete + undo_toast."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        mw.handle_quick_capture_save("to delete", date.today().isoformat(), None)
        task_id = storage.get_visible_tasks()[0].id

        mw._delete_task_with_undo(task_id)

        # Task soft-deleted (deleted_at set)
        visible = storage.get_visible_tasks()
        assert len(visible) == 0  # не в visible
        # Undo toast появился
        assert len(mw._undo_toast._queue) == 1


    def test_e2e_undo_restores_task(e2e_app):
        """TASK-04: click Отменить → deleted_at = None → task visible again."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        mw.handle_quick_capture_save("to restore", date.today().isoformat(), None)
        task_id = storage.get_visible_tasks()[0].id

        mw._delete_task_with_undo(task_id)
        assert len(storage.get_visible_tasks()) == 0

        # Симулируем клик "Отменить" — вызываем undo callback напрямую
        mw._undo_toast._undo(task_id)

        # Task восстановлен
        assert len(storage.get_visible_tasks()) == 1


    # ---------- E2E: Drag-and-Drop (TASK-05, TASK-06) ----------

    def test_e2e_move_task_between_days(e2e_app):
        """TASK-05: _on_task_moved → storage.update_task(day=new_day)."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        today = date.today()
        tomorrow = today + timedelta(days=1)
        mw.handle_quick_capture_save("moving", today.isoformat(), None)
        task_id = storage.get_visible_tasks()[0].id

        # Симулируем DnD commit
        mw._on_task_moved(task_id, tomorrow)

        updated = storage.get_task(task_id)
        assert updated.day == tomorrow.isoformat()


    def test_e2e_move_to_next_week(e2e_app):
        """TASK-06: drop на next-week zone → task.day = next_week_monday."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        today = date.today()
        next_monday = today - timedelta(days=today.weekday()) + timedelta(days=7)
        mw.handle_quick_capture_save("next week task", today.isoformat(), None)
        task_id = storage.get_visible_tasks()[0].id

        mw._on_task_moved(task_id, next_monday)

        updated = storage.get_task(task_id)
        assert updated.day == next_monday.isoformat()


    # ---------- E2E: Week navigation ----------

    def test_e2e_navigate_prev_week(e2e_app):
        """WEEK-02: prev_week → _week_monday shifts -7; day_sections перестроены."""
        mw = e2e_app["mw"]
        initial_days = set(mw._day_sections.keys())
        mw._week_nav.prev_week()
        new_days = set(mw._day_sections.keys())
        assert initial_days != new_days


    def test_e2e_today_button_returns(e2e_app):
        """WEEK-03: today() после prev возвращает к current week."""
        mw = e2e_app["mw"]
        from client.ui.week_navigation import get_current_week_monday
        mw._week_nav.prev_week()
        mw._week_nav.prev_week()
        mw._week_nav.today()
        assert mw._week_nav.get_week_monday() == get_current_week_monday()


    def test_e2e_archive_mode_activates_on_past(e2e_app):
        """WEEK-06: prev_week → on_archive_changed(True) → DaySections.set_archive_mode."""
        mw = e2e_app["mw"]
        # Добавим задачу для today чтобы было видно в секции
        mw.handle_quick_capture_save("today task", date.today().isoformat(), None)
        mw._week_nav.prev_week()
        # Все DaySections должны быть в archive mode
        for ds in mw._day_sections.values():
            assert ds._is_archive is True


    def test_e2e_archive_mode_deactivates_on_today(e2e_app):
        """WEEK-06: today() → on_archive_changed(False) → editing re-enabled."""
        mw = e2e_app["mw"]
        mw._week_nav.prev_week()
        mw._week_nav.today()
        for ds in mw._day_sections.values():
            assert ds._is_archive is False


    # ---------- E2E: Position sorting (TASK-07) ----------

    def test_e2e_position_sort_preserved(e2e_app):
        """TASK-07: tasks рендерятся в порядке position ASC."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        today_iso = date.today().isoformat()
        t1 = Task.new(user_id="u", text="second", day=today_iso, position=2)
        t2 = Task.new(user_id="u", text="first", day=today_iso, position=0)
        t3 = Task.new(user_id="u", text="middle", day=today_iso, position=1)
        storage.add_task(t1)
        storage.add_task(t2)
        storage.add_task(t3)

        mw._refresh_tasks()
        today = date.today()
        ds = mw._day_sections.get(today)
        # Internal _tasks sorted by position
        positions = [t.position for t in ds._tasks]
        assert positions == [0, 1, 2]


    # ---------- E2E: Multiple-days rendering (WEEK-01) ----------

    def test_e2e_tasks_distributed_to_correct_days(e2e_app):
        """WEEK-01: задачи рендерятся в свои DaySections (по task.day)."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        today = date.today()
        monday = today - timedelta(days=today.weekday())
        # Создаём задачи на разные дни недели
        for i in range(3):
            d = monday + timedelta(days=i)
            mw.handle_quick_capture_save(f"task-{i}", d.isoformat(), None)
        mw._refresh_tasks()

        for i in range(3):
            d = monday + timedelta(days=i)
            ds = mw._day_sections.get(d)
            assert ds is not None
            assert len(ds._task_widgets) == 1


    # ---------- E2E: Overdue detection (WEEK-04) ----------

    def test_e2e_overdue_task_rendered(e2e_app, timestamped_task_factory):
        """WEEK-04: overdue task — is_overdue=True, checkbox border=accent_overdue."""
        mw = e2e_app["mw"]
        storage = e2e_app["storage"]

        # Создаём задачу вчера
        overdue_task = timestamped_task_factory(text="overdue", day_offset=-1)
        storage.add_task(overdue_task)
        # Нужно navigation на прошлую неделю чтобы видеть её
        mw._week_nav.prev_week()
        mw._refresh_tasks()

        assert overdue_task.is_overdue() is True


    # ---------- E2E: Empty day → "+" (D-33) ----------

    def test_e2e_empty_day_shows_plus(e2e_app):
        """D-33 (WEEK-01): day без задач → "+" placeholder visible."""
        mw = e2e_app["mw"]
        today = date.today()
        ds = mw._day_sections[today]
        assert ds._plus_label is not None
        mw._root.update_idletasks() if False else e2e_app["root"].update_idletasks()


    # ---------- E2E: Task style switch (WEEK-05) ----------

    def test_e2e_task_style_switch_rebuilds(e2e_app):
        """WEEK-05: handle_task_style_changed → rebuild day_sections с новым style."""
        mw = e2e_app["mw"]
        mw.handle_quick_capture_save("task", date.today().isoformat(), None)
        mw._refresh_tasks()
        # style change
        mw.handle_task_style_changed("line")
        # Verify: hasattr rebuild — _day_sections пересозданы
        assert mw._day_sections  # не пустой
        # Verify task всё ещё есть
        today = date.today()
        ds = mw._day_sections[today]
        assert len(ds._task_widgets) >= 1
    ```
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_e2e_phase4.py -x -q --timeout=90</automated>
  </verify>

  <acceptance_criteria>
    - File client/tests/ui/test_e2e_phase4.py exists
    - grep -c "^def test_e2e_" client/tests/ui/test_e2e_phase4.py >= 10
    - pytest client/tests/ui/test_e2e_phase4.py — все тесты зелёные
    - File size >= 200 lines
    - Покрытие REQ-IDs (комментарии в тестах):
      - WEEK-01 (add renders), WEEK-02 (prev_week), WEEK-03 (today_btn),
        WEEK-04 (overdue), WEEK-05 (style switch), WEEK-06 (archive mode)
      - TASK-01 (quick_capture save), TASK-02 (toggle), TASK-03 (edit save),
        TASK-04 (delete + undo), TASK-05 (move), TASK-06 (next week),
        TASK-07 (position sort)
  </acceptance_criteria>

  <done>
    E2E test suite готов. Все 13 REQ-IDs покрыты integration тестами через реальный
    MainWindow + все Phase 4 компоненты. Full pytest suite остаётся зелёным.
  </done>
</task>

<task type="auto">
  <name>Task 2: Запустить full regression test suite + обновить 04-VALIDATION.md со статусом</name>
  <files>.planning/phases/04-week-tasks/04-VALIDATION.md</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-VALIDATION.md (current state from Plan 04-01)
    - client/tests/ (all test files must pass)
  </read_first>

  <action>
    ШАГ 1 — Запустить full regression:
    `python -m pytest client/tests/ -v --timeout=120`

    Все тесты должны быть зелёными:
      - Phase 1 server тесты (если есть)
      - Phase 2 sync, storage, auth тесты
      - Phase 3 overlay, themes, settings, main_window, tray, notifications, autostart тесты
      - Phase 4 новые тесты (quick_parse, task_widget, day_section, week_navigation,
        quick_capture, edit_dialog, undo_toast, drag_controller, main_window extended,
        app_integration extended, e2e_phase4)

    Если есть падения — исправить перед продолжением (НЕ пропускать).

    ШАГ 2 — Обновить `.planning/phases/04-week-tasks/04-VALIDATION.md`. В конец файла
    ДОБАВИТЬ:

    ```markdown

    ---

    ## Execution Report (Plan 04-11 final)

    **Test suite:** `python -m pytest client/tests/ -v --timeout=120`
    **Result:** ALL GREEN (Phase 1/2/3 regression + Phase 4 new tests)

    ### Coverage Verification

    | REQ-ID | Test(s) | Plan | Status |
    |--------|---------|------|--------|
    | WEEK-01 | test_e2e_tasks_distributed_to_correct_days, test_day_section_renders_tasks | 04-04, 04-11 | ✅ |
    | WEEK-02 | test_nav_arrows_change_week, test_e2e_navigate_prev_week | 04-05, 04-11 | ✅ |
    | WEEK-03 | test_today_btn_visibility, test_e2e_today_button_returns | 04-05, 04-11 | ✅ |
    | WEEK-04 | test_overdue_checkbox_color, test_e2e_overdue_task_rendered | 04-03, 04-11 | ✅ |
    | WEEK-05 | test_three_styles, test_e2e_task_style_switch_rebuilds | 04-03, 04-11 | ✅ |
    | WEEK-06 | test_archive_detection, test_e2e_archive_mode_activates_on_past | 04-05, 04-11 | ✅ |
    | TASK-01 | test_parse_*, test_enter_saves, test_e2e_add_via_quick_capture_save | 04-02, 04-06, 04-11 | ✅ |
    | TASK-02 | test_toggle_done, test_e2e_toggle_done_updates_storage | 04-03, 04-11 | ✅ |
    | TASK-03 | test_dialog_modal, test_e2e_edit_dialog_saves_changes | 04-07, 04-11 | ✅ |
    | TASK-04 | test_undo_reverses, test_e2e_delete_shows_undo_toast, test_e2e_undo_restores_task | 04-08, 04-11 | ✅ |
    | TASK-05 | test_find_drop_zone_hit, test_e2e_move_task_between_days | 04-09, 04-11 | ✅ |
    | TASK-06 | test_next_week_zone_shown_on_drag_start, test_e2e_move_to_next_week | 04-09, 04-11 | ✅ |
    | TASK-07 | test_position_sort, test_e2e_position_sort_preserved | 04-04, 04-11 | ✅ |

    **Coverage: 13/13 ✓**

    ---

    ## Human-Verify Checkpoint

    Переходим к Task 3 (checkpoint:human-verify). После одобрения владельцем — обновить этот
    документ:

    ```yaml
    approved_by_owner: <YYYY-MM-DD>
    approval_notes: <any caveats or issues discovered during manual testing>
    ```
    ```

    ШАГ 3 — Запустить final regression чтобы zero failures: `python -m pytest client/tests/ -v --timeout=120 > /tmp/phase4-test-report.txt 2>&1; tail -20 /tmp/phase4-test-report.txt`. Проверить last 20 строк — должно быть "X passed".
  </action>

  <verify>
    <automated>python -m pytest client/tests/ -q --timeout=120</automated>
  </verify>

  <acceptance_criteria>
    - pytest client/tests/ — 0 failures, 0 errors
    - grep "Coverage: 13/13" .planning/phases/04-week-tasks/04-VALIDATION.md >= 1
    - grep "## Execution Report" .planning/phases/04-week-tasks/04-VALIDATION.md >= 1
    - Все 13 REQ-IDs имеют ✅ в coverage table
  </acceptance_criteria>

  <done>
    Full regression зелёный. Coverage 13/13. Task 3 (human-verify) осталось — не
    автоматизируется.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify checkpoint — Никита запускает приложение на Windows и подтверждает 9 Success Criteria</name>
  <files>— (manual verification — NO files changed by this task, owner tests running application)</files>
  <what-built>
    Phase 4 полностью: недельный вид с 7 секциями дней, quick-capture с smart parse, edit dialog,
    undo-toast, drag-and-drop между днями и на next week, archive dim, keyboard shortcuts.

    Components:
      - WeekNavigation (header + prev/next/today + keyboard)
      - DaySection × 7 (header + tasks + empty "+" + inline add)
      - TaskWidget (3 styles + overdue color + hover icons + theme-aware)
      - QuickCapturePopup (overlay right-click → popup → smart parse → save)
      - EditDialog (modal + 4 fields + validation + keyboard)
      - UndoToastManager (5sec countdown + stack до 3 toasts)
      - DragController (ghost toplevel + bbox hit-test + archive block)

    Integration через MainWindow: storage → CRUD; overlay.on_right_click → quick_capture wire.
  </what-built>

  <how-to-verify>
    Запустить приложение на Windows: `cd S:\Проекты\ежедневник && python main.py`

    Пройти все 9 Success Criteria из 04-UI-SPEC §Success Criteria:

    1. **Quick-capture flow** (TASK-01):
       - Right-click на overlay → popup 400×40 появляется под overlay (gap 8px)
       - Ввести "позвонить завтра 14:00" → Enter
       - Открыть main window (click на overlay или tray "Показать")
       - Перейти на завтрашнюю секцию → задача "позвонить" с временем 14:00 присутствует

    2. **3 task-style** (WEEK-05):
       - Tray → правый клик → "Стиль задач" → выбрать "Cards" / "Lines" / "Minimal"
       - Проверить что tasks перерисовываются МГНОВЕННО (без рестарта app)

    3. **Edit flow** (TASK-03):
       - Hover на задаче → ✏ + 🗑 icons появляются справа
       - Click ✏ → EditDialog открывается, 4 поля (text/day/time/done) + 3 кнопки (Delete/Cancel/Save)
       - Изменить text → Сохранить → задача обновилась

    4. **Delete + Undo** (TASK-04):
       - Click 🗑 на задаче → task fade-out + toast "Удалено • Отменить" внизу окна
       - Click "Отменить" в пределах 5 сек → задача возвращается на место
       - Повторить: удалить → НЕ нажимать "Отменить" → через 5 сек toast исчезает, задача окончательно удалена

    5. **DnD between days** (TASK-05):
       - Зажать мышью на теле задачи + двинуть >5px → ghost появляется, следует за курсором
       - Hover над другим днём → day section highlighted (blue tint)
       - Release → задача переместилась на выбранный день
       - Проверить: release вне dropzones → задача возвращается (cancelled)

    6. **Next-week DnD** (TASK-06):
       - Начать drag задачу → внизу появляется секция "📅 Следующая неделя"
       - Drop задачу в неё → task.day изменяется на понедельник следующей недели (или выбранный день)

    7. **Archive mode** (WEEK-06):
       - ◀ prev-week (несколько раз) → opacity на tasks ~0.7 ("серее"), banner "📦 Архив"
       - Click 🗑 / hover ✏ — заблокированы (editing disabled)
       - Drag — DnD заблокирован
       - Click "Вернуться →" в banner → возврат к current week, full functionality

    8. **Keyboard shortcuts** (WEEK-02/WEEK-03/D-30):
       - Ctrl+Left → prev week
       - Ctrl+Right → next week
       - Ctrl+T → Сегодня (возврат к current)
       - Ctrl+Space → quick-capture popup в центре окна
       - Esc на открытом quick-capture → закрывается
       - Esc на edit dialog → отменяет
       - Ctrl+Enter на edit dialog → сохраняет

    9. **Empty day flow** (D-33):
       - В день без задач виден "+" icon 24×24 по центру секции
       - Hover на "+" → цвет меняется на accent_brand
       - Click "+" → inline input появляется
       - Ввести текст + Enter → задача создаётся в этот день; input остаётся для multi-add
       - Esc → input закрывается, "+" возвращается (если день пустой)

    **Дополнительные edge-case проверки:**
    - Smart parse: "встреча пт" → на ближайшую пятницу; "в 14:00 позвонить" → time=14:00 без "в"
    - Multi-undo: удалить 3 задачи подряд → 3 toasts stacked (max)
    - Multi-monitor: overlay на 2-м мониторе → quick-capture popup на том же мониторе
    - Длинный текст: задача с 200+ символов — wraps без горизонтального скролла
    - Cyrillic: все тексты UI на русском (заголовки, button labels, placeholder)
  </how-to-verify>

  <resume-signal>
    Одобрить Phase 4:
      - "approved" — всё работает как ожидалось, можно переходить к Phase 5
      - "approved with notes" + описать minor issues — одобрено, но записать в STATE.md
      - "issues: <описание>" — вернуть в revise mode, Plan 04-10 + additional plans

    После approval:
      - Обновить 04-VALIDATION.md: добавить `approved_by_owner: YYYY-MM-DD`
      - Обновить ROADMAP.md: Phase 4 → `[x]`
      - Обновить STATE.md: current_phase = 5, status = "Phase 4 complete; Phase 5 ready"
      - Git commit: `docs(04-week-tasks): Phase 4 approved by owner — all 13 REQ-IDs verified`
  </resume-signal>

  <action>
    See <what-built> and <how-to-verify> blocks above — owner runs application manually and
    walks through 9 Success Criteria. This is an unavoidable manual verification step
    (Level 0.01% per gsd checkpoint policy — no CLI can validate real Windows DnD smoothness,
    overlay right-click positioning on multi-monitor, or visual archive dim effect).
  </action>

  <verify>Manual — owner confirms each of 9 Success Criteria from 04-UI-SPEC pass on real Windows.</verify>

  <done>
    Owner types "approved" (or "approved with notes"). Phase 4 is officially complete:
      - ROADMAP.md: Phase 4 → [x]
      - STATE.md: current_phase = 5 (Telegram bot)
      - 04-VALIDATION.md: approved_by_owner timestamp added
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ полностью зелёный (Phase 1/2/3 regression + Phase 4 новые)
- 04-VALIDATION.md: 13/13 coverage table + human-verify checkpoint
- Human sign-off: Никита запустил на Windows и подтвердил 9 Success Criteria
</verification>

<success_criteria>
- All 13 REQ-IDs (WEEK-01..06, TASK-01..07) verified end-to-end
- Regression: Phase 1/2/3 тесты зелёные
- 10+ E2E integration scenarios in test_e2e_phase4.py
- Human-verify checkpoint completed with approval
- ROADMAP.md Phase 4 → [x]
</success_criteria>

<output>
After completion, create `.planning/phases/04-week-tasks/04-11-SUMMARY.md` + update ROADMAP.md + STATE.md
</output>
