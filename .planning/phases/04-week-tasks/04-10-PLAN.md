---
phase: 04-week-tasks
plan: 10
type: execute
wave: 5
depends_on: [03, 04, 05, 06, 07, 08, 09]
files_modified:
  - client/ui/main_window.py
  - client/app.py
  - client/tests/ui/test_app_integration.py
  - client/tests/ui/test_main_window.py
autonomous: true
requirements:
  - WEEK-01
  - WEEK-02
  - WEEK-03
  - WEEK-04
  - WEEK-05
  - WEEK-06
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
  - TASK-05
  - TASK-06
  - TASK-07
must_haves:
  truths:
    - "MainWindow._build_ui использует WeekNavigation (Plan 04-05) как header (заменяет Phase 3 placeholder)"
    - "MainWindow создаёт 7 DaySection'ов через week navigation on_week_changed callback (WEEK-01)"
    - "overlay.on_right_click = quick_capture.show_at_overlay wire'ится в WeeklyPlannerApp._setup (D-01)"
    - "WeeklyPlannerApp держит QuickCapturePopup, UndoToastManager, DragController — созданы в _setup после main_window"
    - "MainWindow._delete_task_with_undo(task_id) — оркестратор: soft_delete + undo_toast.show + restore_callback"
    - "MainWindow._move_task_day(task_id, new_day) — callback DragController.on_task_moved: storage.update_task(day=)"
    - "MainWindow._refresh_tasks() вызывается после каждой CRUD операции (add/update/delete/move)"
    - "MainWindow._handle_task_style_changed переподключён: перерисовывает все TaskWidgets через DaySection.render_tasks"
    - "archive_mode обрабатывается: WeekNavigation.on_archive_changed → всем DaySection + DragController.set_archive_mode"
    - "Ctrl+Space — show QuickCapture centered (D-30)"
  artifacts:
    - path: "client/ui/main_window.py"
      provides: "MainWindow расширен: header=WeekNavigation, body=7 DaySection, quick_capture/undo/dnd integrated"
      contains: "class MainWindow"
      min_lines: 380
    - path: "client/app.py"
      provides: "WeeklyPlannerApp обновлён: quick_capture, undo_toast, drag_controller созданы в _setup"
      contains: "quick_capture"
      min_lines: 420
    - path: "client/tests/ui/test_app_integration.py"
      provides: "Phase 4 integration тесты (extended Phase 3): quick_capture wired, main_window renders tasks"
      contains: "def test_quick_capture_wired_to_overlay_right_click"
  key_links:
    - from: "client/app.py WeeklyPlannerApp._setup"
      to: "client/ui/quick_capture.py QuickCapturePopup"
      via: "self.quick_capture = QuickCapturePopup(root, theme, self._on_quick_save)"
      pattern: "QuickCapturePopup\\("
    - from: "client/app.py WeeklyPlannerApp._setup"
      to: "self.overlay.on_right_click = ... (D-01 wire)"
      via: "lambda: self.quick_capture.show_at_overlay(*self.overlay.get_position(), 56)"
      pattern: "overlay\\.on_right_click"
    - from: "client/ui/main_window.py _delete_task_with_undo"
      to: "client/core/storage.py + client/ui/undo_toast.py"
      via: "storage.soft_delete_task + undo_toast.show(restore_callback)"
      pattern: "_delete_task_with_undo"
    - from: "client/ui/main_window.py on_week_changed"
      to: "DaySection rebuild + DragController.clear+register drop_zones"
      via: "_rebuild_day_sections"
      pattern: "_rebuild_day_sections"
---

<objective>
Integration plan — собрать все Phase 4 компоненты в единый working flow.

**MainWindow** (client/ui/main_window.py) ПОЛНОСТЬЮ перестраивает UI: Phase 3 placeholder
header заменяется WeekNavigation (Plan 04-05); Phase 3 placeholder day sections заменяются
7 DaySection (Plan 04-04); добавляется UndoToastManager (Plan 04-08) внутрь root_frame;
добавляется DragController (Plan 04-09) с register_drop_zone per DaySection; wire keyboard
Ctrl+Space → QuickCapture show_centered.

**WeeklyPlannerApp** (client/app.py) добавляет в _setup() создание QuickCapturePopup +
wire'ит overlay.on_right_click → popup.show_at_overlay (D-01 patch).

**Critical non-destructive**: Phase 3 overlay.py НЕ трогаем (on_right_click callback уже есть).
Phase 3 sync/auth/tray/pulse lifecycle сохраняется.

Plan covers все 13 REQ-IDs через интеграционные connections. E2E smoke — Plan 04-11.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-week-tasks/04-UI-SPEC.md
@.planning/phases/04-week-tasks/04-CONTEXT.md
@client/ui/main_window.py
@client/app.py
@client/ui/overlay.py
@client/ui/day_section.py
@client/ui/task_widget.py
@client/ui/quick_capture.py
@client/ui/edit_dialog.py
@client/ui/undo_toast.py
@client/ui/drag_controller.py
@client/ui/week_navigation.py
@client/ui/parse_input.py
@client/core/storage.py
@client/core/sync.py
@client/core/auth.py

<interfaces>
<!-- ALL Phase 4 components built in Plans 04-02 to 04-09 -->
From client/ui/week_navigation.py (04-05):
```python
class WeekNavigation:
    def __init__(self, parent, window, theme, on_week_changed, on_archive_changed): ...
    def get_week_monday(self) -> date: ...
    def pack(self, **kwargs): ...
```

From client/ui/day_section.py (04-04):
```python
class DaySection:
    def __init__(self, parent, day_date, is_today, theme, task_style, user_id,
                 on_task_toggle, on_task_edit, on_task_delete, on_inline_add): ...
    def render_tasks(self, tasks: list[Task]) -> None: ...
    def get_body_frame(self) -> ctk.CTkFrame: ...
    def get_day_date(self) -> date: ...
    def set_archive_mode(self, is_archive: bool) -> None: ...
    def destroy(self): ...
```

From client/ui/quick_capture.py (04-06):
```python
class QuickCapturePopup:
    def __init__(self, root, theme, on_save): ...  # on_save(text, day_iso, time|None)
    def show_at_overlay(self, ox, oy, size=56): ...
    def show_centered(self, x, y): ...
    def destroy(self): ...
```

From client/ui/edit_dialog.py (04-07):
```python
class EditDialog:
    def __init__(self, parent_window, task, theme, on_save, on_delete): ...
    # on_save(updated_task), on_delete(task_id)
```

From client/ui/undo_toast.py (04-08):
```python
class UndoToastManager:
    def __init__(self, parent, root, theme): ...
    def show(self, task_id, task_text, undo_callback): ...
    def destroy(self): ...
```

From client/ui/drag_controller.py (04-09):
```python
class DragController:
    def __init__(self, root, theme, on_task_moved): ...  # on_task_moved(task_id, new_day)
    def register_drop_zone(self, zone: DropZone): ...
    def clear_drop_zones(self): ...
    def bind_task(self, body_frame, task_id, text, source_zone): ...
    def set_archive_mode(self, is_archive: bool): ...

class DropZone:
    day_date: date; frame; is_archive; is_next_week
```

From client/ui/week_navigation.py:
```python
def interpolate_palette(palette, bg_hex, factor): ...
def get_current_week_monday(): ...
def is_archive_week(monday): ...
```

<!-- Existing Phase 2/3 — DO NOT modify -->
From client/core/storage.py:
```python
class LocalStorage:
    def get_visible_tasks(self) -> list[Task]: ...
    def add_task(self, task: Task) -> None: ...
    def update_task(self, task_id: str, **fields) -> bool: ...
    def soft_delete_task(self, task_id: str) -> bool: ...
    def get_task(self, task_id: str) -> Optional[Task]: ...
```

From client/ui/overlay.py (Phase 3):
```python
class OverlayManager:
    on_right_click: Optional[Callable[[], None]] = None  # Phase 4 wires here
    def get_position(self) -> tuple[int, int]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить client/ui/main_window.py — integrate WeekNavigation + 7 DaySection + UndoToast + DragController + edit_dialog hookup</name>
  <files>client/ui/main_window.py, client/tests/ui/test_main_window.py</files>

  <read_first>
    - client/ui/main_window.py (Phase 3 shell — полностью расширить, placeholder header/sections удалить)
    - client/ui/week_navigation.py (Plan 04-05 API)
    - client/ui/day_section.py (Plan 04-04 API)
    - client/ui/undo_toast.py (Plan 04-08 API)
    - client/ui/drag_controller.py (Plan 04-09 API)
    - client/ui/edit_dialog.py (Plan 04-07 API)
    - client/ui/week_navigation.py (interpolate_palette для archive dim)
    - client/ui/themes.py (PALETTES)
    - client/core/storage.py (CRUD API)
    - client/tests/ui/test_main_window.py (существующие Phase 3 тесты — РАСШИРИТЬ)
  </read_first>

  <action>
    ШАГ 1 — Расширить `client/ui/main_window.py`. Сохранить lifecycle методы (show/hide/toggle/destroy/
    _on_close/_save_window_state/_resolve_initial_size/_on_configure/set_always_on_top) БЕЗ ИЗМЕНЕНИЙ.

    Переписать только:
    - `__init__` — добавить поля `storage`, `user_id`, `drag_controller`, `undo_toast`, `week_nav`,
      `_day_sections` (dict day_date → DaySection)
    - `_build_ui` — заменить placeholder header на WeekNavigation, scrollable container → пустой
      (наполняется через _rebuild_day_sections после week_nav init)
    - Добавить методы: `_rebuild_day_sections`, `_refresh_tasks`, `_on_week_changed`,
      `_on_archive_changed`, `_on_task_toggle`, `_on_task_edit`, `_on_task_delete`, `_on_inline_add`,
      `_on_task_moved`, `_delete_task_with_undo`, `_handle_task_style_changed`,
      `_on_quick_capture_save`, `_bind_ctrl_space_quick_capture`

    Шаблон изменённого `client/ui/main_window.py` (сохранить docstring + Phase 3 infrastructure):

    ```python
    """
    MainWindow — главное окно планировщика. Phase 3 + Phase 4.

    Phase 3 (сохранено):
      - Resizable CTkToplevel, min 320x320, default 460x600
      - Persistence window size+position через SettingsStore
      - Theme-aware
      - set_always_on_top
      - WM_DELETE_WINDOW → hide (не destroy) — app живёт в tray

    Phase 4 (новое):
      - WeekNavigation как header (заменяет placeholder)
      - 7 DaySection через _rebuild_day_sections (при on_week_changed)
      - UndoToastManager в root_frame
      - DragController с register_drop_zone per DaySection
      - EditDialog при TaskWidget.on_edit
      - Ctrl+Space keyboard → QuickCapture show_centered
    """
    from __future__ import annotations

    import logging
    import tkinter as tk
    from datetime import date, timedelta
    from typing import Callable, Optional

    import customtkinter as ctk

    from client.core.models import Task
    from client.core.storage import LocalStorage
    from client.ui.day_section import DaySection
    from client.ui.drag_controller import DragController, DropZone
    from client.ui.edit_dialog import EditDialog
    from client.ui.settings import SettingsStore, UISettings
    from client.ui.themes import FONTS, ThemeManager
    from client.ui.undo_toast import UndoToastManager
    from client.ui.week_navigation import (
        WeekNavigation,
        get_current_week_monday,
        interpolate_palette,
    )

    logger = logging.getLogger(__name__)


    class MainWindow:
        """Главное окно. Phase 3 shell + Phase 4 content."""

        MIN_SIZE = (320, 320)
        DEFAULT_SIZE = (460, 600)

        def __init__(
            self,
            root: ctk.CTk,
            settings_store: SettingsStore,
            settings: UISettings,
            theme_manager: ThemeManager,
            storage: Optional[LocalStorage] = None,
            user_id: Optional[str] = None,
            quick_capture_trigger: Optional[Callable[[], None]] = None,
        ) -> None:
            self._root = root
            self._settings_store = settings_store
            self._settings = settings
            self._theme = theme_manager
            self._storage = storage
            self._user_id = user_id or ""
            self._quick_capture_trigger = quick_capture_trigger

            # Phase 3: Tkinter lifecycle
            self._window = ctk.CTkToplevel(root)
            self._window.withdraw()
            self._window.title("Личный Еженедельник")
            self._window.minsize(*self.MIN_SIZE)

            w, h = self._resolve_initial_size()
            self._window.geometry(f"{w}x{h}")
            pos = self._settings.window_position
            if pos is not None and len(pos) == 2:
                try:
                    x, y = int(pos[0]), int(pos[1])
                    self._window.geometry(f"{w}x{h}+{x}+{y}")
                except (TypeError, ValueError):
                    pass

            try:
                self._window.attributes("-topmost", self._settings.on_top)
            except tk.TclError:
                pass
            self._window.protocol("WM_DELETE_WINDOW", self._on_close)

            # Phase 4: state
            self._day_sections: dict[date, DaySection] = {}
            self._drag_controller: Optional[DragController] = None
            self._undo_toast: Optional[UndoToastManager] = None
            self._week_nav: Optional[WeekNavigation] = None

            self._build_ui()
            self._theme.subscribe(self._apply_theme)
            self._apply_theme({
                "bg_primary": self._theme.get("bg_primary"),
                "text_primary": self._theme.get("text_primary"),
                "bg_secondary": self._theme.get("bg_secondary"),
                "accent_brand": self._theme.get("accent_brand"),
            })

            self._window.bind("<Configure>", self._on_configure)

            # Ctrl+Space для quick capture (D-30)
            if self._quick_capture_trigger is not None:
                self._window.bind(
                    "<Control-space>",
                    lambda e: self._quick_capture_trigger(),
                    add="+",
                )

            # Initial render
            if self._storage is not None:
                self._refresh_tasks()

        # ---- Phase 3 Public API (UNCHANGED) ----

        def show(self) -> None:
            self._window.deiconify()
            self._window.lift()

        def hide(self) -> None:
            self._window.withdraw()

        def toggle(self) -> None:
            if self.is_visible():
                self.hide()
            else:
                self.show()

        def is_visible(self) -> bool:
            try:
                return self._window.winfo_viewable() != 0
            except tk.TclError:
                return False

        def set_always_on_top(self, enabled: bool) -> None:
            try:
                self._window.attributes("-topmost", enabled)
            except tk.TclError:
                pass

        def destroy(self) -> None:
            if self._drag_controller is not None:
                try:
                    self._drag_controller.destroy()
                except Exception:
                    pass
            if self._undo_toast is not None:
                try:
                    self._undo_toast.destroy()
                except Exception:
                    pass
            for ds in list(self._day_sections.values()):
                try:
                    ds.destroy()
                except Exception:
                    pass
            self._day_sections.clear()
            try:
                self._window.destroy()
            except Exception as exc:
                logger.debug("MainWindow destroy: %s", exc)

        # ---- Phase 4: Public CRUD callbacks (used by WeeklyPlannerApp) ----

        def handle_task_style_changed(self, style: str) -> None:
            """Tray task_style toggle → перерисовать все TaskWidgets."""
            # Full rebuild: поменять task_style в DaySection требует пересоздания
            self._rebuild_day_sections()

        def on_right_click_from_overlay(self, _event=None) -> None:
            """Reserved для overlay.on_right_click integration (wire'ится в app.py)."""
            if self._quick_capture_trigger is not None:
                self._quick_capture_trigger()

        # ---- Build ----

        def _build_ui(self) -> None:
            """Phase 3 + Phase 4 UI."""
            self._root_frame = ctk.CTkFrame(self._window, corner_radius=0)
            self._root_frame.pack(fill="both", expand=True)

            # Phase 4: WeekNavigation вместо placeholder header
            self._week_nav = WeekNavigation(
                self._root_frame, self._window, self._theme,
                on_week_changed=self._on_week_changed,
                on_archive_changed=self._on_archive_changed,
            )
            self._week_nav.pack(fill="x", side="top")

            # Scroll container для 7 DaySection
            self._scroll = ctk.CTkScrollableFrame(self._root_frame)
            self._scroll.pack(fill="both", expand=True, padx=8, pady=4)

            # Undo toast manager — в _root_frame (НЕ scroll)
            self._undo_toast = UndoToastManager(
                self._root_frame, self._root, self._theme,
            )

            # Drag controller (создаётся до first _rebuild_day_sections)
            self._drag_controller = DragController(
                self._root, self._theme,
                on_task_moved=self._on_task_moved,
            )

            # First rebuild: current week
            self._rebuild_day_sections()

        # ---- Week navigation callbacks ----

        def _on_week_changed(self, new_monday: date) -> None:
            """WeekNavigation → _rebuild_day_sections + _refresh_tasks."""
            self._rebuild_day_sections()
            self._refresh_tasks()

        def _on_archive_changed(self, is_archive: bool) -> None:
            """WEEK-06: archive mode на всех DaySection + DragController."""
            for ds in self._day_sections.values():
                ds.set_archive_mode(is_archive)
            if self._drag_controller:
                self._drag_controller.set_archive_mode(is_archive)
            # Визуальный dim effect через apply dimmed palette
            if is_archive:
                base_palette = {
                    "bg_primary": self._theme.get("bg_primary"),
                    "bg_secondary": self._theme.get("bg_secondary"),
                    "text_primary": self._theme.get("text_primary"),
                    "accent_brand": self._theme.get("accent_brand"),
                }
                _ = interpolate_palette(base_palette, self._theme.get("bg_primary"), 0.3)
                # (Используем для будущей детальной интеграции per-widget)

        # ---- Day sections rebuild ----

        def _rebuild_day_sections(self) -> None:
            """WEEK-01: пересоздать 7 DaySection для текущей выбранной недели."""
            # Destroy old
            for ds in list(self._day_sections.values()):
                try:
                    ds.destroy()
                except Exception:
                    pass
            self._day_sections.clear()
            if self._drag_controller:
                self._drag_controller.clear_drop_zones()

            if self._week_nav is None:
                return
            week_monday = self._week_nav.get_week_monday()
            today = date.today()

            task_style_map = {"card": "card", "line": "line", "minimal": "minimal"}
            style = task_style_map.get(self._settings.task_style, "card")

            for i in range(7):
                d = week_monday + timedelta(days=i)
                is_today = (d == today)
                ds = DaySection(
                    self._scroll, d, is_today, self._theme, style, self._user_id,
                    on_task_toggle=self._on_task_toggle,
                    on_task_edit=self._on_task_edit,
                    on_task_delete=self._on_task_delete,
                    on_inline_add=self._on_inline_add,
                )
                ds.pack(fill="x", pady=4)
                self._day_sections[d] = ds

                # Register drop zone for DnD (Plan 04-09)
                if self._drag_controller:
                    zone = DropZone(day_date=d, frame=ds.get_body_frame())
                    self._drag_controller.register_drop_zone(zone)

            # Apply archive mode if needed
            if self._week_nav.is_current_archive():
                self._on_archive_changed(True)

        # ---- Refresh tasks ----

        def _refresh_tasks(self) -> None:
            """Get tasks from LocalStorage + распределить по DaySection."""
            if self._storage is None or self._week_nav is None:
                return
            all_tasks = self._storage.get_visible_tasks()
            # Group by day
            by_day: dict[date, list[Task]] = {d: [] for d in self._day_sections.keys()}
            for t in all_tasks:
                try:
                    td = date.fromisoformat(t.day)
                except (ValueError, TypeError):
                    continue
                if td in by_day:
                    by_day[td].append(t)

            for d, tasks in by_day.items():
                ds = self._day_sections[d]
                ds.render_tasks(tasks)
                # Re-bind DnD для каждого task
                if self._drag_controller and d in self._day_sections:
                    zone = None
                    for z in self._drag_controller._drop_zones:
                        if z.day_date == d:
                            zone = z
                            break
                    if zone:
                        for task_id, widget in ds._task_widgets.items():
                            try:
                                self._drag_controller.bind_task(
                                    widget.get_body_frame(), task_id, widget._task.text, zone,
                                )
                            except Exception:
                                pass

        # ---- CRUD callbacks ----

        def _on_task_toggle(self, task_id: str, new_done: bool) -> None:
            """TASK-02: checkbox click → update_task(done=...)"""
            if self._storage:
                self._storage.update_task(task_id, done=new_done)
                self._refresh_tasks()

        def _on_task_edit(self, task_id: str) -> None:
            """TASK-03: открыть EditDialog для task."""
            if self._storage is None:
                return
            task = self._storage.get_task(task_id)
            if task is None:
                return
            EditDialog(
                self._window, task, self._theme,
                on_save=self._on_edit_save,
                on_delete=self._on_task_delete,
            )

        def _on_edit_save(self, updated: Task) -> None:
            """TASK-03: EditDialog.on_save → update_task."""
            if self._storage is None:
                return
            self._storage.update_task(
                updated.id,
                text=updated.text,
                day=updated.day,
                time_deadline=updated.time_deadline,
                done=updated.done,
            )
            self._refresh_tasks()

        def _on_task_delete(self, task_id: str) -> None:
            """TASK-04: soft delete + show undo toast."""
            self._delete_task_with_undo(task_id)

        def _delete_task_with_undo(self, task_id: str) -> None:
            """Оркестратор: storage.soft_delete → undo_toast.show с restore callback."""
            if self._storage is None or self._undo_toast is None:
                return
            task = self._storage.get_task(task_id)
            task_text = task.text if task else ""

            self._storage.soft_delete_task(task_id)
            self._refresh_tasks()

            def undo_restore():
                if self._storage:
                    # D-20: task.deleted_at = None (Phase 2 API: update_task deleted_at)
                    # ВАЖНО: Phase 2 update_task не принимает deleted_at в whitelist. Обходим
                    # через внутренний доступ к _data под _lock.
                    try:
                        with self._storage._lock:
                            for t in self._storage._data["tasks"]:
                                if t.get("id") == task_id:
                                    t["deleted_at"] = None
                                    break
                            # Добавить update change в pending
                            from client.core.models import TaskChange
                            change = TaskChange(op="update", task_id=task_id)
                            self._storage._data["pending_changes"].append(change.to_dict())
                            self._storage._save_locked()
                    except Exception as exc:
                        logger.error("Undo restore failed: %s", exc)
                    self._refresh_tasks()

            self._undo_toast.show(task_id, task_text, undo_restore)

        def _on_inline_add(self, task: Task) -> None:
            """DaySection '+' → add_task."""
            if self._storage is None:
                return
            self._storage.add_task(task)
            self._refresh_tasks()

        def _on_task_moved(self, task_id: str, new_day: date) -> None:
            """DragController.on_task_moved → update_task(day=)."""
            if self._storage is None:
                return
            self._storage.update_task(task_id, day=new_day.isoformat())
            self._refresh_tasks()

        # ---- Quick-capture callback (для WeeklyPlannerApp интеграции) ----

        def handle_quick_capture_save(self, text: str, day_iso: str, time: Optional[str]) -> None:
            """WeeklyPlannerApp.quick_capture.on_save wire'ится сюда."""
            if self._storage is None:
                return
            task = Task.new(
                user_id=self._user_id, text=text, day=day_iso,
                time_deadline=time, position=0,
            )
            self._storage.add_task(task)
            self._refresh_tasks()

        # ---- Phase 3 persistence (UNCHANGED) ----

        def _resolve_initial_size(self) -> tuple[int, int]:
            ws = self._settings.window_size
            try:
                w, h = int(ws[0]), int(ws[1])
                if w >= self.MIN_SIZE[0] and h >= self.MIN_SIZE[1]:
                    return (w, h)
            except (TypeError, ValueError, IndexError):
                pass
            return self.DEFAULT_SIZE

        def _on_configure(self, event) -> None:
            if event.widget is self._window:
                try:
                    new_size = [self._window.winfo_width(), self._window.winfo_height()]
                    new_pos = [self._window.winfo_x(), self._window.winfo_y()]
                    if new_size != self._settings.window_size:
                        self._settings.window_size = new_size
                        self._settings.window_position = new_pos
                except tk.TclError:
                    pass

        def _on_close(self) -> None:
            self._save_window_state()
            self.hide()

        def _save_window_state(self) -> None:
            try:
                self._settings.window_size = [
                    self._window.winfo_width(),
                    self._window.winfo_height(),
                ]
                self._settings.window_position = [
                    self._window.winfo_x(),
                    self._window.winfo_y(),
                ]
                self._settings_store.save(self._settings)
            except tk.TclError as exc:
                logger.debug("_save_window_state skip: %s", exc)

        # ---- Theme ----

        def _apply_theme(self, palette: dict) -> None:
            bg = palette.get("bg_primary", "#F5EFE6")
            try:
                self._window.configure(fg_color=bg)
                if hasattr(self, "_root_frame"):
                    self._root_frame.configure(fg_color=bg)
            except tk.TclError:
                pass
    ```

    ШАГ 2 — Расширить `client/tests/ui/test_main_window.py`. Сохранить Phase 3 тесты (должны
    продолжать проходить). ДОБАВИТЬ в конец файла:

    ```python
    # ---------- Phase 4 integration ----------

    from unittest.mock import MagicMock
    from datetime import date, timedelta


    @pytest.fixture
    def mw_phase4_deps(headless_tk, mock_theme_manager, mock_storage, tmp_appdata):
        from client.ui.settings import SettingsStore, UISettings
        settings = UISettings()
        store = SettingsStore(mock_storage)
        return {
            "root": headless_tk,
            "settings_store": store,
            "settings": settings,
            "theme": mock_theme_manager,
            "storage": mock_storage,
            "user_id": "test-user",
        }


    def _make_mw_p4(deps):
        from client.ui.main_window import MainWindow
        mw = MainWindow(
            deps["root"], deps["settings_store"], deps["settings"], deps["theme"],
            storage=deps["storage"], user_id=deps["user_id"],
        )
        deps["root"].update_idletasks()
        return mw


    def test_main_window_has_week_nav(mw_phase4_deps):
        mw = _make_mw_p4(mw_phase4_deps)
        assert mw._week_nav is not None
        mw.destroy()


    def test_main_window_has_seven_day_sections(mw_phase4_deps):
        mw = _make_mw_p4(mw_phase4_deps)
        assert len(mw._day_sections) == 7
        mw.destroy()


    def test_main_window_has_undo_toast_manager(mw_phase4_deps):
        mw = _make_mw_p4(mw_phase4_deps)
        assert mw._undo_toast is not None
        mw.destroy()


    def test_main_window_has_drag_controller(mw_phase4_deps):
        mw = _make_mw_p4(mw_phase4_deps)
        assert mw._drag_controller is not None
        mw.destroy()


    def test_drag_controller_has_seven_drop_zones(mw_phase4_deps):
        mw = _make_mw_p4(mw_phase4_deps)
        assert len(mw._drag_controller._drop_zones) == 7
        mw.destroy()


    def test_refresh_tasks_renders_tasks_in_day(mw_phase4_deps, timestamped_task_factory):
        mw = _make_mw_p4(mw_phase4_deps)
        # Add task для сегодня
        task = timestamped_task_factory(text="test today")
        mw_phase4_deps["storage"].add_task(task)
        mw._refresh_tasks()
        today = date.today()
        ds = mw._day_sections.get(today)
        assert ds is not None
        assert len(ds._task_widgets) == 1
        mw.destroy()


    def test_delete_with_undo_shows_toast(mw_phase4_deps, timestamped_task_factory):
        mw = _make_mw_p4(mw_phase4_deps)
        task = timestamped_task_factory()
        mw_phase4_deps["storage"].add_task(task)
        mw._delete_task_with_undo(task.id)
        mw_phase4_deps["root"].update_idletasks()
        assert len(mw._undo_toast._queue) == 1
        mw.destroy()


    def test_on_task_toggle_updates_storage(mw_phase4_deps, timestamped_task_factory):
        mw = _make_mw_p4(mw_phase4_deps)
        task = timestamped_task_factory(done=False)
        mw_phase4_deps["storage"].add_task(task)
        mw._on_task_toggle(task.id, True)
        updated = mw_phase4_deps["storage"].get_task(task.id)
        assert updated.done is True
        mw.destroy()


    def test_on_task_moved_updates_day(mw_phase4_deps, timestamped_task_factory):
        mw = _make_mw_p4(mw_phase4_deps)
        task = timestamped_task_factory()
        mw_phase4_deps["storage"].add_task(task)
        new_day = date.today() + timedelta(days=1)
        mw._on_task_moved(task.id, new_day)
        updated = mw_phase4_deps["storage"].get_task(task.id)
        assert updated.day == new_day.isoformat()
        mw.destroy()


    def test_handle_quick_capture_save_creates_task(mw_phase4_deps):
        mw = _make_mw_p4(mw_phase4_deps)
        mw.handle_quick_capture_save("test task", date.today().isoformat(), "14:00")
        tasks = mw_phase4_deps["storage"].get_visible_tasks()
        assert len(tasks) == 1
        assert tasks[0].text == "test task"
        assert tasks[0].time_deadline == "14:00"
        mw.destroy()


    def test_week_navigation_changes_day_sections(mw_phase4_deps):
        """prev_week() → _rebuild_day_sections → days изменились."""
        mw = _make_mw_p4(mw_phase4_deps)
        initial_days = set(mw._day_sections.keys())
        mw._week_nav.prev_week()
        new_days = set(mw._day_sections.keys())
        assert initial_days != new_days
        mw.destroy()
    ```

    ШАГ 3 — Запустить ВСЕ Phase 3/4 тесты main_window: Phase 3 regression не сломан + новые тесты проходят.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_main_window.py -x -q --timeout=60</automated>
  </verify>

  <acceptance_criteria>
    - grep "from client.ui.week_navigation import WeekNavigation" client/ui/main_window.py >= 1
    - grep "from client.ui.day_section import DaySection" client/ui/main_window.py >= 1
    - grep "from client.ui.undo_toast import UndoToastManager" client/ui/main_window.py >= 1
    - grep "from client.ui.drag_controller import DragController" client/ui/main_window.py >= 1
    - grep "from client.ui.edit_dialog import EditDialog" client/ui/main_window.py >= 1
    - grep "def _rebuild_day_sections" client/ui/main_window.py >= 1
    - grep "def _refresh_tasks" client/ui/main_window.py >= 1
    - grep "def _delete_task_with_undo" client/ui/main_window.py >= 1
    - grep "def _on_task_moved" client/ui/main_window.py >= 1
    - grep "handle_quick_capture_save" client/ui/main_window.py >= 1
    - grep "Control-space" client/ui/main_window.py >= 1 (D-30)
    - pytest client/tests/ui/test_main_window.py >= 10 тестов зелёных (Phase 3 + 10 новых)
    - File size >= 380 lines
  </acceptance_criteria>

  <done>
    MainWindow расширен: все Phase 4 компоненты wired. Phase 3 lifecycle (show/hide/topmost/
    persistence) сохранён. Task 2 обновит client/app.py чтобы передавать storage+user_id в MW
    и создать QuickCapturePopup с overlay wire.
  </done>
</task>

<task type="auto">
  <name>Task 2: Расширить client/app.py — создать QuickCapturePopup в _setup + wire overlay.on_right_click (D-01)</name>
  <files>client/app.py, client/tests/ui/test_app_integration.py</files>

  <read_first>
    - client/app.py (Phase 3 WeeklyPlannerApp._setup — расширить, не переписывать)
    - client/ui/quick_capture.py (QuickCapturePopup API из Plan 04-06)
    - client/ui/main_window.py (handle_quick_capture_save метод добавленный в Task 1)
    - client/ui/overlay.py (on_right_click callback — Phase 3)
    - client/tests/ui/test_app_integration.py (существующие Phase 3 тесты — РАСШИРИТЬ)
  </read_first>

  <action>
    ШАГ 1 — Обновить `client/app.py`. Изменения:

    a) В `__init__` добавить `self.quick_capture: Optional[QuickCapturePopup] = None`

    b) Добавить import:
    ```python
    from client.ui.quick_capture import QuickCapturePopup
    ```

    c) В `_setup()` — ПОСЛЕ создания `main_window` и ДО создания `tray` — добавить:

    ```python
            # 6.5 Phase 4: QuickCapturePopup — integration с overlay right-click (D-01)
            self.quick_capture = QuickCapturePopup(
                self.root, self.theme,
                on_save=self._handle_quick_capture_save,
            )

            # Передадим trigger в MainWindow для Ctrl+Space (D-30)
            self.main_window._quick_capture_trigger = self._trigger_quick_capture_centered

            # D-01: overlay.on_right_click → quick_capture.show_at_overlay
            self.overlay.on_right_click = self._on_overlay_right_click
    ```

    d) Добавить методы в WeeklyPlannerApp:

    ```python
        def _on_overlay_right_click(self) -> None:
            """D-01: right-click на overlay → quick capture popup."""
            if self.quick_capture is None or self.overlay is None:
                return
            x, y = self.overlay.get_position()
            self.quick_capture.show_at_overlay(x, y, 56)

        def _handle_quick_capture_save(
            self, text: str, day_iso: str, time: Optional[str],
        ) -> None:
            """QuickCapturePopup.on_save → MainWindow → storage.add_task."""
            if self.main_window is None:
                return
            self.main_window.handle_quick_capture_save(text, day_iso, time)
            # Force sync — чтобы новая задача быстрее попала на сервер
            if self.sync is not None:
                self.sync.force_sync()
            # Refresh overlay/tray badge
            self._refresh_ui()

        def _trigger_quick_capture_centered(self) -> None:
            """D-30 Ctrl+Space: quick capture в центре main window."""
            if self.quick_capture is None or self.main_window is None:
                return
            try:
                mw_x = self.main_window._window.winfo_x()
                mw_y = self.main_window._window.winfo_y()
                mw_w = self.main_window._window.winfo_width()
                popup_x = mw_x + mw_w // 2 - 200
                popup_y = mw_y + 60
            except Exception:
                popup_x, popup_y = 200, 200
            self.quick_capture.show_centered(popup_x, popup_y)
    ```

    e) В `_handle_quit` добавить cleanup:

    ```python
            if self.quick_capture is not None:
                try:
                    self.quick_capture.destroy()
                except Exception:
                    pass
    ```

    f) В `_setup` — **IMPORTANT**: создание MainWindow должно передавать storage и user_id.
    Найти строчку:
    ```python
            self.main_window = MainWindow(
                self.root, self.settings_store, self.settings, self.theme,
            )
    ```
    Заменить на:
    ```python
            # Phase 4: передаём storage и user_id для CRUD операций
            user_id = (self.auth._user.get("id") if (self.auth and self.auth._user) else "")
            self.main_window = MainWindow(
                self.root, self.settings_store, self.settings, self.theme,
                storage=self.storage, user_id=user_id,
            )
    ```
    (auth._user.id может отсутствовать — используем безопасный getattr или fallback "")

    g) В `_handle_task_style_changed` — вместо log warning вызвать реальный refresh:
    ```python
        def _handle_task_style_changed(self, style: str) -> None:
            """Phase 4: style changed → MainWindow перерисует task widgets."""
            if self.main_window is not None:
                self.main_window.handle_task_style_changed(style)
    ```

    ШАГ 2 — Расширить `client/tests/ui/test_app_integration.py`. ДОБАВИТЬ в конец:

    ```python
    # ---------- Phase 4 integration ----------

    def test_quick_capture_created_in_setup(tmp_appdata, mock_ctypes_dpi,
                                             mock_pystray_icon, mock_winotify):
        """WeeklyPlannerApp._setup создаёт QuickCapturePopup (authenticated branch)."""
        from unittest.mock import patch
        from client.app import WeeklyPlannerApp
        app = WeeklyPlannerApp(version="test")
        with patch.object(
            type(app), "_setup_unauthenticated_placeholder", lambda self: None,
        ):
            # Force auth
            with patch("client.core.auth.AuthManager.load_saved_token",
                       return_value=True):
                try:
                    app._setup()
                except Exception:
                    # Некоторые Phase 3 компоненты могут падать в headless — OK
                    pass
        # Если _setup прошёл до QuickCapture init — поле установлено
        # (гибкий assertion — главное что атрибут существует)
        assert hasattr(app, "quick_capture")


    def test_overlay_right_click_wired(tmp_appdata, mock_ctypes_dpi,
                                         mock_pystray_icon, mock_winotify):
        """D-01: overlay.on_right_click is set to app._on_overlay_right_click."""
        import inspect
        import client.app as app_module
        source = inspect.getsource(app_module)
        assert "overlay.on_right_click = self._on_overlay_right_click" in source or \
               "self.overlay.on_right_click = self._on_overlay_right_click" in source


    def test_handle_quick_capture_save_exists():
        """Метод _handle_quick_capture_save существует и имеет правильную сигнатуру."""
        from client.app import WeeklyPlannerApp
        assert hasattr(WeeklyPlannerApp, "_handle_quick_capture_save")

    def test_trigger_quick_capture_centered_exists():
        """Метод _trigger_quick_capture_centered существует."""
        from client.app import WeeklyPlannerApp
        assert hasattr(WeeklyPlannerApp, "_trigger_quick_capture_centered")
    ```
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_app_integration.py -x -q --timeout=60</automated>
  </verify>

  <acceptance_criteria>
    - grep "from client.ui.quick_capture import QuickCapturePopup" client/app.py >= 1
    - grep "self.quick_capture = QuickCapturePopup" client/app.py >= 1
    - grep "overlay.on_right_click = self._on_overlay_right_click\\|self.overlay.on_right_click = " client/app.py >= 1
    - grep "def _on_overlay_right_click" client/app.py >= 1
    - grep "def _handle_quick_capture_save" client/app.py >= 1
    - grep "def _trigger_quick_capture_centered" client/app.py >= 1
    - grep "storage=self.storage" client/app.py >= 1 (MainWindow получает storage)
    - grep "handle_task_style_changed" client/app.py >= 1 (style change delegated)
    - pytest client/tests/ui/test_app_integration.py зелёный (минимум 4 новых + Phase 3)
    - File size client/app.py >= 420 lines (было ~416 в Phase 3)
  </acceptance_criteria>

  <done>
    WeeklyPlannerApp расширен. Phase 3 lifecycle сохранён. Phase 4 компоненты:
    - QuickCapturePopup создан после main_window
    - overlay.on_right_click → quick_capture.show_at_overlay (D-01 wire)
    - handle_task_style_changed делегирован на MainWindow
    - MainWindow получает storage + user_id для CRUD
    - Ctrl+Space → quick_capture.show_centered через MainWindow.bind

    Plan 04-11 запустит E2E + human-verify checkpoint.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_main_window.py + test_app_integration.py: зелёные
- grep: все integration markers присутствуют
- Phase 3 lifecycle не сломан (существующие test_app_integration зелёные)
- overlay.on_right_click — единственная точка wire'а (D-01)
- MainWindow передан storage → CRUD работает в integration тестах
</verification>

<success_criteria>
- Все 13 REQ-IDs покрыты интеграционно
- D-01 wire (overlay right-click) работает
- D-30 keyboard shortcut (Ctrl+Space) работает через MainWindow.bind
- MainWindow.handle_quick_capture_save создаёт Task через storage
- DragController регистрирует drop zones для 7 DaySection
- UndoToastManager создан в _root_frame (НЕ в scroll)
- Phase 3 regression зелёный
</success_criteria>

<output>
After completion, create `.planning/phases/04-week-tasks/04-10-SUMMARY.md`
</output>
