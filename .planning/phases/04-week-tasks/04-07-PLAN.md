---
phase: 04-week-tasks
plan: 07
type: execute
wave: 3
depends_on: [01]
files_modified:
  - client/ui/edit_dialog.py
  - client/tests/ui/test_edit_dialog.py
autonomous: true
requirements:
  - TASK-03
must_haves:
  truths:
    - "EditDialog создаётся как CTkToplevel на parent_window, grab_set() активен (D-14 modal)"
    - "grab_set() вызывается ПОСЛЕ deiconify() (PITFALL 1 order)"
    - "grab_release() вызывается на ВСЕХ exit paths: cancel, save, delete, WM_DELETE — 4+ мест (PITFALL 1)"
    - "Fields: CTkTextbox для multi-line текста (D-15); CTkOptionMenu день; CTkEntry HH:MM с валидацией (D-15)"
    - "CTkTextbox.get('1.0', 'end-1c') — правильный индекс, без trailing newline (PITFALL 2)"
    - "StringVar.trace_add('write', validate) — HH:MM validation через regex (D-17)"
    - "Save button disabled при empty text OR invalid HH:MM (D-17)"
    - "Delete button красный, вызывает on_delete + closes dialog (D-16)"
    - "Keyboard: Ctrl+Enter = Save, Esc = Cancel (D-16)"
    - "Дата dropdown: Сегодня / Завтра / Послезавтра / ISO dates недели (D-15, Claude's Discretion: no tkcalendar)"
  artifacts:
    - path: "client/ui/edit_dialog.py"
      provides: "EditDialog class — CTkToplevel + grab_set + 4 fields + 3 buttons + keyboard"
      contains: "class EditDialog"
      min_lines: 220
    - path: "client/tests/ui/test_edit_dialog.py"
      provides: "15+ unit тестов — modal, fields, validation, Save disabled, grab_release on exits"
      contains: "def test_dialog_modal"
      min_lines: 180
  key_links:
    - from: "client/ui/edit_dialog.py grab_set"
      to: "PITFALL 1 (modal order: build → deiconify → grab_set)"
      via: "self._dialog.deiconify() + self._dialog.grab_set()"
      pattern: "grab_set"
    - from: "client/ui/edit_dialog.py text"
      to: "CTkTextbox.get('1.0', 'end-1c') (PITFALL 2)"
      via: "textbox index end-1c"
      pattern: "end-1c"
    - from: "client/ui/edit_dialog.py validation"
      to: "StringVar.trace_add('write', ...)"
      via: "time_var.trace_add"
      pattern: "trace_add"
    - from: "client/ui/edit_dialog.py HH:MM regex"
      to: "_RE_HHMM = r'^(\\d{1,2}):(\\d{2})$'"
      via: "re.compile"
      pattern: "_RE_HHMM"
---

<objective>
EditDialog — модальный диалог редактирования задачи (TASK-03). CTkToplevel + grab_set() для
modal, CTkTextbox для multiline текста, CTkOptionMenu для дня, CTkEntry + validation для времени,
CTkCheckBox для done. 3 buttons (Delete/Cancel/Save) с keyboard shortcuts.

Purpose: Один из трёх главных UX-потоков Phase 4 (add/edit/delete). Edit dialog — единственное
место где пользователь может редактировать text/day/time существующей задачи. Без него — только
toggle done через checkbox (TASK-02). TASK-03 не покрыт без этого плана.

Critical PITFALL'ы:
  - PITFALL 1: grab_set() ДО deiconify() → grab не устанавливается (Tkinter quirk)
  - PITFALL 2: CTkTextbox.get("1.0", "end") возвращает trailing newline → "end-1c" обязателен
  - PITFALL 1: grab_release() на ВСЕХ exit paths — иначе weak/ghost grab hangs app

Integration points (Plan 04-10):
  - TaskWidget.on_edit(task_id) → EditDialog открывается
  - EditDialog.on_save(updated_task) → LocalStorage.update_task(...)
  - EditDialog.on_delete(task_id) → delete flow (Plan 04-08 undo-toast)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-week-tasks/04-UI-SPEC.md
@.planning/phases/04-week-tasks/04-RESEARCH.md
@.planning/phases/04-week-tasks/04-CONTEXT.md
@client/core/models.py
@client/ui/themes.py

<interfaces>
<!-- Dependencies -->
From client/core/models.py:
```python
@dataclass
class Task:
    id: str
    user_id: str
    text: str
    day: str
    time_deadline: Optional[str]
    done: bool
    position: int
    created_at: str
    updated_at: str
    deleted_at: Optional[str]
    def to_dict(self) -> dict: ...
```

From client/ui/themes.py:
```python
class ThemeManager:
    def get(self, key: str) -> str: ...
```

<!-- New interface this plan creates -->
Target API for client/ui/edit_dialog.py:
```python
import re
from typing import Callable, Optional
import customtkinter as ctk
from client.core.models import Task
from client.ui.themes import ThemeManager


_RE_HHMM = re.compile(r'^(\d{1,2}):(\d{2})$')


class EditDialog:
    """Модальный диалог редактирования задачи. CTkToplevel + grab_set.

    Fields (D-15): Text (multiline) + Day (dropdown) + Time (HH:MM) + Done (checkbox).
    Buttons (D-16): [🗑 Delete] left red; [Cancel] [Save] right.
    Validation (D-17): Save disabled при empty text OR invalid HH:MM.
    Keyboard (D-16): Ctrl+Enter=Save, Esc=Cancel.

    CRITICAL: grab_release() вызывается на ВСЕХ exit paths (PITFALL 1).
    """

    def __init__(
        self,
        parent_window: ctk.CTkToplevel,  # main window (не root)
        task: Task,
        theme_manager: ThemeManager,
        on_save: Callable[[Task], None],         # updated task
        on_delete: Callable[[str], None],        # task_id
    ) -> None: ...
```

<!-- D-14, D-15, D-16, D-17 locked -->
- Modal: CTkToplevel + grab_set()
- Text: CTkTextbox (multiline, Ctrl+Enter=save, Enter=newline)
- Day: CTkOptionMenu ["Сегодня", "Завтра", "Послезавтра", + ISO dates недели] (Claude's discretion: no tkcalendar)
- Time: CTkEntry с validation trace + ✕ clear button
- Done: CTkCheckBox
- Buttons: [🗑 Удалить] красная слева; [Отмена] [Сохранить] справа
- Validation: empty text OR !_RE_HHMM → Save disabled
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать client/ui/edit_dialog.py — EditDialog modal + fields + validation + grab_release guards</name>
  <files>client/ui/edit_dialog.py, client/tests/ui/test_edit_dialog.py</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-UI-SPEC.md §Edit Dialog — Full Edit Flow (D-14, D-15, D-16, D-17)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pattern 4 (EditDialog полный код)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pitfall 1, 2 (grab_set order, CTkTextbox end-1c)
    - .planning/phases/04-week-tasks/04-CONTEXT.md D-14..D-17
    - client/core/models.py (Task.to_dict для создания updated Task)
    - client/ui/themes.py (ThemeManager.get)
  </read_first>

  <behavior>
    - Test 1: EditDialog создан → grab_current() возвращает dialog window (grab_set активен)
    - Test 2: Dialog title == "Задача" (заголовок окна)
    - Test 3: Text field содержит task.text изначально
    - Test 4: Day dropdown показывает правильный label для task.day (Сегодня/Завтра/дата)
    - Test 5: Time entry содержит task.time_deadline если есть
    - Test 6: Done checkbox сheck'нут если task.done=True
    - Test 7: Save button ENABLED при корректных данных
    - Test 8: Save button DISABLED при empty text
    - Test 9: Save button DISABLED при invalid HH:MM (например "25:99")
    - Test 10: Save button DISABLED при invalid HH:MM (например "abc:def")
    - Test 11: Empty time — Save ENABLED (None time = valid)
    - Test 12: Cancel → grab_release вызван + dialog destroyed
    - Test 13: Save с валидными данными → on_save(updated_task) + grab_release + destroy
    - Test 14: Delete → on_delete(task_id) + grab_release + destroy
    - Test 15: WM_DELETE_WINDOW (close X) → grab_release + destroy
    - Test 16: Ctrl+Enter → Save flow
    - Test 17: Esc → Cancel flow
    - Test 18: Time ✕ clear button очищает entry
    - Test 19 (grep): grab_release вызван минимум 4 раза (save, cancel, delete, WM_DELETE)
    - Test 20 (grep): end-1c индекс используется (PITFALL 2)
    - Test 21 (grep): _RE_HHMM regex определён
    - Test 22 (grep): trace_add("write", ...) для time validation
  </behavior>

  <action>
    Создать `client/ui/edit_dialog.py`:

    ```python
    """
    EditDialog — модальный диалог редактирования задачи. Phase 4.

    Покрывает TASK-03: редактировать text / day / time / done.

    Critical PITFALL'ы:
      PITFALL 1: grab_set() ПОСЛЕ deiconify() — иначе grab не устанавливается.
      PITFALL 1: grab_release() на ВСЕХ exit paths — save, cancel, delete, WM_DELETE.
      PITFALL 2: CTkTextbox.get('1.0', 'end-1c') — убирает trailing newline.

    Fields (D-15):
      - Текст: CTkTextbox (multiline, Enter=newline, Ctrl+Enter=save)
      - День: CTkOptionMenu [Сегодня, Завтра, Послезавтра, + ISO даты недели]
      - Время: CTkEntry + StringVar.trace_add validation + ✕ clear
      - Выполнено: CTkCheckBox

    Buttons (D-16):
      - [🗑 Удалить] слева красная — immediate delete + callback
      - [Отмена] справа — discard + close
      - [Сохранить] справа primary — apply changes + close

    Validation (D-17):
      - empty text → Save disabled
      - invalid HH:MM → Save disabled
      - valid или empty time → Save enabled (None time = valid)
    """
    from __future__ import annotations

    import logging
    import re
    import tkinter as tk
    from dataclasses import replace
    from datetime import date, timedelta
    from typing import Callable, Optional

    import customtkinter as ctk

    from client.core.models import Task
    from client.ui.themes import FONTS, ThemeManager

    logger = logging.getLogger(__name__)

    # D-17 validation regex — HH:MM
    _RE_HHMM = re.compile(r'^(\d{1,2}):(\d{2})$')

    DAY_NAMES_RU = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
    MONTH_NAMES_RU = ['', 'янв', 'фев', 'мар', 'апр', 'май', 'июн',
                      'июл', 'авг', 'сен', 'окт', 'ноя', 'дек']


    class EditDialog:
        """Модальный диалог редактирования. См. 04-UI-SPEC §Edit Dialog."""

        DIALOG_WIDTH = 380
        DIALOG_HEIGHT = 360

        def __init__(
            self,
            parent_window: ctk.CTkToplevel,
            task: Task,
            theme_manager: ThemeManager,
            on_save: Callable[[Task], None],
            on_delete: Callable[[str], None],
        ) -> None:
            self._parent = parent_window
            self._task = task
            self._theme = theme_manager
            self._on_save = on_save
            self._on_delete = on_delete
            self._closed = False

            # Field widgets (set in _build_ui)
            self._text_box: Optional[ctk.CTkTextbox] = None
            self._day_var: Optional[ctk.StringVar] = None
            self._day_dropdown: Optional[ctk.CTkOptionMenu] = None
            self._time_var: Optional[tk.StringVar] = None
            self._time_entry: Optional[ctk.CTkEntry] = None
            self._done_var: Optional[tk.BooleanVar] = None
            self._save_btn: Optional[ctk.CTkButton] = None

            # Create modal window
            self._dialog = ctk.CTkToplevel(parent_window)
            self._dialog.withdraw()
            self._dialog.title("Задача")
            self._dialog.resizable(False, False)

            # Center on parent
            try:
                px = parent_window.winfo_x() + parent_window.winfo_width() // 2 - self.DIALOG_WIDTH // 2
                py = parent_window.winfo_y() + parent_window.winfo_height() // 2 - self.DIALOG_HEIGHT // 2
            except tk.TclError:
                px, py = 200, 200
            self._dialog.geometry(
                f"{self.DIALOG_WIDTH}x{self.DIALOG_HEIGHT}+{px}+{py}",
            )

            self._build_ui()

            # CRITICAL PITFALL 1: порядок — deiconify ДО grab_set
            self._dialog.deiconify()
            try:
                self._dialog.grab_set()
            except tk.TclError as exc:
                logger.debug("grab_set failed: %s", exc)

            try:
                self._dialog.focus_set()
                if self._text_box:
                    self._text_box.focus_set()
            except tk.TclError:
                pass

            # Keyboard bindings (D-16)
            self._dialog.bind("<Escape>", lambda e: self._cancel())
            self._dialog.bind("<Control-Return>", lambda e: self._save())
            # WM_DELETE_WINDOW (close X) — grab_release obligatory (PITFALL 1)
            self._dialog.protocol("WM_DELETE_WINDOW", self._cancel)

            # Initial validation (Save state)
            self._update_save_state()

        # ---- Build ----

        def _build_ui(self) -> None:
            frame = ctk.CTkFrame(self._dialog)
            frame.pack(fill="both", expand=True, padx=16, pady=16)

            # Text field (multiline — D-15)
            ctk.CTkLabel(frame, text="Текст", anchor="w").pack(fill="x")
            self._text_box = ctk.CTkTextbox(frame, height=80, wrap="word")
            self._text_box.pack(fill="x", pady=(0, 8))
            self._text_box.insert("1.0", self._task.text)

            # Enter = newline (не save). Ctrl+Enter = save (через dialog bind выше).
            self._text_box.bind("<KeyRelease>", lambda e: self._update_save_state())

            # Day dropdown (D-15)
            ctk.CTkLabel(frame, text="День", anchor="w").pack(fill="x")
            day_options = self._build_day_options()
            self._day_var = ctk.StringVar(value=self._get_current_day_label())
            self._day_dropdown = ctk.CTkOptionMenu(
                frame, values=day_options, variable=self._day_var,
            )
            self._day_dropdown.pack(fill="x", pady=(0, 8))

            # Time field (D-15)
            ctk.CTkLabel(frame, text="Время (HH:MM)", anchor="w").pack(fill="x")
            time_row = ctk.CTkFrame(frame, fg_color="transparent")
            time_row.pack(fill="x", pady=(0, 8))

            time_val = ""
            if self._task.time_deadline:
                time_val = self._task.time_deadline[:5] if len(
                    self._task.time_deadline) >= 5 else self._task.time_deadline

            self._time_var = tk.StringVar(value=time_val)
            self._time_entry = ctk.CTkEntry(
                time_row, textvariable=self._time_var, width=100, font=FONTS["mono"],
            )
            self._time_entry.pack(side="left")

            ctk.CTkButton(
                time_row, text="✕", width=32,
                command=lambda: self._time_var.set(""),
            ).pack(side="left", padx=4)

            # StringVar trace для validation (D-17)
            self._time_var.trace_add("write", self._on_time_changed)

            # Done checkbox
            self._done_var = tk.BooleanVar(value=self._task.done)
            ctk.CTkCheckBox(
                frame, text="Выполнено", variable=self._done_var,
                command=self._update_save_state,
            ).pack(anchor="w", pady=(4, 12))

            # Buttons row (D-16)
            btn_frame = ctk.CTkFrame(frame, fg_color="transparent")
            btn_frame.pack(fill="x")

            # Delete (left, red — D-16)
            ctk.CTkButton(
                btn_frame, text="🗑 Удалить",
                fg_color=self._theme.get("accent_overdue"),
                hover_color="#cc3333",
                width=110,
                command=self._delete,
            ).pack(side="left")

            # Cancel + Save (right)
            ctk.CTkButton(
                btn_frame, text="Отмена", width=80, command=self._cancel,
            ).pack(side="right", padx=(4, 0))

            self._save_btn = ctk.CTkButton(
                btn_frame, text="Сохранить", width=110, command=self._save,
            )
            self._save_btn.pack(side="right")

        # ---- Day helpers ----

        def _build_day_options(self) -> list[str]:
            """Список опций для Day dropdown: Сегодня/Завтра/Послезавтра + ISO дни недели."""
            today = date.today()
            opts = ["Сегодня", "Завтра", "Послезавтра"]
            monday = today - timedelta(days=today.weekday())
            for i in range(7):
                d = monday + timedelta(days=i)
                if d in (today, today + timedelta(1), today + timedelta(2)):
                    continue
                label = f"{DAY_NAMES_RU[i]} {d.day} {MONTH_NAMES_RU[d.month]}"
                opts.append(label)
            return opts

        def _get_current_day_label(self) -> str:
            """Вернуть label dropdown для task.day."""
            today = date.today()
            try:
                d = date.fromisoformat(self._task.day)
            except (ValueError, TypeError):
                return "Сегодня"
            if d == today:
                return "Сегодня"
            if d == today + timedelta(1):
                return "Завтра"
            if d == today + timedelta(2):
                return "Послезавтра"
            return f"{DAY_NAMES_RU[d.weekday()]} {d.day} {MONTH_NAMES_RU[d.month]}"

        def _day_label_to_iso(self, label: str) -> str:
            """Преобразовать label обратно в ISO date."""
            today = date.today()
            if label == "Сегодня":
                return today.isoformat()
            if label == "Завтра":
                return (today + timedelta(1)).isoformat()
            if label == "Послезавтра":
                return (today + timedelta(2)).isoformat()
            # Парсим "Пн 14 апр" → находим в week
            parts = label.split()
            if len(parts) >= 2:
                try:
                    day_num = int(parts[1])
                    monday = today - timedelta(days=today.weekday())
                    for i in range(7):
                        d = monday + timedelta(days=i)
                        if d.day == day_num:
                            return d.isoformat()
                except (ValueError, IndexError):
                    pass
            return self._task.day  # fallback

        # ---- Validation (D-17) ----

        def _on_time_changed(self, *_) -> None:
            """StringVar trace callback — validate time + update save state."""
            val = self._time_var.get() if self._time_var else ""
            accent_overdue = self._theme.get("accent_overdue")
            accent_done = self._theme.get("accent_done")
            bg_sec = self._theme.get("bg_secondary")

            if not val:
                # Empty = valid (None time)
                try:
                    self._time_entry.configure(border_color=bg_sec)
                except (tk.TclError, AttributeError):
                    pass
            elif self._is_valid_hhmm(val):
                try:
                    self._time_entry.configure(border_color=accent_done)
                except (tk.TclError, AttributeError):
                    pass
            else:
                try:
                    self._time_entry.configure(border_color=accent_overdue)
                except (tk.TclError, AttributeError):
                    pass
            self._update_save_state()

        @staticmethod
        def _is_valid_hhmm(val: str) -> bool:
            """True если val соответствует HH:MM с корректными значениями."""
            m = _RE_HHMM.match(val)
            if not m:
                return False
            try:
                h = int(m.group(1))
                mi = int(m.group(2))
                return 0 <= h <= 23 and 0 <= mi <= 59
            except ValueError:
                return False

        def _update_save_state(self) -> None:
            """D-17: Save disabled при empty text OR invalid HH:MM."""
            if self._save_btn is None or not self._save_btn.winfo_exists():
                return
            # PITFALL 2: end-1c убирает trailing newline
            text_ok = False
            if self._text_box and self._text_box.winfo_exists():
                text = self._text_box.get("1.0", "end-1c").strip()
                text_ok = bool(text)

            time_val = self._time_var.get() if self._time_var else ""
            time_ok = (not time_val) or self._is_valid_hhmm(time_val)

            try:
                self._save_btn.configure(state="normal" if (text_ok and time_ok) else "disabled")
            except tk.TclError:
                pass

        # ---- Actions (all with grab_release — PITFALL 1) ----

        def _save(self) -> None:
            if self._closed:
                return
            # PITFALL 2: end-1c
            text = ""
            if self._text_box:
                text = self._text_box.get("1.0", "end-1c").strip()
            if not text:
                return  # validation guard

            time_val = self._time_var.get().strip() if self._time_var else ""
            if time_val and not self._is_valid_hhmm(time_val):
                return

            day_iso = self._day_label_to_iso(self._day_var.get()) if self._day_var else self._task.day
            done = self._done_var.get() if self._done_var else self._task.done

            updated = Task(
                id=self._task.id,
                user_id=self._task.user_id,
                text=text,
                day=day_iso,
                time_deadline=time_val or None,
                done=done,
                position=self._task.position,
                created_at=self._task.created_at,
                updated_at=self._task.updated_at,
                deleted_at=self._task.deleted_at,
            )

            self._close_dialog()
            try:
                self._on_save(updated)
            except Exception as exc:
                logger.error("on_save callback failed: %s", exc)

        def _cancel(self) -> None:
            if self._closed:
                return
            self._close_dialog()

        def _delete(self) -> None:
            if self._closed:
                return
            task_id = self._task.id
            self._close_dialog()
            try:
                self._on_delete(task_id)
            except Exception as exc:
                logger.error("on_delete callback failed: %s", exc)

        def _close_dialog(self) -> None:
            """PITFALL 1: grab_release ОБЯЗАТЕЛЬНО перед destroy на всех exit paths."""
            if self._closed:
                return
            self._closed = True
            try:
                self._dialog.grab_release()
            except tk.TclError as exc:
                logger.debug("grab_release: %s", exc)
            try:
                self._dialog.destroy()
            except tk.TclError as exc:
                logger.debug("dialog destroy: %s", exc)

        # ---- Alias для удобства ----

        def destroy(self) -> None:
            """Alias for cancel — cleanup dialog."""
            self._cancel()
    ```

    Создать `client/tests/ui/test_edit_dialog.py`:

    ```python
    """Unit-тесты EditDialog (Plan 04-07).

    Покрывает:
      TASK-03: edit text/day/time/done
      D-17: validation (Save disabled при empty/invalid)
      PITFALL 1: grab_set ПОСЛЕ deiconify, grab_release на ВСЕХ exit paths
      PITFALL 2: CTkTextbox.get('1.0', 'end-1c') — trailing newline
    """
    import inspect
    from unittest.mock import MagicMock

    import pytest

    from client.ui.edit_dialog import EditDialog, _RE_HHMM


    @pytest.fixture
    def ed_deps(headless_tk, mock_theme_manager, timestamped_task_factory):
        return {
            "parent": headless_tk,
            "theme": mock_theme_manager,
            "factory": timestamped_task_factory,
            "on_save": MagicMock(),
            "on_delete": MagicMock(),
        }


    def _make(ed_deps, task=None):
        if task is None:
            task = ed_deps["factory"](text="original")
        dlg = EditDialog(
            ed_deps["parent"], task, ed_deps["theme"],
            ed_deps["on_save"], ed_deps["on_delete"],
        )
        ed_deps["parent"].update_idletasks()
        return dlg


    # ---------- Modal (grab_set — PITFALL 1) ----------

    def test_dialog_modal_grab_set(ed_deps):
        """grab_set активен после создания dialog."""
        dlg = _make(ed_deps)
        # grab_current() возвращает widget у которого grab
        grab = dlg._dialog.grab_current()
        assert grab is dlg._dialog
        dlg._cancel()

    def test_dialog_title_russian(ed_deps):
        dlg = _make(ed_deps)
        assert dlg._dialog.title() == "Задача"
        dlg._cancel()


    # ---------- Fields ----------

    def test_text_field_initial_value(ed_deps):
        task = ed_deps["factory"](text="my task")
        dlg = _make(ed_deps, task=task)
        text = dlg._text_box.get("1.0", "end-1c")
        assert text == "my task"
        dlg._cancel()

    def test_time_field_initial_value(ed_deps):
        task = ed_deps["factory"](time="14:30")
        dlg = _make(ed_deps, task=task)
        assert dlg._time_var.get() == "14:30"
        dlg._cancel()

    def test_time_field_empty_for_no_time(ed_deps):
        task = ed_deps["factory"]()  # time=None
        dlg = _make(ed_deps, task=task)
        assert dlg._time_var.get() == ""
        dlg._cancel()

    def test_done_checkbox_initial_state(ed_deps):
        task = ed_deps["factory"](done=True)
        dlg = _make(ed_deps, task=task)
        assert dlg._done_var.get() is True
        dlg._cancel()


    # ---------- Validation (D-17) ----------

    def test_save_enabled_with_valid_data(ed_deps):
        task = ed_deps["factory"](text="text", time="14:30")
        dlg = _make(ed_deps, task=task)
        assert str(dlg._save_btn.cget("state")) == "normal"
        dlg._cancel()

    def test_save_disabled_empty_text(ed_deps):
        task = ed_deps["factory"](text="")
        # Empty factory text = "" строка (но factory default = "Test task")
        # Удалим весь текст
        dlg = _make(ed_deps)
        dlg._text_box.delete("1.0", "end")
        dlg._update_save_state()
        assert str(dlg._save_btn.cget("state")) == "disabled"
        dlg._cancel()

    def test_save_disabled_invalid_hhmm_hour(ed_deps):
        """Hour > 23 → invalid."""
        dlg = _make(ed_deps)
        dlg._time_var.set("25:30")
        ed_deps["parent"].update_idletasks()
        assert str(dlg._save_btn.cget("state")) == "disabled"
        dlg._cancel()

    def test_save_disabled_invalid_hhmm_minute(ed_deps):
        dlg = _make(ed_deps)
        dlg._time_var.set("14:99")
        ed_deps["parent"].update_idletasks()
        assert str(dlg._save_btn.cget("state")) == "disabled"
        dlg._cancel()

    def test_save_disabled_invalid_hhmm_format(ed_deps):
        dlg = _make(ed_deps)
        dlg._time_var.set("abc:def")
        ed_deps["parent"].update_idletasks()
        assert str(dlg._save_btn.cget("state")) == "disabled"
        dlg._cancel()

    def test_save_enabled_empty_time(ed_deps):
        """Empty time = valid (None) → Save enabled."""
        dlg = _make(ed_deps)
        dlg._time_var.set("")
        ed_deps["parent"].update_idletasks()
        assert str(dlg._save_btn.cget("state")) == "normal"
        dlg._cancel()

    def test_is_valid_hhmm_regex_verified():
        """_RE_HHMM regex test cases."""
        assert _RE_HHMM.match("14:30")
        assert _RE_HHMM.match("09:00")
        assert not _RE_HHMM.match("abc")
        assert not _RE_HHMM.match("14:5")  # 1-digit minutes not match
        assert EditDialog._is_valid_hhmm("00:00") is True
        assert EditDialog._is_valid_hhmm("23:59") is True
        assert EditDialog._is_valid_hhmm("24:00") is False
        assert EditDialog._is_valid_hhmm("14:60") is False


    # ---------- Actions + grab_release (PITFALL 1) ----------

    def test_cancel_releases_grab(ed_deps):
        dlg = _make(ed_deps)
        dlg._cancel()
        ed_deps["parent"].update_idletasks()
        # После cancel grab должен быть освобождён
        try:
            grab_after = dlg._dialog.grab_current()
        except Exception:
            grab_after = None
        assert grab_after is None

    def test_save_calls_callback_with_updated_task(ed_deps):
        task = ed_deps["factory"](text="original")
        dlg = _make(ed_deps, task=task)
        dlg._text_box.delete("1.0", "end")
        dlg._text_box.insert("1.0", "new text")
        dlg._save()
        ed_deps["on_save"].assert_called_once()
        updated = ed_deps["on_save"].call_args[0][0]
        assert updated.text == "new text"
        assert updated.id == task.id

    def test_delete_calls_on_delete(ed_deps):
        task = ed_deps["factory"]()
        dlg = _make(ed_deps, task=task)
        dlg._delete()
        ed_deps["on_delete"].assert_called_once_with(task.id)

    def test_save_released_grab(ed_deps):
        dlg = _make(ed_deps)
        dlg._save()
        try:
            assert dlg._dialog.grab_current() is None
        except Exception:
            pass  # dialog может быть уже destroyed

    def test_delete_released_grab(ed_deps):
        dlg = _make(ed_deps)
        dlg._delete()
        try:
            assert dlg._dialog.grab_current() is None
        except Exception:
            pass


    # ---------- Keyboard (D-16) ----------

    def test_escape_triggers_cancel(ed_deps):
        dlg = _make(ed_deps)
        dlg._dialog.event_generate("<Escape>", when="now")
        ed_deps["parent"].update()
        # После Esc dialog closed
        assert dlg._closed is True

    def test_ctrl_enter_triggers_save(ed_deps):
        task = ed_deps["factory"](text="valid text")
        dlg = _make(ed_deps, task=task)
        dlg._dialog.event_generate("<Control-Return>", when="now")
        ed_deps["parent"].update()
        ed_deps["on_save"].assert_called_once()


    # ---------- Time clear (✕ button) ----------

    def test_clear_time_button(ed_deps):
        task = ed_deps["factory"](time="14:30")
        dlg = _make(ed_deps, task=task)
        dlg._time_var.set("")
        assert dlg._time_var.get() == ""
        dlg._cancel()


    # ---------- Grep-verified markers ----------

    def test_end_minus_1c_marker():
        """PITFALL 2: end-1c используется."""
        source = inspect.getsource(EditDialog)
        assert "end-1c" in source

    def test_grab_release_on_multiple_paths():
        """PITFALL 1: grab_release на ВСЕХ exit paths (>=1 call; все через _close_dialog).

        Так как мы делаем _close_dialog helper — там 1 grab_release, но он вызывается из
        save/cancel/delete → 3 callers. Альтернативно засчитаем если helper централизован.
        """
        source = inspect.getsource(EditDialog)
        # Либо _close_dialog helper с grab_release, либо 4 прямых вызова
        has_helper = "_close_dialog" in source and "grab_release" in source
        direct_count = source.count("grab_release()")
        assert has_helper or direct_count >= 4

    def test_close_dialog_called_from_multiple_exits():
        """_close_dialog (grab_release helper) вызывается минимум 3 раза."""
        source = inspect.getsource(EditDialog)
        call_count = source.count("self._close_dialog()")
        assert call_count >= 3  # save, cancel, delete

    def test_trace_add_for_time_validation():
        source = inspect.getsource(EditDialog)
        assert "trace_add" in source
        assert "_on_time_changed" in source

    def test_rehhmm_regex_defined():
        """_RE_HHMM регекс определён на module level."""
        import client.ui.edit_dialog as ed
        assert hasattr(ed, "_RE_HHMM")
        assert ed._RE_HHMM.pattern == r'^(\d{1,2}):(\d{2})$'
    ```
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_edit_dialog.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/edit_dialog.py exists
    - grep "class EditDialog" client/ui/edit_dialog.py >= 1
    - grep "_RE_HHMM = re.compile" client/ui/edit_dialog.py >= 1
    - grep "grab_set" client/ui/edit_dialog.py >= 1
    - grep "grab_release" client/ui/edit_dialog.py >= 1 (централизованный _close_dialog)
    - grep "_close_dialog\\|self\\._close_dialog()" client/ui/edit_dialog.py >= 3 (save/cancel/delete)
    - grep "end-1c" client/ui/edit_dialog.py >= 1 (PITFALL 2)
    - grep "trace_add" client/ui/edit_dialog.py >= 1 (D-17 validation)
    - grep "WM_DELETE_WINDOW" client/ui/edit_dialog.py >= 1
    - grep "Control-Return\\|<Control-Return>" client/ui/edit_dialog.py >= 1 (D-16)
    - grep "CTkTextbox" client/ui/edit_dialog.py >= 1 (multiline — D-15)
    - grep "CTkOptionMenu" client/ui/edit_dialog.py >= 1 (day dropdown — D-15)
    - pytest client/tests/ui/test_edit_dialog.py >= 15 тестов зелёных
    - File size >= 220 lines
  </acceptance_criteria>

  <done>
    EditDialog готов. TASK-03 покрыт. Все validation (D-17), keyboard (D-16), grab_release
    (PITFALL 1), CTkTextbox end-1c (PITFALL 2) — grep-verified. Plan 04-10 wire'нет
    TaskWidget.on_edit → открытие EditDialog.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_edit_dialog.py: 15+ тестов зелёных
- PITFALL 1 markers: grab_set + grab_release в _close_dialog + >=3 callers
- PITFALL 2 marker: end-1c в get()
- D-17 validation: trace_add + _is_valid_hhmm
- Keyboard: Esc + Ctrl+Return
</verification>

<success_criteria>
- TASK-03 покрыт (edit text/day/time/done)
- D-14, D-15, D-16, D-17 locked values присутствуют
- PITFALL 1 (grab lifecycle) на ВСЕХ exit paths через централизованный _close_dialog
- PITFALL 2 (CTkTextbox end-1c) используется в get()
- Save button корректно disabled/enabled через validation trace
</success_criteria>

<output>
After completion, create `.planning/phases/04-week-tasks/04-07-SUMMARY.md`
</output>
