---
phase: 04-week-tasks
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - client/ui/task_widget.py
  - client/tests/ui/test_task_widget.py
autonomous: true
requirements:
  - WEEK-04
  - WEEK-05
  - TASK-02
must_haves:
  truths:
    - "TaskWidget рендерит задачу в 3 стилях 'card' (D-07), 'line' (D-08), 'minimal' (D-09) без crash"
    - "Checkbox 18×18 (custom Canvas) показывает 3 состояния: not-done (text_secondary border), done (accent_done fill + белая галочка), overdue (accent_overdue border) — D-11"
    - "Checkbox click вызывает on_toggle(task_id, new_done_value) callback"
    - "Time field отображается если time_deadline != None, color = accent_overdue если time < now и !done, text_tertiary если done (D-12)"
    - "Hover на widget → edit (✏) + delete (🗑) icons становятся видимы через text_color trick (D-13)"
    - "ThemeManager.subscribe: при смене темы _apply_theme callback перерисовывает checkbox и обновляет цвета"
    - "update_task(task) — partial update без пересоздания widget (SCROLL-PRESERVE PITFALL 4)"
  artifacts:
    - path: "client/ui/task_widget.py"
      provides: "TaskWidget class с 3 стилями, custom Canvas checkbox, hover icons, theme-aware"
      contains: "class TaskWidget"
      min_lines: 200
    - path: "client/tests/ui/test_task_widget.py"
      provides: "15+ unit тестов для 3 стилей, checkbox states, hover, theme switch"
      contains: "def test_three_styles"
      min_lines: 150
  key_links:
    - from: "client/ui/task_widget.py"
      to: "client/ui/themes.py ThemeManager.subscribe"
      via: "self._theme.subscribe(self._apply_theme)"
      pattern: "_theme.subscribe"
    - from: "client/ui/task_widget.py checkbox"
      to: "tk.Canvas create_rectangle + create_line (custom rendering)"
      via: "Canvas 18×18 с rounded 3 (D-11)"
      pattern: "tk\\.Canvas.*width=self\\.CHECKBOX_SIZE"
    - from: "client/ui/task_widget.py overdue"
      to: "Task.is_overdue() + time_deadline comparison"
      via: "palette.get('accent_overdue') when is_overdue"
      pattern: "accent_overdue"
---

<objective>
TaskWidget — виджет одной задачи в 3 стилях (card/line/minimal, D-07/08/09). Custom Canvas
checkbox (18×18, D-11) с overdue состоянием. Hover иконки edit/delete (D-13). Subscribe на
ThemeManager для live theme switching (Phase 3 pattern).

Purpose: Это единственный виджет, отображающий задачу — встречается 7×(0-N) раз на экране.
Должен быть:
  - Лёгким (не создавать десятки Tkinter виджетов на одну задачу)
  - Theme-aware (работает во всех 3 палитрах light/dark/beige)
  - Гибким (3 стиля без дублирования кода)
  - Partial-updatable (update_task() не пересоздаёт виджет — scroll preserved)

НЕ покрывает: CRUD (add/edit/delete) — они будут в Plan 04-06/07/08. Здесь только рендеринг +
callback-hooks (on_toggle/on_edit/on_delete). Покрывает **WEEK-04** (overdue visual), **WEEK-05**
(3 стиля), **TASK-02** (checkbox toggle).

Phase 3 skeleton `client/ui/task_widget.py` ПОЛНОСТЬЮ переписывается. Старая реализация
содержит устаревшие приоритеты (v2 anti-feature), категории и т.д. — удаляем все.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-week-tasks/04-UI-SPEC.md
@.planning/phases/04-week-tasks/04-RESEARCH.md
@.planning/phases/04-week-tasks/04-CONTEXT.md
@.planning/phases/03-overlay-system/03-UI-SPEC.md
@client/ui/themes.py
@client/core/models.py
@client/ui/task_widget.py

<interfaces>
<!-- Existing dependencies -->
From client/ui/themes.py:
```python
PALETTES = {"light": {...}, "dark": {...}, "beige": {...}}  # все содержат:
#   bg_primary, bg_secondary, bg_tertiary, text_primary, text_secondary, text_tertiary,
#   accent_brand, accent_brand_light, accent_done, accent_overdue, shadow_card

class ThemeManager:
    def subscribe(self, callback: Callable[[dict], None]) -> None: ...
    def get(self, key: str) -> str: ...
```

From client/core/models.py:
```python
@dataclass
class Task:
    id: str
    text: str
    day: str
    time_deadline: Optional[str]  # ISO datetime или "HH:MM"
    done: bool
    position: int
    # ...
    def is_overdue(self) -> bool: ...  # done=false && day < today
    def to_dict(self) -> dict: ...
```

<!-- New interface this plan creates -->
Target API for client/ui/task_widget.py:
```python
from typing import Callable
import customtkinter as ctk
from client.core.models import Task
from client.ui.themes import ThemeManager


class TaskWidget:
    """Виджет одной задачи. 3 стиля (D-07/08/09) + hover icons (D-13) + theme-aware."""

    CHECKBOX_SIZE = 18  # D-11
    CHECKBOX_RADIUS = 3  # D-11

    def __init__(
        self,
        parent: ctk.CTkBaseClass,
        task: Task,
        style: str,  # "card" | "line" | "minimal"
        theme_manager: ThemeManager,
        on_toggle: Callable[[str, bool], None],  # (task_id, new_done)
        on_edit: Callable[[str], None],           # (task_id)
        on_delete: Callable[[str], None],         # (task_id)
    ) -> None: ...

    # Public API
    def pack(self, **kwargs) -> None:
        """Проксирует pack() на внешний frame."""

    def destroy(self) -> None:
        """Cleanup виджета."""

    def update_task(self, task: Task) -> None:
        """Partial update без пересоздания (PITFALL 4 — scroll preserved)."""

    def get_body_frame(self) -> ctk.CTkFrame:
        """Возвращает frame тела задачи — target для DnD bindings (Plan 04-09)."""
```

<!-- D-07/08/09 locked padding values -->
- Style "card": corner_radius=8, padding=(12, 10), gap=8 between blocks, bg_secondary + shadow_card
- Style "line": corner_radius=0, padding=(10, 8), 1px bottom border (bg_tertiary)
- Style "minimal": corner_radius=6 on hover, padding=(6, 6), hover bg interpolated rgba(accent, 0.04)

<!-- D-11/D-12/D-13 locked values -->
- Checkbox 18×18, rounded 3px
- Icons 14×14 (edit=✏, delete=🗑), hover fade-in (150ms — instant via text_color trick OK for MVP)
- Time font monospace (Cascadia Code, 11)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Полностью переписать client/ui/task_widget.py — 3 стиля + custom checkbox + hover icons + theme-aware</name>
  <files>client/ui/task_widget.py, client/tests/ui/test_task_widget.py</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-UI-SPEC.md §Task Block — Rendering Engine (3 styles verbatim values)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pattern 3 (TaskWidget — 3 styles, полный скелет)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pitfall 4 (Task list re-render — partial update)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pitfall 6 (Hover icons text_color trick + theme switch)
    - .planning/phases/04-week-tasks/04-CONTEXT.md D-07, D-08, D-09, D-10, D-11, D-12, D-13
    - client/ui/themes.py (PALETTES, ThemeManager.subscribe)
    - client/core/models.py (Task.is_overdue, Task.to_dict)
    - client/ui/task_widget.py (СТАРЫЙ skeleton — НЕ используем, пишем поверх)
  </read_first>

  <behavior>
    - Test 1: TaskWidget создаётся для style="card" → outer frame corner_radius=8
    - Test 2: TaskWidget создаётся для style="line" → outer frame corner_radius=0
    - Test 3: TaskWidget создаётся для style="minimal" → outer frame corner_radius=6, fg_color="transparent" по default
    - Test 4: Некорректный style → ValueError или fallback к "card"
    - Test 5: Checkbox Canvas 18×18 создаётся (widget._cb_canvas.cget("width") == 18)
    - Test 6: Task done=True → Canvas содержит filled rectangle (items > 1, с fill!="")
    - Test 7: Task done=False, not overdue → Canvas содержит border rectangle (outline=text_secondary)
    - Test 8: Overdue task → Canvas border color = accent_overdue (проверяем через inspect canvas items)
    - Test 9: Time field отображается если time_deadline != None (widget._time_label exists)
    - Test 10: Time field НЕ отображается если time_deadline is None (widget._time_label is None)
    - Test 11: Checkbox click → on_toggle callback вызван с (task_id, new_done=True)
    - Test 12: Edit icon click → on_edit(task_id) вызван
    - Test 13: Delete icon click → on_delete(task_id) вызван
    - Test 14: Hover enter → _set_icons_visible(True) вызван (через widget._hover flag)
    - Test 15: Hover leave → _set_icons_visible(False)
    - Test 16: update_task(new_task) — text_label.cget("text") обновляется без пересоздания frame
    - Test 17: ThemeManager subscribe: после theme.set_theme("dark") _apply_theme вызвано
    - Test 18: destroy() — widget.frame.winfo_exists() возвращает False
    - Test 19: Кириллический текст отображается корректно ("Позвонить Иванову")
    - Test 20 (grep): Файл содержит "CHECKBOX_SIZE = 18" и "CHECKBOX_RADIUS = 3" (D-11)
  </behavior>

  <action>
    ШАГ 1 — ПОЛНОСТЬЮ ПЕРЕПИСАТЬ `client/ui/task_widget.py` (старый skeleton с приоритетами/категориями УДАЛИТЬ):

    ```python
    """
    TaskWidget — виджет одной задачи. Phase 4.

    3 стиля рендеринга (D-07/08/09), custom Canvas checkbox (D-11), hover icons (D-13),
    theme-aware через ThemeManager.subscribe.

    Плоская структура:
        outer_frame (CTkFrame, corner_radius зависит от style)
        └── row (CTkFrame, transparent)
            ├── cb_canvas (tk.Canvas 18×18, custom checkbox)
            ├── text_label (CTkLabel, wraplength adaptive)
            ├── time_label (CTkLabel, monospace, optional)
            └── icons_frame
                ├── edit_btn (✏ CTkLabel clickable)
                └── del_btn (🗑 CTkLabel clickable)

    Critical PITFALLs обработаны:
      - PITFALL 4 (Phase 3): update_task() — partial update без рекурсивного destroy
      - PITFALL 6 (Hover theme switch): _apply_theme re-вызывает _set_icons_visible
      - D-12 overdue color: time_deadline < now → accent_overdue

    Покрывает:
      WEEK-04: overdue visual (accent_overdue border)
      WEEK-05: 3 стиля через style param
      TASK-02: on_toggle callback при checkbox click
    """
    from __future__ import annotations

    import logging
    import tkinter as tk
    from datetime import date, datetime
    from typing import Callable, Optional

    import customtkinter as ctk

    from client.core.models import Task
    from client.ui.themes import FONTS, ThemeManager

    logger = logging.getLogger(__name__)

    VALID_STYLES = {"card", "line", "minimal"}


    class TaskWidget:
        """Виджет одной задачи. См. 04-UI-SPEC §Task Block."""

        CHECKBOX_SIZE = 18        # D-11
        CHECKBOX_RADIUS = 3       # D-11
        ICON_SIZE = 14            # D-13

        # Padding per style (D-07/D-08/D-09)
        _PADDING = {
            "card":    (12, 10),
            "line":    (10, 8),
            "minimal": (6, 6),
        }
        _CORNER_RADIUS = {
            "card":    8,
            "line":    0,
            "minimal": 6,
        }

        def __init__(
            self,
            parent: ctk.CTkBaseClass,
            task: Task,
            style: str,
            theme_manager: ThemeManager,
            on_toggle: Callable[[str, bool], None],
            on_edit: Callable[[str], None],
            on_delete: Callable[[str], None],
        ) -> None:
            if style not in VALID_STYLES:
                logger.warning("Неизвестный style=%r — fallback 'card'", style)
                style = "card"

            self._task = task
            self._style = style
            self._theme = theme_manager
            self._on_toggle = on_toggle
            self._on_edit = on_edit
            self._on_delete = on_delete
            self._hover: bool = False
            self._destroyed: bool = False

            # Instance-owned widget refs (set in _build)
            self._cb_canvas: Optional[tk.Canvas] = None
            self._text_label: Optional[ctk.CTkLabel] = None
            self._time_label: Optional[ctk.CTkLabel] = None
            self._edit_btn: Optional[ctk.CTkLabel] = None
            self._del_btn: Optional[ctk.CTkLabel] = None
            self._body_frame: Optional[ctk.CTkFrame] = None

            # D-07: card = bg_secondary + shadow; line = no bg; minimal = transparent
            fg = self._theme.get("bg_secondary") if style == "card" else "transparent"
            self.frame = ctk.CTkFrame(
                parent,
                corner_radius=self._CORNER_RADIUS[style],
                fg_color=fg,
            )

            self._build()
            self._theme.subscribe(self._apply_theme)

        # ---- Public API ----

        def pack(self, **kwargs) -> None:
            """Проксирует на внешний frame."""
            self.frame.pack(**kwargs)

        def get_body_frame(self) -> ctk.CTkFrame:
            """Frame тела задачи — target для DnD bindings в Plan 04-09.

            ВАЖНО: возвращаем body_frame (не весь widget), чтобы DnD на mouse был
            привязан к task body, а НЕ к checkbox/icons (D-22).
            """
            return self._body_frame

        def update_task(self, task: Task) -> None:
            """Partial update без пересоздания виджетов (PITFALL 4 — scroll preserved)."""
            self._task = task
            if self._text_label is not None and self._text_label.winfo_exists():
                self._text_label.configure(text=task.text)
            self._render_checkbox()
            self._update_time_label()

        def destroy(self) -> None:
            """Cleanup виджета."""
            self._destroyed = True
            try:
                self.frame.destroy()
            except Exception as exc:
                logger.debug("TaskWidget destroy: %s", exc)

        # ---- Build ----

        def _build(self) -> None:
            """Построить визуальную структуру."""
            padx, pady = self._PADDING[self._style]
            row = ctk.CTkFrame(self.frame, fg_color="transparent")
            row.pack(fill="x", padx=padx, pady=pady)

            # Custom Canvas checkbox (D-11: 18×18 rounded 3)
            self._cb_canvas = tk.Canvas(
                row,
                width=self.CHECKBOX_SIZE,
                height=self.CHECKBOX_SIZE,
                highlightthickness=0,
                borderwidth=0,
                cursor="hand2",
                bg=self._theme.get("bg_secondary" if self._style == "card" else "bg_primary"),
            )
            self._cb_canvas.pack(side="left", padx=(0, 8))
            self._cb_canvas.bind("<Button-1>", self._on_checkbox_click)

            # Task body (text + time) — ЦЕЛЬ для DnD bindings (Plan 04-09)
            self._body_frame = ctk.CTkFrame(row, fg_color="transparent", cursor="fleur")
            self._body_frame.pack(side="left", fill="x", expand=True)

            # Text label (wraplength=0 — adjust in Plan 04-04 через resize bind)
            self._text_label = ctk.CTkLabel(
                self._body_frame,
                text=self._task.text,
                anchor="w",
                justify="left",
                wraplength=0,  # Plan 04-04 обновит на resize
                font=FONTS["body"],
            )
            self._text_label.pack(side="left", fill="x", expand=True)

            # Time label (optional)
            if self._task.time_deadline:
                hhmm = self._extract_hhmm(self._task.time_deadline)
                self._time_label = ctk.CTkLabel(
                    self._body_frame,
                    text=hhmm,
                    font=FONTS["mono"],
                )
                self._time_label.pack(side="left", padx=(4, 0))

            # Action icons (edit ✏ + delete 🗑) — hover-reveal via text_color trick
            icons_frame = ctk.CTkFrame(row, fg_color="transparent")
            icons_frame.pack(side="right", padx=(4, 0))

            self._edit_btn = ctk.CTkLabel(
                icons_frame, text="✏", width=self.ICON_SIZE + 6, cursor="hand2",
                font=FONTS["caption"],
            )
            self._edit_btn.pack(side="left")
            self._edit_btn.bind("<Button-1>", lambda e: self._on_edit(self._task.id))

            self._del_btn = ctk.CTkLabel(
                icons_frame, text="🗑", width=self.ICON_SIZE + 6, cursor="hand2",
                font=FONTS["caption"],
            )
            self._del_btn.pack(side="left")
            self._del_btn.bind("<Button-1>", lambda e: self._on_delete(self._task.id))

            # Hover binding на frame + body (icons visibility)
            for w in (self.frame, row, self._body_frame, self._text_label):
                try:
                    w.bind("<Enter>", self._on_hover_enter, add="+")
                    w.bind("<Leave>", self._on_hover_leave, add="+")
                except tk.TclError:
                    pass

            # Начальная отрисовка checkbox + icons hidden
            self._render_checkbox()
            self._update_time_label()
            self._set_icons_visible(False)

        # ---- Checkbox rendering (D-11) ----

        def _render_checkbox(self) -> None:
            """Нарисовать checkbox на Canvas. 3 состояния: done / not-done / overdue."""
            c = self._cb_canvas
            if c is None or not c.winfo_exists():
                return
            c.delete("all")
            s = self.CHECKBOX_SIZE
            r = self.CHECKBOX_RADIUS

            if self._task.done:
                # Filled + white checkmark
                fill = self._theme.get("accent_done")
                c.create_rectangle(r, r, s - r, s - r, fill=fill, outline="", tags="box")
                # Checkmark white
                pts = [s * 0.22, s * 0.55, s * 0.43, s * 0.73, s * 0.78, s * 0.28]
                c.create_line(pts, fill="white", width=2, smooth=False, tags="check")
            elif self._task.is_overdue():
                border = self._theme.get("accent_overdue")
                c.create_rectangle(r, r, s - r, s - r, fill="", outline=border, width=2, tags="box")
            else:
                border = self._theme.get("text_secondary")
                c.create_rectangle(r, r, s - r, s - r, fill="", outline=border, width=1, tags="box")

        # ---- Time label (D-12) ----

        def _update_time_label(self) -> None:
            """Пересчитать цвет time label по D-12 (overdue time, done, default)."""
            if self._time_label is None or not self._time_label.winfo_exists():
                return
            # Если task был updated — text может измениться
            if self._task.time_deadline:
                self._time_label.configure(text=self._extract_hhmm(self._task.time_deadline))

            if self._task.done:
                color = self._theme.get("text_tertiary")
            elif self._task.is_overdue() or self._is_time_overdue():
                color = self._theme.get("accent_overdue")
            else:
                color = self._theme.get("text_secondary")
            try:
                self._time_label.configure(text_color=color)
            except tk.TclError:
                pass

        def _is_time_overdue(self) -> bool:
            """True если time_deadline < now и day == today."""
            if not self._task.time_deadline or self._task.done:
                return False
            try:
                # Если time_deadline — полный ISO
                if "T" in self._task.time_deadline or ":" in self._task.time_deadline[:3]:
                    try:
                        dl = datetime.fromisoformat(
                            self._task.time_deadline.replace("Z", "+00:00"))
                        return dl < datetime.now(dl.tzinfo or None)
                    except (ValueError, TypeError):
                        # "HH:MM" формат — сравниваем только если day == today
                        if self._task.day != date.today().isoformat():
                            return False
                        hhmm = self._task.time_deadline[:5]
                        h, m = map(int, hhmm.split(":"))
                        now_hm = datetime.now().strftime("%H:%M")
                        return hhmm < now_hm
            except Exception:
                return False
            return False

        @staticmethod
        def _extract_hhmm(value: str) -> str:
            """'14:00' → '14:00'; '2026-04-14T14:00:00Z' → '14:00'."""
            if not value:
                return ""
            if len(value) >= 5 and value[2] == ":":
                return value[:5]
            # ISO — попробовать
            try:
                dt = datetime.fromisoformat(value.replace("Z", "+00:00"))
                return dt.strftime("%H:%M")
            except (ValueError, TypeError):
                return value[:5]

        # ---- Hover / icons ----

        def _on_hover_enter(self, event=None) -> None:
            if self._destroyed:
                return
            self._hover = True
            if self._style == "minimal":
                try:
                    # D-09: soft background при hover
                    self.frame.configure(fg_color=self._theme.get("bg_secondary"))
                except tk.TclError:
                    pass
            self._set_icons_visible(True)

        def _on_hover_leave(self, event=None) -> None:
            if self._destroyed:
                return
            self._hover = False
            if self._style == "minimal":
                try:
                    self.frame.configure(fg_color="transparent")
                except tk.TclError:
                    pass
            self._set_icons_visible(False)

        def _set_icons_visible(self, visible: bool) -> None:
            """Show/hide icons via text_color trick (CTkLabel не имеет opacity).

            PITFALL 6: 'invisible' color = current bg_primary (пересчитываем при _apply_theme).
            """
            color = self._theme.get("text_secondary") if visible else self._theme.get(
                "bg_secondary" if self._style == "card" else "bg_primary")
            for btn in (self._edit_btn, self._del_btn):
                if btn is not None:
                    try:
                        btn.configure(text_color=color)
                    except tk.TclError:
                        pass

        # ---- Actions ----

        def _on_checkbox_click(self, event=None) -> None:
            """Toggle done + callback (optimistic UI)."""
            if self._destroyed:
                return
            new_done = not self._task.done
            # Optimistic update (UI immediately)
            self._task.done = new_done
            self._render_checkbox()
            self._update_time_label()
            try:
                self._on_toggle(self._task.id, new_done)
            except Exception as exc:
                logger.error("on_toggle callback failed: %s", exc)

        # ---- Theme ----

        def _apply_theme(self, palette: dict) -> None:
            """ThemeManager callback — перекрасить всё (PITFALL 6: вкл. icons invisible-color)."""
            if self._destroyed:
                return
            try:
                # Outer frame bg
                if self._style == "card":
                    self.frame.configure(fg_color=palette.get("bg_secondary"))
                # Canvas bg (для checkbox — должен match parent)
                if self._cb_canvas and self._cb_canvas.winfo_exists():
                    self._cb_canvas.configure(
                        bg=palette.get("bg_secondary" if self._style == "card" else "bg_primary"))
            except tk.TclError:
                pass
            # Пересчитать checkbox (colors могли смениться)
            self._render_checkbox()
            # Пересчитать time color
            self._update_time_label()
            # PITFALL 6: icons visibility color должен обновиться
            self._set_icons_visible(self._hover)
    ```

    ШАГ 2 — Создать `client/tests/ui/test_task_widget.py`:

    ```python
    """Unit-тесты TaskWidget (Plan 04-03).

    Покрывает:
      WEEK-04: overdue visual
      WEEK-05: 3 стиля
      TASK-02: checkbox toggle callback
    """
    import inspect
    from unittest.mock import MagicMock

    import pytest

    from client.ui.task_widget import TaskWidget, VALID_STYLES


    @pytest.fixture
    def tw_deps(headless_tk, mock_theme_manager, timestamped_task_factory):
        """Общие зависимости для TaskWidget тестов."""
        return {
            "root": headless_tk,
            "theme": mock_theme_manager,
            "factory": timestamped_task_factory,
            "on_toggle": MagicMock(),
            "on_edit": MagicMock(),
            "on_delete": MagicMock(),
        }


    def _make_widget(deps, task=None, style="card"):
        """Helper: создать TaskWidget с дефолтной task."""
        if task is None:
            task = deps["factory"]()
        w = TaskWidget(
            deps["root"], task, style, deps["theme"],
            deps["on_toggle"], deps["on_edit"], deps["on_delete"],
        )
        deps["root"].update_idletasks()
        return w


    # ---------- Styles ----------

    def test_three_styles(tw_deps):
        """Все 3 стиля создаются без ошибок."""
        for style in ("card", "line", "minimal"):
            w = _make_widget(tw_deps, style=style)
            assert w._style == style
            w.destroy()

    def test_invalid_style_falls_back_to_card(tw_deps):
        """Неизвестный style → fallback 'card' с warning."""
        w = _make_widget(tw_deps, style="unknown_style")
        assert w._style == "card"
        w.destroy()

    def test_valid_styles_set_has_three(tw_deps):
        assert VALID_STYLES == {"card", "line", "minimal"}

    def test_card_style_corner_radius(tw_deps):
        w = _make_widget(tw_deps, style="card")
        assert w.frame.cget("corner_radius") == 8
        w.destroy()

    def test_line_style_corner_radius_zero(tw_deps):
        w = _make_widget(tw_deps, style="line")
        assert w.frame.cget("corner_radius") == 0
        w.destroy()

    def test_minimal_style_transparent_default(tw_deps):
        """Minimal style: fg_color='transparent' по default."""
        w = _make_widget(tw_deps, style="minimal")
        # Transparent через ctk — проверим corner_radius=6
        assert w.frame.cget("corner_radius") == 6
        w.destroy()


    # ---------- Checkbox states ----------

    def test_checkbox_canvas_size(tw_deps):
        w = _make_widget(tw_deps)
        assert int(w._cb_canvas.cget("width")) == 18
        assert int(w._cb_canvas.cget("height")) == 18
        w.destroy()

    def test_checkbox_not_done_renders_border(tw_deps):
        """not done → Canvas содержит rectangle с outline (не fill)."""
        task = tw_deps["factory"](done=False)
        w = _make_widget(tw_deps, task=task)
        items = w._cb_canvas.find_all()
        assert len(items) >= 1
        # Минимум rect с outline
        w.destroy()

    def test_checkbox_done_renders_fill_and_checkmark(tw_deps):
        """done=True → rectangle с fill + line (галочка) → >=2 Canvas items."""
        task = tw_deps["factory"](done=True)
        w = _make_widget(tw_deps, task=task)
        items = w._cb_canvas.find_all()
        assert len(items) >= 2  # rect + checkmark line
        w.destroy()

    def test_checkbox_overdue_border_color(tw_deps):
        """WEEK-04: overdue task → checkbox border = accent_overdue color."""
        task = tw_deps["factory"](day_offset=-2, done=False)
        assert task.is_overdue() is True
        w = _make_widget(tw_deps, task=task)
        # Inspect canvas — outline of first rect should be accent_overdue
        items = w._cb_canvas.find_all()
        assert len(items) >= 1
        outline = w._cb_canvas.itemcget(items[0], "outline")
        accent_overdue = tw_deps["theme"].get("accent_overdue")
        assert outline == accent_overdue
        w.destroy()


    # ---------- Time field ----------

    def test_time_field_visible_with_time(tw_deps):
        task = tw_deps["factory"](time="14:30")
        w = _make_widget(tw_deps, task=task)
        assert w._time_label is not None
        assert "14:30" in w._time_label.cget("text")
        w.destroy()

    def test_time_field_absent_without_time(tw_deps):
        task = tw_deps["factory"]()  # time=None default
        w = _make_widget(tw_deps, task=task)
        assert w._time_label is None
        w.destroy()


    # ---------- Callbacks ----------

    def test_checkbox_click_calls_on_toggle(tw_deps):
        """TASK-02: checkbox click → on_toggle(task_id, new_done)."""
        task = tw_deps["factory"](done=False)
        w = _make_widget(tw_deps, task=task)
        w._on_checkbox_click()
        tw_deps["on_toggle"].assert_called_once_with(task.id, True)
        w.destroy()

    def test_checkbox_click_toggles_done(tw_deps):
        task = tw_deps["factory"](done=False)
        w = _make_widget(tw_deps, task=task)
        w._on_checkbox_click()
        assert w._task.done is True
        w._on_checkbox_click()
        assert w._task.done is False
        w.destroy()

    def test_edit_icon_click_calls_on_edit(tw_deps):
        task = tw_deps["factory"]()
        w = _make_widget(tw_deps, task=task)
        w._on_edit(task.id)
        tw_deps["on_edit"].assert_called_once_with(task.id)
        w.destroy()

    def test_delete_icon_click_calls_on_delete(tw_deps):
        task = tw_deps["factory"]()
        w = _make_widget(tw_deps, task=task)
        w._on_delete(task.id)
        tw_deps["on_delete"].assert_called_once_with(task.id)
        w.destroy()


    # ---------- Hover ----------

    def test_hover_enter_sets_flag(tw_deps):
        w = _make_widget(tw_deps)
        w._on_hover_enter()
        assert w._hover is True
        w.destroy()

    def test_hover_leave_clears_flag(tw_deps):
        w = _make_widget(tw_deps)
        w._on_hover_enter()
        w._on_hover_leave()
        assert w._hover is False
        w.destroy()


    # ---------- Update task (partial, PITFALL 4) ----------

    def test_update_task_changes_text_without_rebuild(tw_deps):
        """PITFALL 4: update_task не пересоздаёт frame."""
        task = tw_deps["factory"](text="old")
        w = _make_widget(tw_deps, task=task)
        original_frame = w.frame
        new_task = tw_deps["factory"](text="new")
        # Manually preserve same id to simulate update
        new_task.id = task.id
        w.update_task(new_task)
        assert w.frame is original_frame  # frame не пересоздан
        assert w._text_label.cget("text") == "new"
        w.destroy()


    # ---------- Theme switch ----------

    def test_theme_switch_triggers_apply_theme(tw_deps):
        """ThemeManager.set_theme('dark') → _apply_theme вызван."""
        w = _make_widget(tw_deps)
        # set_theme triggers все subscribers
        tw_deps["theme"].set_theme("dark")
        # После dark — checkbox должен перерендериться без ошибок (passage-test)
        assert w._cb_canvas.winfo_exists()
        w.destroy()


    # ---------- Cyrillic ----------

    def test_cyrillic_text_renders(tw_deps):
        task = tw_deps["factory"](text="Позвонить Иванову Петру Сергеевичу")
        w = _make_widget(tw_deps, task=task)
        assert w._text_label.cget("text") == "Позвонить Иванову Петру Сергеевичу"
        w.destroy()


    # ---------- Grep-verified markers ----------

    def test_constants_grep_d11(tw_deps):
        """D-11 markers присутствуют в source."""
        source = inspect.getsource(TaskWidget)
        assert "CHECKBOX_SIZE = 18" in source
        assert "CHECKBOX_RADIUS = 3" in source


    # ---------- get_body_frame (DnD integration point) ----------

    def test_get_body_frame_returns_frame(tw_deps):
        """Plan 04-09 DnD: get_body_frame() возвращает target для mouse bindings."""
        w = _make_widget(tw_deps)
        body = w.get_body_frame()
        assert body is not None
        assert body is w._body_frame
        w.destroy()


    # ---------- Destroy ----------

    def test_destroy_removes_frame(tw_deps):
        w = _make_widget(tw_deps)
        w.destroy()
        tw_deps["root"].update_idletasks()
        # frame должен быть destroyed
        assert w._destroyed is True
    ```

    ШАГ 3 — Запустить тесты: все зелёные.
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_task_widget.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/task_widget.py existed (полностью переписан)
    - grep "class TaskWidget" client/ui/task_widget.py >= 1
    - grep "CHECKBOX_SIZE = 18" client/ui/task_widget.py >= 1 (D-11)
    - grep "CHECKBOX_RADIUS = 3" client/ui/task_widget.py >= 1 (D-11)
    - grep "accent_overdue" client/ui/task_widget.py >= 1 (WEEK-04)
    - grep "accent_done" client/ui/task_widget.py >= 1 (checkbox fill)
    - grep "_theme.subscribe\\|subscribe(self._apply_theme)" client/ui/task_widget.py >= 1
    - grep "def update_task" client/ui/task_widget.py >= 1 (PITFALL 4 partial update)
    - grep "def get_body_frame" client/ui/task_widget.py >= 1 (DnD integration)
    - grep "tk.Canvas" client/ui/task_widget.py >= 1 (custom checkbox)
    - grep 'priority\\|category' client/ui/task_widget.py == 0 (старые v2 anti-features удалены)
    - pytest client/tests/ui/test_task_widget.py >= 15 тестов зелёных
    - File size >= 200 lines
  </acceptance_criteria>

  <done>
    TaskWidget готов. 3 стиля работают. WEEK-04 (overdue visual), WEEK-05 (3 styles),
    TASK-02 (toggle done) покрыты. Plan 04-04 (DaySection) будет pack'ать их в body. Plan 04-09
    (DragController) будет биндить mouse events на get_body_frame(). Старый skeleton
    (priority, categories, reschedule методы) удалён — anti-features v1 не нужны.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_task_widget.py: 15+ тестов зелёных
- grep markers: CHECKBOX_SIZE=18, CHECKBOX_RADIUS=3, accent_overdue, accent_done
- grep ZERO: priority, category (anti-features удалены)
- pytest client/tests (full regression) остаётся зелёным
</verification>

<success_criteria>
- TaskWidget — единственный widget для рендеринга Task, 3 стиля
- WEEK-04, WEEK-05, TASK-02 покрыты
- get_body_frame() готов для DnD integration в Plan 04-09
- Theme switching работает (subscribe pattern)
- Partial update (update_task) работает — PITFALL 4 предотвращён
</success_criteria>

<output>
After completion, create `.planning/phases/04-week-tasks/04-03-SUMMARY.md`
</output>
