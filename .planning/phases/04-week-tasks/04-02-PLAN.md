---
phase: 04-week-tasks
plan: 02
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - client/ui/parse_input.py
  - client/tests/test_quick_parse.py
autonomous: true
requirements:
  - TASK-01
must_haves:
  truths:
    - "parse_quick_input('встреча завтра 15:30') → {text='встреча', day=завтра ISO, time='15:30'}"
    - "parse_quick_input('позвонить в 14:00') → {text='позвонить', time='14:00', day=сегодня} (предлог 'в' удаляется)"
    - "parse_quick_input('заехать на склад пт') → day=ближайшая пятница (ISO)"
    - "parse_quick_input('перезвонить Лене') без parse → day=сегодня, time=None, text='перезвонить Лене' (fallback D-04)"
    - "parse_quick_input('9:00 звонок') нормализует время '9:00' → '09:00'"
    - "format_date_range_ru(monday, sunday) возвращает '14-20 апр' или '28 апр - 4 май' для кросс-месячного range"
    - "D-06 соблюдается: weekday в today → today (не след. неделя)"
  artifacts:
    - path: "client/ui/parse_input.py"
      provides: "parse_quick_input(raw) + format_date_range_ru(monday, sunday) + _RE_TIME + _WEEKDAY_MAP + _RE_RELATIVE"
      contains: "def parse_quick_input"
      min_lines: 80
    - path: "client/tests/test_quick_parse.py"
      provides: "20+ unit тестов для parse_quick_input и format_date_range_ru"
      contains: "def test_parse_"
      min_lines: 200
  key_links:
    - from: "client/ui/parse_input.py _WEEKDAY_MAP"
      to: "datetime.date.weekday() convention (Пн=0)"
      via: "(target_wd - current_wd) % 7"
      pattern: "_WEEKDAY_MAP"
    - from: "client/ui/parse_input.py _RE_TIME"
      to: "regex compiled (raw string)"
      via: "re.compile(r'...')"
      pattern: "_RE_TIME"
---

<objective>
Smart parse входного текста quick-capture (TASK-01) — pure Python модуль без Tkinter.
Извлекает `HH:MM`, относительный день (сегодня/завтра/послезавтра), день недели (пн..вс) из
произвольного русского текста. Остаток = text. Fallback: day=сегодня, time=None.

Также реализует `format_date_range_ru(monday, sunday)` для header навигации —
используется Plan 04-05 (week navigation) чтобы показывать "Неделя 16 • 14-20 апр".

Purpose: Core Value продукта = speed-of-capture. Без parse пользователь вынужден кликать
в dropdown'ы после каждого ввода — нарушает обещание "4 действия". Smart parse — единственная
деталь которая превращает quick-capture из "ещё одного input" в "моментальный захват мысли".

Вся логика — stdlib re + datetime, zero dependencies. Тесты purely Python (no Tk). TDD-стиль:
сначала пишем тесты (RED), потом реализацию (GREEN).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-week-tasks/04-UI-SPEC.md
@.planning/phases/04-week-tasks/04-RESEARCH.md
@.planning/phases/04-week-tasks/04-CONTEXT.md

<interfaces>
<!-- Новый модуль client/ui/parse_input.py -->
Target API:
```python
# client/ui/parse_input.py
"""Smart parse quick-capture text → {text, day, time}."""
from __future__ import annotations

import re
from datetime import date, timedelta
from typing import Optional, TypedDict

# Regex patterns (D-04)
_RE_TIME = re.compile(r'(?:^|\s)(\d{1,2}:\d{2})(?:\s|$)')
_RE_PREPOSITION_TIME = re.compile(r'\bв\s+(\d{1,2}:\d{2})\b', re.IGNORECASE)
_RE_RELATIVE = re.compile(r'\b(сегодня|завтра|послезавтра)\b', re.IGNORECASE)

# Weekday map (D-04, D-06)
_WEEKDAY_MAP = {"пн": 0, "вт": 1, "ср": 2, "чт": 3, "пт": 4, "сб": 5, "вс": 6}

# Russian month names for date range formatting
MONTH_NAMES_RU = ['', 'янв', 'фев', 'мар', 'апр', 'май', 'июн',
                  'июл', 'авг', 'сен', 'окт', 'ноя', 'дек']


class ParseResult(TypedDict):
    text: str
    day: str            # ISO "YYYY-MM-DD"
    time: Optional[str] # "HH:MM" или None


def parse_quick_input(raw: str) -> ParseResult:
    """Извлечь time/day/text из произвольного русского текста.

    Priority: HH:MM first → relative day → weekday → remainder = text.
    Fallback: day=today, time=None.

    Args:
        raw: текст пользователя из quick-capture (CTkEntry.get()).

    Returns:
        {"text": str, "day": "YYYY-MM-DD", "time": "HH:MM" | None}
    """


def format_date_range_ru(monday: date, sunday: date) -> str:
    """'14-20 апр' или '28 апр - 4 май'. Используется week navigation header."""
```

<!-- D-04 locked values (VERBATIM из 04-UI-SPEC Smart Parse) -->
- HH:MM: `r'(?:^|\s)(\d{1,2}:\d{2})(?:\s|$)'`
- Weekday map: `{"пн":0,"вт":1,"ср":2,"чт":3,"пт":4,"сб":5,"вс":6}`
- Relative: `r'\b(сегодня|завтра|послезавтра)\b'`
- Preposition time: `r'\bв\s+(\d{1,2}:\d{2})\b'` (удаляем "в" перед временем)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Написать unit-тесты для parse_quick_input и format_date_range_ru (RED)</name>
  <files>client/tests/test_quick_parse.py</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-UI-SPEC.md §Smart parse (natural input) — 4 patterns table
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pattern 2 (Smart Parse RU) — verbatim algorithm
    - .planning/phases/04-week-tasks/04-CONTEXT.md D-04, D-05, D-06 (parse rules)
  </read_first>

  <behavior>
    - RED: написать test file где тесты ДОЛЖНЫ упасть (модуль parse_input.py ещё не существует)
    - 20+ тестов покрывающих: HH:MM, relative, weekday, mixed, edge cases, fallback, кросс-месячный range
  </behavior>

  <action>
    Создать `client/tests/test_quick_parse.py`:

    ```python
    """Unit-тесты smart parse quick-capture (TASK-01, D-04/D-05/D-06).

    Pure Python — не требует Tk. Запускается быстро в отдельном файле.
    """
    from datetime import date, timedelta

    import pytest

    from client.ui.parse_input import (
        parse_quick_input,
        format_date_range_ru,
        _WEEKDAY_MAP,
        MONTH_NAMES_RU,
    )


    # ---------- HH:MM patterns ----------

    def test_parse_time_simple():
        r = parse_quick_input("встреча 14:00")
        assert r["time"] == "14:00"
        assert r["text"] == "встреча"
        assert r["day"] == date.today().isoformat()

    def test_parse_time_with_preposition():
        """'позвонить в 14:00' → text='позвонить' (предлог 'в' удаляется вместе со временем)."""
        r = parse_quick_input("позвонить в 14:00")
        assert r["time"] == "14:00"
        assert r["text"] == "позвонить"

    def test_parse_time_normalizes_9_to_09():
        """'9:00' → '09:00' (двухзначный формат часов)."""
        r = parse_quick_input("звонок 9:00")
        assert r["time"] == "09:00"

    def test_parse_time_keeps_leading_zero_format():
        """'09:30' остаётся '09:30' без изменений."""
        r = parse_quick_input("работа 09:30")
        assert r["time"] == "09:30"

    def test_parse_time_minutes_keep_format():
        r = parse_quick_input("дело 14:05")
        assert r["time"] == "14:05"


    # ---------- Relative day ----------

    def test_parse_relative_today():
        r = parse_quick_input("сегодня купить хлеб")
        assert r["day"] == date.today().isoformat()
        assert "сегодня" not in r["text"]

    def test_parse_relative_tomorrow():
        r = parse_quick_input("встреча завтра 15:30")
        assert r["day"] == (date.today() + timedelta(1)).isoformat()
        assert r["time"] == "15:30"
        assert r["text"] == "встреча"

    def test_parse_relative_day_after_tomorrow():
        r = parse_quick_input("послезавтра сдать отчёт")
        assert r["day"] == (date.today() + timedelta(2)).isoformat()
        assert "послезавтра" not in r["text"]

    def test_parse_relative_case_insensitive():
        r = parse_quick_input("ЗАВТРА важно")
        assert r["day"] == (date.today() + timedelta(1)).isoformat()


    # ---------- Weekday ----------

    def test_parse_weekday_pt():
        """Пятница (ближайшая) — D-06 compliant."""
        r = parse_quick_input("заехать на склад пт")
        d = date.fromisoformat(r["day"])
        assert d.weekday() == 4
        assert r["text"] == "заехать на склад"

    def test_parse_weekday_today_returns_today():
        """D-06: если weekday == today.weekday() — возвращаем today (не next week)."""
        today = date.today()
        wd_name = list(_WEEKDAY_MAP.keys())[today.weekday()]  # например 'ср' если сегодня среда
        r = parse_quick_input(f"дело {wd_name}")
        assert r["day"] == today.isoformat()

    def test_parse_weekday_nearest_future():
        """Если день прошёл — берётся следующей недели."""
        today = date.today()
        # Выбираем день который прошёл (yesterday.weekday())
        yesterday_wd = (today.weekday() - 1) % 7
        wd_name = list(_WEEKDAY_MAP.keys())[yesterday_wd]
        r = parse_quick_input(f"встреча {wd_name}")
        parsed = date.fromisoformat(r["day"])
        # Должен быть в будущем (>= today), максимум +6 дней
        delta_days = (parsed - today).days
        assert 0 <= delta_days <= 7
        # Не должно быть today (мы спрашивали про yesterday weekday)
        if yesterday_wd != today.weekday():
            assert parsed != today

    def test_parse_weekday_case_insensitive():
        r = parse_quick_input("работа ПН")
        d = date.fromisoformat(r["day"])
        assert d.weekday() == 0


    # ---------- Mixed patterns ----------

    def test_parse_weekday_and_time():
        r = parse_quick_input("отчёт пт 10:30")
        d = date.fromisoformat(r["day"])
        assert d.weekday() == 4
        assert r["time"] == "10:30"
        assert r["text"] == "отчёт"

    def test_parse_relative_and_time():
        r = parse_quick_input("завтра встреча 14:00")
        assert r["day"] == (date.today() + timedelta(1)).isoformat()
        assert r["time"] == "14:00"
        assert r["text"] == "встреча"

    def test_parse_preposition_and_weekday():
        """'встреча в 9:00 пт' — preposition time + weekday."""
        r = parse_quick_input("встреча в 9:00 пт")
        assert r["time"] == "09:00"
        d = date.fromisoformat(r["day"])
        assert d.weekday() == 4
        assert r["text"] == "встреча"


    # ---------- Fallback (no parse) ----------

    def test_parse_no_match_fallback_today():
        """Ничего не распознано → day=today, time=None, text=original (D-04)."""
        r = parse_quick_input("перезвонить Лене")
        assert r["day"] == date.today().isoformat()
        assert r["time"] is None
        assert r["text"] == "перезвонить Лене"

    def test_parse_empty_text_returns_empty():
        r = parse_quick_input("")
        assert r["text"] == ""
        assert r["day"] == date.today().isoformat()
        assert r["time"] is None

    def test_parse_whitespace_only():
        r = parse_quick_input("   ")
        assert r["text"] == ""
        assert r["day"] == date.today().isoformat()

    def test_parse_strips_extra_whitespace():
        """Лишние пробелы после удаления parse-меток чистятся."""
        r = parse_quick_input("встреча  завтра  14:00")
        assert "  " not in r["text"]
        assert r["text"] == "встреча"


    # ---------- Priority order (algorithm verification) ----------

    def test_parse_priority_time_first():
        """HH:MM first — время '10:30' не попадает в text."""
        r = parse_quick_input("отчёт сегодня 10:30")
        assert r["time"] == "10:30"
        assert "10:30" not in r["text"]

    def test_parse_priority_relative_wins_over_weekday():
        """Если есть 'завтра' и 'пт' — используется relative (завтра), не weekday."""
        r = parse_quick_input("дело завтра пт")
        # Relative найден раньше — day = завтра
        assert r["day"] == (date.today() + timedelta(1)).isoformat()


    # ---------- Cyrillic text preservation ----------

    def test_parse_preserves_cyrillic():
        r = parse_quick_input("Позвонить Иванову Петру Сергеевичу 14:00")
        assert r["text"] == "Позвонить Иванову Петру Сергеевичу"

    def test_parse_cyrillic_punctuation():
        r = parse_quick_input("Купить: молоко, хлеб, яйца")
        assert "молоко" in r["text"]
        assert r["time"] is None


    # ---------- format_date_range_ru ----------

    def test_format_date_range_same_month():
        r = format_date_range_ru(date(2026, 4, 14), date(2026, 4, 20))
        assert r == "14-20 апр"

    def test_format_date_range_cross_month():
        r = format_date_range_ru(date(2026, 4, 28), date(2026, 5, 4))
        assert "28 апр" in r
        assert "4 май" in r

    def test_format_date_range_january():
        r = format_date_range_ru(date(2026, 1, 5), date(2026, 1, 11))
        assert r == "5-11 янв"

    def test_format_date_range_december():
        r = format_date_range_ru(date(2026, 12, 21), date(2026, 12, 27))
        assert r == "21-27 дек"


    # ---------- Data integrity ----------

    def test_weekday_map_has_all_seven():
        """_WEEKDAY_MAP должна содержать все 7 дней."""
        assert set(_WEEKDAY_MAP.keys()) == {"пн", "вт", "ср", "чт", "пт", "сб", "вс"}
        assert set(_WEEKDAY_MAP.values()) == {0, 1, 2, 3, 4, 5, 6}

    def test_month_names_has_thirteen_entries():
        """MONTH_NAMES_RU[0] placeholder + 12 месяцев."""
        assert len(MONTH_NAMES_RU) == 13
        assert MONTH_NAMES_RU[1] == "янв"
        assert MONTH_NAMES_RU[12] == "дек"
    ```
  </action>

  <verify>
    <automated>python -m pytest client/tests/test_quick_parse.py --collect-only -q</automated>
  </verify>

  <acceptance_criteria>
    - File client/tests/test_quick_parse.py exists
    - grep -c "^def test_" client/tests/test_quick_parse.py >= 20
    - pytest --collect-only показывает >= 20 test functions
    - Файл компилируется (нет SyntaxError) — verified через pytest --collect-only
    - Тесты ДОЛЖНЫ падать при запуске (RED: модуль parse_input.py ещё не существует) — это ожидаемо для TDD
  </acceptance_criteria>

  <done>
    RED фаза TDD: тесты написаны, модуль ещё не существует. Task 2 будет реализовать parse_input.py
    и тесты должны стать зелёными (GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Реализовать client/ui/parse_input.py — regex-based smart parse (GREEN)</name>
  <files>client/ui/parse_input.py</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pattern 2 (полный код шаблон Smart Parse RU)
    - .planning/phases/04-week-tasks/04-UI-SPEC.md §Smart Parse (algorithm priority)
    - .planning/phases/04-week-tasks/04-CONTEXT.md D-04, D-06
    - client/tests/test_quick_parse.py (только что написанный — что должно пройти)
  </read_first>

  <behavior>
    - GREEN: все тесты из Task 1 проходят
    - Algorithm order (D-04): preposition-time → time → relative → weekday → fallback
    - D-06: (target_wd - current_wd) % 7 — если 0 → today
    - format_date_range_ru: same-month = "D1-D2 мес", cross-month = "D1 мес1 - D2 мес2"
  </behavior>

  <action>
    Создать `client/ui/parse_input.py`:

    ```python
    """
    Smart parse quick-capture text → {text, day, time}.

    Pure Python — regex-based, zero dependencies. Используется QuickCapturePopup
    (Plan 04-06) для мгновенного parsing ввода пользователя без открытия полного
    edit dialog.

    Algorithm priority (D-04):
      1. Preposition-time: "в HH:MM" → time + remove "в"
      2. Plain HH:MM: anywhere in text
      3. Relative day: сегодня / завтра / послезавтра
      4. Weekday (только если relative не найден): пн/вт/ср/чт/пт/сб/вс
      5. Fallback: day=today, time=None

    D-06 compliance: если weekday в тексте совпадает с today.weekday() —
    возвращаем today (не next week).

    Использование:
        result = parse_quick_input("встреча завтра 15:30")
        # {"text": "встреча", "day": "2026-04-18", "time": "15:30"}

    Также экспортирует format_date_range_ru(monday, sunday) — используется
    Plan 04-05 (week navigation header) для "Неделя 16 • 14-20 апр".
    """
    from __future__ import annotations

    import re
    from datetime import date, timedelta
    from typing import Optional, TypedDict

    # ==== Regex patterns (verbatim из 04-UI-SPEC §Smart Parse) ====
    # HH:MM — 1-2 digit hours, 2 digit minutes, surrounded by whitespace/boundary
    _RE_TIME = re.compile(r'(?:^|\s)(\d{1,2}:\d{2})(?:\s|$)')

    # Preposition "в" before time — удаляется вместе со временем
    _RE_PREPOSITION_TIME = re.compile(r'\bв\s+(\d{1,2}:\d{2})\b', re.IGNORECASE)

    # Relative day keywords
    _RE_RELATIVE = re.compile(r'\b(сегодня|завтра|послезавтра)\b', re.IGNORECASE)

    # ==== Weekday mapping (Пн=0..Вс=6 per Python datetime convention) ====
    _WEEKDAY_MAP: dict[str, int] = {
        "пн": 0, "вт": 1, "ср": 2, "чт": 3, "пт": 4, "сб": 5, "вс": 6,
    }

    # ==== Russian month names (1-indexed; [0] is placeholder) ====
    MONTH_NAMES_RU: list[str] = [
        '', 'янв', 'фев', 'мар', 'апр', 'май', 'июн',
        'июл', 'авг', 'сен', 'окт', 'ноя', 'дек',
    ]


    class ParseResult(TypedDict):
        """Результат parse_quick_input."""
        text: str
        day: str            # ISO "YYYY-MM-DD"
        time: Optional[str] # "HH:MM" или None


    def parse_quick_input(raw: str) -> ParseResult:
        """Извлечь time/day/text из произвольного русского текста.

        D-04 algorithm priority: preposition-time → time → relative → weekday → fallback.

        Args:
            raw: Текст пользователя из quick-capture input.

        Returns:
            {"text": str, "day": "YYYY-MM-DD", "time": "HH:MM" | None}
        """
        text = raw.strip()
        today = date.today()
        extracted_day: Optional[date] = None
        extracted_time: Optional[str] = None

        # 1. Preposition-time: "в HH:MM" → удаляем вместе с "в"
        m = _RE_PREPOSITION_TIME.search(text)
        if m:
            extracted_time = m.group(1)
            text = text[:m.start()] + text[m.end():]
        else:
            # 2. Plain HH:MM (wrapped with whitespace/boundary)
            m = _RE_TIME.search(text)
            if m:
                extracted_time = m.group(1)
                # Удаляем только captured group из original text
                start = m.start(1)
                end = m.end(1)
                text = text[:start] + text[end:]

        # Нормализация времени: '9:00' → '09:00'
        if extracted_time:
            parts = extracted_time.split(":")
            extracted_time = f"{int(parts[0]):02d}:{parts[1]}"

        # 3. Relative day
        m = _RE_RELATIVE.search(text)
        if m:
            kw = m.group(1).lower()
            if kw == 'сегодня':
                extracted_day = today
            elif kw == 'завтра':
                extracted_day = today + timedelta(days=1)
            elif kw == 'послезавтра':
                extracted_day = today + timedelta(days=2)
            text = text[:m.start()] + text[m.end():]

        # 4. Weekday (только если relative не найден)
        if extracted_day is None:
            pattern = r'\b(' + '|'.join(_WEEKDAY_MAP.keys()) + r')\b'
            m = re.search(pattern, text, re.IGNORECASE)
            if m:
                target_wd = _WEEKDAY_MAP[m.group(1).lower()]
                current_wd = today.weekday()
                # D-06: если weekday == today → today (delta=0); иначе next occurrence
                delta = (target_wd - current_wd) % 7
                extracted_day = today + timedelta(days=delta)
                text = text[:m.start()] + text[m.end():]

        # 5. Fallback
        if extracted_day is None:
            extracted_day = today

        # Очистить множественные пробелы и trim
        text = ' '.join(text.split())

        return {
            "text": text,
            "day": extracted_day.isoformat(),
            "time": extracted_time,
        }


    def format_date_range_ru(monday: date, sunday: date) -> str:
        """Форматирует диапазон недели для header navigation.

        Примеры:
            monday=2026-04-14, sunday=2026-04-20 → "14-20 апр"
            monday=2026-04-28, sunday=2026-05-04 → "28 апр - 4 май"

        Args:
            monday: Понедельник недели.
            sunday: Воскресенье недели.

        Returns:
            Русская строка-представление диапазона.
        """
        if monday.month == sunday.month:
            return f"{monday.day}-{sunday.day} {MONTH_NAMES_RU[monday.month]}"
        return (
            f"{monday.day} {MONTH_NAMES_RU[monday.month]} - "
            f"{sunday.day} {MONTH_NAMES_RU[sunday.month]}"
        )
    ```
  </action>

  <verify>
    <automated>python -m pytest client/tests/test_quick_parse.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/parse_input.py exists
    - grep "def parse_quick_input" client/ui/parse_input.py >= 1
    - grep "def format_date_range_ru" client/ui/parse_input.py >= 1
    - grep "_RE_TIME" client/ui/parse_input.py >= 1
    - grep "_WEEKDAY_MAP" client/ui/parse_input.py >= 1
    - grep "_RE_RELATIVE" client/ui/parse_input.py >= 1
    - grep "MONTH_NAMES_RU" client/ui/parse_input.py >= 1
    - grep "(target_wd - current_wd) % 7" client/ui/parse_input.py >= 1 (D-06 explicit)
    - pytest client/tests/test_quick_parse.py — все 20+ тестов зелёные
    - File size >= 80 lines (min_lines check)
  </acceptance_criteria>

  <done>
    GREEN TDD: parse_input.py реализован, все 20+ тестов из Task 1 зелёные. TASK-01 parse-часть
    покрыта. Модуль готов к импорту в Plan 04-06 (QuickCapturePopup) и Plan 04-05 (week navigation
    через format_date_range_ru).
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/test_quick_parse.py: 20+ тестов зелёные
- grep markers: _RE_TIME, _WEEKDAY_MAP, _RE_RELATIVE, MONTH_NAMES_RU присутствуют
- D-06 markers: (target_wd - current_wd) % 7
- No Tkinter imports в parse_input.py (pure Python)
</verification>

<success_criteria>
- TASK-01 parse-часть покрыта (quick-capture popup использует parse_quick_input)
- format_date_range_ru готов для Plan 04-05 week navigation header
- 20+ unit тестов покрывают все locked D-04 patterns
- Zero dependencies (stdlib only)
</success_criteria>

<output>
After completion, create `.planning/phases/04-week-tasks/04-02-SUMMARY.md`
</output>
