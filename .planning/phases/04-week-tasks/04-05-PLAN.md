---
phase: 04-week-tasks
plan: 05
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - client/ui/week_navigation.py
  - client/tests/ui/test_week_navigation.py
autonomous: true
requirements:
  - WEEK-02
  - WEEK-03
  - WEEK-06
must_haves:
  truths:
    - "WeekNavigation умеет вычислять _week_monday текущего просмотра (по умолчанию = current_week_monday)"
    - "prev_week() / next_week() сдвигают _week_monday на ±7 дней и вызывают on_week_changed(new_monday) callback"
    - "today() сбрасывает _week_monday к current_week_monday и вызывает on_week_changed"
    - "'Сегодня' button видна только когда _week_monday != current_week_monday (WEEK-03)"
    - "header_text содержит 'Неделя N • DD-DD мес' через format_date_range_ru (D-29)"
    - "is_archive_week() → True когда _week_monday < current_week_monday (D-31)"
    - "Archive banner '📦 Архив • неделя N • Вернуться →' отображается при архивной неделе"
    - "Keyboard shortcuts: Ctrl+Left/Right = prev/next, Ctrl+T = today (D-30)"
    - "Dim effect через color interpolation (не real opacity — CTk не поддерживает)"
  artifacts:
    - path: "client/ui/week_navigation.py"
      provides: "WeekNavigation + archive detection + keyboard bindings + interpolate_palette helper"
      contains: "class WeekNavigation"
      min_lines: 200
    - path: "client/tests/ui/test_week_navigation.py"
      provides: "15+ unit тестов — nav, today button, archive, keyboard, ISO week"
      contains: "def test_nav_arrows_change_week"
      min_lines: 150
  key_links:
    - from: "client/ui/week_navigation.py format_week_header"
      to: "client/ui/parse_input.py format_date_range_ru"
      via: "import format_date_range_ru (Plan 04-02)"
      pattern: "format_date_range_ru"
    - from: "client/ui/week_navigation.py ISO week"
      to: "date.isocalendar()[1]"
      via: "stdlib datetime"
      pattern: "isocalendar"
    - from: "client/ui/week_navigation.py archive dim"
      to: "color interpolation (bg blend)"
      via: "interpolate_palette(palette, bg, factor=0.5)"
      pattern: "interpolate_palette"
---

<objective>
WeekNavigation — контроллер навигации по неделям: header с ◀/▶/Сегодня buttons, ISO week
calculation, archive detection, keyboard shortcuts, dim palette для прошлых недель.

Purpose: WEEK-02 (стрелки ±неделя), WEEK-03 ("Сегодня" button), WEEK-06 (archive через нав).
Без этого — пользователь залочен на текущей неделе. Архив — read-only визуализация (dim 0.7
через color interpolation — D-31 workaround из 04-RESEARCH Pattern 6).

Изолированный модуль: WeekNavigation не содержит логику рендеринга секций (это
MainWindow/DaySection в Plan 04-10). Только: state (_week_monday), computed properties
(is_archive, header_text), callbacks (on_week_changed), UI header frame с buttons.

Также экспортирует вспомогательные функции: `get_week_monday(d)`, `get_iso_week_number(d)`,
`interpolate_palette(palette, bg, factor)` — используются MainWindow (Plan 04-10) и
DaySection (Plan 04-04 set_archive_mode).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-week-tasks/04-UI-SPEC.md
@.planning/phases/04-week-tasks/04-RESEARCH.md
@.planning/phases/04-week-tasks/04-CONTEXT.md
@client/ui/parse_input.py
@client/ui/themes.py

<interfaces>
<!-- Dependencies (Plan 04-02) -->
From client/ui/parse_input.py:
```python
def format_date_range_ru(monday: date, sunday: date) -> str: ...
```

From client/ui/themes.py:
```python
PALETTES: dict[str, dict[str, str]]
class ThemeManager:
    def subscribe(self, callback): ...
    def get(self, key: str) -> str: ...
```

<!-- New interface this plan creates -->
Target API for client/ui/week_navigation.py:
```python
from datetime import date
from typing import Callable, Optional
import customtkinter as ctk
from client.ui.themes import ThemeManager


def get_week_monday(d: date) -> date:
    """Понедельник недели (Python weekday Пн=0)."""

def get_current_week_monday() -> date:
    """get_week_monday(date.today())."""

def get_iso_week_number(d: date) -> int:
    """ISO week number (1-53)."""

def is_archive_week(week_monday: date) -> bool:
    """True если week_monday < current_week_monday."""

def format_week_header(week_monday: date) -> str:
    """'Неделя 16 • 14-20 апр'."""

def interpolate_palette(palette: dict, bg_hex: str, factor: float) -> dict:
    """Blend каждый hex цвет с bg на factor (0.5 = 50% towards bg = dim effect)."""


class WeekNavigation:
    """Header нав недели + state _week_monday + callbacks + keyboard."""

    def __init__(
        self,
        parent: ctk.CTkBaseClass,
        window: ctk.CTkToplevel,  # root для <Control-Left> bindings
        theme_manager: ThemeManager,
        on_week_changed: Callable[[date], None],  # (new_week_monday)
        on_archive_changed: Callable[[bool], None],  # (is_archive_now)
    ) -> None: ...

    # Public API
    def get_week_monday(self) -> date: ...
    def set_week_monday(self, monday: date) -> None: ...
    def prev_week(self) -> None: ...
    def next_week(self) -> None: ...
    def today(self) -> None: ...
    def is_current_archive(self) -> bool: ...

    # UI
    def pack(self, **kwargs) -> None: ...
    def destroy(self) -> None: ...
```

<!-- D-29/30/31 locked -->
- Header: "◀ Неделя N • DD-DD мес • Сегодня ▶"
- "Сегодня" button visible ONLY когда _week_monday != current (D-29)
- Ctrl+Left/Right/T (D-30)
- Archive: opacity 0.7 эмулируется через palette interpolation (PALETTES blend)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать client/ui/week_navigation.py — WeekNavigation + helpers + archive detection</name>
  <files>client/ui/week_navigation.py, client/tests/ui/test_week_navigation.py</files>

  <read_first>
    - .planning/phases/04-week-tasks/04-UI-SPEC.md §Header — Navigation (D-29), §Archive (D-31), §Focus & Keyboard (D-30)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Pattern 6 (Week Navigation + Archive, полный код)
    - .planning/phases/04-week-tasks/04-RESEARCH.md §Archive dim via color interpolation
    - .planning/phases/04-week-tasks/04-CONTEXT.md D-29, D-30, D-31, D-32
    - client/ui/parse_input.py (format_date_range_ru)
    - client/ui/themes.py (PALETTES, ThemeManager)
  </read_first>

  <behavior>
    - Test 1: get_week_monday(date(2026, 4, 15)) → date(2026, 4, 13) (понедельник)
    - Test 2: get_iso_week_number(date(2026, 4, 14)) → int (ISO week 16 для этой даты)
    - Test 3: is_archive_week(monday в прошлом) → True
    - Test 4: is_archive_week(current_monday) → False
    - Test 5: is_archive_week(monday в будущем) → False (D-32: future = active)
    - Test 6: format_week_header(date(2026, 4, 13)) → содержит "Неделя" + номер + диапазон
    - Test 7: WeekNavigation.__init__ по умолчанию _week_monday = current_week_monday
    - Test 8: prev_week() → _week_monday сдвигается на -7 дней + on_week_changed вызван
    - Test 9: next_week() → _week_monday сдвигается на +7 дней + on_week_changed вызван
    - Test 10: today() → _week_monday = current_week_monday
    - Test 11: "Сегодня" button hidden при создании (current week) — на current _week_monday button.winfo_ismapped() = False
    - Test 12: После prev_week() → "Сегодня" button visible (WEEK-03)
    - Test 13: После today() → "Сегодня" button снова hidden
    - Test 14: Archive detection: prev_week() → is_current_archive() = True, on_archive_changed(True) вызван
    - Test 15: next_week() (будущее) → is_current_archive() = False
    - Test 16: Keyboard Ctrl+Left → prev_week вызван (через event_generate or direct call)
    - Test 17: Keyboard Ctrl+Right → next_week вызван
    - Test 18: Keyboard Ctrl+T → today вызван
    - Test 19: interpolate_palette({...}, "#000000", 0.5) → каждый hex цвет блендится 50% с #000000
    - Test 20: interpolate_palette сохраняет не-hex ключи (например "shadow_card" = "rgba(...)") без изменений
    - Test 21: Header label содержит "Неделя N" + date range (format_week_header)
  </behavior>

  <action>
    Создать `client/ui/week_navigation.py`:

    ```python
    """
    WeekNavigation — контроллер навигации недели. Phase 4.

    Реализует:
      - Header: "◀ Неделя N • DD-DD мес • Сегодня ▶" (D-29)
      - Prev/next/today callbacks (WEEK-02, WEEK-03)
      - Archive detection: _week_monday < current_monday → is_archive_week (WEEK-06, D-31)
      - Keyboard shortcuts: Ctrl+Left/Right/T (D-30)
      - interpolate_palette helper для archive dim effect (04-RESEARCH Pattern 6 §Archive dim)

    Purpose: полная WEEK-навигационная логика в одном модуле. MainWindow (Plan 04-10)
    использует этот модуль для управления видимой неделей. DaySection (Plan 04-04)
    принимает is_archive через set_archive_mode().

    D-32: future weeks — full functionality (только past = archive, dim).
    """
    from __future__ import annotations

    import logging
    import tkinter as tk
    from datetime import date, timedelta
    from typing import Callable, Optional

    import customtkinter as ctk

    from client.ui.parse_input import format_date_range_ru
    from client.ui.themes import FONTS, ThemeManager

    logger = logging.getLogger(__name__)


    # ==== Public helpers (used by MainWindow Plan 04-10 and DaySection Plan 04-04) ====

    def get_week_monday(d: date) -> date:
        """Понедельник недели для данной даты. Python weekday: Пн=0..Вс=6."""
        return d - timedelta(days=d.weekday())


    def get_current_week_monday() -> date:
        """Понедельник текущей недели."""
        return get_week_monday(date.today())


    def get_iso_week_number(d: date) -> int:
        """ISO week number (1-53)."""
        return d.isocalendar()[1]


    def is_archive_week(week_monday: date) -> bool:
        """D-31: week_monday < current_monday → архив.

        D-32: current_monday и future NE архив.
        """
        return week_monday < get_current_week_monday()


    def format_week_header(week_monday: date) -> str:
        """'Неделя 16 • 14-20 апр' (D-29)."""
        week_num = get_iso_week_number(week_monday)
        sunday = week_monday + timedelta(days=6)
        return f"Неделя {week_num} • {format_date_range_ru(week_monday, sunday)}"


    def interpolate_palette(palette: dict, bg_hex: str, factor: float) -> dict:
        """Blend каждый hex цвет с bg_hex на factor (0.0..1.0).

        factor=0.0 → palette без изменений
        factor=0.5 → 50% blend с bg (dim effect для archive)
        factor=1.0 → все цвета = bg (monochrome)

        Не-hex значения (rgba(...) для shadow_card) — оставляются без изменений.
        """
        def parse_hex(h: str) -> tuple[int, int, int]:
            h = h.lstrip("#")
            if len(h) != 6:
                return (128, 128, 128)
            try:
                return int(h[0:2], 16), int(h[2:4], 16), int(h[4:6], 16)
            except ValueError:
                return (128, 128, 128)

        def blend(c1: str, c2: str, t: float) -> str:
            r1, g1, b1 = parse_hex(c1)
            r2, g2, b2 = parse_hex(c2)
            r = int(r1 + (r2 - r1) * t)
            g = int(g1 + (g2 - g1) * t)
            b = int(b1 + (b2 - b1) * t)
            return f"#{r:02x}{g:02x}{b:02x}"

        result: dict = {}
        for key, color in palette.items():
            if not isinstance(color, str) or not color.startswith("#") or len(color) != 7:
                # rgba(...) или non-hex — не трогаем
                result[key] = color
            else:
                result[key] = blend(color, bg_hex, factor)
        return result


    # ==== Widget class ====

    class WeekNavigation:
        """Header navigation: ◀ Неделя N • DD-DD мес • Сегодня ▶ (D-29).

        Также управляет keyboard shortcuts (D-30) и archive detection (WEEK-06).
        """

        ARROW_WIDTH = 32
        TODAY_BTN_WIDTH = 80

        def __init__(
            self,
            parent: ctk.CTkBaseClass,
            window: ctk.CTkToplevel,
            theme_manager: ThemeManager,
            on_week_changed: Callable[[date], None],
            on_archive_changed: Callable[[bool], None],
        ) -> None:
            self._parent = parent
            self._window = window
            self._theme = theme_manager
            self._on_week_changed = on_week_changed
            self._on_archive_changed = on_archive_changed
            self._destroyed = False

            self._week_monday: date = get_current_week_monday()
            self._last_is_archive: bool = False

            # Widgets
            self._header_frame: Optional[ctk.CTkFrame] = None
            self._prev_btn: Optional[ctk.CTkButton] = None
            self._next_btn: Optional[ctk.CTkButton] = None
            self._today_btn: Optional[ctk.CTkButton] = None
            self._header_label: Optional[ctk.CTkLabel] = None
            self._archive_banner: Optional[ctk.CTkFrame] = None

            self._build()
            self._bind_keyboard()
            self._theme.subscribe(self._apply_theme)
            self._update_header()

        # ---- Public API ----

        def pack(self, **kwargs) -> None:
            if self._header_frame is not None:
                self._header_frame.pack(**kwargs)

        def get_week_monday(self) -> date:
            return self._week_monday

        def set_week_monday(self, monday: date) -> None:
            self._week_monday = get_week_monday(monday)
            self._update_header()
            self._notify_changes()

        def prev_week(self) -> None:
            """WEEK-02: сдвиг на -7 дней."""
            self._week_monday = self._week_monday - timedelta(days=7)
            self._update_header()
            self._notify_changes()

        def next_week(self) -> None:
            """WEEK-02: сдвиг на +7 дней."""
            self._week_monday = self._week_monday + timedelta(days=7)
            self._update_header()
            self._notify_changes()

        def today(self) -> None:
            """WEEK-03: возврат к current week."""
            self._week_monday = get_current_week_monday()
            self._update_header()
            self._notify_changes()

        def is_current_archive(self) -> bool:
            return is_archive_week(self._week_monday)

        def destroy(self) -> None:
            self._destroyed = True
            try:
                if self._header_frame is not None:
                    self._header_frame.destroy()
            except Exception:
                pass

        # ---- Build ----

        def _build(self) -> None:
            """Построить header UI + archive banner (initially hidden)."""
            self._header_frame = ctk.CTkFrame(
                self._parent, fg_color=self._theme.get("bg_primary"), corner_radius=0,
            )

            # Row с ◀ Неделя ... ▶ Сегодня
            row = ctk.CTkFrame(self._header_frame, fg_color="transparent", height=40)
            row.pack(fill="x")
            row.pack_propagate(False)

            self._prev_btn = ctk.CTkButton(
                row, text="◀", width=self.ARROW_WIDTH, command=self.prev_week,
            )
            self._prev_btn.pack(side="left", padx=4, pady=4)

            self._header_label = ctk.CTkLabel(
                row, text="Неделя —", font=FONTS["h1"],
            )
            self._header_label.pack(side="left", expand=True)

            self._today_btn = ctk.CTkButton(
                row, text="Сегодня", width=self.TODAY_BTN_WIDTH, command=self.today,
            )
            # Не pack по default — виден только для не-current

            self._next_btn = ctk.CTkButton(
                row, text="▶", width=self.ARROW_WIDTH, command=self.next_week,
            )
            self._next_btn.pack(side="right", padx=4, pady=4)

            # Archive banner (D-31, initially hidden)
            self._archive_banner = ctk.CTkFrame(
                self._header_frame,
                fg_color=self._theme.get("bg_tertiary"),
                corner_radius=0,
                height=32,
            )
            ctk.CTkLabel(
                self._archive_banner,
                text="📦 Архив",
                font=FONTS["caption"],
            ).pack(side="left", padx=8, pady=6)
            ctk.CTkLabel(
                self._archive_banner,
                text="Вернуться →",
                font=FONTS["caption"],
                text_color=self._theme.get("accent_brand"),
                cursor="hand2",
            ).pack(side="right", padx=8)
            # Click на "Вернуться" → today
            for child in self._archive_banner.winfo_children():
                try:
                    child.bind("<Button-1>", lambda e: self.today())
                except tk.TclError:
                    pass

        def _bind_keyboard(self) -> None:
            """D-30: Ctrl+Left/Right/T keyboard shortcuts."""
            self._window.bind("<Control-Left>", lambda e: self.prev_week(), add="+")
            self._window.bind("<Control-Right>", lambda e: self.next_week(), add="+")
            self._window.bind("<Control-t>", lambda e: self.today(), add="+")
            self._window.bind("<Control-T>", lambda e: self.today(), add="+")

        # ---- Header updates ----

        def _update_header(self) -> None:
            """Обновить header_label + visibility today_btn + archive banner."""
            if self._destroyed:
                return
            if self._header_label and self._header_label.winfo_exists():
                try:
                    self._header_label.configure(text=format_week_header(self._week_monday))
                except tk.TclError:
                    pass
            self._update_today_button_visibility()
            self._update_archive_banner()

        def _update_today_button_visibility(self) -> None:
            """WEEK-03: 'Сегодня' button visible только для не-current."""
            if self._today_btn is None:
                return
            is_current = self._week_monday == get_current_week_monday()
            try:
                if is_current:
                    self._today_btn.pack_forget()
                else:
                    # Показываем перед next_btn
                    self._today_btn.pack(side="right", padx=4, pady=4, before=self._next_btn)
            except tk.TclError:
                pass

        def _update_archive_banner(self) -> None:
            """Archive banner visible только для архивных недель (D-31)."""
            if self._archive_banner is None:
                return
            is_archive = self.is_current_archive()
            try:
                if is_archive:
                    self._archive_banner.pack(fill="x")
                else:
                    self._archive_banner.pack_forget()
            except tk.TclError:
                pass

        def _notify_changes(self) -> None:
            """Вызвать callbacks on_week_changed + on_archive_changed."""
            try:
                self._on_week_changed(self._week_monday)
            except Exception as exc:
                logger.error("on_week_changed failed: %s", exc)

            is_archive_now = self.is_current_archive()
            if is_archive_now != self._last_is_archive:
                self._last_is_archive = is_archive_now
                try:
                    self._on_archive_changed(is_archive_now)
                except Exception as exc:
                    logger.error("on_archive_changed failed: %s", exc)

        # ---- Theme ----

        def _apply_theme(self, palette: dict) -> None:
            if self._destroyed:
                return
            try:
                if self._header_frame and self._header_frame.winfo_exists():
                    self._header_frame.configure(fg_color=palette.get("bg_primary"))
                if self._archive_banner and self._archive_banner.winfo_exists():
                    self._archive_banner.configure(fg_color=palette.get("bg_tertiary"))
            except tk.TclError:
                pass
    ```

    Создать `client/tests/ui/test_week_navigation.py`:

    ```python
    """Unit-тесты WeekNavigation (Plan 04-05).

    Покрывает:
      WEEK-02: стрелки prev/next
      WEEK-03: "Сегодня" button visibility
      WEEK-06: archive detection (past weeks)
    """
    from datetime import date, timedelta
    from unittest.mock import MagicMock

    import pytest

    from client.ui.week_navigation import (
        WeekNavigation,
        get_week_monday,
        get_current_week_monday,
        get_iso_week_number,
        is_archive_week,
        format_week_header,
        interpolate_palette,
    )


    # ---------- Helpers ----------

    def test_get_week_monday():
        # Вторник 2026-04-14 → понедельник 2026-04-13
        tuesday = date(2026, 4, 14)
        assert get_week_monday(tuesday) == date(2026, 4, 13)

    def test_get_week_monday_for_sunday():
        # Воскресенье 2026-04-12 → понедельник 2026-04-06
        sunday = date(2026, 4, 12)
        assert get_week_monday(sunday) == date(2026, 4, 6)

    def test_get_iso_week_number():
        # Неделя 16 ISO — содержит 2026-04-14
        assert get_iso_week_number(date(2026, 4, 14)) > 0
        assert isinstance(get_iso_week_number(date(2026, 4, 14)), int)

    def test_is_archive_week_past():
        past_monday = get_current_week_monday() - timedelta(days=7)
        assert is_archive_week(past_monday) is True

    def test_is_archive_week_current():
        """Current week — НЕ archive."""
        assert is_archive_week(get_current_week_monday()) is False

    def test_is_archive_week_future():
        """D-32: future = not archive."""
        future_monday = get_current_week_monday() + timedelta(days=7)
        assert is_archive_week(future_monday) is False

    def test_format_week_header_contains_week_number():
        header = format_week_header(date(2026, 4, 13))
        assert "Неделя" in header
        assert "•" in header

    def test_format_week_header_contains_date_range():
        header = format_week_header(date(2026, 4, 13))
        # Ожидаем "13-19 апр" или похожий diapason
        assert "апр" in header or "май" in header


    # ---------- interpolate_palette ----------

    def test_interpolate_palette_factor_zero():
        """factor=0.0 → без изменений."""
        pal = {"bg_primary": "#F5EFE6", "accent_brand": "#1E73E8"}
        r = interpolate_palette(pal, "#000000", 0.0)
        assert r["bg_primary"] == "#f5efe6"  # normalization через blend даст тот же
        # Allow for normalization
        assert r["accent_brand"].lower() == "#1e73e8"

    def test_interpolate_palette_factor_half():
        """factor=0.5 → цвет блендится 50% с bg."""
        pal = {"c": "#ffffff"}
        r = interpolate_palette(pal, "#000000", 0.5)
        # #ffffff blend с #000000 на 50% = #7f7f7f (int(255 * 0.5) = 127)
        assert r["c"] == "#7f7f7f"

    def test_interpolate_palette_preserves_non_hex():
        """shadow_card = 'rgba(...)' не hex — не трогается."""
        pal = {"bg": "#ffffff", "shadow_card": "rgba(0, 0, 0, 0.3)"}
        r = interpolate_palette(pal, "#000000", 0.5)
        assert r["shadow_card"] == "rgba(0, 0, 0, 0.3)"

    def test_interpolate_palette_invalid_hex_unchanged():
        pal = {"broken": "#xyz", "ok": "#ff0000"}
        r = interpolate_palette(pal, "#000000", 0.5)
        # broken может остаться как есть или перейти через parse_hex fallback
        assert r["ok"] != pal["ok"]  # ok-цвет изменился


    # ---------- WeekNavigation class ----------

    @pytest.fixture
    def wn_deps(headless_tk, mock_theme_manager):
        return {
            "parent": headless_tk,
            "window": headless_tk,
            "theme": mock_theme_manager,
            "on_week_changed": MagicMock(),
            "on_archive_changed": MagicMock(),
        }


    def _make(wn_deps):
        return WeekNavigation(
            wn_deps["parent"], wn_deps["window"], wn_deps["theme"],
            wn_deps["on_week_changed"], wn_deps["on_archive_changed"],
        )


    def test_initial_week_monday_is_current(wn_deps):
        wn = _make(wn_deps)
        assert wn.get_week_monday() == get_current_week_monday()
        wn.destroy()


    def test_prev_week_shifts_minus_seven(wn_deps):
        wn = _make(wn_deps)
        initial = wn.get_week_monday()
        wn.prev_week()
        assert wn.get_week_monday() == initial - timedelta(days=7)
        wn_deps["on_week_changed"].assert_called_with(wn.get_week_monday())
        wn.destroy()


    def test_next_week_shifts_plus_seven(wn_deps):
        wn = _make(wn_deps)
        initial = wn.get_week_monday()
        wn.next_week()
        assert wn.get_week_monday() == initial + timedelta(days=7)
        wn.destroy()


    def test_today_resets_to_current(wn_deps):
        wn = _make(wn_deps)
        wn.prev_week()
        wn.prev_week()
        wn.today()
        assert wn.get_week_monday() == get_current_week_monday()
        wn.destroy()


    def test_today_button_hidden_on_current(wn_deps):
        """WEEK-03: 'Сегодня' button hidden когда на current week."""
        wn = _make(wn_deps)
        wn_deps["window"].update_idletasks()
        assert not wn._today_btn.winfo_ismapped()
        wn.destroy()


    def test_today_button_visible_after_prev(wn_deps):
        """WEEK-03: после prev_week — "Сегодня" button появляется."""
        wn = _make(wn_deps)
        wn.prev_week()
        wn_deps["window"].update_idletasks()
        assert wn._today_btn.winfo_ismapped()
        wn.destroy()


    def test_today_button_hidden_after_today(wn_deps):
        wn = _make(wn_deps)
        wn.prev_week()
        wn.today()
        wn_deps["window"].update_idletasks()
        assert not wn._today_btn.winfo_ismapped()
        wn.destroy()


    def test_is_current_archive_past(wn_deps):
        """WEEK-06: prev_week → is_current_archive = True."""
        wn = _make(wn_deps)
        wn.prev_week()
        assert wn.is_current_archive() is True
        wn.destroy()


    def test_is_current_archive_future(wn_deps):
        """D-32: future week не archive."""
        wn = _make(wn_deps)
        wn.next_week()
        assert wn.is_current_archive() is False
        wn.destroy()


    def test_on_archive_changed_called_when_enter_archive(wn_deps):
        wn = _make(wn_deps)
        wn_deps["on_archive_changed"].reset_mock()
        wn.prev_week()
        wn_deps["on_archive_changed"].assert_called_with(True)
        wn.destroy()


    def test_on_archive_changed_called_when_leave_archive(wn_deps):
        wn = _make(wn_deps)
        wn.prev_week()
        wn_deps["on_archive_changed"].reset_mock()
        wn.today()
        wn_deps["on_archive_changed"].assert_called_with(False)
        wn.destroy()


    def test_on_archive_changed_not_called_twice(wn_deps):
        """Оптимизация: callback не вызывается если state не изменился."""
        wn = _make(wn_deps)
        wn.prev_week()
        wn_deps["on_archive_changed"].reset_mock()
        wn.prev_week()  # остаёмся в archive
        wn_deps["on_archive_changed"].assert_not_called()
        wn.destroy()


    def test_header_label_updates_on_nav(wn_deps):
        wn = _make(wn_deps)
        original_text = wn._header_label.cget("text")
        wn.prev_week()
        new_text = wn._header_label.cget("text")
        assert new_text != original_text
        wn.destroy()


    def test_archive_banner_visible_in_archive(wn_deps):
        wn = _make(wn_deps)
        wn.prev_week()
        wn_deps["window"].update_idletasks()
        assert wn._archive_banner.winfo_ismapped()
        wn.destroy()


    def test_archive_banner_hidden_in_current(wn_deps):
        wn = _make(wn_deps)
        wn_deps["window"].update_idletasks()
        assert not wn._archive_banner.winfo_ismapped()
        wn.destroy()


    # ---------- Keyboard shortcuts (D-30) ----------

    def test_keyboard_ctrl_left_calls_prev(wn_deps):
        """Ctrl+Left → prev_week."""
        wn = _make(wn_deps)
        initial = wn.get_week_monday()
        # Симулируем — event_generate
        wn_deps["window"].event_generate("<Control-Left>", when="now")
        wn_deps["window"].update()
        assert wn.get_week_monday() == initial - timedelta(days=7)
        wn.destroy()

    def test_keyboard_ctrl_right_calls_next(wn_deps):
        wn = _make(wn_deps)
        initial = wn.get_week_monday()
        wn_deps["window"].event_generate("<Control-Right>", when="now")
        wn_deps["window"].update()
        assert wn.get_week_monday() == initial + timedelta(days=7)
        wn.destroy()

    def test_keyboard_ctrl_t_calls_today(wn_deps):
        wn = _make(wn_deps)
        wn.prev_week()
        wn_deps["window"].event_generate("<Control-t>", when="now")
        wn_deps["window"].update()
        assert wn.get_week_monday() == get_current_week_monday()
        wn.destroy()
    ```
  </action>

  <verify>
    <automated>python -m pytest client/tests/ui/test_week_navigation.py -x -q --timeout=30</automated>
  </verify>

  <acceptance_criteria>
    - File client/ui/week_navigation.py exists
    - grep "class WeekNavigation" client/ui/week_navigation.py >= 1
    - grep "def get_week_monday" client/ui/week_navigation.py >= 1
    - grep "def is_archive_week" client/ui/week_navigation.py >= 1
    - grep "def format_week_header" client/ui/week_navigation.py >= 1
    - grep "def interpolate_palette" client/ui/week_navigation.py >= 1
    - grep "isocalendar" client/ui/week_navigation.py >= 1
    - grep "Control-Left\|Control-Right\|Control-t" client/ui/week_navigation.py >= 3 (D-30)
    - grep "from client.ui.parse_input import format_date_range_ru" client/ui/week_navigation.py >= 1
    - grep "archive_banner\|📦 Архив" client/ui/week_navigation.py >= 1 (D-31)
    - pytest client/tests/ui/test_week_navigation.py >= 18 тестов зелёных
    - File size >= 200 lines
  </acceptance_criteria>

  <done>
    WeekNavigation готов. WEEK-02 (nav), WEEK-03 (today button), WEEK-06 (archive banner) покрыты.
    Plan 04-10 (integration) wire'нет on_week_changed → main_window.refresh_day_sections(),
    on_archive_changed → каждая DaySection.set_archive_mode(is_archive).
    interpolate_palette — helper для archive dim effect.
  </done>
</task>

</tasks>

<verification>
- pytest client/tests/ui/test_week_navigation.py: 18+ тестов зелёных
- grep markers: isocalendar, Control-Left/Right/t, format_date_range_ru import
- Keyboard shortcuts работают через event_generate
- interpolate_palette корректно блендит hex цвета
</verification>

<success_criteria>
- WEEK-02, WEEK-03, WEEK-06 покрыты unit tests
- is_archive_week, interpolate_palette, format_week_header готовы к импорту из MainWindow
- Keyboard shortcuts D-30 зафиксированы
- on_week_changed / on_archive_changed callbacks готовы для Plan 04-10 integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-week-tasks/04-05-SUMMARY.md`
</output>
