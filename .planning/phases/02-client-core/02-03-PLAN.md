---
phase: 02-client-core
plan: 03
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
files_modified:
  - client/core/logging_setup.py
  - client/tests/test_logging.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "setup_client_logging() настраивает RotatingFileHandler в logs_dir/client.log с maxBytes=1MB, backupCount=5"
    - "Фильтр SecretFilter маскирует строки содержащие 'access_token', 'refresh_token', 'Bearer ' в любых log-records (D-29)"
    - "После setup_client_logging() вызов logging.getLogger('client.test').info('msg') пишет в файл и НЕ дублирует при повторных вызовах"
    - "Логгер 'requests' и 'urllib3' установлены на WARNING (не засоряют DEBUG)"
  artifacts:
    - path: client/core/logging_setup.py
      provides: "setup_client_logging(paths) + SecretFilter — вызывается один раз при старте app"
      contains: "def setup_client_logging"
  key_links:
    - from: "client/core/logging_setup.py"
      to: "AppPaths.logs_dir"
      via: "RotatingFileHandler пишет в paths.logs_dir / config.LOG_FILE_NAME"
      pattern: "RotatingFileHandler"
    - from: "SecretFilter"
      to: "Все логгеры"
      via: "addFilter на root logger — наследуется всеми child-логгерами"
      pattern: "addFilter"
---

<objective>
Создать `client/core/logging_setup.py` — единая точка инициализации логирования для клиента. RotatingFileHandler в `%APPDATA%/ЛичныйЕженедельник/logs/client.log` (D-27), фильтр против утечки токенов (D-29), идемпотентность (повторные вызовы не плодят handlers).

Purpose: AuthManager и SyncManager (плены 04, 06, 07) логируют сетевые операции, ошибки 401, конфликты merge. Без централизованной инициализации каждый модуль пытался бы конфигурировать root logger → дублирующиеся записи или утечка JWT в логи.

Output: setup_client_logging(paths) + SecretFilter + 4 unit-теста.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-client-core/02-CONTEXT.md
@.planning/phases/02-client-core/02-RESEARCH.md
@.planning/phases/02-client-core/02-02-PLAN.md
@client/core/config.py
@client/core/paths.py

<interfaces>
<!-- Зависимости из Plan 02 -->

Из client/core/config.py:
```python
LOG_FILE_NAME: str = "client.log"
LOG_ROTATION_MAX_BYTES: int = 1_000_000
LOG_ROTATION_BACKUP_COUNT: int = 5
```

Из client/core/paths.py:
```python
class AppPaths:
    logs_dir: Path  # уже существует после ensure()
```

stdlib: `logging.handlers.RotatingFileHandler`, `logging.Filter`, `logging.Formatter`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: client/core/logging_setup.py — setup_client_logging + SecretFilter</name>
  <files>client/core/logging_setup.py, client/tests/test_logging.py</files>
  <read_first>
    - .planning/phases/02-client-core/02-CONTEXT.md §D-27, D-28, D-29
    - .planning/phases/02-client-core/02-RESEARCH.md §LocalStorage init + atomic write (показывает _setup_logging паттерн)
    - client/core/config.py (созданный в Plan 02)
    - client/core/paths.py (созданный в Plan 02)
  </read_first>
  <behavior>
    - setup_client_logging(paths: AppPaths, level=logging.DEBUG) вызывается один раз при старте app
    - При повторном вызове НЕ создаёт дублирующих handlers (проверяет уникальный atomictic-marker)
    - Создаёт RotatingFileHandler с maxBytes=config.LOG_ROTATION_MAX_BYTES, backupCount=config.LOG_ROTATION_BACKUP_COUNT, encoding="utf-8"
    - Format: "%(asctime)s %(name)s %(levelname)s %(message)s"
    - Логгеры 'requests', 'urllib3' выставляются на WARNING (не засоряют файл)
    - SecretFilter.filter(record) возвращает True но мутирует record.msg/record.args чтобы заменить "access_token=XXX" → "access_token=***", "refresh_token=YYY" → "refresh_token=***", "Bearer ZZZ" → "Bearer ***"
    - После setup, logger.info("Bearer eyJ123") пишет в файл строку с "Bearer ***" (НЕ "Bearer eyJ123")
  </behavior>
  <action>
    Создать `client/core/logging_setup.py`:

    ```python
    """
    Единая инициализация логирования клиента.

    Использование (один раз при старте app):
        from client.core import AppPaths
        from client.core.logging_setup import setup_client_logging

        paths = AppPaths(); paths.ensure()
        setup_client_logging(paths)

    После этого все `logging.getLogger("client.X")` пишут в logs/client.log с rotation.
    """
    from __future__ import annotations

    import logging
    import re
    from logging.handlers import RotatingFileHandler

    from client.core import config
    from client.core.paths import AppPaths

    # Атрибут-маркер на root logger — для идемпотентности setup_client_logging.
    _SETUP_MARKER = "_planner_client_logging_initialized"

    # Регулярки для маскировки секретов в log-сообщениях (D-29).
    # Покрываем варианты: token=value, "token": "value", Bearer value
    _SECRET_PATTERNS: list[tuple[re.Pattern, str]] = [
        (re.compile(r'(Bearer\s+)[A-Za-z0-9._\-]+', re.IGNORECASE), r'\1***'),
        (re.compile(r'(access_token["\'\s:=]+)[A-Za-z0-9._\-]+', re.IGNORECASE), r'\1***'),
        (re.compile(r'(refresh_token["\'\s:=]+)[A-Za-z0-9._\-]+', re.IGNORECASE), r'\1***'),
    ]


    class SecretFilter(logging.Filter):
        """
        Маскирует JWT и refresh tokens в log-записях (D-29).

        Применяется к record.msg и каждому элементу record.args.
        Возвращает True (запись пропускается, но с замаскированным содержимым).
        """

        def filter(self, record: logging.LogRecord) -> bool:  # noqa: A003
            try:
                if isinstance(record.msg, str):
                    record.msg = self._mask(record.msg)
                if record.args:
                    if isinstance(record.args, dict):
                        record.args = {k: self._mask_value(v) for k, v in record.args.items()}
                    elif isinstance(record.args, tuple):
                        record.args = tuple(self._mask_value(a) for a in record.args)
            except Exception:  # filter не должен ронять логирование
                pass
            return True

        @staticmethod
        def _mask(text: str) -> str:
            for pattern, repl in _SECRET_PATTERNS:
                text = pattern.sub(repl, text)
            return text

        @classmethod
        def _mask_value(cls, value):
            if isinstance(value, str):
                return cls._mask(value)
            return value


    def setup_client_logging(paths: AppPaths, level: int = logging.DEBUG) -> RotatingFileHandler:
        """
        Настроить root logger клиента (один раз).

        Параметры:
            paths: AppPaths уже с вызванным ensure() (logs_dir существует).
            level: уровень root logger (default DEBUG для богатой диагностики в файле).

        Возвращает: созданный RotatingFileHandler (полезно для тестов и unit testing).

        Идемпотентность: повторный вызов возвращает существующий handler из root logger.
        """
        root = logging.getLogger()

        # Идемпотентность через маркер на root logger
        if getattr(root, _SETUP_MARKER, False):
            for h in root.handlers:
                if isinstance(h, RotatingFileHandler):
                    return h
            # Нет handler но маркер есть — странное состояние, продолжим установку

        log_file = paths.logs_dir / config.LOG_FILE_NAME
        handler = RotatingFileHandler(
            log_file,
            maxBytes=config.LOG_ROTATION_MAX_BYTES,
            backupCount=config.LOG_ROTATION_BACKUP_COUNT,
            encoding="utf-8",
        )
        handler.setFormatter(logging.Formatter(
            "%(asctime)s %(name)s %(levelname)s %(message)s"
        ))
        handler.addFilter(SecretFilter())

        root.addHandler(handler)
        root.setLevel(level)
        setattr(root, _SETUP_MARKER, True)

        # Шумные библиотеки → WARNING (D-28)
        logging.getLogger("requests").setLevel(logging.WARNING)
        logging.getLogger("urllib3").setLevel(logging.WARNING)
        logging.getLogger("keyring").setLevel(logging.WARNING)

        return handler


    def reset_client_logging() -> None:
        """
        Сбросить состояние логирования (только для тестов!).
        Удаляет все handlers и снимает маркер с root logger.
        """
        root = logging.getLogger()
        for h in list(root.handlers):
            root.removeHandler(h)
            try:
                h.close()
            except Exception:
                pass
        if hasattr(root, _SETUP_MARKER):
            delattr(root, _SETUP_MARKER)
    ```

    Создать `client/tests/test_logging.py`:

    ```python
    """Unit-тесты client/core/logging_setup.py."""
    import logging

    import pytest

    from client.core import AppPaths, config
    from client.core.logging_setup import (
        SecretFilter, reset_client_logging, setup_client_logging,
    )


    @pytest.fixture(autouse=True)
    def _reset_logging():
        """Сбрасывает root logger перед/после каждого теста — изоляция."""
        reset_client_logging()
        yield
        reset_client_logging()


    def test_setup_creates_log_file(tmp_appdata):
        """setup_client_logging создаёт файл client.log в logs_dir."""
        paths = AppPaths(); paths.ensure()
        setup_client_logging(paths)
        log = logging.getLogger("client.test")
        log.info("первая запись")

        log_file = paths.logs_dir / config.LOG_FILE_NAME
        assert log_file.exists()
        content = log_file.read_text(encoding="utf-8")
        assert "первая запись" in content
        assert "client.test" in content


    def test_setup_is_idempotent(tmp_appdata):
        """Повторный вызов setup НЕ дублирует handlers."""
        paths = AppPaths(); paths.ensure()
        h1 = setup_client_logging(paths)
        h2 = setup_client_logging(paths)
        # Повторный вызов возвращает тот же handler
        assert h1 is h2
        # Только один RotatingFileHandler в root
        from logging.handlers import RotatingFileHandler
        rfh_count = sum(
            1 for h in logging.getLogger().handlers
            if isinstance(h, RotatingFileHandler)
        )
        assert rfh_count == 1


    def test_secret_filter_masks_bearer(tmp_appdata):
        """SecretFilter маскирует 'Bearer XYZ' → 'Bearer ***'."""
        paths = AppPaths(); paths.ensure()
        setup_client_logging(paths)
        log = logging.getLogger("client.test")
        log.error("Sync request: Bearer eyJhbGciOiJIUzI1NiJ9.payload.sig")

        log_file = paths.logs_dir / config.LOG_FILE_NAME
        content = log_file.read_text(encoding="utf-8")
        assert "Bearer ***" in content
        assert "eyJhbGciOiJIUzI1NiJ9" not in content


    def test_secret_filter_masks_refresh_token(tmp_appdata):
        """SecretFilter маскирует refresh_token=VALUE."""
        paths = AppPaths(); paths.ensure()
        setup_client_logging(paths)
        log = logging.getLogger("client.test")
        log.error("Refresh failed: refresh_token=secretvalue123")

        log_file = paths.logs_dir / config.LOG_FILE_NAME
        content = log_file.read_text(encoding="utf-8")
        assert "refresh_token=***" in content
        assert "secretvalue123" not in content


    def test_secret_filter_unit_mask():
        """Прямой unit-тест SecretFilter._mask."""
        f = SecretFilter()
        assert f._mask("Bearer abc.def.ghi") == "Bearer ***"
        assert f._mask('"access_token": "xyz123"') == '"access_token": "***"'
        assert f._mask("refresh_token=foo-bar_baz.qux") == "refresh_token=***"
        # Без секретов — без изменений
        assert f._mask("normal log message") == "normal log message"


    def test_noisy_loggers_set_to_warning(tmp_appdata):
        """requests/urllib3 на WARNING — не засоряют DEBUG."""
        paths = AppPaths(); paths.ensure()
        setup_client_logging(paths, level=logging.DEBUG)
        assert logging.getLogger("requests").level == logging.WARNING
        assert logging.getLogger("urllib3").level == logging.WARNING
    ```

    Коммит: `feat(02-03): logging_setup с RotatingFileHandler + SecretFilter (D-27, D-29)`
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник" &amp;&amp; python -m pytest client/tests/test_logging.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Файл client/core/logging_setup.py содержит `def setup_client_logging` и `class SecretFilter`
    - `grep -c "RotatingFileHandler" client/core/logging_setup.py` >= 1
    - `grep "Bearer" client/core/logging_setup.py` находит regex
    - `grep "_SETUP_MARKER" client/core/logging_setup.py` находит маркер идемпотентности
    - `python -m pytest client/tests/test_logging.py -v` → 6 passed
    - После теста log-файл tmp_path/ЛичныйЕженедельник/logs/client.log содержит "Bearer ***" (не оригинальный токен)
  </acceptance_criteria>
  <done>
    logging_setup.py готов к использованию плагинами 04-07. SecretFilter защищает от утечки JWT (D-29). 6 unit-тестов зелёные.
  </done>
</task>

</tasks>

<verification>
```bash
cd "S:/Проекты/ежедневник"
python -m pytest client/tests -v --timeout=10
```
Ожидание: 27 passed (3 infra + 11 models + 7 paths + 6 logging).
</verification>

<success_criteria>
1. `python -m pytest client/tests/test_logging.py -v` → 6 passed
2. `from client.core.logging_setup import setup_client_logging, SecretFilter` работает
3. SecretFilter маскирует Bearer/access_token/refresh_token в любых log-сообщениях
4. setup_client_logging идемпотентен (повторный вызов = тот же handler)
5. RotatingFileHandler пишет в %APPDATA%/ЛичныйЕженедельник/logs/client.log
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-core/02-03-SUMMARY.md`.
</output>
