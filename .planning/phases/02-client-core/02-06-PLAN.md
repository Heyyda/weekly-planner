---
phase: 02-client-core
plan: 06
type: execute
wave: 3
depends_on: ["02-04", "02-05"]
files_modified:
  - client/core/api_client.py
  - client/tests/test_api_client.py
autonomous: true
requirements: ["SYNC-06"]
must_haves:
  truths:
    - "SyncApiClient.post_sync(since, changes) шлёт POST /api/sync с правильным wire-форматом и возвращает {server_timestamp, changes}"
    - "При 401 SyncApiClient вызывает auth.refresh_access() и ретраит ОДИН раз с новым токеном"
    - "При network error / 5xx возвращает ApiResult(ok=False, retry_after=backoff_seconds, error_kind='network'|'server')"
    - "При 4xx (не-401) возвращает ApiResult(ok=False, error_kind='client', message=...)"
    - "Idempotent CREATE: повторный отправленный CREATE с тем же task_id — сервер примет (server side, SRV-02) — клиент НЕ дедуплицирует на своём конце (SYNC-06)"
    - "Backoff state в SyncApiClient: счётчик _consecutive_errors, _current_backoff (1→60s cap)"
    - "On success — сбрасывает backoff в BACKOFF_BASE"
  artifacts:
    - path: client/core/api_client.py
      provides: "SyncApiClient — authenticated /api/sync caller с 401-refresh-retry + exponential backoff"
      contains: "class SyncApiClient"
  key_links:
    - from: "SyncApiClient.post_sync"
      to: "POST {API_BASE}/sync"
      via: "JSON body: {since, changes: [TaskChange.to_wire(), ...]}; header: Bearer access_token"
      pattern: "/sync"
    - from: "SyncApiClient on 401"
      to: "AuthManager.refresh_access()"
      via: "одна попытка refresh + retry; при втором 401 — fail (нужен повторный логин)"
      pattern: "refresh_access"
---

<objective>
Создать `client/core/api_client.py` — единая точка HTTP-вызовов к серверу. Обёртывает `requests.Session`, инжектит `Authorization: Bearer` через AuthManager, обрабатывает 401 (refresh → retry один раз), реализует exponential backoff state (счётчик + cap 60s), маппит сетевые/HTTP ошибки в типизированный `ApiResult`.

Purpose: SyncManager (Plan 07) — это оркестратор; вся "грязная" HTTP-логика инкапсулируется здесь, чтобы SyncManager оставался читаемым (drain → push → pull → merge). Без этого слоя SyncManager одновременно занимался бы wire-форматом, refresh-логикой, backoff math и threading — нечитаемая мешанина.

Покрытие: SYNC-06 (UUID идемпотентность — клиент отправляет UUID, сервер примет повторный CREATE как UPDATE — это server-side, но клиент должен корректно ретраить без удаления task_id).

Output: SyncApiClient + ApiResult dataclass + 9 unit-тестов.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-client-core/02-CONTEXT.md
@.planning/phases/02-client-core/02-RESEARCH.md
@.planning/phases/02-client-core/02-04-PLAN.md
@.planning/phases/02-client-core/02-05-PLAN.md
@client/core/auth.py
@client/core/storage.py
@client/core/models.py
@client/core/config.py
@server/api/sync_schemas.py

<interfaces>
Из client/core/auth.py:
  AuthManager.bearer_header() → {"Authorization": "Bearer ..."} | raises AuthError
  AuthManager.refresh_access() → bool (True=success, False=offline) | raises AuthExpiredError на 401
  AuthManager.get_access_token() → str | None

Из client/core/models.py:
  TaskChange.to_wire() → dict (для отправки в /sync)

Из client/core/config.py:
  API_BASE = "https://planner.heyda.ru/api"
  HTTP_TIMEOUT_SECONDS = 10.0
  BACKOFF_BASE = 1.0, BACKOFF_CAP = 60.0

Серверный контракт POST /api/sync:
  Request:
    {"since": "ISO-datetime" | null,
     "changes": [{"op":"create"|"update"|"delete", "task_id": "uuid", ...}, ...]}
    Header: Authorization: Bearer <access_token>
  Response 200:
    {"server_timestamp": "ISO-datetime",
     "changes": [TaskState, ...]}
  Response 401:
    Bearer expired/invalid → клиент должен сделать /auth/refresh и ретраить
  Response 5xx / network error:
    Backoff и ретрай через _sync_loop в Plan 07
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: client/core/api_client.py — SyncApiClient + ApiResult с 401-refresh + backoff state</name>
  <files>client/core/api_client.py</files>
  <read_first>
    - client/core/auth.py (bearer_header, refresh_access, AuthExpiredError)
    - client/core/config.py (API_BASE, HTTP_TIMEOUT_SECONDS, BACKOFF_*)
    - client/core/models.py (TaskChange.to_wire)
    - server/api/sync_schemas.py (wire format SyncIn/SyncOut)
    - .planning/phases/02-client-core/02-RESEARCH.md §SyncManager._attempt_sync (показывает целевой паттерн)
    - .planning/phases/02-client-core/02-CONTEXT.md §D-13 (backoff 1→2→4→...→60s)
  </read_first>
  <behavior>
    Публичный API:
      ApiResult(dataclass): ok: bool, status: int, payload: dict | None, error_kind: str | None, message: str | None, retry_after: float | None
        ApiResult.success(payload, status=200) — factory
        ApiResult.network_error(exc, retry_after) — factory
        ApiResult.server_error(status, retry_after, message) — factory
        ApiResult.client_error(status, message) — factory
        ApiResult.auth_expired() — factory (особый случай: нужен повторный логин)

      SyncApiClient(auth_manager: AuthManager)
        post_sync(since: str | None, changes: list[TaskChange]) → ApiResult
          1. собирает payload {"since": since, "changes": [c.to_wire() for c in changes]}
          2. шлёт POST с bearer_header()
          3. если 401 → auth.refresh_access() (одна попытка) → retry один раз
          4. если 200 → ApiResult.success(json) + reset backoff
          5. если 5xx или network → ApiResult.server/network_error + увеличить backoff
          6. если 4xx (не 401) → ApiResult.client_error
          7. возвращает ApiResult, не raise

        _next_backoff() → float — увеличивает _current_backoff в 2 раза, cap BACKOFF_CAP
        _reset_backoff() → None — сбрасывает в BACKOFF_BASE
        current_backoff property → float

    Internal:
      self._session: requests.Session
      self._consecutive_errors: int
      self._current_backoff: float

    Не блокирует на refresh: если refresh.AuthExpiredError → возвращает ApiResult.auth_expired() (sync манагер останавливает поток).
  </behavior>
  <action>
    Создать `client/core/api_client.py`:

    ```python
    """
    SyncApiClient — обёртка для POST /api/sync с auth + backoff + retry.

    Назначение: спрятать HTTP-детали от SyncManager. SyncManager не должен знать
    про requests, headers, JSON-сериализацию или 401 retry. Он работает в терминах
    ApiResult (ok/error_kind) и вызывает retry через свой _sync_loop.

    Backoff state хранится здесь (счётчик _consecutive_errors + _current_backoff).
    SyncManager читает .current_backoff чтобы знать сколько ждать перед следующим
    циклом.
    """
    from __future__ import annotations

    import logging
    from dataclasses import dataclass
    from typing import Optional

    import requests

    from client.core import config
    from client.core.auth import AuthError, AuthExpiredError, AuthManager
    from client.core.models import TaskChange

    logger = logging.getLogger(__name__)


    @dataclass
    class ApiResult:
        """Результат HTTP-вызова. Никогда не raise — SyncManager инспектирует поля."""
        ok: bool
        status: int = 0
        payload: Optional[dict] = None
        error_kind: Optional[str] = None      # "network" | "server" | "client" | "auth_expired"
        message: Optional[str] = None
        retry_after: Optional[float] = None   # секунд до следующей попытки

        @classmethod
        def success(cls, payload: dict, status: int = 200) -> "ApiResult":
            return cls(ok=True, status=status, payload=payload)

        @classmethod
        def network_error(cls, exc: Exception, retry_after: float) -> "ApiResult":
            return cls(
                ok=False, status=0, error_kind="network",
                message=f"Сетевая ошибка: {exc}", retry_after=retry_after,
            )

        @classmethod
        def server_error(cls, status: int, retry_after: float, message: str = "") -> "ApiResult":
            return cls(
                ok=False, status=status, error_kind="server",
                message=message or f"Сервер вернул {status}", retry_after=retry_after,
            )

        @classmethod
        def client_error(cls, status: int, message: str = "") -> "ApiResult":
            # 4xx (не 401): bug на клиенте, retry бесполезен
            return cls(
                ok=False, status=status, error_kind="client",
                message=message or f"Ошибка клиента {status}",
            )

        @classmethod
        def auth_expired(cls) -> "ApiResult":
            # Refresh не сработал — нужен Telegram-код
            return cls(
                ok=False, status=401, error_kind="auth_expired",
                message="Сессия истекла — нужен повторный логин",
            )


    class SyncApiClient:
        """Authenticated HTTP-клиент для /api/sync с 401-refresh + backoff state."""

        def __init__(self, auth_manager: AuthManager) -> None:
            self._auth = auth_manager
            self._api_base = config.API_BASE
            self._session = requests.Session()
            self._consecutive_errors = 0
            self._current_backoff = config.BACKOFF_BASE

        @property
        def current_backoff(self) -> float:
            """Сколько секунд ждать перед следующей попыткой (для SyncManager._sync_loop)."""
            return self._current_backoff

        @property
        def consecutive_errors(self) -> int:
            return self._consecutive_errors

        def reset_backoff(self) -> None:
            """Сбросить backoff (обычно вызывается автоматически на success)."""
            self._consecutive_errors = 0
            self._current_backoff = config.BACKOFF_BASE

        def _bump_backoff(self) -> float:
            """Удвоить delay (cap BACKOFF_CAP). Возвращает новое значение."""
            self._consecutive_errors += 1
            self._current_backoff = min(self._current_backoff * 2, config.BACKOFF_CAP)
            return self._current_backoff

        def post_sync(
            self, since: Optional[str], changes: list[TaskChange]
        ) -> ApiResult:
            """
            POST /api/sync с auth + 401 retry.

            since: ISO-строка last_sync_at | None (для full resync, D-19)
            changes: список TaskChange — отправляется через .to_wire()

            Возвращает ApiResult; никогда не raise (offline-tolerant).
            """
            wire_changes = [c.to_wire() for c in changes]
            payload = {"since": since, "changes": wire_changes}

            # Первая попытка с текущим access_token
            result = self._do_post_sync(payload)
            if result.status != 401:
                return result

            # 401 → пробуем refresh + retry один раз
            logger.info("Sync 401 — пробуем refresh access_token")
            try:
                refreshed = self._auth.refresh_access()
            except AuthExpiredError:
                logger.warning("Refresh expired — нужен повторный логин")
                return ApiResult.auth_expired()

            if not refreshed:
                # Network error в refresh — оставляем backoff на следующий цикл
                logger.warning("Refresh не удался (offline?) — backoff и ретрай позже")
                retry_after = self._bump_backoff()
                return ApiResult.server_error(401, retry_after, "Refresh failed")

            # Refresh OK — повторяем POST один раз
            return self._do_post_sync(payload)

        def _do_post_sync(self, payload: dict) -> ApiResult:
            """Внутренний: один HTTP вызов без retry-логики."""
            url = f"{self._api_base}/sync"

            try:
                headers = self._auth.bearer_header()
            except AuthError:
                logger.error("post_sync без auth — bearer_header raised AuthError")
                return ApiResult.auth_expired()

            try:
                resp = self._session.post(
                    url, json=payload, headers=headers,
                    timeout=config.HTTP_TIMEOUT_SECONDS,
                )
            except requests.exceptions.RequestException as exc:
                retry_after = self._bump_backoff()
                logger.error("post_sync network error: %s (backoff %.1fs)", exc, retry_after)
                return ApiResult.network_error(exc, retry_after)

            if resp.status_code == 200:
                self.reset_backoff()
                try:
                    body = resp.json()
                except ValueError as exc:
                    logger.error("post_sync 200 но JSON не парсится: %s", exc)
                    retry_after = self._bump_backoff()
                    return ApiResult.server_error(200, retry_after, "Invalid JSON")
                return ApiResult.success(body)

            if resp.status_code == 401:
                # НЕ bump_backoff здесь — refresh + retry в post_sync()
                return ApiResult(ok=False, status=401, error_kind="auth", message="401")

            if 500 <= resp.status_code < 600:
                retry_after = self._bump_backoff()
                logger.error("post_sync 5xx %d (backoff %.1fs)", resp.status_code, retry_after)
                return ApiResult.server_error(resp.status_code, retry_after)

            # 4xx (кроме 401) — клиентская ошибка, бессмысленно ретраить
            logger.error("post_sync 4xx %d", resp.status_code)
            return ApiResult.client_error(resp.status_code, resp.text[:200])
    ```

    Коммит: `feat(02-06): SyncApiClient — auth + 401-retry + exponential backoff`
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник" && python -c "from client.core.api_client import SyncApiClient, ApiResult; from client.core.auth import AuthManager; c = SyncApiClient(AuthManager()); print('OK', c.current_backoff)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class SyncApiClient" client/core/api_client.py` == 1
    - `grep -c "class ApiResult" client/core/api_client.py` == 1
    - `grep -c "refresh_access" client/core/api_client.py` >= 1
    - `grep -c "_bump_backoff" client/core/api_client.py` >= 2
    - `grep -c "BACKOFF_CAP" client/core/api_client.py` >= 1
    - `python -c "from client.core.api_client import SyncApiClient, ApiResult; from client.core.auth import AuthManager; SyncApiClient(AuthManager())"` exit code 0
  </acceptance_criteria>
  <done>
    api_client.py создан, импорт работает, классы экспортируются.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: client/tests/test_api_client.py — 9 unit-тестов wire-format, 401 refresh, backoff</name>
  <files>client/tests/test_api_client.py</files>
  <read_first>
    - client/core/api_client.py (свежесозданный)
    - client/core/auth.py (для AuthManager mock setup)
    - client/tests/conftest.py (mock_api fixture)
    - client/tests/test_auth.py (паттерн fake_keyring fixture)
  </read_first>
  <behavior>
    Тесты:
    1. test_post_sync_happy_path — 200 → ApiResult.ok=True, payload содержит server_timestamp+changes; backoff reset
    2. test_post_sync_wire_format — request body содержит "since" + "changes" с правильной структурой to_wire
    3. test_post_sync_includes_bearer — заголовок Authorization присутствует
    4. test_post_sync_401_then_refresh_then_200 — первый запрос 401, refresh OK, второй запрос 200
    5. test_post_sync_401_refresh_expired — refresh raises AuthExpiredError → ApiResult.auth_expired()
    6. test_post_sync_500_increases_backoff — backoff удваивается, retry_after возвращён
    7. test_post_sync_network_error_increases_backoff — ConnectionError → ApiResult.network_error + backoff bump
    8. test_post_sync_400_client_error_no_backoff — 400 → client_error, backoff НЕ растёт
    9. test_backoff_caps_at_60s — 10 ошибок подряд → backoff не превышает BACKOFF_CAP
    10. test_reset_backoff_on_success — после ошибок и потом success, backoff = BACKOFF_BASE
    11. test_idempotent_create_uuid_preserved — повторный post_sync с тем же task_id отправляет тот же UUID (SYNC-06)
  </behavior>
  <action>
    Создать `client/tests/test_api_client.py`:

    ```python
    """Unit-тесты client/core/api_client.py — wire format, 401 retry, backoff."""
    import json

    import pytest
    import requests

    from client.core import config
    from client.core.api_client import ApiResult, SyncApiClient
    from client.core.auth import AuthExpiredError, AuthManager
    from client.core.models import TaskChange


    @pytest.fixture
    def auth_with_token():
        """AuthManager с уже установленным access_token (минуем verify_code)."""
        m = AuthManager()
        m.access_token = "test-jwt-token"
        m._refresh_token = "test-rt"
        return m


    @pytest.fixture
    def client(auth_with_token):
        return SyncApiClient(auth_with_token)


    def _ok_sync_response():
        return {
            "server_timestamp": "2026-04-15T10:00:00.000000Z",
            "changes": [],
        }


    def test_post_sync_happy_path(client, mock_api, api_base):
        mock_api.post(f"{api_base}/sync", json=_ok_sync_response(), status_code=200)
        result = client.post_sync(since=None, changes=[])
        assert result.ok is True
        assert result.status == 200
        assert "server_timestamp" in result.payload
        assert client.consecutive_errors == 0
        assert client.current_backoff == config.BACKOFF_BASE


    def test_post_sync_wire_format(client, mock_api, api_base):
        """Request body содержит {since, changes:[{op, task_id, ...}]}."""
        captured = {}

        def matcher(request, _ctx):
            captured["body"] = request.json()
            captured["headers"] = dict(request.headers)
            return _ok_sync_response()

        mock_api.post(f"{api_base}/sync", json=matcher, status_code=200)
        changes = [
            TaskChange(op="create", task_id="t-1", text="hello",
                       day="2026-04-14", done=False, position=0),
            TaskChange(op="update", task_id="t-2", done=True),
            TaskChange(op="delete", task_id="t-3"),
        ]
        client.post_sync(since="2026-04-14T00:00:00Z", changes=changes)
        body = captured["body"]
        assert body["since"] == "2026-04-14T00:00:00Z"
        assert len(body["changes"]) == 3
        # CREATE имеет полный набор полей
        assert body["changes"][0]["op"] == "create"
        assert body["changes"][0]["task_id"] == "t-1"
        assert body["changes"][0]["text"] == "hello"
        # UPDATE — partial
        assert body["changes"][1] == {"op": "update", "task_id": "t-2", "done": True}
        # DELETE — минимум
        assert body["changes"][2] == {"op": "delete", "task_id": "t-3"}


    def test_post_sync_includes_bearer(client, mock_api, api_base):
        captured = {}

        def matcher(request, _ctx):
            captured["auth"] = request.headers.get("Authorization", "")
            return _ok_sync_response()

        mock_api.post(f"{api_base}/sync", json=matcher, status_code=200)
        client.post_sync(since=None, changes=[])
        assert captured["auth"] == "Bearer test-jwt-token"


    def test_post_sync_401_refresh_then_200(client, mock_api, api_base, monkeypatch):
        """401 → refresh_access() возвращает True → второй запрос 200."""
        # Mock auth.refresh_access — обновляет токен в self.access_token
        def fake_refresh():
            client._auth.access_token = "rotated-jwt"
            return True

        monkeypatch.setattr(client._auth, "refresh_access", fake_refresh)

        # Первый POST → 401, второй → 200
        responses = [
            {"status_code": 401, "json": {"error": {"code": "INVALID", "message": "expired"}}},
            {"status_code": 200, "json": _ok_sync_response()},
        ]
        mock_api.post(f"{api_base}/sync", responses)
        result = client.post_sync(since=None, changes=[])
        assert result.ok is True
        assert client._auth.access_token == "rotated-jwt"


    def test_post_sync_401_refresh_expired_returns_auth_expired(client, mock_api, api_base, monkeypatch):
        """refresh_access raises AuthExpiredError → ApiResult.auth_expired()."""
        def fake_refresh():
            raise AuthExpiredError("expired")

        monkeypatch.setattr(client._auth, "refresh_access", fake_refresh)
        mock_api.post(f"{api_base}/sync", status_code=401,
                      json={"error": {"code": "INVALID", "message": "x"}})
        result = client.post_sync(since=None, changes=[])
        assert result.ok is False
        assert result.error_kind == "auth_expired"


    def test_post_sync_500_bumps_backoff(client, mock_api, api_base):
        mock_api.post(f"{api_base}/sync", status_code=500, text="server error")
        result = client.post_sync(since=None, changes=[])
        assert result.ok is False
        assert result.error_kind == "server"
        assert result.retry_after >= config.BACKOFF_BASE * 2  # удвоилось хотя бы раз
        assert client.consecutive_errors == 1


    def test_post_sync_network_error_bumps_backoff(client, mock_api, api_base):
        mock_api.post(f"{api_base}/sync", exc=requests.ConnectionError("offline"))
        result = client.post_sync(since=None, changes=[])
        assert result.ok is False
        assert result.error_kind == "network"
        assert result.retry_after is not None
        assert client.consecutive_errors == 1


    def test_post_sync_400_client_error_no_backoff(client, mock_api, api_base):
        """400 — баг на клиенте, retry бесполезен, backoff НЕ растёт."""
        mock_api.post(f"{api_base}/sync", status_code=400, text="bad request")
        result = client.post_sync(since=None, changes=[])
        assert result.error_kind == "client"
        # consecutive_errors не увеличился (4xx не bump)
        assert client.consecutive_errors == 0


    def test_backoff_caps_at_60s(client, mock_api, api_base):
        mock_api.post(f"{api_base}/sync", status_code=500)
        for _ in range(15):  # достаточно чтобы превысить cap
            client.post_sync(since=None, changes=[])
        assert client.current_backoff == config.BACKOFF_CAP


    def test_reset_backoff_on_success(client, mock_api, api_base):
        # Сначала несколько ошибок
        mock_api.post(f"{api_base}/sync", [
            {"status_code": 500},
            {"status_code": 500},
            {"status_code": 200, "json": _ok_sync_response()},
        ])
        client.post_sync(since=None, changes=[])
        client.post_sync(since=None, changes=[])
        assert client.current_backoff > config.BACKOFF_BASE
        # Третья — успех
        client.post_sync(since=None, changes=[])
        assert client.current_backoff == config.BACKOFF_BASE
        assert client.consecutive_errors == 0


    def test_idempotent_create_preserves_uuid(client, mock_api, api_base):
        """SYNC-06: повторный CREATE с тем же task_id отправляет тот же UUID."""
        captured_bodies = []

        def matcher(request, _ctx):
            captured_bodies.append(request.json())
            return _ok_sync_response()

        mock_api.post(f"{api_base}/sync", json=matcher, status_code=200)
        change = TaskChange(op="create", task_id="fixed-uuid-123",
                            text="hello", day="2026-04-14")
        client.post_sync(since=None, changes=[change])
        client.post_sync(since=None, changes=[change])
        # Оба запроса отправили тот же task_id (клиентский UUID — основа idempotency)
        assert captured_bodies[0]["changes"][0]["task_id"] == "fixed-uuid-123"
        assert captured_bodies[1]["changes"][0]["task_id"] == "fixed-uuid-123"
    ```

    Коммит: `test(02-06): unit-тесты SyncApiClient — wire format, 401 refresh, backoff`
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник" && python -m pytest client/tests/test_api_client.py -v --timeout=10 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest client/tests/test_api_client.py -v` → 11 passed
    - test_post_sync_wire_format подтверждает что body содержит правильные поля
    - test_post_sync_401_refresh_then_200 подтверждает retry-with-refresh работу
    - test_backoff_caps_at_60s подтверждает D-13 cap
    - test_idempotent_create_preserves_uuid подтверждает SYNC-06
    - Время выполнения < 5 секунд
  </acceptance_criteria>
  <done>
    SyncApiClient покрыт 11 unit-тестами. SYNC-06 (idempotent CREATE через client UUID) верифицирован. Готов к использованию SyncManager в Plan 07.
  </done>
</task>

</tasks>

<verification>
```bash
cd "S:/Проекты/ежедневник"
python -m pytest client/tests -v --timeout=60
```
Ожидание: 75+ passed.
</verification>

<success_criteria>
1. `python -m pytest client/tests/test_api_client.py -v` → 11 passed
2. SyncApiClient.post_sync шлёт правильный wire-формат
3. 401 → refresh → retry один раз; повторный 401 → ApiResult.auth_expired()
4. Backoff: bump на network/5xx, reset на 200, cap 60s
5. 4xx (не 401) → client_error без backoff bump
6. SYNC-06: client-generated UUID сохраняется при повторных CREATE отправках
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-core/02-06-SUMMARY.md`.
</output>
