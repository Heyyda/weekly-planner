---
phase: 02-client-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - client/__init__.py
  - client/tests/__init__.py
  - client/tests/conftest.py
  - client/tests/test_infrastructure.py
  - client/requirements-dev.txt
  - client/pyproject.toml
autonomous: true
requirements: []
must_haves:
  truths:
    - "pytest обнаруживает client/tests/ и выполняет тесты без CollectError"
    - "Fixture tmp_appdata подменяет APPDATA так, что LocalStorage пишет в изолированный tmp_path"
    - "Fixture mock_api (requests_mock) перехватывает запросы к https://planner.heyda.ru/api"
  artifacts:
    - path: client/tests/conftest.py
      provides: "Shared fixtures (tmp_appdata, mock_api)"
      min_lines: 30
    - path: client/pyproject.toml
      provides: "pytest config с testpaths=client/tests"
      contains: "testpaths"
    - path: client/requirements-dev.txt
      provides: "Dev-зависимости (requests-mock, pytest-mock)"
      contains: "requests-mock"
  key_links:
    - from: "pytest"
      to: "client/tests/"
      via: "testpaths в client/pyproject.toml"
      pattern: "testpaths.*client/tests"
    - from: "client/tests/conftest.py"
      to: "APPDATA env var"
      via: "monkeypatch.setenv в tmp_appdata fixture"
      pattern: "monkeypatch\\.setenv.*APPDATA"
---

<objective>
Создать тестовую инфраструктуру для Фазы 2: pytest config, shared fixtures (tmp_appdata, mock_api через requests-mock), dev-зависимости. Это Wave 0 — все последующие задачи фазы зависят от этой инфраструктуры для автоматической верификации.

Purpose: Dowstream задачи требуют `<automated>` проверок через pytest. Без conftest с tmp_appdata каждый тест LocalStorage ломал бы реальный `%APPDATA%/ЛичныйЕженедельник`. Без requests-mock тесты SyncManager/AuthManager требовали бы живой сервер.

Output: client/tests/ пакет + pyproject.toml с pytest config + requirements-dev.txt + маркер-тест `test_infrastructure.py` который проверяет что fixtures работают.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-client-core/02-CONTEXT.md
@.planning/phases/02-client-core/02-RESEARCH.md
@.planning/phases/02-client-core/02-VALIDATION.md
@server/pyproject.toml
@server/requirements-dev.txt
@client/__init__.py

<interfaces>
<!-- Существующий server/pyproject.toml — образец структуры pytest-config. Копируем паттерн, но свой testpaths. -->

Из server/pyproject.toml:
```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
```

Из server/requirements-dev.txt (образец стиля):
```
-r requirements.txt
pytest>=8.0.0
pytest-timeout>=2.2.0
```

Клиент — синхронный (не async), поэтому asyncio_mode НЕ нужен. requests-mock thread-safe (важно для тестов SYNC-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать client/pyproject.toml + client/requirements-dev.txt + client/tests/__init__.py</name>
  <files>client/pyproject.toml, client/requirements-dev.txt, client/tests/__init__.py</files>
  <read_first>
    - server/pyproject.toml (образец структуры pytest config)
    - server/requirements-dev.txt (стиль зависимостей)
    - requirements.txt (корневой — для -r ссылки)
    - .planning/phases/02-client-core/02-RESEARCH.md §Test Framework
    - .planning/phases/02-client-core/02-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    Создать три файла:

    **1. `client/pyproject.toml`** — pytest config для клиентских тестов.
    Содержимое:
    ```toml
    # pytest config для клиентской части Личного Еженедельника.
    # Тесты синхронные (client sync, без asyncio) — asyncio_mode не указан.

    [tool.pytest.ini_options]
    testpaths = ["client/tests"]
    python_files = ["test_*.py"]
    python_classes = ["Test*"]
    python_functions = ["test_*"]
    addopts = [
        "-ra",
        "--strict-markers",
        "--tb=short",
        "--timeout=10",
    ]
    markers = [
        "integration: integration tests (могут быть медленнее)",
        "unit: быстрые unit-тесты",
    ]
    ```

    **2. `client/requirements-dev.txt`** — dev-зависимости клиента.
    Содержимое:
    ```
    # Dev-зависимости для client тестов (не попадают в PyInstaller build).
    # Устанавливаются локально: pip install -r client/requirements-dev.txt
    -r ../requirements.txt

    pytest>=8.0.0
    pytest-mock>=3.14.0
    pytest-timeout>=2.2.0
    requests-mock>=1.12.0
    ```

    Примечание: `-r ../requirements.txt` — ссылка на корневой requirements.txt (там уже keyring, requests, etc.).

    **3. `client/tests/__init__.py`** — пустой маркер пакета.
    Содержимое (одна строка-комментарий):
    ```python
    # Тестовый пакет для клиентской части Личного Еженедельника.
    ```

    ВАЖНО: `client/__init__.py` уже существует (пустой) — НЕ создавать повторно, НЕ модифицировать.

    Коммит: `chore(02-01): pytest-инфраструктура для client-тестов`
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник" &amp;&amp; pip install -r client/requirements-dev.txt --quiet &amp;&amp; python -m pytest client/tests --collect-only -q 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Файл client/pyproject.toml существует, содержит строку с `testpaths` и `client/tests`
    - Файл client/requirements-dev.txt существует, содержит `requests-mock` и `pytest-timeout`
    - Файл client/tests/__init__.py существует (может быть пустым с комментарием)
    - Команда `pip install -r client/requirements-dev.txt` завершается exit code 0
    - Команда `python -m pytest client/tests --collect-only` завершается без CollectError (даже если 0 тестов собрано — на этом этапе это OK)
  </acceptance_criteria>
  <done>
    pytest обнаруживает client/tests/ как testpath; dev-зависимости устанавливаются без ошибок.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать client/tests/conftest.py с fixtures tmp_appdata и mock_api + маркер-тест</name>
  <files>client/tests/conftest.py, client/tests/test_infrastructure.py</files>
  <read_first>
    - client/pyproject.toml (созданный в Task 1)
    - .planning/phases/02-client-core/02-RESEARCH.md §conftest.py для client/tests/
    - .planning/phases/02-client-core/02-VALIDATION.md §Wave 0 Requirements (what fixtures cover)
    - server/tests/conftest.py (если есть — для стиля)
  </read_first>
  <behavior>
    - Fixture `tmp_appdata` подменяет APPDATA env var на `tmp_path` (pytest builtin) через monkeypatch → последующий `os.environ.get("APPDATA")` возвращает путь к tmp_path
    - Fixture `mock_api` возвращает контекстный requests_mock.Mocker — все HTTP вызовы перехватываются
    - Fixture `api_base` возвращает константу базового URL `"https://planner.heyda.ru/api"` для использования в тестах
    - test_infrastructure.py содержит три pytest-теста:
      1. `test_tmp_appdata_isolates_path` — проверяет что `os.environ["APPDATA"]` == строковое представление tmp_path (не реальный %APPDATA%)
      2. `test_mock_api_intercepts_request` — регистрирует mock на GET к api_base+"/health" возвращающий 200 {"status":"ok"}, делает requests.get, получает 200
      3. `test_api_base_constant` — проверяет что api_base содержит "planner.heyda.ru"
  </behavior>
  <action>
    Создать два файла.

    **1. `client/tests/conftest.py`**:
    ```python
    """
    Shared pytest fixtures для client/tests/.

    Fixtures:
    - tmp_appdata: изолирует APPDATA в tmp_path (LocalStorage пишет в tmp)
    - mock_api: requests_mock.Mocker для перехвата HTTP
    - api_base: базовый URL API (константа)
    """
    import os
    from pathlib import Path

    import pytest
    import requests_mock as req_mock_module


    API_BASE_URL = "https://planner.heyda.ru/api"


    @pytest.fixture
    def api_base() -> str:
        """Базовый URL API, используемый во всех client-тестах."""
        return API_BASE_URL


    @pytest.fixture
    def tmp_appdata(tmp_path, monkeypatch) -> Path:
        """
        Подменяет переменную окружения APPDATA на tmp_path.
        LocalStorage и RotatingFileHandler будут писать в tmp_path/ЛичныйЕженедельник/.
        """
        monkeypatch.setenv("APPDATA", str(tmp_path))
        # LOCALAPPDATA тоже устанавливаем (fallback в client/core/paths.py)
        monkeypatch.setenv("LOCALAPPDATA", str(tmp_path))
        return tmp_path


    @pytest.fixture
    def mock_api():
        """
        requests_mock.Mocker для перехвата HTTP-вызовов.
        Использование:
            def test_sync(mock_api, api_base):
                mock_api.post(f"{api_base}/sync", json={"server_timestamp": "...", "changes": []})
                ... requests.post(f"{api_base}/sync") ...
        """
        with req_mock_module.Mocker() as m:
            yield m
    ```

    **2. `client/tests/test_infrastructure.py`**:
    ```python
    """
    Маркер-тест: проверка что Wave 0 fixtures работают.
    Эти тесты должны проходить ДО реализации LocalStorage/SyncManager/AuthManager.
    """
    import os
    import requests


    def test_tmp_appdata_isolates_path(tmp_appdata):
        """tmp_appdata подменяет APPDATA на tmp_path (изоляция от реальной AppData)."""
        assert os.environ["APPDATA"] == str(tmp_appdata)
        assert "ЛичныйЕженедельник" not in os.environ["APPDATA"]  # ещё не создана subdir


    def test_mock_api_intercepts_request(mock_api, api_base):
        """mock_api перехватывает реальный requests.get — никаких живых HTTP в тестах."""
        mock_api.get(f"{api_base}/health", json={"status": "ok"}, status_code=200)
        resp = requests.get(f"{api_base}/health", timeout=5)
        assert resp.status_code == 200
        assert resp.json() == {"status": "ok"}


    def test_api_base_constant(api_base):
        """api_base указывает на production-домен (совпадает с AuthManager/SyncManager константами)."""
        assert api_base == "https://planner.heyda.ru/api"
    ```

    Коммит: `test(02-01): conftest fixtures + маркер-тест инфраструктуры`
  </action>
  <verify>
    <automated>cd "S:/Проекты/ежедневник" &amp;&amp; python -m pytest client/tests/test_infrastructure.py -v 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Файл client/tests/conftest.py содержит `@pytest.fixture` 3 раза (tmp_appdata, mock_api, api_base)
    - Файл client/tests/test_infrastructure.py содержит 3 функции test_*
    - Команда `python -m pytest client/tests/test_infrastructure.py -v` возвращает exit code 0
    - Вывод содержит "3 passed" (ровно три теста зелёные)
    - `grep -c "monkeypatch.setenv" client/tests/conftest.py` возвращает >= 2 (APPDATA и LOCALAPPDATA)
  </acceptance_criteria>
  <done>
    pytest выполняет 3 теста test_infrastructure.py зелёными. Fixtures готовы к использованию последующими планами.
  </done>
</task>

</tasks>

<verification>
Финальная проверка Wave 0:
```bash
cd "S:/Проекты/ежедневник"
pip install -r client/requirements-dev.txt
python -m pytest client/tests -v --timeout=10
```
Ожидание: 3 passed (test_tmp_appdata_isolates_path, test_mock_api_intercepts_request, test_api_base_constant).
</verification>

<success_criteria>
1. `python -m pytest client/tests -v` → 3 passed, 0 failed
2. `client/pyproject.toml`, `client/requirements-dev.txt`, `client/tests/__init__.py`, `client/tests/conftest.py`, `client/tests/test_infrastructure.py` существуют
3. Все плагины установлены: `requests-mock`, `pytest-mock`, `pytest-timeout`
4. Последующие плены могут использовать fixtures `tmp_appdata`, `mock_api`, `api_base` без дополнительной настройки
</success_criteria>

<output>
After completion, create `.planning/phases/02-client-core/02-01-SUMMARY.md`.
</output>
